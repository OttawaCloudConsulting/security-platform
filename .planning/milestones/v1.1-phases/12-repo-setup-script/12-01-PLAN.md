# Plan: 12-01 — Fix bash 3.2 compat, add require_git_repo call, add .markdownlintignore

**Phase:** 12 — Repo Setup Script
**Requirements:** DIST-01, DIST-02, DIST-03, DIST-05
**Estimated:** ~10 min

## Objective

Close the three identified gaps in setup.sh so it reliably onboards any git repo on macOS (bash 3.2) and Linux.

## Tasks

### Task 1: Fix `declare -A` bash 4+ syntax (Bug)

Replace the two associative arrays (`TOOL_REPOS`, `HOOK_REPOS`) with plain variables. These are only used in `generate_versions_conf()` and `resolve_hook_versions()` to map tool names to GitHub repo slugs. Replace with direct variable references — the indirection isn't needed since the mapping is static and small.

### Task 2: Call `require_git_repo()` before configure/setup (Bug)

Add `require_git_repo` call before the `configure` and `setup` case branches. The `install` and `check` commands don't need a git repo.

### Task 3: Add `generate_markdownlintignore()` (Gap)

Add a function that generates `.markdownlintignore` with common directory excludes shared across all repos: `node_modules/`, `.terraform/`, `.planning/`, `.claude/`, `cdk.out/`. Uses the existing `write_config` pattern (skip if exists).

### Task 4: Validate

1. Run `bash setup.sh configure` in a fresh temp git repo — verify all 6 config files created
2. Run again — verify idempotent (0 files created, existing content preserved)
3. Run `bash setup.sh setup` in a fresh temp git repo — verify tools checked + configs + hooks
4. Verify script runs under `/bin/bash` (3.2) without errors

## Success Criteria

1. `setup.sh` runs without errors under `/bin/bash` (bash 3.2)
2. `configure` command generates 6 files: `.pre-commit-config.yaml`, `.gitleaksignore`, `.markdownlint.jsonc`, `.markdownlint-fix.markdownlint.jsonc`, `.markdownlint-cli2.yaml`, `.markdownlintignore`
3. Running configure outside a git repo produces a clear error message
4. Second run is idempotent — no files overwritten, no errors

## Files Modified

- `repos/security-platform/workstation/setup.sh`
