---
phase: 06-sca-and-container-cli-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/development-security-stack-option-1.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "trivy --version succeeds and shows version >= 0.69.2"
    - "syft version succeeds and shows a current version"
    - "grype version succeeds and shows version >= 0.88.0"
    - "Trivy vulnerability database is refreshed (not stale from 2026-02-22)"
  artifacts:
    - path: "docs/development-security-stack-option-1.md"
      provides: "Verified version notes for Trivy, Syft, and Grype"
      contains: "Verified"
  key_links:
    - from: "brew (Homebrew)"
      to: "trivy, syft, grype binaries on PATH"
      via: "brew install/upgrade"
      pattern: "trivy --version && syft version && grype version"
---

<objective>
Install and verify the three SCA and container security CLI tools (Trivy, Syft, Grype) via Homebrew so they are available on PATH and meet minimum version requirements.

Purpose: Phase 8 (CLI Tool Scanning Validation) depends on these tools being installed. This phase makes them available locally.
Output: Three CLI tools on PATH with verified versions, main doc updated with actual installed versions.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sca-and-container-cli-tools/06-CONTEXT.md
@.planning/phases/06-sca-and-container-cli-tools/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install/upgrade Trivy, Syft, and Grype via Homebrew and verify versions</name>
  <files>none (CLI tool installation, no project files modified)</files>
  <read_first>
    - .planning/phases/06-sca-and-container-cli-tools/06-CONTEXT.md (locked decisions on installation method)
    - .planning/phases/06-sca-and-container-cli-tools/06-RESEARCH.md (version requirements and pitfalls)
  </read_first>
  <action>
    Install/upgrade the three SCA tools via Homebrew in this order:

    1. **Trivy upgrade + DB refresh:**
       ```
       brew upgrade trivy
       ```
       - If already at latest, this is fine (exits 0 with "already installed" message)
       - Then refresh the vulnerability database (stale since 2026-02-22):
       ```
       trivy image --download-db-only
       ```
       - Verify: `trivy --version` must show >= 0.69.2

    2. **Syft fresh install:**
       ```
       brew install syft
       ```
       - No custom tap needed (now in main Homebrew formulae)
       - Verify: `syft version` must succeed and show a version number

    3. **Grype fresh install:**
       ```
       brew install grype
       ```
       - No custom tap needed (now in main Homebrew formulae)
       - Verify: `grype version` must show >= 0.88.0 (DB schema v5 EOL was 2026-03-06)
       - If Homebrew version is below 0.88.0 (unlikely -- research shows 0.109.1): fall back to curl-based install from Anchore GitHub releases
       - Note: First `grype db check` or scan will download the ~200MB vulnerability DB. The `grype version` command does NOT trigger this download.

    4. **Final verification:**
       Run all three version commands to confirm:
       ```
       trivy --version && syft version && grype version
       ```
  </action>
  <verify>
    <automated>trivy --version && syft version && grype version</automated>
  </verify>
  <acceptance_criteria>
    - `trivy --version` output contains a version number >= 0.69.2
    - `syft version` output contains a version number (e.g., 1.42.x or later)
    - `grype version` output contains a version number >= 0.88.0
    - `trivy image --download-db-only` completed successfully (DB refreshed from 2026-02-22 stale state)
  </acceptance_criteria>
  <done>All three tools respond to version commands on PATH with versions meeting minimum requirements. Trivy DB is freshly downloaded.</done>
</task>

<task type="auto">
  <name>Task 2: Update main doc with verified installed versions</name>
  <files>docs/development-security-stack-option-1.md</files>
  <read_first>
    - docs/development-security-stack-option-1.md (lines 253-352: current Trivy, Syft, and Grype setup sections)
    - .planning/phases/06-sca-and-container-cli-tools/06-CONTEXT.md (documentation update decision)
  </read_first>
  <action>
    Update `docs/development-security-stack-option-1.md` to record the actual verified versions. Make two edits:

    1. **Trivy section (around line 261-262):** After the `brew install trivy` line in the Setup code block, add a comment noting the verified version:
       ```
       # Verified: vX.Y.Z (2026-03-16)
       ```
       Use the actual version from `trivy --version` output.

    2. **Syft + Grype section (around lines 307-318):** After each install command in the Setup code block, add verified version comments:
       ```
       brew install syft
       # Verified: vX.Y.Z (2026-03-16)
       ```
       ```
       brew install grype
       # Verified: vX.Y.Z (2026-03-16) — requires >= 0.88.0 for DB schema v6
       ```
       Use actual versions from `syft version` and `grype version` output.

    The format is at Claude's discretion per CONTEXT.md, but must include actual version numbers and verification date. Do NOT change any other content in the file. Do NOT add scan examples or modify usage sections.
  </action>
  <verify>
    <automated>grep -c "Verified:" docs/development-security-stack-option-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/development-security-stack-option-1.md` contains at least 3 lines matching "Verified:" (one for each tool)
    - The Trivy section (lines 253-292 area) contains a "Verified:" comment with a version >= 0.69.2
    - The Syft+Grype section (lines 296-352 area) contains "Verified:" comments for both Syft and Grype
    - Grype verified line mentions ">= 0.88.0" or "DB schema v6"
    - No other content in the file is modified beyond adding version verification comments
  </acceptance_criteria>
  <done>Main documentation updated with actual installed versions for all three SCA tools, matching the Phase 5 pattern of recording verified versions.</done>
</task>

</tasks>

<verification>
Run all three version commands and confirm the doc has been updated:

```bash
trivy --version && syft version && grype version && grep "Verified:" docs/development-security-stack-option-1.md
```
</verification>

<success_criteria>
1. `trivy --version` shows >= 0.69.2
2. `syft version` shows a current version
3. `grype version` shows >= 0.88.0
4. `docs/development-security-stack-option-1.md` contains verified version notes for all three tools
5. Trivy vulnerability database has been refreshed
</success_criteria>

<output>
After completion, create `.planning/phases/06-sca-and-container-cli-tools/06-01-SUMMARY.md`
</output>
