---
phase: 07-sast-and-iac-cli-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/development-security-stack-option-1.md]
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-06]

must_haves:
  truths:
    - "semgrep --version succeeds and shows a current version"
    - "checkov --version succeeds and shows a current version"
    - "gitleaks version succeeds and shows a current version"
    - "Main doc has verified version notes for all three tools"
  artifacts:
    - path: "docs/development-security-stack-option-1.md"
      provides: "Verified version notes for Semgrep, Checkov, and Gitleaks"
      contains: "Verified:"
  key_links:
    - from: "semgrep binary"
      to: "$PATH"
      via: "pip install to system Python 3.10 bin"
      pattern: "semgrep --version"
    - from: "checkov binary"
      to: "$PATH"
      via: "pip install (already present)"
      pattern: "checkov --version"
    - from: "gitleaks binary"
      to: "$PATH"
      via: "brew install (already present from Phase 5)"
      pattern: "gitleaks version"
---

<objective>
Install Semgrep CE via pip and verify all three SAST/IaC CLI tools (Semgrep, Checkov, Gitleaks) are on PATH with current versions, then update the main documentation with verified version notes.

Purpose: Complete the security CLI toolchain (Phase 7 of 9) so Phase 8 can validate scanning output.
Output: Three CLI tools verified on PATH, main doc updated with version notes.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sast-and-iac-cli-tools/07-CONTEXT.md
@.planning/phases/07-sast-and-iac-cli-tools/07-RESEARCH.md
@.planning/phases/06-sca-and-container-cli-tools/06-01-SUMMARY.md

<interfaces>
<!-- No code interfaces needed -- this is CLI tool installation and doc editing only -->
<!-- Phase 6 established the version note pattern in the main doc: -->
<!-- After a tool's install command in a bash code block, add: -->
<!-- # Verified: vX.Y.Z (YYYY-MM-DD) -->
<!-- See lines 264, 313, 320 of docs/development-security-stack-option-1.md for examples -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Semgrep and verify all three tools on PATH</name>
  <files>none (CLI installation, no project files modified)</files>
  <read_first>
    - .planning/phases/07-sast-and-iac-cli-tools/07-RESEARCH.md (install commands, version expectations, pitfalls)
  </read_first>
  <action>
Install Semgrep CE via pip and verify all three tools are on PATH:

1. Install Semgrep:
   ```bash
   pip3 install semgrep --break-system-packages
   ```
   Use `pip3` explicitly (not `pip`) to route to system Python 3.10 where Checkov lives.

2. Verify Semgrep installed correctly:
   ```bash
   which semgrep
   # Expected: /Library/Frameworks/Python.framework/Versions/3.10/bin/semgrep or similar
   semgrep --version
   # Expected: 1.155.0 or similar current version
   ```
   If `which semgrep` shows an unexpected location (e.g., pyenv shim), investigate before proceeding.

3. Verify Checkov (already installed, no action needed):
   ```bash
   which checkov
   # Expected: /Library/Frameworks/Python.framework/Versions/3.10/bin/checkov
   checkov --version
   # Expected: 3.2.396 (or newer if pip resolved an upgrade as dependency)
   ```

4. Verify Gitleaks (already installed from Phase 5, no action needed):
   ```bash
   which gitleaks
   # Expected: /opt/homebrew/bin/gitleaks
   gitleaks version
   # Expected: v8.30.0 (note: subcommand, NOT --version flag)
   ```

Do NOT run any scans -- this phase is install/verify only. Scanning validation is Phase 8.
Do NOT upgrade Checkov -- keep existing version (conservative choice per Claude's discretion).
Do NOT reinstall Gitleaks -- already present from Phase 5.
  </action>
  <verify>
    <automated>semgrep --version && checkov --version && gitleaks version</automated>
  </verify>
  <acceptance_criteria>
    - `semgrep --version` exits 0 and prints a version number (e.g., 1.155.0)
    - `checkov --version` exits 0 and prints a version number (e.g., 3.2.396)
    - `gitleaks version` exits 0 and prints a version number (e.g., v8.30.0)
    - `which semgrep` returns a path (tool is on PATH)
  </acceptance_criteria>
  <done>All three tools respond to version commands with exit code 0 and a version string</done>
</task>

<task type="auto">
  <name>Task 2: Update main doc with verified version notes</name>
  <files>docs/development-security-stack-option-1.md</files>
  <read_first>
    - docs/development-security-stack-option-1.md (lines 166-190 for Semgrep setup block, lines 214-240 for Checkov setup block, lines 365-381 for Gitleaks setup block)
    - docs/development-security-stack-option-1.md (lines 260-265 for Trivy version note pattern from Phase 6)
    - docs/development-security-stack-option-1.md (lines 310-321 for Syft/Grype version note pattern from Phase 6)
  </read_first>
  <action>
Add verified version comments to the main doc following the Phase 6 pattern. Use the actual versions observed in Task 1 output.

**Phase 6 pattern reference** (from lines 264, 313, 320):
```
# Verified: v0.69.3 (2026-03-16) — requires >= 0.69.2
# Verified: v1.42.2 (2026-03-16)
# Verified: v0.109.1 (2026-03-16) — requires >= 0.88.0 for DB schema v6
```

Apply the same pattern to these three locations:

1. **Semgrep** (line ~170, after `pip install semgrep --break-system-packages` inside the Setup code block):
   Add on the line after `pip install semgrep --break-system-packages`:
   ```
   # Verified: v{ACTUAL_VERSION} (2026-03-16)
   ```
   Where {ACTUAL_VERSION} is whatever `semgrep --version` returned in Task 1.

2. **Checkov** (line ~217, after `pip install checkov --break-system-packages` inside the Setup code block):
   Add on the line after `pip install checkov --break-system-packages`:
   ```
   # Verified: v{ACTUAL_VERSION} (2026-03-16)
   ```
   Where {ACTUAL_VERSION} is whatever `checkov --version` returned in Task 1.

3. **Gitleaks** (line ~368, after `brew install gitleaks` inside the Setup code block):
   Add on the line after `brew install gitleaks`:
   ```
   # Verified: v{ACTUAL_VERSION} (2026-03-16) — CLI tool; also used as pre-push hook (Phase 5)
   ```
   Where {ACTUAL_VERSION} is whatever `gitleaks version` returned in Task 1.
   The suffix distinguishes the CLI verification from the Phase 5 hook documentation.

Do NOT modify any other sections of the document.
Do NOT change the install commands themselves -- only add comment lines after them.
  </action>
  <verify>
    <automated>grep -n "Verified:" docs/development-security-stack-option-1.md | grep -E "(semgrep|checkov|gitleaks|Semgrep|Checkov|Gitleaks)" || grep -c "Verified:" docs/development-security-stack-option-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Verified:" docs/development-security-stack-option-1.md` shows at least 6 lines (3 from Phase 6 + 3 new)
    - Line after `pip install semgrep --break-system-packages` contains `# Verified: v`
    - Line after `pip install checkov --break-system-packages` contains `# Verified: v`
    - Line after `brew install gitleaks` contains `# Verified: v`
    - No other lines in the document were changed (diff shows only 3 added lines)
  </acceptance_criteria>
  <done>Main doc has verified version notes for Semgrep, Checkov, and Gitleaks matching the Phase 6 pattern</done>
</task>

</tasks>

<verification>
All three tools on PATH with version verification:
```bash
semgrep --version && checkov --version && gitleaks version
```

Documentation updated with version notes:
```bash
grep -c "Verified:" docs/development-security-stack-option-1.md
# Expected: 6 (3 from Phase 6 + 3 from Phase 7)
```
</verification>

<success_criteria>
- TOOL-04: `semgrep --version` succeeds with current version
- TOOL-05: `checkov --version` succeeds with current version
- TOOL-06: `gitleaks version` succeeds with current version
- Main doc has 6 total "Verified:" comments (3 Phase 6 + 3 Phase 7)
</success_criteria>

<output>
After completion, create `.planning/phases/07-sast-and-iac-cli-tools/07-01-SUMMARY.md`
</output>
