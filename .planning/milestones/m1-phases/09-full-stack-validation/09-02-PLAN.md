---
phase: 09-full-stack-validation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docs/development-security-stack-option-1.md
autonomous: true
requirements:
  - PCOM-04

must_haves:
  truths:
    - "pre-commit run --all-files exits 0 in aws-zabbix-monitoring-solution"
    - "pre-commit run --all-files exits 0 in terraform-pipelines"
    - "pre-commit run gitleaks --all-files --hook-stage pre-push exits 0 in both repos"
    - "Main doc contains validation stamp for pre-commit full-stack pass"
  artifacts:
    - path: "docs/development-security-stack-option-1.md"
      provides: "Validation stamp in pre-commit section"
      contains: "Validated: pre-commit run --all-files passed"
  key_links:
    - from: "pre-commit run --all-files"
      to: "exit code 0"
      via: "all hooks pass or skip cleanly"
      pattern: "Passed|Skipped"
    - from: "pre-commit run gitleaks --all-files --hook-stage pre-push"
      to: "exit code 0"
      via: "gitleaks finds no secrets"
      pattern: "Passed"
---

<objective>
Run `pre-commit run --all-files` in both target repositories (aws-zabbix-monitoring-solution and terraform-pipelines) and achieve exit code 0. Fix genuine violations in source code or suppress false positives with inline annotations. Then validate Gitleaks pre-push hook separately in both repos. Finally, add a validation stamp to the main documentation.

Purpose: This is the capstone validation for Milestone 1 -- proving all hooks pass cleanly across the entire codebase, satisfying PCOM-04.

Output: Clean pre-commit runs in both repos, any code fixes committed, validation stamp in main doc.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-full-stack-validation/09-CONTEXT.md
@.planning/phases/09-full-stack-validation/09-RESEARCH.md
@.planning/phases/09-full-stack-validation/09-01-SUMMARY.md
</context>

<interfaces>
<!-- Hook relevance per repo (from RESEARCH.md investigation) -->

aws-zabbix-monitoring-solution hooks that RUN:
  - ruff, ruff-format (1 Python file)
  - shellcheck (3 shell scripts)
  - yamllint (YAML files)
  - markdownlint (many .md files)
  - eslint (CDK TypeScript source files)
  - npm-audit (package-lock.json)

aws-zabbix-monitoring-solution hooks that SKIP:
  - terraform_fmt, terraform_validate (no .tf files)
  - hadolint (no Dockerfiles outside node_modules)

terraform-pipelines hooks that RUN:
  - terraform_fmt (36 .tf files)
  - terraform_validate (36 .tf files, .terraform dirs exist)
  - shellcheck (4 shell scripts)
  - yamllint (YAML buildspecs)
  - markdownlint (23 .md files)

terraform-pipelines hooks that SKIP:
  - ruff, ruff-format (no .py files)
  - hadolint (no Dockerfiles)
  - eslint (no .js/.ts files)
  - npm-audit (no package-lock.json)

Suppression patterns (from prior phases):
  - ShellCheck: # shellcheck disable=SCXXXX  -- reason
  - Python/Ruff: # noqa: FXXX -- reason
  - ESLint: // eslint-disable-next-line @typescript-eslint/rule-name
  - Markdownlint: <!-- markdownlint-disable MDXXX -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Validate aws-zabbix-monitoring-solution -- all hooks pass</name>
  <files>
    (files vary based on which hooks report violations -- fixes will be committed in-repo)
  </files>
  <read_first>
    repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml
    repos/aws-zabbix-monitoring-solution/.markdownlint.json
    repos/aws-zabbix-monitoring-solution/.markdownlintignore
    repos/aws-zabbix-monitoring-solution/.gitleaksignore
  </read_first>
  <action>
    1. cd into `repos/aws-zabbix-monitoring-solution` (must be on `feature/add-pre-commit` branch -- verify with `git branch --show-current`)

    2. Ensure environment prereqs:
       - Verify `node_modules/` exists (needed for ESLint `npx eslint`): `ls node_modules/.package-lock.json`
       - If missing, run `npm install`

    3. Run `pre-commit run --all-files` and capture output
       - Expected: some hooks pass, some may fail on first run

    4. For each failing hook, apply the remediation strategy from CONTEXT.md:
       - **Auto-fixable hooks** (ruff-format, terraform_fmt): let them auto-fix, then `git add` the changed files and re-run
       - **Genuine code issues**: fix the code directly
       - **False positives or behavioral suggestions**: suppress with inline annotation and comment explaining why
       - Suppression patterns:
         - ShellCheck: `# shellcheck disable=SCXXXX  -- reason`
         - Ruff: `# noqa: FXXX -- reason`
         - ESLint: `// eslint-disable-next-line @typescript-eslint/rule-name`
         - Markdownlint: `<!-- markdownlint-disable MDXXX -->`

    5. After fixing/suppressing, stage all changes and re-run `pre-commit run --all-files`
       - Repeat until exit code 0

    6. Commit all fixes with message: "fix: resolve pre-commit violations for full-stack validation"
       - Add only specific changed files, not `git add .`

    7. Run Gitleaks validation separately:
       `pre-commit run gitleaks --all-files --hook-stage pre-push`
       - Must exit 0 (aws-zabbix has .gitleaksignore with 40 baseline suppressions from Phase 5)

    IMPORTANT: If any hook fails in an unexpected way (not a simple lint violation), STOP and report rather than guessing at a fix. Particularly watch for:
    - ESLint "Cannot find module" errors (npm install may be needed)
    - Thousands of files being checked (node_modules leaking through)
    - hadolint failures (should skip -- no Dockerfiles in tracked files)
  </action>
  <verify>
    <automated>cd repos/aws-zabbix-monitoring-solution && pre-commit run --all-files 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pre-commit run --all-files` in repos/aws-zabbix-monitoring-solution exits with code 0
    - Output shows each hook as either "Passed" or "Skipped" (no "Failed")
    - `pre-commit run gitleaks --all-files --hook-stage pre-push` exits with code 0
    - Any fixes or suppressions are committed to the feature/add-pre-commit branch
  </acceptance_criteria>
  <done>pre-commit run --all-files exits 0 in aws-zabbix-monitoring-solution, Gitleaks pre-push check passes</done>
</task>

<task type="auto">
  <name>Task 2: Validate terraform-pipelines -- all hooks pass</name>
  <files>
    (files vary based on which hooks report violations -- fixes will be committed in-repo)
  </files>
  <read_first>
    repos/terraform-pipelines/.pre-commit-config.yaml
    repos/terraform-pipelines/.markdownlint.json
    repos/terraform-pipelines/.markdownlintignore
    repos/terraform-pipelines/.gitleaksignore
  </read_first>
  <action>
    1. cd into `repos/terraform-pipelines` (must be on `feature/add-pre-commit` branch -- verify with `git branch --show-current`)

    2. Ensure environment prereqs:
       - Verify `.terraform/` directories exist in terraform module dirs (needed for terraform_validate)
       - Research from Phase 9 confirms 13 `.terraform/` directories already initialized
       - If terraform validate fails with provider errors, run `terraform init` in the affected directory

    3. Run `pre-commit run --all-files` and capture output
       - Expected: terraform_fmt, terraform_validate, shellcheck, yamllint, markdownlint will RUN
       - Expected: ruff, ruff-format, hadolint, eslint, npm-audit will SKIP (no matching files)

    4. For each failing hook, apply same remediation strategy as Task 1:
       - **Auto-fixable** (terraform_fmt): let auto-fix run, stage, re-run
       - **Genuine code issues**: fix directly
       - **False positives**: suppress inline with comment
       - ShellCheck suppression: `# shellcheck disable=SCXXXX  -- reason`
       - Markdownlint suppression: `<!-- markdownlint-disable MDXXX -->`

    5. After fixing/suppressing, stage changes and re-run until exit code 0

    6. Commit all fixes with message: "fix: resolve pre-commit violations for full-stack validation"
       - Add only specific changed files

    7. Run Gitleaks validation separately:
       `pre-commit run gitleaks --all-files --hook-stage pre-push`
       - Must exit 0

    IMPORTANT: terraform-pipelines has no JS/TS, no Python, no Dockerfiles. The ESLint and npm-audit hooks use `language: system` with file pattern matching -- they should skip cleanly because no files match. If they unexpectedly run or fail, STOP and investigate.
  </action>
  <verify>
    <automated>cd repos/terraform-pipelines && pre-commit run --all-files 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pre-commit run --all-files` in repos/terraform-pipelines exits with code 0
    - Output shows each hook as either "Passed" or "Skipped" (no "Failed")
    - `pre-commit run gitleaks --all-files --hook-stage pre-push` exits with code 0
    - Any fixes or suppressions are committed to the feature/add-pre-commit branch
  </acceptance_criteria>
  <done>pre-commit run --all-files exits 0 in terraform-pipelines, Gitleaks pre-push check passes</done>
</task>

<task type="auto">
  <name>Task 3: Add validation stamp to main documentation</name>
  <files>
    docs/development-security-stack-option-1.md
  </files>
  <read_first>
    docs/development-security-stack-option-1.md (lines 1314-1425 -- the Pre-commit Configuration section)
  </read_first>
  <action>
    1. Open `docs/development-security-stack-option-1.md`

    2. Find the "## Pre-commit Configuration" section (around line 1314)

    3. Add a validation stamp comment inside the YAML code block, right after the opening line `# .pre-commit-config.yaml`. Place it as the second line of the code block:
       ```
       # Validated: pre-commit run --all-files passed (aws-zabbix, terraform-pipelines) -- 2026-03-16
       ```
       This follows the established "Validated:" pattern from Phase 8 (e.g., `# Validated: semgrep scan completed, JSON output produced (2026-03-16)`)

    4. Also update the hadolint hook entry in the main doc's YAML code block from `hadolint-docker` to `hadolint` to match the actual deployed config:
       Change:
       ```yaml
       - id: hadolint-docker
       ```
       To:
       ```yaml
       - id: hadolint
       ```

    5. Update the pre-commit-terraform rev in the main doc code block from `v1.96.0` to `v1.105.0` to match the actual deployed config (was autoupdated in Phase 4).

    6. Update the ruff rev from `v0.8.4` to `v0.15.6` to match actual deployed config.

    7. Commit the doc change in the security_solution repo (not inside a target repo): "docs(09): add pre-commit validation stamp and sync config versions"
  </action>
  <verify>
    <automated>grep -c "Validated: pre-commit run --all-files passed" docs/development-security-stack-option-1.md && grep -c "id: hadolint$" docs/development-security-stack-option-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/development-security-stack-option-1.md` contains the line `# Validated: pre-commit run --all-files passed (aws-zabbix, terraform-pipelines) -- 2026-03-16`
    - The hadolint hook entry in the main doc shows `id: hadolint` (not `hadolint-docker`)
    - The pre-commit-terraform rev shows `v1.105.0` (not `v1.96.0`)
    - The ruff rev shows `v0.15.6` (not `v0.8.4`)
    - Change is committed
  </acceptance_criteria>
  <done>Main documentation has validation stamp and config versions synced with actual deployed state</done>
</task>

</tasks>

<verification>
1. `cd repos/aws-zabbix-monitoring-solution && pre-commit run --all-files` exits 0
2. `cd repos/terraform-pipelines && pre-commit run --all-files` exits 0
3. `cd repos/aws-zabbix-monitoring-solution && pre-commit run gitleaks --all-files --hook-stage pre-push` exits 0
4. `cd repos/terraform-pipelines && pre-commit run gitleaks --all-files --hook-stage pre-push` exits 0
5. `grep "Validated: pre-commit run --all-files passed" docs/development-security-stack-option-1.md` returns a match
6. `grep "id: hadolint$" docs/development-security-stack-option-1.md` returns a match (not hadolint-docker)
</verification>

<success_criteria>
- PCOM-04 satisfied: `pre-commit run --all-files` passes cleanly in both target repositories
- All 9 Tier 1 hooks execute without failure (passing or skipping as appropriate per repo)
- Gitleaks Tier 2 hook validated separately in both repos
- All violations either fixed in codebase or suppressed with inline annotations
- Main documentation updated with validation stamp and synced config versions
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-stack-validation/09-02-SUMMARY.md`
</output>
