---
phase: 04-infrastructure-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repos/aws-zabbix-monitoring-solution/package-lock.json
  - repos/aws-zabbix-monitoring-solution/package.json
  - repos/terraform-pipelines/.pre-commit-config.yaml
autonomous: true
requirements: [LINT-07, LINT-08, LINT-09]

must_haves:
  truths:
    - "npm audit hook passes cleanly on package-lock.json in aws-zabbix-monitoring-solution"
    - "terraform_fmt hook auto-formats HCL files in terraform-pipelines"
    - "terraform_validate hook validates HCL syntax in terraform-pipelines"
    - "All three infrastructure hooks pass without errors on their respective repos"
  artifacts:
    - path: "repos/aws-zabbix-monitoring-solution/package-lock.json"
      provides: "Updated dependencies with zero high/critical vulnerabilities"
      contains: "resolved"
    - path: "repos/terraform-pipelines/.pre-commit-config.yaml"
      provides: "Pre-commit config with terraform_fmt and terraform_validate hooks"
      contains: "terraform_fmt"
  key_links:
    - from: ".pre-commit-config.yaml (npm-audit hook)"
      to: "npm audit --audit-level=high"
      via: "local hook entry"
      pattern: "npm audit --audit-level=high"
    - from: ".pre-commit-config.yaml (terraform hooks)"
      to: "pre-commit-terraform"
      via: "antonbabenko/pre-commit-terraform repo"
      pattern: "terraform_fmt"
---

<objective>
Validate that all three Phase 4 infrastructure hooks work end-to-end: npm audit passes cleanly after fixing existing vulnerabilities in aws-zabbix-monitoring-solution, and terraform_fmt + terraform_validate pass cleanly against real .tf files in a cloned terraform-pipelines repo.

Purpose: Complete the Tier 1 linting hook suite by adding infrastructure-focused hooks (dependency audit, HCL formatting, HCL syntax validation) that catch issues before they reach CI.
Output: Both target repos have clean-passing infrastructure hooks with all existing violations fixed.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-infrastructure-hooks/04-CONTEXT.md
@.planning/phases/04-infrastructure-hooks/04-RESEARCH.md

<interfaces>
<!-- npm-audit hook config (already in .pre-commit-config.yaml) -->
```yaml
- repo: local
  hooks:
    - id: npm-audit
      name: npm audit
      entry: npm audit --audit-level=high
      language: system
      files: package-lock\.json$
      pass_filenames: false
```

<!-- terraform hooks config (already in .pre-commit-config.yaml) -->
```yaml
- repo: https://github.com/antonbabenko/pre-commit-terraform
  rev: v1.96.0
  hooks:
    - id: terraform_fmt
    - id: terraform_validate
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix npm audit violations and verify npm-audit hook</name>
  <files>repos/aws-zabbix-monitoring-solution/package-lock.json, repos/aws-zabbix-monitoring-solution/package.json</files>
  <read_first>
    - repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml
    - repos/aws-zabbix-monitoring-solution/package.json
    - .planning/phases/04-infrastructure-hooks/04-RESEARCH.md (npm audit section — 2 high vulns: minimatch ReDoS, fast-xml-parser stack overflow)
  </read_first>
  <action>
    1. Navigate to `repos/aws-zabbix-monitoring-solution/`
    2. Run `npm audit --audit-level=high` to confirm current state (expect exit code 1, 2 high vulnerabilities: minimatch ReDoS in aws-cdk-lib, fast-xml-parser stack overflow in @aws-sdk/xml-builder)
    3. Run `npm audit fix` to resolve auto-fixable vulnerabilities
    4. Run `npm audit --audit-level=high` again — expect exit code 0
    5. If unfixable vulnerabilities remain (no patch available):
       - Document which vulns are unfixable
       - Change hook entry to `npm audit --audit-level=critical` (Claude's discretion per CONTEXT.md)
       - Update the `.pre-commit-config.yaml` in both aws-zabbix-monitoring-solution AND security-platform repos
    6. Stage the updated `package-lock.json` and `package.json` (if changed)
    7. Run `pre-commit run npm-audit --files package-lock.json` to verify hook passes (exit 0)
    8. Commit the dependency fixes on the `feature/add-pre-commit` branch with message describing npm audit fix
  </action>
  <verify>
    <automated>cd repos/aws-zabbix-monitoring-solution && npm audit --audit-level=high; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm audit --audit-level=high` exits with code 0 in aws-zabbix-monitoring-solution
    - `pre-commit run npm-audit --files package-lock.json` exits with code 0 (Passed)
    - package-lock.json changes committed on feature/add-pre-commit branch
  </acceptance_criteria>
  <done>npm audit hook passes cleanly — zero high/critical vulnerabilities in aws-zabbix-monitoring-solution dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Clone terraform-pipelines, deploy pre-commit config, fix terraform fmt violations</name>
  <files>repos/terraform-pipelines/.pre-commit-config.yaml</files>
  <read_first>
    - repos/security-platform/.pre-commit-config.yaml (canonical config to copy)
    - .planning/phases/04-infrastructure-hooks/04-CONTEXT.md (locked decisions: clone to repos/terraform-pipelines/, use same config)
    - .planning/phases/04-infrastructure-hooks/04-RESEARCH.md (pitfall 5: yamllint/markdownlint may trigger on terraform-pipelines files)
  </read_first>
  <action>
    1. Clone terraform-pipelines repo:
       ```
       cd repos/
       gh repo clone OttawaCloudConsulting/terraform-pipelines
       ```
    2. Create a feature branch (consistent with established pattern):
       ```
       cd terraform-pipelines
       git checkout -b feature/add-pre-commit
       ```
    3. Copy canonical pre-commit config:
       ```
       cp ../security-platform/.pre-commit-config.yaml .
       ```
    4. Run `pre-commit autoupdate` to get latest pre-commit-terraform version (user locked decision: do NOT stay pinned to v1.96.0)
    5. Run `pre-commit install` to activate hooks
    6. Run `pre-commit run terraform_fmt --all-files` to identify formatting violations across all 36 .tf files
    7. If terraform_fmt auto-fixes files (rewrites in-place):
       - Review the changes (git diff)
       - Stage all reformatted .tf files
       - Re-run `pre-commit run terraform_fmt --all-files` to confirm clean pass (exit 0)
    8. Commit formatting fixes on feature/add-pre-commit branch
    9. If yamllint or markdownlint failures appear during hook runs, either:
       - Fix them (clean-slate pattern) if straightforward
       - Or run terraform hooks specifically with `pre-commit run terraform_fmt --all-files` to stay focused on phase scope
  </action>
  <verify>
    <automated>cd repos/terraform-pipelines && pre-commit run terraform_fmt --all-files; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `repos/terraform-pipelines/` directory exists with cloned repo
    - `repos/terraform-pipelines/.pre-commit-config.yaml` exists and contains `terraform_fmt`
    - `pre-commit run terraform_fmt --all-files` exits with code 0 (all .tf files properly formatted)
    - terraform-pipelines is on branch `feature/add-pre-commit`
    - pre-commit-terraform rev in .pre-commit-config.yaml is NOT v1.96.0 (was autoupdated)
  </acceptance_criteria>
  <done>terraform-pipelines has pre-commit installed with terraform_fmt passing cleanly on all .tf files — zero formatting violations</done>
</task>

<task type="auto">
  <name>Task 3: Verify terraform_validate hook and demonstrate end-to-end violation detection</name>
  <files>repos/terraform-pipelines/.pre-commit-config.yaml</files>
  <read_first>
    - repos/terraform-pipelines/.pre-commit-config.yaml (verify terraform_validate hook is configured)
    - repos/terraform-pipelines/examples/default/minimal/main.tf (test target for validation)
    - .planning/phases/04-infrastructure-hooks/04-RESEARCH.md (pitfall 2: terraform validate needs provider init — hook handles automatically; pitfall 3: validates per-directory not per-file)
  </read_first>
  <action>
    1. Run `pre-commit run terraform_validate --all-files` in terraform-pipelines
       - First run may take 30+ seconds per directory (downloads providers via automatic `terraform init`)
       - The hook handles init fallback automatically (tries validate, inits if it fails, retries)
       - terraform validate does NOT require AWS credentials (syntax/consistency check only)
    2. If validation errors appear:
       - Distinguish between real syntax errors (fix them) and provider/module resolution issues
       - For provider issues: ensure `terraform init` completed successfully in that directory
       - For real errors: fix the .tf files and commit
    3. After terraform_validate passes, demonstrate end-to-end violation detection for BOTH hooks:
       a. terraform_fmt test: In `examples/default/minimal/main.tf`, add a deliberately unformatted resource block (e.g., misaligned `=` signs), stage it, run `pre-commit run terraform_fmt --files examples/default/minimal/main.tf` — verify it catches and auto-fixes the violation
       b. terraform_validate test: Create a temporary `.tf` file with invalid HCL syntax (e.g., `resource "aws_instance" "test" { ami = }`) — stage it, run `pre-commit run terraform_validate --files <temp-file>` — verify it catches the syntax error
       c. npm audit test: In aws-zabbix-monitoring-solution, run `pre-commit run npm-audit --files package-lock.json` — verify it passes (exit 0) confirming the hook triggers on package-lock.json changes
    4. Clean up: revert any deliberately introduced violations (git checkout the test files, delete temp files)
    5. Final verification: run all three hooks one more time to confirm clean state:
       - In terraform-pipelines: `pre-commit run terraform_fmt --all-files && pre-commit run terraform_validate --all-files`
       - In aws-zabbix-monitoring-solution: `pre-commit run npm-audit --files package-lock.json`
  </action>
  <verify>
    <automated>cd repos/terraform-pipelines && pre-commit run terraform_validate --all-files; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `pre-commit run terraform_validate --all-files` exits with code 0 in terraform-pipelines
    - End-to-end test: terraform_fmt detects deliberately unformatted HCL (hook rewrites file, exits non-zero)
    - End-to-end test: terraform_validate detects invalid HCL syntax (hook exits non-zero)
    - End-to-end test: npm-audit hook passes on package-lock.json (exit 0)
    - All test artifacts cleaned up (no deliberate violations remain)
  </acceptance_criteria>
  <done>All three infrastructure hooks (npm-audit, terraform_fmt, terraform_validate) verified working end-to-end — each passes cleanly on existing code and catches newly introduced violations</done>
</task>

</tasks>

<verification>
All three infrastructure hooks pass cleanly:
1. `cd repos/aws-zabbix-monitoring-solution && pre-commit run npm-audit --files package-lock.json` — exit 0
2. `cd repos/terraform-pipelines && pre-commit run terraform_fmt --all-files` — exit 0
3. `cd repos/terraform-pipelines && pre-commit run terraform_validate --all-files` — exit 0
</verification>

<success_criteria>
- LINT-07: npm audit hook runs on package-lock.json changes and passes (zero high/critical vulns)
- LINT-08: terraform_fmt auto-formats HCL files on commit (verified with deliberate violation test)
- LINT-09: terraform_validate catches HCL syntax errors on commit (verified with invalid HCL test)
- All existing violations fixed (npm audit fix applied, terraform fmt formatting applied)
- Both target repos on feature/add-pre-commit branches with clean hook state
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure-hooks/04-01-SUMMARY.md`
</output>
