# Phase 20.1: Close gap: Retroactive VERIFICATION.md for Phases 14, 17, 18 - Pattern Map

**Mapped:** 2026-09-14
**Files analyzed:** 8 (4 creates, 1 near-certain modify, 3 conditional: 1 create + 2 modify groups)
**Analogs found:** 8 / 8 (5 exact, 2 role-match, 1 partial)

This is a documentation-verification phase. "Role" below means document role; "data flow" means how
evidence enters the document. No source code is written — every deliverable is a Markdown artifact
under `.planning/`, plus (conditionally) trivial corrections to this repo's `docs/`.

---

## File Classification

| New/Modified File | Create/Modify | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|---|
| `.planning/phases/14-workflow-foundation-and-action-pinning/14-VERIFICATION.md` | CREATE | verification report | read-only live-API query → tabular evidence | `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-VERIFICATION.md` | exact (SC4/Dependabot row: partial) |
| `.planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md` | CREATE | verification report | read-only live-API query → tabular evidence | `19-VERIFICATION.md` | exact |
| `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-VERIFICATION.md` | CREATE | verification report | read-only live-API query → tabular evidence | `19-VERIFICATION.md` | exact |
| `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` | MODIFY (near-certain) | deferred-item ledger | append-only status annotation | `19-.../deferred-items.md:26-30` (D-19-A RESOLVED block) | role-match (shape differs — see below) |
| `.planning/phases/14-workflow-foundation-and-action-pinning/deferred-items.md` | MODIFY (conditional) | deferred-item ledger | append-only status annotation | `19-.../deferred-items.md:26-30` | role-match (shape differs) |
| `.planning/phases/18-.../deferred-items.md` **or** `.planning/phases/20.1-.../deferred-items.md` | CREATE (conditional, only if a new non-trivial finding) | deferred-item ledger | new-finding record | `.planning/phases/19-.../deferred-items.md:1-30` | exact |
| `docs/development-security-stack-option-1.md`, `docs/milestone-plan/milestone-2-cicd-gate.md` | MODIFY (conditional, D-03 trivial-fix only) | reference documentation | in-place text correction | existing doc-drift fixes recorded in `17-.../deferred-items.md:12-17` | partial |
| `.planning/phases/20.1-.../20.1-0N-SUMMARY.md` | CREATE (GSD executor output) | execution summary | narrative + evidence | `.planning/phases/19-.../19-07-SUMMARY.md` | exact (standard GSD artifact) |

**Explicitly out of this map:** `.planning/ROADMAP.md` plan-checkbox updates and `.planning/STATE.md`
session/metric rows. Those are orchestrator/executor-owned GSD bookkeeping, not phase deliverables, and
they already have their own tooling path.

---

## Pattern Assignments

### `14-VERIFICATION.md`, `17-VERIFICATION.md`, `18-VERIFICATION.md` (verification report, live-query → tabular evidence)

**Analog:** `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-VERIFICATION.md` (139 lines — read it whole; it is the format contract per CONTEXT D-04).

All three files share one analog and one skeleton. The per-file differences are: which ROADMAP success
criteria fill the Observable Truths table, which CICD requirement fills Requirements Coverage, and which
`deferred-items.md` the Gaps Summary must report status on.

**Frontmatter pattern** (`19-VERIFICATION.md:1-7`):

```markdown
---
phase: 19-pipeline-validation-via-branch-target-prs
verified: 2026-09-13T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---
```

`phase:` must equal the containing directory slug byte-for-byte (`14-workflow-foundation-and-action-pinning`,
`17-sarif-upload-and-artifact-retention`, `18-configurable-gate-mode-and-branch-protection`). All three
phases have exactly 4 ROADMAP success criteria, so `score:` is `N/4`.

**Header block pattern** (`19-VERIFICATION.md:9-14`):

```markdown
# Phase 19: Pipeline Validation via Branch-Target PRs Verification Report

**Phase Goal:** The complete pipeline is proven end-to-end inside this repo against deliberately seeded findings, with no second repo required.
**Verified:** 2026-09-13 (session date; live repo state read 2026-09-13/14 per artifact timestamps)
**Status:** passed
**Re-verification:** No — initial verification
```

**DO NOT copy the `Re-verification` line verbatim.** These three reports are retroactive, authored ~3 days
after the phases closed. RESEARCH.md:463-497 (structure block; the line itself at `:479`) mandates the variant:
`**Re-verification:** No — initial verification (retroactive, authored in Phase 20.1)`.
`**Phase Goal:**` is copied verbatim from ROADMAP.md (`:54` for Phase 14, `:115` for Phase 17, `:137` for Phase 18).

**Method-section pattern** (`19-VERIFICATION.md:16-23`):

```markdown
## Method

This verification does not trust SUMMARY.md narrative. Every load-bearing claim in the four success criteria
was independently re-queried against the live `OttawaCloudConsulting/security-platform` repository via `gh`
(PRs #9–#12, run conclusions, check-runs, code-scanning alerts/analyses, repo variables, branch ruleset) in
this session, and the results are reproduced below next to the SUMMARY's claim. All spot-checks matched
exactly — no figure in the four SUMMARYs (19-03 through 19-07) that this report re-queried differed from the
live repository.
```

Copy the "does not trust SUMMARY.md narrative" opening and the "reproduced below next to the SUMMARY's
claim" closing. Add, per RESEARCH.md:498-505, three things `19-VERIFICATION.md` had no need to state:
(a) the retroactive authoring date and that three days of subsequent activity passed over this config
without breaking it; (b) the single pinned evidence snapshot (PR #13 / head `c06d2729…` / run `34870572604`
/ repo `visibility: public`, RESEARCH.md:182-200) that all three documents must cite identically; (c) the
read-only scope — no variable set, no ruleset PUT, no re-run of Phase 18's experiment.

A second Method-section analog worth borrowing for honesty-under-correction is
`20-VERIFICATION.md:20-32`, which narrates a mid-verification methodological correction (a stale local
clone) and states that "the second run is the one that counts as evidence." If the executor hits the same
class of mistake, narrate it that way rather than silently re-running.

**Observable Truths table pattern** (`19-VERIFICATION.md:27-36`) — the load-bearing pattern of the whole
document:

```markdown
### Observable Truths (ROADMAP Success Criteria)

| # | Truth (from ROADMAP.md) | Status | Evidence |
|---|---|---|---|
| SC1 | A branch-target PR carrying seeded findings for all five scan categories produces a detection from each of the five jobs | ✓ VERIFIED | PR #10, run `34786019516`. Live re-query of `code-scanning/analyses?ref=refs/pull/10/merge` in this session: `checkov=14, gitleaks=11, semgrep=8, trivy-fs=6, trivy-image=58, tflint=3` — matches 19-03-SUMMARY's per-job table exactly (rule ids: …). |

**Score:** 4/4 truths verified
```

Every Evidence cell has four parts, in this order:
1. **Identifier** — PR number, run ID, commit SHA, alert ID
2. **Command or endpoint** — `code-scanning/analyses?ref=refs/pull/10/merge`, `gh run view … --json conclusion`
3. **Observed value** — the actual numbers/strings returned *this session*
4. **Reconciliation** — "matches `NN-0X-SUMMARY`'s … exactly", naming the specific SUMMARY the claim came from

Row sources: Phase 14 SCs at `ROADMAP.md:59-62`, Phase 17 SCs at `ROADMAP.md:120-123`, Phase 18 SCs at
`ROADMAP.md:142-145`. Truth text is copied verbatim from those lines (RESEARCH.md's Don't-Hand-Roll table:
paraphrase is how "can be made required" silently becomes "is required").

**Counterintuitive-result narration** (`19-VERIFICATION.md:34`) — reuse this move, it is why Phase 19's
report reads as evidence rather than advocacy:

```markdown
Findings were NOT zero (8/11/14/58/6 across categories, per 19-06) — green reflects `continue-on-error: true`
under report-only, not a clean tree; this distinction is stated explicitly in the SUMMARY and is not a gap
```

Direct application: Phase 14 SC3's checkout SHA on `origin/main` no longer matches the pin Phase 14
recorded (RESEARCH.md PITFALL-6, lines 408-421). That drift **is** CICD-05's proof (Dependabot PR #5
bumped it), not a defect — say so in exactly this shape, leading with the observation, then the mechanism.

**Required Artifacts table pattern** (`19-VERIFICATION.md:38-47`):

```markdown
| Artifact | Expected | Status | Details |
|---|---|---|---|
| `repos/security-platform/fixtures/vulnerable.py` | SAST seed, on `main` | ✓ VERIFIED | Confirmed present on `origin/main` (live `contents` API read, header matches) |
| `.planning/REQUIREMENTS.md` | VAL-01 marked complete | ✓ VERIFIED | Live grep: line 32 `[x] VAL-01`, line 72 `Complete` |
```

Note the two-tier habit: product artifacts are confirmed via live `contents` API against
`security-platform`, planning artifacts via local line-cited grep in this repo. RESEARCH.md's Repo
Attribution Map (lines 121-147) is the authority for which side each artifact lives on — never cite this
repo's blueprint as evidence of pipeline behavior.

**Key Link Verification preamble pattern** (`19-VERIFICATION.md:51-57`) — reuse nearly verbatim; the same
tooling mismatch will recur:

```markdown
The `gsd-sdk query verify.key-links` tool reported most Phase 19 links as `verified: false, detail: "Source
file not found"`. This is a tooling-fit mismatch, not a defect: Phase 19's key links are live CI/GitHub API
facts (a run's check-run conclusions, a code-scanning alert, a repo variable's presence), not static
source-file greps the tool is built to pattern-match. Each of those live links was independently re-verified
by direct `gh`/`gh api` calls in this session …
```

Table rows use a distinct status token — `✓ WIRED (live)` (`19-VERIFICATION.md:59-64`):

```markdown
| From | To | Via | Status | Details |
|---|---|---|---|---|
| `gh variable set GATE_MODE=blocking` | `continue-on-error` evaluating false | env resolution chain (Phase 18) | ✓ WIRED (live) | Live run `34791497579` conclusion=`failure`, 5/5 check-runs failure |
```

That exact row is Phase 18's own chain — 18-VERIFICATION.md should carry the equivalent built on Phase 18's
own triple (`34668611172` success → `34669534855` failure → `34669700643` success, RESEARCH.md:202-227),
with Phase 19's PR #11 triple cited as an independent second witness.

**Requirements Coverage table pattern** (`19-VERIFICATION.md:66-70`):

```markdown
| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| VAL-01 | 19-01 through 19-07 | Full pipeline validated in this repo using branch-target PRs (no second repo required) | ✓ SATISFIED | Marked complete in `.planning/REQUIREMENTS.md` (both tracking locations, live-read). All four SC rows above independently re-verified live. No orphaned requirements — REQUIREMENTS.md maps only VAL-01 to Phase 19. |
```

Note the closing "No orphaned requirements — REQUIREMENTS.md maps only X to Phase N" sentence; it appears
in both 19 (`:70`) and 16 (`:93`). Per-file mapping (`REQUIREMENTS.md:11-15` for text, `:60-64` for
traceability): 14 → CICD-05; 17 → CICD-02, CICD-03; 18 → CICD-04, CICD-06. Requirement descriptions are
copied verbatim from `REQUIREMENTS.md:11-15`. Both tracking locations (the checkbox list and the
traceability table) get a live read.

**Anti-Patterns Found pattern** (`19-VERIFICATION.md:72-80`), including the pre-existing-marker disclaimer:

```markdown
| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | No `TBD`/`FIXME`/`XXX` debt markers found in any phase-modified file (inner: … ; outer: …) | — | — |

Note: `.planning/ROADMAP.md:194` contains `**Plans**: TBD` under the *Phase 20* section — a pre-existing
plan-count placeholder for a not-yet-planned future phase, unrelated to Phase 19's own content. Not a debt
marker introduced by this phase.
```

Anticipate the same hit: as of this mapping `ROADMAP.md:253` reads `- [ ] TBD (run /gsd-plan-phase 20.1
to break down)` — a Phase 20.1 planning placeholder, unrelated to Phases 14/17/18. That line is rewritten
when planning completes, so re-grep at execution time rather than citing this line number; if a `TBD`
remains anywhere, disclaim it in the same shape.
If a real non-blocking blemish turns up, `16-VERIFICATION.md:99-101` shows the severity vocabulary for a
populated table: `ℹ️ Info` and `⚠️ Warning` with an explicit "recorded as a warning, not deferred" rationale.

**Probe Execution pattern** (`19-VERIFICATION.md:101-105`):

```markdown
Step 7c: SKIPPED (no probes declared). Checked `grep -rn 'probe-' 19-0*-PLAN.md 19-VALIDATION.md` (no hits)
and `find repos/security-platform/scripts -path '*/tests/probe-*.sh'` (no hits). This phase is validated via
live GitHub Actions runs and API reads, not local probe scripts.
```

Re-run those two greps per phase rather than asserting the result.

**Human Verification Required pattern** (`19-VERIFICATION.md:107-113`) — the "already-closed checkpoint"
treatment, which 17 SC2 (inline PR annotations) needs:

```markdown
None. `grep -l human-check` on all 7 PLAN.md files returned no matches — no `<verify><human-check>` blocks
were deferred to end-of-phase. The one human-in-the-loop step this phase required (SC3's UI confirmation,
19-04 Task 2) was a `checkpoint:human-verify` gate that already halted execution and received the operator's
verbatim `approved` reply during the phase — it is closed, not deferred, and is reflected as VERIFIED in the
SC3 truth row above.
```

RESEARCH.md:531-536 fixes the wording for all three files: *"None — the UI confirmations these phases
required were `checkpoint:human-verify` gates closed during the original phases, cited above; no new human
verification is deferred by this report."* For 17 SC2 specifically, lead with the API-verifiable part
(`output.annotations_count` = 10/6/2/2/1 on our own check-runs, RESEARCH command 02-f) and corroborate with
the 17-05/17-07 checkpoint — do not rest the row on the checkpoint alone.

**Gaps Summary pattern** (`19-VERIFICATION.md:115-134`) — two moves to copy:

```markdown
No gaps. All four ROADMAP success criteria are independently confirmed against live GitHub state, not merely
asserted in SUMMARY.md prose. Two items are worth surfacing as informational context, neither a gap:

1. **SC2's literal wording departure.** …
2. **SC4's tautological-green limitation.** … Not concealed, not a gap in this phase's own scope.

Four items remain in `deferred-items.md` as explicitly OPEN and handed to Phase 20 / GSD tooling maintainers
(D-19-B, D-19-C, D-19-D, D-19-E) — none of them block VAL-01 or any of the four success criteria; they are
out-of-scope observations the phase surfaced rather than silently dropped. D-19-A is CLOSED (verified live:
`grep -c 'GH013|Push Protection'` = 4 on `origin/main`).
```

(a) numbered "informational, not a gap" items with an explicit non-concealment sentence; (b) a closing
paragraph giving each `deferred-items.md` entry a current OPEN/CLOSED verdict with the live evidence that
settles it. RESEARCH.md:571-595 already pre-resolves Phase 17 item 1 as CLOSED (`contents/cicd/.github/
workflows/security.yml` → 404 after PR #13) and flags items 2-7 as single greps in this repo. Phase 18 has
no `deferred-items.md` — state that explicitly rather than omitting the paragraph.

**Footer pattern** (`19-VERIFICATION.md:136-139`):

```markdown
---

*Verified: 2026-09-13*
*Verifier: Claude (gsd-verifier)*
```

(`16-VERIFICATION.md:116-117` uses `_underscores_` — prefer 19's asterisks for format parity with D-04.)

---

### `17-.../deferred-items.md` and `14-.../deferred-items.md` (deferred-item ledger, append-only status annotation)

**Analog:** `.planning/phases/19-pipeline-validation-via-branch-target-prs/deferred-items.md:26-30`

**Status-annotation pattern** (appended in place under the original D-19-A entry, original text untouched):

```markdown
**RESOLVED 2026-09-14 by plan 19-07.** The correction was carried by PR #11 and merged into `main` as merge
commit `b4cb207` on the operator's D-10 decision. Verified from `origin/main`:
`git show origin/main:fixtures/README.md | grep -c 'GH013\|Push Protection'` → **4** (was **0** before the
merge). This item is closed; it is not handed to Phase 20.
```

Shape: bold `**RESOLVED <date> by <plan>.**` → what closed it → `Verified from origin/main:` + the literal
command and its output, with a before/after contrast → an explicit "this item is closed / not handed to X".

**Structural mismatch the planner must handle.** The three ledgers have three different shapes, so the
D-19-A annotation cannot be pasted in as-is:

| File | Shape | Where a status annotation goes |
|---|---|---|
| `19-.../deferred-items.md` | `## D-19-X` headed sections (`:5`, `:31`, `:50`, `:59`, `:93`) | appended inside the section — the analog |
| `17-.../deferred-items.md` | a 7-row Markdown table (header `:9`, rows `:11-17`) plus `## Carried forward…` (`:19`) and `## Observed…` (`:27`) | **do not rewrite table cells** — append a new dated `## Status re-check 2026-09-14 (Phase 20.1)` section carrying a `\| # \| Live status \| Evidence \|` table, item numbers referencing rows 1-7 |
| `14-.../deferred-items.md` | numbered prose list (`:10`, `:15`, `:20`, `:24`) under `## gsd-sdk state handler defects` (`:5`) plus `## STATE.md metric rows…` (`:32`) | append a new dated section; do not renumber the existing list |

Phase 14's five items are `gsd-sdk` tooling defects, not pipeline defects — RESEARCH.md:573-576 is explicit
that none of them bear on CICD-05. Re-check whether they still reproduce; if unchanged, report OPEN and
out of scope for CICD-05's verdict, in the report's Gaps Summary and (optionally) as an appended status
note. Do not attempt to fix them here.

---

### `18-.../deferred-items.md` or `20.1-.../deferred-items.md` (CREATE, conditional)

**Analog:** `.planning/phases/19-pipeline-validation-via-branch-target-prs/deferred-items.md:1-30`

```markdown
# Phase 19 — Deferred Items

Out-of-scope discoveries logged during execution. Not actioned in the plan that found them.

## D-19-A — `fixtures/README.md` edit point 4 understates the control stack (found: plan 19-03, Task 1)

[what was expected] … Measured 2026-09-13 during 19-03: that is incomplete. [what was observed, with the
literal error string / API response] …

| Layer | Documented expectation | Measured 2026-09-13 |
|---|---|---|

**Not actioned here:** [why the phase boundary forbids it]. Owner: [phase or role].
```

Entry ID convention is `D-<phase>-<letter>` (`D-19-A` … `D-19-E`), so new Phase 20.1 findings would be
`D-20.1-A`, and a finding attributed to Phase 18's scope would be `D-18-A`.

**Open decision for the planner:** CONTEXT D-03 says non-trivial findings go "in that phase's
VERIFICATION.md Gaps Summary and in `deferred-items.md`" without saying which file when the verified phase
has none (Phase 18). Recommendation, for D-02 parity: create the ledger in the *verified* phase's own
directory (`18-.../deferred-items.md`) when the finding is about that phase's subject matter; use
`20.1-.../deferred-items.md` only for findings about the verification work itself (e.g. GSD tooling).
Whichever is chosen, apply it consistently across all three reports and say so in each Gaps Summary.

---

### `docs/…` trivial fixes (MODIFY, conditional — D-03 only)

**Analog:** the drift already catalogued in `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md:12-17` (items 2, 3, 4, 7).

These are single greps in **this** repo (RESEARCH.md:593): the blueprint's Grype-based `sca:` example, the
blueprint example's `push: branches: [main]` trigger, `docs/milestone-plan/milestone-2-cicd-gate.md` M2-F3's
`grype-results.json`, and the blueprint's seven `actions/checkout@<SHA>  # v4` comments. Per D-03, resolve
each to CLOSED / still-OPEN with evidence; fix inline **only** where the fix is a single value or reference.

Hard constraints from `CLAUDE.md` when touching `docs/development-security-stack-option-1.md`: preserve the
ASCII architecture diagrams, the 4-phase layered structure, the tool coverage matrices, and the security
tools comparison table. `docs/adr/` records are append-only — do not modify accepted ADRs (ADR-016/ADR-017
are cited as evidence here, not edited). Any change to `docs/adoption-guide.md` must keep
`bash scripts/check-adoption-guide.sh` at 15/15 PASS.

---

## Shared Patterns

### Status vocabulary (per-table, not global)

**Source:** `19-VERIFICATION.md` — tokens differ by table and must not be homogenised.
**Apply to:** all three VERIFICATION.md files.

| Table | Token | Line |
|---|---|---|
| Observable Truths | `✓ VERIFIED` | `19-VERIFICATION.md:31-34` |
| Required Artifacts | `✓ VERIFIED` (`✓ VERIFIED (via gsd-sdk verify.artifacts)` when tool-sourced) | `:42-47` |
| Key Link Verification | `✓ WIRED (live)` | `:61-64` |
| Requirements Coverage | `✓ SATISFIED` | `:70` |
| Behavioral Spot-Checks | `✓ PASS` (`✓ PASS (matches SUMMARY)` on the first reconciliation row) | `:86-99` |

`20-VERIFICATION.md` drops the `✓` glyph (`:40-43`, `:51-59`). Follow **19**, not 20 — D-04 names 19 as the
format contract.

### Non-clean status vocabulary (if anything fails to verify)

**Source:** three precedents, none in `19-VERIFICATION.md` (which is 100% clean).
**Apply to:** any row that cannot be closed on live evidence.

1. **Documented override in frontmatter** — `16-VERIFICATION.md:6-11`:

```yaml
overrides_applied: 1
overrides:
  - must_have: "Terraform provider and module version pinning is checked, and floating or unpinned versions are reported as findings (ROADMAP Success Criterion 3)"
    reason: "tflint's default bundled ruleset … does NOT flag a floating range such as version = \">= 3.0\" — measured live on run 34614017396 and documented in ADR-015's Tradeoff section. …"
    accepted_by: "cturner (via 16-07 Task 1 checkpoint, recorded verbatim as 'Approved — merge')"
    accepted_at: "2026-09-11T15:27:38Z"
```

   Its truth row reads `✓ PASSED (override)` (`16-VERIFICATION.md:38`) and its score line spells out the
   arithmetic: `**Score:** 3/4 truths directly verified + 1/4 passed via documented, user-approved override
   = 4/4` (`:41`). An override requires a *pre-existing* recorded human approval — it is not something this
   phase can mint for itself.
2. **`PARTIAL — see human verification`** in Requirements Coverage, paired with a numbered
   Test / Expected / Why-human block in Human Verification Required —
   `10-VERIFICATION.md:93` and `:112-133`.
3. **`✓ VERIFIED (was ✗ FAILED)`** plus a "Gap Closure" table when a defect is found and fixed inside the
   verification pass — `13-VERIFICATION.md:46`, `:71`. This is the shape for a D-03 trivial inline fix.

### Repo attribution discipline

**Source:** RESEARCH.md:121-147 (Repo Attribution Map); enforced in practice by `19-VERIFICATION.md:42-47`.
**Apply to:** every evidence cell in all three reports.

Any statement about *what the pipeline does* is sourced from `OttawaCloudConsulting/security-platform` via
`gh api`/`gh run`/`gh pr`/`gh variable`. Any statement about *what was decided or promised* may be sourced
from this repo. Phase 17's ledger records four separate cases where this repo's docs drifted ahead of or
behind the live workflow — this is why the rule exists.

### One pinned evidence snapshot across all three documents

**Source:** RESEARCH.md:182-200.
**Apply to:** all three VERIFICATION.md files.

PR `#13` / head `c06d2729b123094bcbb48e03c1155b72c7727b8c` / run `34870572604` / merged
`2026-09-14T17:04:38Z` / repo `visibility: public`. Re-confirm once, then carry verbatim into all three so
the documents cannot contradict each other. Record the `public` visibility explicitly — it is why the
code-scanning reads succeed with this token, and it is load-bearing for reproducibility.

### Behavioral Spot-Checks rows come from the research command reference

**Source:** `19-VERIFICATION.md:82-99` for the row shape; RESEARCH.md:228-331 for the row content.
**Apply to:** all three VERIFICATION.md files.

```markdown
| Behavior | Command | Result | Status |
|---|---|---|---|
| PR #10 state | `gh pr view 10 --json state,mergedAt,mergeCommit` | `MERGED`, `2026-09-13T22:34:18Z`, `80e91de…` | ✓ PASS (matches SUMMARY) |
| Branch ruleset unchanged | `gh api rules/branches/main --jq '.[].type'` | `deletion`, `non_fast_forward` | ✓ PASS |
```

One row per command ID in RESEARCH.md §Verified Command Reference — **27 commands total**, all executed
live during research: `02-a`…`02-f` (CICD-02, RESEARCH.md:233-247), `03-a`…`03-d` (CICD-03, `:248-261`),
`04-a`…`04-e` (CICD-04, `:262-289`), `05-a`…`05-d` (CICD-05, `:290-311`), `06-a`…`06-h` (CICD-06,
`:312-331`). The smaller subset tabled at RESEARCH.md:513-529 is only the IDs that map 1:1 to an SC row —
it is not the full set, and dropping the remainder would thin the spot-check table. Note the last
column sometimes carries a one-clause interpretation (`19-VERIFICATION.md:98`:
`✓ PASS — corroborates the "merged out of band by operator via web UI" deviation narrative`).

### Read-only / do-not-mutate discipline

**Source:** RESEARCH.md §Don't Hand-Roll and §Live Evidence Inventory (lines 538-570); project rule
`.claude/rules/defensive-protocol-v2-anti-slop.md`.
**Apply to:** every command any plan runs in this phase.

- `GATE_MODE` is unset by design — **do not set it**; empty `gh variable list` is the documented default
  (RESEARCH PITFALL-5).
- Do **not** re-run Phase 18's blocking/report-only experiment; the historical triple is still queryable.
- `scripts/set-required-checks.sh` is dry-run by default — never pass `--apply`.
- `scripts/check-workflow-uploads.sh` is the existing offline gate for CICD-02/03 invariants (10 checks,
  0 failures) — run it, don't write a replacement.
- Scripts are invoked as `bash scripts/<name>.sh`; never `chmod +x`.
- Install nothing. If a task proposes a package install, that is a scope escape.

### Known query traps to narrate rather than trip over

**Source:** RESEARCH.md:332-460 (PITFALL-1 … PITFALL-7).
**Apply to:** the executor's commands and the Method/Evidence prose.

`code-scanning/analyses?ref=refs/heads/main` returns `[]` (query `refs/pull/N/merge`); 6 SARIF categories ≠
5 artifacts ≠ 5 check-runs; 12 check-runs on a head SHA of which only 5 are this pipeline's (filter
`.app.id == 15368`); `branches/main/protection` 404s by design; `grep -c` on a `${{ }}` pattern silently
returns 0. Each of these produces a number that looks like a failure and is not — state the mechanism in
the evidence cell, the way `19-VERIFICATION.md:34` does.

---

## No Analog Found

| File / element | Role | Data Flow | Reason |
|---|---|---|---|
| 14 SC4 evidence row (Dependabot opened a PR) | verification report row | scheduled-job history query | No VERIFICATION.md in this repo verifies Dependabot behavior. Build the row from RESEARCH.md:202-227 instead: PR `#5` (`dependabot/github_actions/actions/checkout-7.0.1`, merged 2026-09-10T19:40:34Z), Dependabot job run `34872527866`, plus PITFALL-6's checkout-SHA drift as corroboration. Use the `19-VERIFICATION.md:31` four-part evidence-cell shape. |
| A fully retroactive VERIFICATION.md | verification report | — | All 15 existing VERIFICATION.md files were written contemporaneously with their phase. The retroactive framing (frontmatter `Re-verification` variant, Method paragraph, "three days of subsequent activity did not break it" argument) has no analog and comes from RESEARCH.md:498-505. |
| A VERIFICATION.md with `status: failed` | verification report | — | No precedent exists — all 15 are `status: passed`. If a phase fails, compose from the three non-clean precedents in §Shared Patterns rather than inventing a fourth vocabulary. |
| Deferred-item ledger for Phase 18 | deferred-item ledger | — | Phase 18 has no `deferred-items.md` (only 14, 16, 17, 19 do). See the open decision under §`18-.../deferred-items.md`. |

---

## Metadata

**Analog search scope:** `.planning/phases/*/`, `.planning/milestones/*/*/` (all 15 `*-VERIFICATION.md`),
all 4 `deferred-items.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `CLAUDE.md`,
`.claude/rules/`.
**Project skills:** `.claude/skills/` enumerated (cdk-testing, create-prd, itsg-assessment,
nist-csf-assessment, nist-fedramp-assessment, occ-skill-creator, occ-skill-refactor, rule-creator,
terraform-testing) — none apply to authoring Markdown verification reports, so no `SKILL.md` was loaded.
The governing project rules for this phase are `.claude/rules/defensive-protocol-v2-anti-slop.md`
(stop-on-failure, evidence standards, `bash script.sh` invocation, no `chmod +x`) and `CLAUDE.md`
(ADRs append-only; preserve blueprint diagrams/matrices).
**Files scanned:** 15 VERIFICATION reports (4 read in detail: 19 in full, 20/16/15 heads, 16/10/13 tails),
4 deferred-item ledgers, 2 planning indexes, project rules.
**Primary analog:** `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-VERIFICATION.md` (139 lines).
**Pattern extraction date:** 2026-09-14
