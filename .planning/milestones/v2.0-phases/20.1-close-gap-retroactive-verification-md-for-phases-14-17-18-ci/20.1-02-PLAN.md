---
phase: 20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci
plan: 02
type: execute
wave: 2
depends_on: ["20.1-01"]
files_modified:
  - .planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md
  - .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md
  - docs/development-security-stack-option-1.md
  - docs/milestone-plan/milestone-2-cicd-gate.md
  - docs/adoption-guide.md
autonomous: true
requirements: [CICD-02, CICD-03]
must_haves:
  truths:
    - "CICD-02 is re-confirmed from live code-scanning analyses on refs/pull/13/merge — six unique SARIF categories arriving distinctly server-side — not from SUMMARY prose"
    - "CICD-03 is re-confirmed from the live artifacts API on run 34870572604 — five unexpired artifacts whose expires_at minus created_at is 90 days"
    - "The 6 categories / 5 artifacts / 5 check-runs / 7 analyses asymmetry is written into the report as expected structure, never as drift"
    - "Empty code-scanning results on refs/heads/main are narrated as a designed consequence of the PR-only trigger, never as a CICD-02 failure"
    - "Each of the seven entries in Phase 17's deferred-items.md carries a current OPEN/CLOSED verdict backed by a command and its output"
  artifacts:
    - path: ".planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md"
      provides: "Retroactive Phase 17 verification report in 19-VERIFICATION.md format"
      contains: "Observable Truths (ROADMAP Success Criteria)"
      min_lines: 90
    - path: ".planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md"
      provides: "Dated Phase 20.1 status re-check section appended below the existing table"
      contains: "Status re-check"
  key_links:
    - from: ".planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md"
      to: "the pinned evidence snapshot from plan 20.1-01"
      via: "verbatim head SHA cited in the Method section"
      pattern: "c06d2729b123094bcbb48e03c1155b72c7727b8c"
    - from: ".planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md"
      to: "CICD-02 and CICD-03"
      via: "Requirements Coverage rows"
      pattern: "CICD-03"
---

<objective>
Author `17-VERIFICATION.md` — the retroactive verification report for Phase 17 (SARIF Upload and Artifact
Retention) — re-confirming CICD-02 and CICD-03 against live GitHub state, and resolve the current status of
every entry in Phase 17's `deferred-items.md`.

Purpose: Phase 17 shipped SARIF upload and 90-day artifact retention with seven plans and no VERIFICATION.md.
This plan closes that gap on live evidence and settles the seven deferred items that later phases have been
citing as open.
Output: `17-VERIFICATION.md`, an appended dated status section in Phase 17's `deferred-items.md`, and — only
where a fix is a single value or reference (D-03) — inline corrections to this repo's drifted docs.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-CONTEXT.md
@.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-RESEARCH.md
@.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-PATTERNS.md
@.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-01-SUMMARY.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-VERIFICATION.md

Shell variable used throughout: `R=OttawaCloudConsulting/security-platform`.

The pinned evidence snapshot comes from `20.1-01-SUMMARY.md` §Evidence Snapshot (pinned): PR `#13`, head
`c06d2729b123094bcbb48e03c1155b72c7727b8c`, run `34870572604`, `origin/main` HEAD as measured there, merged
`2026-09-14T17:04:38Z`, repo `visibility: public`. Use those values verbatim. If a live re-read disagrees
with them, STOP and report — do not silently re-pin.

Read-only scope: `gh api`, `gh pr view/list`, `gh run view/list/download`, `git diff`, `grep`,
`bash scripts/check-workflow-uploads.sh` only. No `gh variable set`, no `--apply`, no PR creation or merge,
no push to `security-platform`, no package install.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-query the CICD-02 and CICD-03 evidence set and resolve Phase 17's seven deferred items</name>
  <files>(no file writes in this repo — artifact downloads go to the session scratchpad directory)</files>
  <read_first>
    - `.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-RESEARCH.md` §Verified Command Reference (CICD-02 commands 02-a..02-f, CICD-03 commands 03-a..03-d), §Common Pitfalls PITFALL-1 / PITFALL-2 / PITFALL-3, §Deferred Items — current status
    - `.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-01-SUMMARY.md` (the pinned snapshot)
    - `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` (7-row table plus `## Carried forward…` and `## Observed…` sections)
    - `repos/security-platform/scripts/check-workflow-uploads.sh`
  </read_first>
  <action>
    Set `R=OttawaCloudConsulting/security-platform`. First re-prove the grep surface: `git -C
    repos/security-platform fetch origin` then `git -C repos/security-platform diff origin/main --
    .github/workflows/security.yml .github/workflows/pr-security.yml` must be empty, and
    `git -C repos/security-platform rev-parse origin/main` must equal the `origin/main` SHA recorded in
    `20.1-01-SUMMARY.md`. A Dependabot PR can land between waves; a non-empty diff or a moved SHA is a STOP.

    Then run the CICD-02 set, capturing raw output to the session scratchpad:
    - 02-a: `gh api "repos/$R/code-scanning/analyses?ref=refs/pull/13/merge" --jq '[.[]|{category,results_count}]'` — expect 7 analyses across 6 categories: `semgrep`(7), `checkov`(14), `trivy-fs`(6), `tflint`(0 and 3), `trivy-image`(58), `gitleaks`(11).
    - 02-b: `gh api "repos/$R/code-scanning/analyses?per_page=100" --jq '[.[]|.ref]|unique'` — expect only `refs/pull/{8,9,10,11,12,13}/merge` and NO `refs/heads/*`. This is the control that makes PITFALL-1 self-explaining: pair it with the fact that `pr-security.yml` declares `on: pull_request: {}` and no `push` trigger. An empty result on a `refs/heads/main` filter is designed behavior (Phase 17 D-02), not a defect — never report CICD-02 as failed on that basis.
    - 02-c: decoded `security.yml` piped through `grep -n 'category:'` — expect 6 hits, one per `upload-sarif` step, values `semgrep`, `checkov`, `trivy-fs`, `tflint`, `trivy-image`, `gitleaks`, all unique.
    - 02-d: same pipe, `grep -c 'upload-sarif@b96794f015dfd88f77b49b1c93e0fa7110f94c63'` — expect 6 (codeql-action v4.38.0).
    - 02-e: `bash scripts/check-workflow-uploads.sh` from `repos/security-platform` — expect `PASS - 10 checks, 0 failures`, exit 0. This gate exists because of the broken-but-green failure mode (uploads under `continue-on-error: true`); its PASS is positive security evidence for CICD-02/03, not a lint result.
    - 02-f: `gh api repos/$R/commits/c06d2729b123094bcbb48e03c1155b72c7727b8c/check-runs --jq '.check_runs[] | "\(.output.annotations_count) \(.app.id) \(.name)"'` — expect our five `app.id 15368` checks carrying annotation counts `IaC — Checkov`=10, `SCA — Trivy Filesystem`=6, `Secrets — Gitleaks`=2, `Container — Trivy Image`=2, `SAST — Semgrep CE`=1, and all third-party checks (apps 57789 / 46505) at 0. Always filter `select(.app.id == 15368)` when the question is "what does our workflow report" — the head SHA carries 12 check-runs from three apps (PITFALL-3).

    Then the CICD-03 set:
    - 03-a: `gh api repos/$R/actions/runs/34870572604/artifacts --jq '.artifacts[]|{name,created_at,expires_at,expired,size_in_bytes}'` — expect 5 artifacts, all `expired=false`: `semgrep-results`, `checkov-results`, `sca-results`, `trivy-image-results`, `gitleaks-results`.
    - 03-b: compute `expires_at - created_at` from 03-a — expect exactly 90 days (created 2026-09-14, expires 2026-12-13).
    - 03-c: decoded `security.yml` piped through `grep -n 'retention-days'` — expect 5 occurrences, all `90`.
    - 03-d: `gh run download 34870572604 -R $R -n sca-results --dir <session scratchpad>` — expect `npm-audit-1.json`, `pip-audit-1.json`, `tflint.sarif`, `trivy-fs.json`, `trivy-fs.sarif`. Download into the scratchpad, NEVER into the repo tree. This listing is the whole of Phase 17 SC4: npm audit and pip-audit emit no SARIF, and their numbered-per-input JSON reaching the artifact set is the documented fallback path.

    SECRET HANDLING (binding): the pipeline scans `fixtures/secret.env`, so gitleaks output contains seeded
    credential material. Cite findings as RuleID + File + line only. Any `--jq` over scan output must
    exclude `.Secret` and `.Match`. Do not download `gitleaks-results`. Nothing resembling a key, token or
    match string may be written into a file under `.planning/` or `docs/` — this repo's own gitleaks
    pre-commit hook and GitHub push protection will block the commit, and the leak would be real.

    Finally resolve each of the seven entries in Phase 17's `deferred-items.md` to CLOSED or still-OPEN with
    a command and its output:
    1. Stale `cicd/.github/workflows/security.yml` mirror template — `gh api repos/$R/contents/cicd/.github/workflows/security.yml` (research observed a 404 after PR #13 removed it; re-run rather than inherit).
    2. Blueprint's Grype-based `sca:` example — grep `docs/development-security-stack-option-1.md`.
    3. Blueprint example's `push: branches: [main]` trigger — grep the same file; note it conflicts with the PR-only policy behind PITFALL-1.
    4. `docs/milestone-plan/milestone-2-cicd-gate.md` M2-F3 listing `grype-results.json` — grep that file.
    5. Private-repo Code Security licence limitation — grep `docs/adoption-guide.md` (PR #13 shipped a "private-repo SARIF guard"; likely CLOSED).
    6. SARIF size/result limits at consumer scale — informational; state status.
    7. Blueprint's seven `actions/checkout@<SHA>  # v4` comments — grep the blueprint; interacts with PITFALL-6.
  </action>
  <verify>
    <automated>R=OttawaCloudConsulting/security-platform; test -z "$(git -C repos/security-platform diff origin/main -- .github/workflows/security.yml .github/workflows/pr-security.yml)" && test "$(gh api "repos/$R/code-scanning/analyses?ref=refs/pull/13/merge" --jq '[.[].category]|unique|length')" = "6" && test "$(gh api repos/$R/actions/runs/34870572604/artifacts --jq '[.artifacts[]|select(.expired==false)]|length')" = "5" && test "$(gh api repos/$R/commits/c06d2729b123094bcbb48e03c1155b72c7727b8c/check-runs --jq '[.check_runs[]|select(.app.id==15368)]|length')" = "5" && (cd repos/security-platform && bash scripts/check-workflow-uploads.sh) && echo CICD-02-03-EVIDENCE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `gh api "repos/$R/code-scanning/analyses?ref=refs/pull/13/merge" --jq '[.[].category]|unique'` returns exactly the six values `semgrep`, `checkov`, `trivy-fs`, `tflint`, `trivy-image`, `gitleaks`.
    - The unfiltered analyses list (02-b) contains no `refs/heads/` entry, and that observation is captured as the control for PITFALL-1.
    - `gh api repos/$R/actions/runs/34870572604/artifacts` returns 5 artifacts, all `expired: false`, and the `expires_at - created_at` arithmetic equals 90 days.
    - Decoded live `security.yml` yields 6 unique `category:` values and 5 `retention-days: 90` occurrences.
    - `gh run download 34870572604 -R $R -n sca-results` writes `npm-audit-1.json`, `pip-audit-1.json`, `tflint.sarif`, `trivy-fs.json`, `trivy-fs.sarif` into the session scratchpad; `git status --porcelain repos/ .planning/ docs/` shows no downloaded artifact files in any tracked tree.
    - `bash scripts/check-workflow-uploads.sh` prints `PASS - 10 checks, 0 failures` and exits 0.
    - All five `app.id == 15368` check-runs on `c06d272…` report non-zero `output.annotations_count`; the counts are recorded verbatim.
    - Each of Phase 17's seven deferred items has a recorded verdict (CLOSED or OPEN) with the literal command and its output; none is left as "unknown".
    - No `gh variable set`, `--apply`, `gh pr create`, `gh pr merge`, or push was executed.
  </acceptance_criteria>
  <done>CICD-02 and CICD-03 evidence captured live with raw output in the scratchpad, PITFALL-1/2/3 controls observed rather than assumed, and all seven Phase 17 deferred items resolved to a verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Author 17-VERIFICATION.md, append the deferred-items status re-check, and apply only single-value D-03 fixes</name>
  <files>.planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md, .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md, docs/development-security-stack-option-1.md, docs/milestone-plan/milestone-2-cicd-gate.md, docs/adoption-guide.md</files>
  <read_first>
    - `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-VERIFICATION.md` (the format contract; read it whole)
    - `.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-PATTERNS.md` (all section patterns, the status vocabulary table, the deferred-ledger structural-mismatch table)
    - `.planning/ROADMAP.md` lines 113-130 (Phase 17 goal + the four success criteria — copy truth text verbatim)
    - `.planning/REQUIREMENTS.md` (CICD-02 and CICD-03 descriptions verbatim, plus both tracking locations)
    - `.planning/phases/17-sarif-upload-and-artifact-retention/17-01-SUMMARY.md` through `17-07-SUMMARY.md` and `17-VALIDATION.md` (the claims being re-tested)
    - `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` (append target — table shape, do not rewrite cells)
    - `CLAUDE.md` (blueprint editing constraints: preserve ASCII diagrams, 4-phase structure, tool coverage matrices; `docs/adr/` is append-only)
  </read_first>
  <action>
    Create `17-VERIFICATION.md` in the Phase 17 directory using the same section order as
    `19-VERIFICATION.md` and the same frontmatter keys, with `phase: 17-sarif-upload-and-artifact-retention`
    matching the directory slug byte-for-byte and
    `**Re-verification:** No — initial verification (retroactive, authored in Phase 20.1)`.
    `status:` and `score: N/4` are written AFTER Task 1's commands have run, from what was observed — do not
    pre-fill `passed`. Non-clean rows use the PATTERNS vocabulary (`PARTIAL — see human verification`, or
    `✓ VERIFIED (was ✗ FAILED)` plus a Gap Closure table if a D-03 fix was applied); a frontmatter
    `override` requires a pre-existing recorded human approval and cannot be minted here.

    Method section: retroactive framing, the pinned snapshot from `20.1-01-SUMMARY.md` cited verbatim
    (PR #13, head `c06d2729b123094bcbb48e03c1155b72c7727b8c`, run `34870572604`, `origin/main` SHA, repo
    `visibility: public`, with the note that server-side scan records hang off the PR head while workflow
    YAML is read from `origin/main`), and the read-only scope.

    Observable Truths — four rows, truth text verbatim from ROADMAP, four-part Evidence cells (identifier ->
    command/endpoint -> observed value this session -> reconciliation naming the specific SUMMARY):
    - SC1 (per-scanner attribution in the Security tab, no tool overwriting another): 02-a proves the six categories arrive distinctly server-side; 02-c proves the mechanism in source. Cite both.
    - SC2 (inline annotations on the PR diff): lead with the API-verifiable half — 02-f's `output.annotations_count` of 10/6/2/2/1 on our five `app.id 15368` checks, with all third-party checks at 0 — then corroborate with the `checkpoint:human-verify` closed during 17-05/17-07 with the operator's recorded approval. Mark the checkpoint closed, not deferred. State plainly that visual rendering was not re-observed in this session and is not being claimed as re-verified.
    - SC3 (downloadable JSON artifacts, one per scan job including SCA sub-scans, with an explicit retention period): 03-a, 03-b, 03-c.
    - SC4 (a tool without native SARIF still reaches the Security tab or the artifact set via a documented conversion step): 03-d's file listing.

    Write the counting asymmetry into the document as expected structure, in its own short paragraph or in
    the SC1/SC3 cells: 6 SARIF categories (tflint uploads its own category from inside the SCA job), 5
    artifacts (`sca-results` bundles tflint's and trivy-fs's outputs), 5 `security / …` check-runs (one per
    job; the caller job emits none), and 7 analyses on PR #13 (tflint's single SARIF carries two drivers, so
    one category yields two analyses with results_count 0 and 3 under a shared `sarif_id`). Also state
    PITFALL-1 explicitly: `code-scanning/analyses?ref=refs/heads/main` is empty because `pr-security.yml` is
    `on: pull_request` only — a designed consequence of Phase 17 D-02, evidenced by 02-b's ref list.

    Required Artifacts: product artifacts (`security.yml` upload-sarif and upload-artifact steps, the five
    artifact names) confirmed via live `contents`/artifacts API against `security-platform`; planning
    artifacts (CICD-02/CICD-03 checkboxes and traceability rows in `REQUIREMENTS.md`, ADR-016 at
    `docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md`) via local line-cited grep. Read
    ADR-016 as evidence; do not edit it — `docs/adr/` is append-only.

    Key Link Verification: reuse the `19-VERIFICATION.md:51-57` preamble about `gsd-sdk query
    verify.key-links` reporting `verified: false, detail: "Source file not found"` for live CI/API facts —
    a tooling-fit mismatch, not a defect — then table the links with `✓ WIRED (live)`: upload-sarif step ->
    code-scanning analysis category; upload-artifact step -> artifacts API entry with `expires_at`;
    `steps.<id>.outcome` assertion -> the broken-but-green mitigation evidenced by 02-e. Cross-reference
    (do not duplicate) the byte-exact check-run-name consistency check, which plan 20.1-03 owns under
    CICD-04.

    Requirements Coverage: one row each for CICD-02 and CICD-03, descriptions copied verbatim from
    `REQUIREMENTS.md`, source plans 17-01 through 17-07, and the closing sentence "No orphaned requirements
    — REQUIREMENTS.md maps only CICD-02 and CICD-03 to Phase 17." Both tracking locations get a live local
    read with line numbers.

    Anti-Patterns Found: re-grep `TBD`/`FIXME`/`XXX` across Phase 17's files at execution time and disclaim
    any pre-existing placeholder. Behavioral Spot-Checks: one row per command executed in Task 1
    (02-a…02-f, 03-a…03-d, plus the clone re-diff), in the `| Behavior | Command | Result | Status |` shape.
    Probe Execution: actually run `grep -rn 'probe-' .planning/phases/17-*/17-0*-PLAN.md
    .planning/phases/17-*/17-VALIDATION.md` and `find repos/security-platform/scripts -path
    '*/tests/probe-*.sh'` and report the observed result. Human Verification Required: the standard "None —
    the UI confirmations these phases required were `checkpoint:human-verify` gates closed during the
    original phases, cited above; no new human verification is deferred by this report."

    Gaps Summary: numbered "informational, not a gap" items with an explicit non-concealment sentence,
    followed by a closing paragraph giving each of the seven `deferred-items.md` entries its current
    OPEN/CLOSED verdict with the evidence that settles it, naming the owner phase for anything still open.

    Then append to Phase 17's `deferred-items.md` a new dated section `## Status re-check 2026-09-14 (Phase
    20.1)` carrying a `| # | Live status | Evidence |` table whose item numbers reference the existing rows
    1-7. DO NOT rewrite any existing table cell, heading, or carried-forward section — append only.

    D-03 inline fixes, tightly bounded: a fix is permitted ONLY when it is a single value or a single
    reference — for example replacing a `grype-results.json` filename with the artifact name the pipeline
    actually produces, or correcting a `# v4` version comment to the pinned version now on `origin/main`.
    Anything larger — restructuring the blueprint's `sca:` example, changing its trigger block, rewriting
    adoption-guide prose — is NOT trivial: leave it, record it in the Gaps Summary and in the appended
    ledger section with an owner, and move on. Before changing any docs line, apply Chesterton's Fence and
    state in the report why the current value exists. Hard constraints when touching
    `docs/development-security-stack-option-1.md`: preserve the ASCII architecture diagrams, the 4-phase
    layered structure, the tool coverage matrices and the security tools comparison table. If
    `docs/adoption-guide.md` is touched at all, run `bash scripts/check-adoption-guide.sh` and require
    15/15 PASS. Never edit an accepted ADR. If no fix qualifies as trivial, modify none of the three docs
    files — an empty docs diff is a valid and expected outcome.

    Commit with `gsd-sdk query commit "docs(20.1-02): add retroactive 17-VERIFICATION.md" --files <the
    files actually changed>` — stage by explicit path only; this repo has ~370 pre-existing uncommitted
    `.claude/` files, so never `git add -A`. Confirm with `git diff HEAD~1 HEAD --name-only`.
  </action>
  <verify>
    <automated>V=.planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md; test -f "$V" && grep -q '^phase: 17-sarif-upload-and-artifact-retention$' "$V" && grep -q '^status: ' "$V" && grep -q '^score: ' "$V" && grep -qF 'c06d2729b123094bcbb48e03c1155b72c7727b8c' "$V" && grep -qF '34870572604' "$V" && grep -qF 'Observable Truths (ROADMAP Success Criteria)' "$V" && grep -qF 'CICD-02' "$V" && grep -qF 'CICD-03' "$V" && grep -qF 'retention-days' "$V" && grep -qF 'refs/pull/13/merge' "$V" && grep -qF 'retroactive, authored in Phase 20.1' "$V" && grep -qF 'Gaps Summary' "$V" && grep -qF 'Status re-check' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md && echo 17-VERIFICATION-OK</automated>
  </verify>
  <acceptance_criteria>
    - `17-VERIFICATION.md` exists with frontmatter keys `phase`, `verified`, `status`, `score`, `overrides_applied`; `phase` equals `17-sarif-upload-and-artifact-retention`.
    - The Observable Truths table has four rows SC1-SC4 with truth text matching ROADMAP.md Phase 17 verbatim, and `**Score:** N/4` is present.
    - Every Evidence cell contains a literal command or endpoint plus the value observed this session; no cell rests solely on a SUMMARY citation.
    - The document states the 6 categories / 5 artifacts / 5 check-runs / 7 analyses asymmetry as expected structure, including the tflint two-driver explanation.
    - The document states that `code-scanning/analyses` filtered to `refs/heads/main` is empty by design because `pr-security.yml` is `on: pull_request` only, citing the observed ref list.
    - The SC2 row cites `output.annotations_count` values `10`, `6`, `2`, `2`, `1` and states that visual rendering was not re-observed in this session.
    - The SC3 row cites 5 artifacts and the 90-day `expires_at - created_at` arithmetic; the SC4 row lists the five `sca-results` filenames including `npm-audit-1.json` and `pip-audit-1.json`.
    - Requirements Coverage has CICD-02 and CICD-03 rows with descriptions verbatim from `REQUIREMENTS.md` plus the "No orphaned requirements" sentence.
    - The Gaps Summary assigns each of the seven Phase 17 deferred items an explicit OPEN or CLOSED verdict with evidence.
    - `deferred-items.md` gained a `## Status re-check 2026-09-14 (Phase 20.1)` section and `git diff HEAD~1 HEAD -- .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` shows additions only.
    - No secret material: `git diff HEAD~1 HEAD` contains no `Secret`/`Match` field value from any scan output, and this repo's gitleaks pre-commit hook passes on the commit.
    - If `docs/adoption-guide.md` was modified, `bash scripts/check-adoption-guide.sh` exits 0 with 15/15 PASS. If no D-03 fix qualified as trivial, `git diff HEAD~1 HEAD --name-only` lists no file under `docs/`.
    - No accepted file under `docs/adr/` appears in `git diff HEAD~1 HEAD --name-only`.
  </acceptance_criteria>
  <done>`17-VERIFICATION.md` exists with a status derived from observed live output across all four Phase 17 success criteria, CICD-02 and CICD-03 re-confirmed from the code-scanning and artifacts APIs, all seven deferred items given a verdict, and any doc drift either fixed as a single-value correction or recorded with an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This session -> `OttawaCloudConsulting/security-platform` (live GitHub) | Authenticated `gh` token can mutate the repo under verification; this plan crosses read-only |
| Downloaded scan artifacts / SARIF -> committed markdown in this repo | `sca-results` and gitleaks output carry seeded credential material from `fixtures/secret.env` |
| Live pipeline behavior -> this repo's `docs/` | A D-03 "fix" written from documentation rather than from live output would re-introduce the drift Phase 17 already logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-02a | Information Disclosure | Evidence cells and artifact downloads | mitigate | Cite RuleID + File + line only; `--jq` filters exclude `.Secret` and `.Match`; `gitleaks-results` is not downloaded; `gh run download` targets the session scratchpad via `--dir`, never the repo tree; acceptance criterion asserts the commit contains no secret field value |
| T-20.1-07 | Repudiation | Broken-but-green uploads (`continue-on-error: true` masking a 403) | mitigate | Re-run `bash scripts/check-workflow-uploads.sh` (10 checks) and record its PASS as security evidence, plus the live analyses count proving uploads actually landed |
| T-20.1-08 | Tampering | D-03 inline edits to `docs/` | mitigate | Only single-value/single-reference corrections permitted; blueprint diagrams, 4-phase structure and matrices preserved; `docs/adr/` untouched (append-only); `bash scripts/check-adoption-guide.sh` must stay 15/15 if the adoption guide is touched |
| T-20.1-01 | Tampering | `gh` commands against `security-platform` | mitigate | Read-only allowlist; no `gh variable set`, `--apply`, PR create/merge, or push |
| T-20.1-09 | Spoofing | Which run/PR the evidence belongs to | mitigate | Snapshot values read from `20.1-01-SUMMARY.md` and re-proven by a clone diff + `origin/main` SHA equality check at task start; divergence is a STOP |
| T-20.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs occur; a task proposing one is a scope escape to be rejected |
</threat_model>

<verification>
- `test -f .planning/phases/17-sarif-upload-and-artifact-retention/17-VERIFICATION.md`
- `gh api "repos/OttawaCloudConsulting/security-platform/code-scanning/analyses?ref=refs/pull/13/merge" --jq '[.[].category]|unique|length'` returns `6`
- `gh api repos/OttawaCloudConsulting/security-platform/actions/runs/34870572604/artifacts --jq '[.artifacts[]|select(.expired==false)]|length'` returns `5`
- `cd repos/security-platform && bash scripts/check-workflow-uploads.sh` exits 0
- `git diff HEAD~1 HEAD --name-only` lists only the intended files
</verification>

<success_criteria>
- `17-VERIFICATION.md` exists, follows the `19-VERIFICATION.md` section order, and scores all four ROADMAP Phase 17 success criteria on live evidence.
- CICD-02 re-confirmed from six unique live SARIF categories; CICD-03 re-confirmed from five unexpired artifacts with 90-day retention.
- The counting asymmetry and the PR-only trigger consequence are documented as expected structure.
- All seven Phase 17 deferred items carry a current verdict, in both the report and an appended ledger section.
- No secret material and no non-trivial doc rewrite entered the commit.
</success_criteria>

<output>
Create `.planning/phases/20.1-close-gap-retroactive-verification-md-for-phases-14-17-18-ci/20.1-02-SUMMARY.md` when done.
</output>
