---
phase: 20.1
slug: close-gap-retroactive-verification-md-for-phases-14-17-18-ci
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-14
---

# Phase 20.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — documentation repo (`CLAUDE.md`: "not buildable software"). Validation = live `gh` reads + two existing static gates. Do NOT introduce pytest/jest. |
| **Config file** | none; `repos/security-platform/scripts/check-workflow-uploads.sh` is the offline gate |
| **Quick run command** | `bash repos/security-platform/scripts/check-workflow-uploads.sh` (offline, < 2s, exit 0 = PASS) |
| **Full suite command** | Quick run + the §Verified Command Reference set (02-a…02-e, 03-a…03-d, 04-a…04-e, 05-a…05-d, 06-a…06-g) in 20.1-RESEARCH.md + `bash repos/security-platform/scripts/set-required-checks.sh --out /tmp/ruleset.json` |
| **Estimated runtime** | ~2s offline; ~60–90s for the full live set (network-bound) |

---

## Sampling Rate

- **After every task commit:** Run `bash repos/security-platform/scripts/check-workflow-uploads.sh`
- **After every document authored:** Run that phase's command subset from RESEARCH.md §Verified Command Reference, paste raw output into the VERIFICATION.md Evidence cells
- **Before phase gate:** All three VERIFICATION.md files exist with a `status:` in frontmatter; full live command set re-run once; `gsd-sdk query verify.artifacts` clean
- **Max feedback latency:** ~90 seconds (network-bound live `gh` calls)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 20.1-01-T1 | 01 | 1 | CICD-05 | T-20.1-01, T-20.1-04, T-20.1-05 | Evidence snapshot pinned (PR #13 / `c06d272…` / run `34870572604`) and local clone verified byte-identical to live `origin/main` for the 6 load-bearing files | integration | `git -C repos/security-platform diff origin/main -- <6 load-bearing files>` → empty | ✅ | ⬜ pending |
| 20.1-01-T2 | 01 | 1 | CICD-05 | T-20.1-02, T-20.1-03, T-20.1-06 | `14-VERIFICATION.md` authored covering all 4 Phase 14 SCs; Phase 14 deferred-items re-checked | file check + integration | `test -f 14-VERIFICATION.md`; Dependabot bump-merge evidence per §Test Framework | ✅ | ⬜ pending |
| 20.1-02-T1 | 02 | 2 | CICD-02, CICD-03 | T-20.1-01, T-20.1-09 | 6 unique SARIF categories reach code scanning on the PR ref; 5 artifacts unexpired at 90-day retention | integration (live API) | `gh api "repos/$R/code-scanning/analyses?ref=refs/pull/13/merge" --jq '[.[].category]\|unique\|length'` → `6`; `gh api repos/$R/actions/runs/34870572604/artifacts --jq '[.artifacts[]\|select(.expired==false)]\|length'` → `5` | ✅ | ⬜ pending |
| 20.1-02-T2 | 02 | 2 | CICD-02, CICD-03 | T-20.1-02a, T-20.1-07, T-20.1-08 | `17-VERIFICATION.md` authored; all 7 Phase 17 deferred items resolved; only single-value D-03 `docs/` fixes applied, if any | file check | `test -f 17-VERIFICATION.md`; `bash scripts/check-workflow-uploads.sh` → 10/10 PASS | ✅ | ⬜ pending |
| 20.1-03-T1 | 03 | 3 | CICD-04, CICD-06 | T-20.1-10, T-20.1-12 | 5 byte-exact required-check contexts (incl. U+2014 em dash) live on the pinned commit; gate-mode fallback terminates at `report-only` | integration | `gh api repos/$R/commits/c06d272…/check-runs --jq '[.check_runs[]\|select(.app.id==15368)]\|length'` → `5`; `grep -cF "continue-on-error: \${{ env.GATE_MODE == 'report-only' }}"` → `11` | ✅ | ⬜ pending |
| 20.1-03-T2 | 03 | 3 | CICD-04, CICD-06 | T-20.1-11, T-20.1-13, T-20.1-14 | `18-VERIFICATION.md` authored; opposite verdicts reproducible on the same config (`34669534855` failure / `34669700643` success); ruleset dry-run only | file check + integration | `test -f 18-VERIFICATION.md`; `gh run view 34669534855 --json conclusion` → `failure`; `34669700643` → `success` | ✅ | ⬜ pending |
| 20.1-03-T3 | 03 | 3 | CICD-04 | T-20.1-10, T-20.1-13 | Cross-document 5-context consistency gate passes across all three VERIFICATION.md reports; 19-format parity confirmed | integration | Required-check context strings byte-identical (`grep -F`) across 5 source documents | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. All validation instruments (live `gh` API, two existing static gates in `security-platform`) exist and were executed successfully during research (20.1-RESEARCH.md, §Verified Command Reference, 26 commands run). No new test file, fixture, or framework install is needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GitHub required-status-check ruleset rendering (branch protection UI) | CICD-04 | `gh api` returns raw ruleset JSON, not the rendered UI state; helper script output is the accepted proxy | Run `bash repos/security-platform/scripts/set-required-checks.sh --out /tmp/r.json`, confirm exit 0 and 5 contexts present in JSON |

---

## Validation Sign-Off

- [x] All tasks have automated verify (live `gh` API / static grep) — no Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none missing)
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Note (tooling-fit, inherited from Phase 19):** `gsd-sdk query verify.key-links` will report `verified: false, detail: "Source file not found"` for this phase's key links — the links are live CI/API facts, not static source greps. This is a tooling-fit mismatch, not a defect; do not let it block the phase gate.

**Approval:** pending
