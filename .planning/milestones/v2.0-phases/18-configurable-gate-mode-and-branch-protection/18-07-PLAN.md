---
phase: 18-configurable-gate-mode-and-branch-protection
plan: 07
type: execute
wave: 7
depends_on: [18-06]
files_modified:
  - docs/adr/adr017-configurable-gate-mode-and-required-checks.md
  - docs/adr/README.md
autonomous: true
requirements: [CICD-04, CICD-06]

must_haves:
  truths:
    - "The decision record states what was decided, what it cost, and what was measured rather than assumed"
    - "The five-not-six correction to D-06 is recorded with its evidence, so it cannot resurface as a discrepancy"
    - "What remains unverified is named in those words, including the item ADR-016 handed to this phase"
    - "The record is discoverable from the ADR index"
  artifacts:
    - path: "docs/adr/adr017-configurable-gate-mode-and-required-checks.md"
      provides: "the accepted decision record for CICD-04 and CICD-06"
      contains: "**Status:** Accepted"
      min_lines: 45
    - path: "docs/adr/README.md"
      provides: "the ADR-017 index row"
      contains: "ADR-017"
  key_links:
    - from: "docs/adr/README.md"
      to: "docs/adr/adr017-configurable-gate-mode-and-required-checks.md"
      via: "a markdown link in the index table, matching the ADR-015/ADR-016 row shape"
      pattern: "adr017-configurable-gate-mode-and-required-checks\\.md"
    - from: "ADR-017"
      to: "ADR-002"
      via: "citation as the decision this one extends — never an edit to ADR-002"
      pattern: "ADR-002"
---

<objective>
Record the phase's decisions as ADR-017, a new file, following the shape Phases 16 and 17 established —
and record the two places where the evidence contradicted the decisions as written, with the evidence
attached.

Purpose: `docs/adr/` is append-only per CLAUDE.md, and this phase changes the enforcement posture of the
entire CI gate. Two of its inputs need permanent correction rather than a note in a summary: D-06's sixth
required check does not exist, and D-07 step 1's "red when seeded" is unobservable in report-only by
D-04's own definition. Both are corrections of fact, not of intent, and both will otherwise resurface as
apparent discrepancies during verification or in Phase 19 and 20.

ADR-016 also handed this phase an explicit open item (whether a Dependabot-triggered run can obtain
`security-events: write`), recorded "in those words so that Phases 18 through 20 do not build on them".
That thread gets acknowledged here rather than dropped.

Output: ADR-017 accepted and indexed, ADR-001 and ADR-002 untouched.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-CONTEXT.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-RESEARCH.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-PATTERNS.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-04-SUMMARY.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-SUMMARY.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-06-SUMMARY.md

<interfaces>
<!-- FILE NAME (matches the ADR-015/016 convention):                                                        -->
<!--   docs/adr/adr017-configurable-gate-mode-and-required-checks.md                                        -->
<!-- HEADER SHAPE (ADR-016 L1-5):                                                                            -->
<!--   # ADR-017: <Title Case Title>                                                                         -->
<!--   **Status:** Accepted                                                                                  -->
<!--   **Date:** <execution date>                                                                             -->
<!--   **Addresses:** CICD-04, CICD-06 — <one clause naming both outcomes>                                    -->
<!-- SECTION SHAPE: Context (bold-lead constraint bullets, each a measured fact plus its consequence);        -->
<!--   Decision (bold-lead decision bullets with the measurement or citation inline, including an             -->
<!--   "X was considered and rejected" clause per ADR-015 L26-29); Consequences (opens `**Improved:**` with   -->
<!--   measured before/after numbers, then a series of `**Tradeoff — <one-line summary>.**` paragraphs);      -->
<!--   `## What was NOT verified` (what WAS measured and must not be re-litigated, then a numbered list of    -->
<!--   what remains open, in those words).                                                                    -->
<!--                                                                                                        -->
<!-- INDEX ROW (append after the ADR-016 row, same four columns, same date format, Accepted):                 -->
<!--   | [ADR-016](adr016-sarif-upload-attribution-and-artifact-retention.md) | SARIF Upload Attribution      -->
<!--     and Scan Artifact Retention | 2026-09-11 | Accepted |                                                -->
<!--                                                                                                        -->
<!-- THE TWO CORRECTIONS THIS RECORD MUST CARRY:                                                              -->
<!--   D-06 said SIX job-level checks including the `security` wrapper. FIVE. A caller job that `uses:` a      -->
<!--     reusable workflow emits NO check run of its own. Evidence, three independent observations:            -->
<!--     14-02 (a one-job callee produced exactly one check, `security / Placeholder`); the Phase 18 research  -->
<!--     read of the Phase 17 head SHA (12 names, none a bare `security`); and 18-04's own run.                -->
<!--   D-07 step 1 said "confirm the checks appear green when clean, red when seeded, while still in           -->
<!--     report-only". Red-on-findings is unobservable in report-only by D-04's definition. Step 1 confirms    -->
<!--     the checks APPEAR and conclude green; red is observable only after the flip.                          -->
<!--                                                                                                        -->
<!-- ADR-016 L63 HAND-OFF, to be acknowledged: whether a Dependabot-triggered run can obtain                   -->
<!--   `security-events: write` is not verified, and the observation was handed to Phase 18. This phase did    -->
<!--   not resolve it either. 17-04 likewise left open whether `upload-artifact` itself succeeds on fork and   -->
<!--   Dependabot runs (it authenticates with ACTIONS_RUNTIME_TOKEN, not GITHUB_TOKEN).                        -->
<!--                                                                                                        -->
<!-- ADR-002 (Accepted, append-only) is the decision this one EXTENDS: it mandated branch protection in the    -->
<!--   abstract and noted that the gate has no enforcement effect whatsoever without it. Cite it. Never edit   -->
<!--   it, and never edit ADR-001.                                                                             -->
</interfaces>

<notes>
**Append-only means append-only.** Create one new file and add one row to the index. Do not touch
ADR-001, ADR-002, or any other existing record, even to fix a typo.

**Write what was measured, not what the plan predicted.** 18-04 and 18-05 produced real numbers; use
those. Where a plan written earlier in the phase expected something different from what was observed, the
observation wins and the ADR says so — that is the precedent 17-06 set when it recorded annotation
rendering as observed rather than unverified.

**Five, never six.** If the number six appears as the size of the required-check set, it is wrong.

**Do not claim the fork question is resolved.** It is LOW-confidence community evidence, deliberately
routed to Phase 19 (VAL-01). Saying so is the decision; guessing is not.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-017</name>
  <files>docs/adr/adr017-configurable-gate-mode-and-required-checks.md</files>
  <read_first>
    - docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md (the structural analog in full — header block, bold-lead Context bullets, bold-lead Decision bullets, the `**Improved:**` plus `**Tradeoff — …**` Consequences shape, and the `## What was NOT verified` closing section this record must mirror; also the L63 hand-off to this phase)
    - docs/adr/adr002-require-branch-protection.md (the accepted decision ADR-017 extends and must cite without editing)
    - docs/adr/adr001-remove-continue-on-error.md (the accepted decision that carves out the upload/artifact tolerances this phase deliberately left unconditional)
    - docs/adr/adr015-tflint-terraform-pin-checking.md (the smaller-scale example of a decision bullet carrying an explicit "considered and rejected" clause, and of stating a limit as plainly as a benefit)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-SUMMARY.md (the measured before/after numbers the Consequences section opens with, and the operator's Q5 decision)
  </read_first>
  <action>
    Create `docs/adr/adr017-configurable-gate-mode-and-required-checks.md` with the header block
    (`**Status:** Accepted`, the execution date, `**Addresses:** CICD-04, CICD-06 — …`) and four sections
    in ADR-016's shape.

    Context — bold-lead constraint bullets, each a measured fact with its consequence: every scan step
    carried an unconditional `continue-on-error: true` since Phase 15, so report-only was hardcoded and no
    consumer could choose otherwise. A called workflow reads the CALLER repository's variables while a
    caller's workflow-level `env` is NOT propagated into it, which makes the repository variable the only
    mechanism that lets a copy-paste consumer switch modes without editing YAML. A `workflow_call` string
    input with no default is the empty string, and an explicitly-passed empty value counts as *provided*
    and suppresses a declared default — so a bare `vars` passthrough in a `with:` block is a trap. GitHub
    does not validate a `type: string` input against a value list. A caller job that `uses:` a reusable
    workflow emits no check run of its own. `PUT /repos/{o}/{r}/rulesets/{id}` replaces the entire
    document. And this repository's `fixtures/` exists to make every scanner fire, against a ruleset
    reporting `bypass_actors: []` and `current_user_can_bypass: "never"`.

    Decision — bold-lead bullets, each a full decision sentence with its measurement or citation inline:
    the flag is a single global string enum `blocking` | `report-only` defaulting to `report-only`
    (D-01/D-02/D-03) rather than a boolean or per-job flags; it is resolved exactly once, at workflow
    level, through the input-then-variable-then-literal chain; it is applied to exactly eleven scan-step
    tolerances as `== 'report-only'` and never `!= 'blocking'`, because the first fails closed on a typo
    and the second fails open, leaving a repository un-gated while branch protection reports green; the
    eleven ADR-001 upload and artifact tolerances stay unconditional, since a transient upload hiccup must
    not block a merge; the caller in this repository passes no `with:` block at all, which is what makes
    "switch modes without editing YAML" literally true and what a literal would have destroyed; a literal
    `with: gate_mode: blocking` is nonetheless the documented form for a PUBLIC repository intending to
    block, as the mitigation for the unresolved fork question; enum validation is one intolerant step
    inside each of the five jobs and never a sixth job, because a sixth job would emit a sixth check run
    and break the frozen set; three SCA scan guards gained `always() &&` so blocking mode does not skip
    scanners whose verify steps then fail on missing reports; the required-check list is FIVE byte-exact
    contexts pinned to `integration_id` 15368, accompanied by a `pull_request` rule because status checks
    alone do not require a PR (extending ADR-002); ruleset changes are read-modify-write only, shipped as
    `scripts/set-required-checks.sh`, dry-run by default; and this repository's own `main` [record the
    operator's Q5 decision from 18-05 and the reason].

    Include the rejected alternatives explicitly, ADR-015 style: per-step expressions on all eleven lines
    (works, but repeats the fallback semantics eleven times and leaves the variable path nowhere to plug
    in); a terminal per-job gate step reading `steps.*.outcome` (would have avoided the skipped-step
    problem entirely but contradicts locked D-04, so it was rejected on decision grounds, not technical
    ones); classic branch protection (the repository is governed by a ruleset and the classic endpoint
    404s by design); a severity-cutoff knob (pip-audit's JSON carries no severity field, so no cutoff can
    apply uniformly across the five jobs); and requiring the six per-driver code-scanning checks (17-07
    observed the `Checkov` code-scanning check concluding failure on a PR that merged, so requiring it
    would have blocked that PR).

    Record BOTH corrections as their own bullets with the evidence attached: D-06's five-not-six, citing
    all three observations by identifier — 14-02's single-job check name, the research read of the Phase 17
    head SHA `fbe0071…` returning twelve names, and THIS phase's own run id from 18-04 (cite the number, not
    "this phase's run", so a reader can go and look); and D-07 step 1's unobservable red, with the corrected
    three-step ordering.

    Consequences — open with `**Improved:**` and the measured before/after from 18-04 and 18-05: one
    commit, two verdicts, five checks green then five red then green again, switched by a single CLI
    command with no YAML edit; artifact count and code-scanning categories identical in both modes. Then
    `**Tradeoff — …**` paragraphs for each real cost: blocking is severity-agnostic, so a low-severity
    finding blocks a merge exactly as a critical one does; the flag is repository-wide, so it cannot be
    varied per branch or per PR; the `always() &&` guards changed report-only behaviour too, deliberately,
    by running scanners that previously skipped after an unrelated failure; the five job names and the
    caller's job id are now load-bearing configuration, so renaming any of them silently converts a
    required check into a permanently-pending one; and an invalid gate mode fails each job at its first
    step, before checkout, so the trailing `always()` upload steps also fail on missing files — noisy, and
    chosen deliberately over a late failure.

    Close with `## What was NOT verified`: first what WAS measured and should not be re-litigated (the five
    names and app id, the two live verdicts on one commit, the artifact and category survival under
    blocking, the ruleset's pre-existing rule set). Then a numbered list of what remains open, in those
    words: whether a fork PR can read repository variables at all, deliberately routed to Phase 19
    (VAL-01), with the consequence stated — a public repo running blocking could see a fork degrade to
    report-only while a required check still reports green; ADR-016's open question about whether a
    Dependabot-triggered run can obtain `security-events: write`, which this phase did not resolve either;
    whether `upload-artifact` succeeds on fork and Dependabot runs, inherited from 17-04 and still an
    observation rather than a measurement; and, unless the operator opted in during 18-05, the exact
    `pull_request` rule parameter set GitHub requires on `PUT`, which has been exercised only against a
    local dry run.

    Commit with an explicit pathspec: `docs(18-07): record ADR-017 for configurable gate mode and required checks`.
  </action>
  <verify>
    <automated>test -f docs/adr/adr017-configurable-gate-mode-and-required-checks.md && wc -l docs/adr/adr017-configurable-gate-mode-and-required-checks.md</automated>
    <automated>python3 - docs/adr/adr017-configurable-gate-mode-and-required-checks.md <<'PY'
import re, sys
src = open(sys.argv[1], encoding="utf-8").read()
assert src.startswith("# ADR-017:"), src[:60]
for needle in ["**Status:** Accepted", "**Date:**", "**Addresses:** CICD-04, CICD-06",
               "## Context", "## Decision", "## Consequences", "## What was NOT verified",
               "**Improved:**", "ADR-002", "ADR-001"]:
    assert needle in src, "missing section or citation: %r" % needle
assert src.count("**Tradeoff") >= 3, "Consequences needs its Tradeoff paragraphs"
for c in ["security / SAST — Semgrep CE", "security / IaC — Checkov",
          "security / SCA — Trivy Filesystem", "security / Container — Trivy Image",
          "security / Secrets — Gitleaks"]:
    assert c in src, "missing byte-exact context: %r" % c
assert re.search(r"\bfive\b", src, re.I), "the five-not-six correction must be stated in words"
assert "report-only" in src and "blocking" in src and "GATE_MODE" in src
assert "15368" in src
for open_item in ["fork", "Dependabot", "Phase 19"]:
    assert open_item in src, "unverified item not carried forward: %r" % open_item
print("ADR-017 structure, citations and corrections all present")
PY</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git diff HEAD~1 HEAD --name-only</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git log --oneline -1 -- docs/adr/adr001-remove-continue-on-error.md docs/adr/adr002-require-branch-protection.md; echo "the last commit touching ADR-001/ADR-002 must predate this phase (append-only)"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists at the exact path `docs/adr/adr017-configurable-gate-mode-and-required-checks.md`, is at least 45 lines, and opens with `# ADR-017:` plus the three header lines including `**Addresses:** CICD-04, CICD-06`
    - All four sections are present in ADR-016's order, Consequences opens with `**Improved:**` and carries at least three `**Tradeoff — …**` paragraphs
    - The five contexts appear byte-exactly; `integration_id` 15368 is named; the required-check set is described as five and never as six
    - Both corrections are recorded as their own bullets: D-06's five-not-six with all three observations cited, and D-07 step 1's unobservable red with the corrected ordering
    - The rejected alternatives include all five named in the action, each with its rejection reason
    - The Consequences state the measured before/after from 18-04/18-05 with real run identifiers, not adjectives
    - `## What was NOT verified` lists the fork-`vars` question routed to Phase 19, ADR-016's Dependabot `security-events` hand-off, the `upload-artifact` fork/Dependabot observation, and (unless the operator opted in) the unexercised `pull_request` PUT parameter set
    - `git log` shows no commit from this phase touching ADR-001 or ADR-002
    - `git diff HEAD~1 HEAD --name-only` lists exactly the new ADR file
  </acceptance_criteria>
  <done>The enforcement posture of the whole gate has a decision record that a future reader can act on, including the parts that are still unknown.</done>
</task>

<task type="auto">
  <name>Task 2: Index ADR-017 and cross-check the record against live state</name>
  <files>docs/adr/README.md</files>
  <read_first>
    - docs/adr/README.md (the file being modified — the index table's four columns, its date format, and the ADR-015/ADR-016 rows appended by Phases 16 and 17, which this row must match exactly)
    - docs/adr/adr017-configurable-gate-mode-and-required-checks.md (the record just written — its `#` heading must be reproduced verbatim as the row's title)
  </read_first>
  <action>
    Append exactly one row to the index table in `docs/adr/README.md`, immediately after the ADR-016 row:
    the same four columns, a markdown link whose target is the new file name, the title copied verbatim
    from the ADR's `#` heading (minus the `ADR-017: ` prefix, matching how the other rows are written), the
    same date format as the ADR's `**Date:**` line, and the status `Accepted`. Change nothing else in the
    file — not the intro paragraph, not the red-team link, not any existing row.

    Then cross-check the record against live state rather than against the plan: re-read the five check-run
    names from `gh api repos/OttawaCloudConsulting/security-platform/commits/SHA/check-runs` for the phase
    PR's head SHA and byte-compare them against the strings as written in ADR-017 and in the blueprint
    corrected by 18-06. Compare the bytes, not the rendering — a hyphen, an en dash and an em dash look
    similar and behave differently. If any string differs, fix the documents rather than the API, and
    report it prominently: a required context that differs by one byte is a branch that can never merge.

    Also confirm the ruleset state described in ADR-017 still matches live: read
    `rules/branches/main` and state whether what the ADR says about this repository's own `main` is still
    true at the moment of writing.

    Commit with an explicit pathspec: `docs(18-07): index ADR-017`.
  </action>
  <verify>
    <automated>grep -n "ADR-017" docs/adr/README.md</automated>
    <automated>python3 - docs/adr/README.md docs/adr/adr017-configurable-gate-mode-and-required-checks.md <<'PY'
import sys
readme = open(sys.argv[1], encoding="utf-8").read().splitlines()
adr = open(sys.argv[2], encoding="utf-8").read().splitlines()
title = adr[0].split(":", 1)[1].strip()
rows = [l for l in readme if l.startswith("| [ADR-")]
last_two = rows[-2:]
print("\n".join(last_two))
assert "ADR-016" in last_two[0] and "ADR-017" in last_two[1], "the new row must sit directly after ADR-016"
row = last_two[1]
assert "adr017-configurable-gate-mode-and-required-checks.md" in row, row
assert title in row, "row title must match the ADR heading verbatim: %r" % title
assert row.rstrip().endswith("Accepted |"), row
assert row.count("|") == rows[-2].count("|"), "column count differs from the ADR-016 row"
print("index row shape matches the established convention")
PY</automated>
    <automated>cd repos/security-platform && SHA=$(git rev-parse HEAD) && gh api "repos/OttawaCloudConsulting/security-platform/commits/$SHA/check-runs" --jq '.check_runs[] | select(.app.id == 15368) | .name' | sort > /tmp/live-contexts.txt; cat /tmp/live-contexts.txt</automated>
    <automated>python3 - /tmp/live-contexts.txt docs/adr/adr017-configurable-gate-mode-and-required-checks.md docs/development-security-stack-option-1.md <<'PY'
import sys
live = [l.rstrip("\n") for l in open(sys.argv[1], encoding="utf-8") if l.strip()]
assert len(live) == 5, "expected five github-actions check runs, got %d: %r" % (len(live), live)
for doc in sys.argv[2:]:
    src = open(doc, encoding="utf-8").read()
    missing = [c for c in live if c not in src]
    assert not missing, "%s does not contain these live contexts byte-exactly: %r" % (doc, missing)
    print("ok", doc)
for c in live:
    print("bytes:", c.encode("utf-8"))
PY</automated>
    <automated>gh api repos/OttawaCloudConsulting/security-platform/rules/branches/main --jq '.[].type'</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git diff HEAD~1 HEAD --name-only</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/README.md` contains exactly one new row, positioned directly after the ADR-016 row, with the same column count, the verbatim ADR title, the ADR's own date, and the status `Accepted`
    - Nothing else in `README.md` changed — the diff is a single added line
    - The live check-run read returns exactly five `github-actions` names, and every one appears byte-exactly in BOTH ADR-017 and the corrected blueprint; the UTF-8 byte dump of each is recorded in the SUMMARY
    - The live `rules/branches/main` read is recorded and matches what ADR-017 states about this repository's own `main`
    - `git diff HEAD~1 HEAD --name-only` lists exactly `docs/adr/README.md`
  </acceptance_criteria>
  <done>The record is discoverable, and its most dangerous strings have been compared against the live API byte for byte rather than read for plausibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision record → future phases | Phases 19 and 20 will build on what this file says; an unqualified claim here becomes an unexamined assumption there |
| Documented context strings → live check-run names | A one-byte difference between them is a permanently unmergeable branch |
| Append-only ADR directory → this plan's edits | Editing an accepted record would erase a decision rather than supersede it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-22 | Repudiation | An unverified claim recorded as settled | mitigate | `## What was NOT verified` is mandatory and its four items are enumerated as acceptance criteria; the fork question is explicitly routed to Phase 19 rather than answered |
| T-18-03 | Denial of Service | A context string that differs by one byte from the live check-run name | mitigate | Task 2 byte-compares the ADR and the blueprint against a live `check-runs` read and dumps the UTF-8 bytes |
| T-18-23 | Tampering | Editing ADR-001 or ADR-002 instead of superseding them | mitigate | Append-only stated in the notes; `git log` on both files asserted to carry no commit from this phase; ADR-002 is cited as extended |
| T-18-24 | Repudiation | The D-06 "six" resurfacing as a verification discrepancy | mitigate | The five-not-six correction is a named decision bullet with three cited observations, and the word six is forbidden as the set size |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installed; documentation edits plus read-only `gh` calls |
</threat_model>

<verification>
- ADR-017 exists at the conventional path with all four sections, the header block, and at least three Tradeoff paragraphs.
- Five byte-exact contexts and `integration_id` 15368 present; the set is described as five.
- Both corrections recorded with evidence; five rejected alternatives each with a reason.
- `## What was NOT verified` carries the fork question, the ADR-016 Dependabot hand-off, the upload-artifact observation, and the PUT parameter set.
- One new index row, correctly positioned and shaped; nothing else in README changed.
- Live byte comparison of the five contexts against both documents passes.
- No commit from this phase touches ADR-001 or ADR-002.
</verification>

<success_criteria>
- The phase's decisions, and the two corrections to its own inputs, are permanently recorded with evidence.
- What is still unknown is written down as unknown, including the item inherited from ADR-016.
- The record is indexed and its critical strings are proven byte-identical to live state.
</success_criteria>

<output>
Create `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-07-SUMMARY.md` when done.
Record: the ADR's section inventory and line count; the two corrections as written; the list of rejected
alternatives; the four items in the not-verified section; the index row as appended; the live five-context
read with its UTF-8 byte dump and the comparison result against both documents; and the live
`rules/branches/main` read at the time of writing.
</output>
