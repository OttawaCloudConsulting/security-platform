---
phase: 16-sca-ecosystem-coverage
plan: 05
type: execute
wave: 5
depends_on: [16-04]
files_modified: []
autonomous: true
requirements: [SCA-01, SCA-02, SCA-03]

must_haves:
  truths:
    - "On a live run against a repo containing package-lock.json, the SCA job's log shows npm dependency vulnerabilities WITH their severity levels (Criterion 1)"
    - "On the same run, the SCA job's log shows Python advisories from pip-audit (Criterion 2)"
    - "On the same run, the SCA job's log shows at least one Terraform provider-or-module PINNING rule id under tflint v0.64.0 (Criterion 3)"
    - "The five check-run names are unchanged from 15-05-SUMMARY and the pull request is still mergeable despite the findings (D-04 report-only)"
    - "The CI rule-id set is compared against the workstation v0.61.0 set rather than assumed identical"
    - "Criterion 4's clean-skip path is reported honestly as proven by the smoke gate's empty-repo test and the static step guards, NOT claimed as a live observation"
  artifacts:
    - path: ".planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md"
      provides: "Live-run evidence: PR number, run id, per-sub-scan log excerpts, severity histogram, advisory count, rule-id set, check-run names, mergeable state"
      contains: "check-run"
  key_links:
    - from: "repos/security-platform feature branch"
      to: "OttawaCloudConsulting/security-platform pull request"
      via: "git push -u origin HEAD then gh pr create"
      pattern: "feature/phase-16-sca-ecosystem-coverage"
    - from: "sca job detect steps"
      to: "sca job scan and verification steps"
      via: "outputs.found guard resolving true on a repo that has all three ecosystems"
      pattern: "FOUND"
---

<objective>
Prove the three sub-scans on live infrastructure. Push the Phase 16 branch to
`OttawaCloudConsulting/security-platform`, open a pull request, and read the resulting run: the SCA check
must show npm vulnerabilities with severity levels, Python advisories, and at least one Terraform pinning
rule id — with the five check-run names unchanged and the PR still mergeable.

Purpose: every claim so far is a workstation measurement. CI differs in three ways that matter and have
never been exercised together — tflint is v0.64.0 there against v0.61.0 here, pip-audit and tflint are
installed fresh by the job, and the detect steps run under a real `$GITHUB_OUTPUT`. 16-RESEARCH explicitly
instructs that the rule ids be re-confirmed under v0.64.0 rather than assumed.

Output: no source file changes. A pull request, a run id, and a SUMMARY carrying measured live evidence
for Success Criteria 1, 2 and 3 plus an honest account of how Criterion 4 is evidenced.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-five-parallel-scan-jobs/15-05-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-03-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-04-SUMMARY.md

<interfaces>
<!-- Check-run naming: a reusable-workflow call surfaces as "<caller-job-id> / <called-job-name>".     -->
<!-- CONFIRMED in Phase 14-02, recorded in 15-05-SUMMARY. The five expected names on this PR:          -->
<!--   security / SAST — Semgrep CE                                                                    -->
<!--   security / IaC — Checkov                                                                        -->
<!--   security / SCA — Trivy Filesystem     <- unchanged by Phase 16, deliberately                    -->
<!--   security / Container — Trivy Image                                                              -->
<!--   security / Secrets — Gitleaks                                                                   -->
<!--                                                                                                   -->
<!-- What must be observed in the SCA job log, and which criterion each line evidences:                -->
<!--   detect steps -> three "FOUND n …" lines (this repo has all three ecosystems)                    -->
<!--   SCA-01 -> metadata.vulnerabilities histogram with non-zero high and/or critical                 -->
<!--             => Criterion 1 "with severity levels"                                                 -->
<!--   SCA-02 -> per-package advisory counts => Criterion 2 "reports Python advisories"                -->
<!--   SCA-03 -> ruleId set intersecting {terraform_required_providers, terraform_module_version,      -->
<!--             terraform_module_pinned_source} => Criterion 3.                                       -->
<!--             terraform_required_version ALONE does NOT satisfy Criterion 3.                        -->
<!--   evidence -> guarded `ls -l` lines showing non-zero-byte npm-audit-*.json, pip-audit-*.json,     -->
<!--               tflint.sarif                                                                        -->
</interfaces>

<notes>
**TARGET REPOSITORY.** The push goes to `OttawaCloudConsulting/security-platform` via
`git -C repos/security-platform push -u origin HEAD`. **NEVER run `git push` from the outer repository** —
its `origin` points at the same URL but the two histories are disjoint, so a push from the outer repo
would dump unrelated history onto the product repo.

**Resolve the repo slug at runtime, never hardcode it:**
`SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##')`
and pass `--repo "$SLUG"` to every `gh` call.

**Do not merge in this plan.** Merging is irreversible on a shared branch; 16-07 carries the human
checkpoint that authorises it.

**Do not "fix" a red check by weakening the workflow.** If a report-verification step fails, it is doing
its job — the report was an error object or was missing. STOP and REPORT with the raw log. Silent retry is
forbidden by the project's anti-slop rule.

**Criterion 4 cannot be observed on this PR.** The product repo has a lockfile, a requirements file and
`.tf` files, so all three detectors report FOUND and nothing skips. Do not manufacture a fake skip by
deleting fixtures. The skip path's evidence is the 16-03 empty-`git init` negative test plus the 16-04
static guard assertions. Say exactly that in the SUMMARY.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the branch, open the pull request, and wait for the run to conclude</name>
  <files></files>
  <read_first>
    - .planning/phases/15-five-parallel-scan-jobs/15-04-SUMMARY.md (the Phase 15 live-run procedure and the evidence shape that worked)
    - .planning/phases/16-sca-ecosystem-coverage/16-04-SUMMARY.md (what was committed and which report filenames to expect)
    - repos/security-platform/.github/workflows/pr-security.yml (the caller job id, which prefixes every check-run name)
  </read_first>
  <action>
    Confirm readiness before pushing: `git -C repos/security-platform status --porcelain` empty, HEAD on
    `feature/phase-16-sca-ecosystem-coverage`, and `git -C repos/security-platform log origin/main..HEAD --oneline`
    showing the phase commits from 16-01, 16-02, 16-03 and 16-04. Any other state is STOP / REPORT / WAIT.

    Push with `git -C repos/security-platform push -u origin HEAD`. Resolve the slug at runtime as shown in
    the notes. Open the PR with `gh pr create --repo "$SLUG" --base main --head feature/phase-16-sca-ecosystem-coverage`,
    titled for the phase, with a body stating this is report-only (D-04 still in force), listing SCA-01,
    SCA-02 and SCA-03, and naming the four tools now running inside the single `sca` job. Record the PR
    number.

    Wait for completion rather than polling ad hoc — `gh pr checks --repo "$SLUG" <pr> --watch`, or
    `gh run watch` against the run id resolved from the head SHA. Record the run id, each check's
    conclusion, and the full list of check-run names exactly as GitHub reports them.

    Do not merge. Do not rerun a failed job before reporting it.
  </action>
  <verify>
    <automated>SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##'); SHA=$(git -C repos/security-platform rev-parse HEAD); gh api "repos/$SLUG/commits/$SHA/check-runs" --jq '.check_runs[] | "\(.name) :: \(.status) :: \(.conclusion)"'</automated>
    <automated>SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##'); SHA=$(git -C repos/security-platform rev-parse HEAD); gh api "repos/$SLUG/commits/$SHA/check-runs" --jq '[.check_runs[].name] | join("|")' | grep -q 'SCA — Trivy Filesystem'; gh api "repos/$SLUG/commits/$SHA/check-runs" --jq '[.check_runs[] | select(.conclusion != "success")] | length' | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - The branch is pushed and a PR exists against `main` in `OttawaCloudConsulting/security-platform`; the PR number is recorded
    - The check-runs API for the head SHA returns five checks whose names match the five recorded in 15-05-SUMMARY, including `SCA — Trivy Filesystem` unchanged
    - Every check's `conclusion` is `success` — findings do not fail a check under D-04
    - The run id is recorded in the SUMMARY
    - No merge was performed and no job was rerun
  </acceptance_criteria>
  <done>A live PR run has concluded with five successful checks under the unchanged check-run names, and its identifiers are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Extract per-sub-scan evidence from the SCA job log and map it to the success criteria</name>
  <files></files>
  <read_first>
    - .planning/phases/16-sca-ecosystem-coverage/16-03-SUMMARY.md (the workstation baseline: npm histogram, pip-audit advisory count, tflint rule-id set, tflint 0.61.0)
    - .planning/phases/16-sca-ecosystem-coverage/16-RESEARCH.md lines 646-665 (Environment Availability — the deliberate workstation/CI version split and the instruction to re-confirm rule ids under v0.64.0)
    - .planning/ROADMAP.md Phase 16 Success Criteria (the four statements this evidence must map onto)
  </read_first>
  <action>
    Resolve the SCA job id from `gh api repos/$SLUG/actions/runs/<run-id>/jobs`, download its log with
    `gh run view --repo "$SLUG" <run-id> --log --job <sca-job-id>`, and extract verbatim the lines that
    evidence each criterion:
    - the three `FOUND n …` detector lines, proving all three ecosystems resolved and nothing was skipped;
    - the npm verification step's `metadata.vulnerabilities` histogram including its high and critical
      counts — the literal evidence for Criterion 1's "with severity levels";
    - the pip-audit verification step's per-package advisory counts — evidence for Criterion 2;
    - the tflint verification step's `ruleId` set together with the version line from the tflint install
      step — evidence for Criterion 3, valid only if the set intersects `{terraform_required_providers,
      terraform_module_version, terraform_module_pinned_source}`. If the only rule reported is
      `terraform_required_version`, Criterion 3 is NOT met: STOP and REPORT rather than recording a pass;
    - the three guarded `ls -l` lines with their byte sizes.

    Compare the CI rule-id set against the workstation set from 16-03-SUMMARY and state explicitly whether
    v0.64.0 fired the same rules as v0.61.0. A difference is a finding to record, not a problem to hide.

    Confirm report-only behaviour survived three more scanners:
    `gh pr view --repo "$SLUG" <pr> --json mergeable,mergeStateStatus`.

    Write the SUMMARY last. Map each excerpt to the criterion it satisfies. State plainly that Criterion 4
    was not observed live — nothing skipped, because the repo has all three ecosystems — and that its
    evidence is the 16-03 empty-repo negative test plus the 16-04 static guard assertions. Record the four
    forward-facing facts Phase 17 must not have to infer: the numbered per-input report filenames; that
    neither npm audit nor pip-audit emits SARIF while tflint does; that pip-audit's JSON carries no
    severity or CVSS field; and that `--audit-level` does not filter npm's report even though Trivy's
    `--severity` does filter its own.
  </action>
  <verify>
    <automated>F=.planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md; test -f "$F" && grep -q 'metadata' "$F" && grep -Eq 'terraform_required_providers|terraform_module_version|terraform_module_pinned_source' "$F" && grep -q 'pip-audit' "$F" && grep -q '0.64.0' "$F" && grep -qi 'criterion 4' "$F" && grep -qi 'mergeable' "$F"</automated>
    <automated>SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##'); PR=$(gh pr list --repo "$SLUG" --head feature/phase-16-sca-ecosystem-coverage --json number --jq '.[0].number'); gh pr view --repo "$SLUG" "$PR" --json mergeable,mergeStateStatus --jq '"mergeable=\(.mergeable) state=\(.mergeStateStatus)"' | grep -q 'mergeable=MERGEABLE'</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes the three `FOUND n …` detector lines from the live log
    - It quotes the npm `metadata.vulnerabilities` histogram with non-zero high and/or critical, mapped to Criterion 1
    - It quotes pip-audit's per-package advisory counts, mapped to Criterion 2
    - It quotes the tflint `ruleId` set, which intersects the three pinning rule ids, mapped to Criterion 3, alongside the CI tflint version
    - It states whether the v0.64.0 rule-id set matches the v0.61.0 workstation set from 16-03-SUMMARY
    - It quotes the three guarded `ls -l` lines with non-zero byte sizes
    - `gh pr view --json mergeable` reports `MERGEABLE`, recorded in the SUMMARY
    - It states explicitly that Criterion 4 was not observed live and names the two artefacts that do evidence it
    - It records all four Phase 17 hand-forwards (numbered filenames, no SARIF from npm/pip-audit, no severity field in pip-audit JSON, `--audit-level` does not filter the npm report)
    - Every figure in the SUMMARY is traceable to a quoted log line from this run — none copied from RESEARCH or from 16-03
  </acceptance_criteria>
  <done>The three new success criteria are backed by quoted live log evidence, the version comparison is stated rather than assumed, and Criterion 4's evidence is described honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch → remote `origin` | A push to a shared repository; the outer repo shares the same remote URL with a disjoint history |
| GitHub Actions runner → PR check status | The check conclusion is the signal a human will act on |
| job log → phase evidence | Log text is the only evidence for three success criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Tampering | A `git push` issued from the outer docs repo would push a disjoint history onto `security-platform` | mitigate | Every git command scoped `git -C repos/security-platform`; the plan states the hazard and the executor confirms HEAD and the commit list before pushing |
| T-16-21 | Repudiation | Recording "Criterion 3 met" from a `terraform_required_version` finding, which says nothing about pinning | mitigate | Evidence must quote a rule id from the three-member pinning set; the action instructs STOP / REPORT otherwise |
| T-16-22 | Repudiation | Claiming a live clean-skip observation that did not occur | mitigate | Explicit instruction not to delete fixtures to manufacture a skip; the SUMMARY must name the smoke-gate negative test and the static guards as the actual evidence |
| T-16-23 | Information Disclosure | A green check masking a sub-scan that produced an error report | mitigate | The 16-04 report-verification steps are not `continue-on-error`, so this surfaces as a red step; the evidence extraction quotes the histogram, advisory counts and rule ids rather than the check conclusion alone |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No installs performed by this plan. The runner-side `pip install pip-audit==2.10.1` and checksum-verified tflint download were fixed in 16-04; both packages are Approved in 16-RESEARCH's Package Legitimacy Audit |
</threat_model>

<verification>
- Five check-runs on the head SHA, names matching 15-05-SUMMARY, all concluding `success`
- SCA job log contains three `FOUND n …` lines, an npm severity histogram, pip-audit advisory counts, and a tflint rule-id set intersecting the pinning set
- Three guarded `ls -l` lines show non-zero-byte reports
- `gh pr view --json mergeable` reports `MERGEABLE`
- No merge performed
</verification>

<success_criteria>
Success Criteria 1, 2 and 3 are each backed by a quoted line from a live CI log on a real pull request,
the check-run names Phase 18 depends on are unchanged, and the pull request remains mergeable despite the
findings.
</success_criteria>

<output>
Create `.planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md` when done, containing the PR number,
run id, the five verbatim check-run names, the per-criterion log excerpts, the tflint v0.64.0 vs v0.61.0
rule-id comparison, the mergeable state, and the four Phase 17 hand-forwards.
</output>
