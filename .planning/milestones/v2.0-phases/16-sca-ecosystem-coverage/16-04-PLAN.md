---
phase: 16-sca-ecosystem-coverage
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - repos/security-platform/.github/workflows/security.yml
autonomous: true
requirements: [SCA-01, SCA-02, SCA-03]

must_haves:
  truths:
    - "The npm, Python and Terraform sub-scans are STEPS inside the existing `sca` job — the workflow still declares exactly five jobs with zero `needs:`"
    - "The `sca` job's `name:` is byte-identical to the Phase 15 value, so Phase 18's required-check list stays valid"
    - "Each sub-scan runs only when its detector found files, and the detector logs a clear message on both branches"
    - "Each sub-scan is followed by a report-content verification step that is NOT continue-on-error, so an error-object report turns the step red instead of reading as clean"
    - "tflint is installed from a checksum-verified release archive, never `curl | sh`"
    - "A repo missing an ecosystem cannot turn the job red through a `ls: file not found` in the evidence step"
  artifacts:
    - path: "repos/security-platform/.github/workflows/security.yml"
      provides: "sca job extended with three detect/scan/verify step groups plus guarded evidence steps"
      contains: "detect-terraform.sh"
  key_links:
    - from: ".github/workflows/security.yml sca job detect steps"
      to: "scripts/detect-npm.sh / detect-python.sh / detect-terraform.sh"
      via: "bash scripts/detect-<x>.sh <list-file> — the same implementation the smoke gate tests"
      pattern: "bash scripts/detect-"
    - from: "detect step outputs.found"
      to: "scan, verify and evidence steps"
      via: "step-level if: guard"
      pattern: "outputs.found == 'true'"
---

<objective>
Wire the three proven sub-scans into CI. Extend the existing `sca` job in
`repos/security-platform/.github/workflows/security.yml` with tool installs, three detect steps calling
the shared detector scripts, three guarded scan step groups, three report-verification steps, and guarded
evidence steps — then prove statically that the workflow still declares exactly five jobs with the same
five check-run names.

Purpose: 16-RESEARCH derives the "steps, not jobs" constraint from three independent in-repo documents —
CICD-01's "5 parallel jobs" is already marked Complete, Phase 17 Criterion 3 maps artifacts per job
"including the SCA sub-scans", and 15-05-SUMMARY recorded five verbatim check-run names specifically for
Phase 18's branch protection. A sixth job would break all three.

**Path correction the executor must not trip over:** the file modified here is
`repos/security-platform/.github/workflows/security.yml` — the live workflow on `main`, 188 lines, whose
`sca` job sits at lines 76-106 and whose Gitleaks two-step split sits at lines 168-184.
`repos/security-platform/cicd/.github/workflows/security.yml` is the STALE reference template (it still
runs Grype, installs via `curl | sh`, and uploads artifacts). It is cited by 16-PATTERNS as the source of
the detect-then-guard idiom only. Do not modify it.

Output: a `sca` job running four tools, statically validated and committed on the phase branch. No push
and no live run in this plan — that is 16-05.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-sca-ecosystem-coverage/16-RESEARCH.md
@.planning/phases/16-sca-ecosystem-coverage/16-PATTERNS.md
@.planning/phases/16-sca-ecosystem-coverage/16-02-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-03-SUMMARY.md

<interfaces>
<!-- The five verbatim check-run job names that must remain byte-identical (em-dash U+2014 in each):    -->
<!--   sast      -> "SAST — Semgrep CE"                                                                  -->
<!--   iac       -> "IaC — Checkov"                                                                      -->
<!--   sca       -> "SCA — Trivy Filesystem"   <- stays WRONG-but-frozen; Phase 18 hard-codes it         -->
<!--   container -> "Container — Trivy Image"                                                            -->
<!--   secrets   -> "Secrets — Gitleaks"                                                                 -->
<!--                                                                                                    -->
<!-- In-file analogs (repos/security-platform/.github/workflows/security.yml):                           -->
<!--   pip-based tool install           -> line 29 (pip install semgrep==1.177.0) + the A7 fallback      -->
<!--                                       comment ladder at lines 30-32                                 -->
<!--   checksum-verified binary install -> lines 158-166 (Gitleaks: curl -o, sha256sum -c -, extract)    -->
<!--   two-step split for a tool with   -> lines 168-184 (Gitleaks SARIF then JSON; second step          -->
<!--     no dual-output flag               if: always(), both continue-on-error: true  # D-04)           -->
<!--   guarded evidence step            -> cicd/.github/workflows/security.yml lines 153/161/168         -->
<!--                                       (if: always() && steps.X.outputs.exists == 'true')            -->
<!--   job/step banner comment style    -> lines 76-79                                                   -->
<!--                                                                                                    -->
<!-- Install values, measured (16-RESEARCH.md lines 84-108):                                             -->
<!--   pip-audit -> pip install pip-audit==2.10.1                                                        -->
<!--                No actions/setup-python: Python 3.12.3 + pip 24.0 preinstalled on the runner.        -->
<!--                No actions/setup-node either: npm 10.9.8 preinstalled.                               -->
<!--   tflint    -> v0.64.0, asset tflint_linux_amd64.zip, SHA-256                                       -->
<!--                cca9d13e2e1d7a2c627af60ff899a3c9b74212899416aeb96ec764d2ef954537                     -->
<!--                Ships a .ZIP, not a .tar.gz, so the Gitleaks `tar xzf` line CANNOT be copied.        -->
<!--                `unzip` is preinstalled on the runner.                                               -->
<!--                                                                                                    -->
<!-- Detector contract (frozen by 16-02; 16-02-SUMMARY.md is authoritative):                             -->
<!--   bash scripts/detect-npm.sh npm-lockfiles.txt       -> found=true|false, exit 0, logs both branches-->
<!--   bash scripts/detect-python.sh py-reqs.txt                                                         -->
<!--   bash scripts/detect-terraform.sh tf-files.txt                                                     -->
</interfaces>

<notes>
**TARGET REPOSITORY.** The file is `repos/security-platform/.github/workflows/security.yml`. All git
operations use `git -C repos/security-platform`. Never push from the outer repo, and do not push at all
in this plan.

**Do NOT rename the `sca` job's `name:`.** It reads `SCA — Trivy Filesystem` and is now inaccurate, and it
stays anyway: 15-05-SUMMARY recorded it as one of five verbatim check-run names that Phase 18 hard-codes
into its required-check list. Renaming it would leave branch protection pointing at a check that no longer
exists — silently non-blocking. Update the banner COMMENT above the job to describe the four tools; leave
the `name:` value untouched.

**Do NOT add new jobs.** Four scanners now run inside `sca`. Job count stays 5, `needs:` count stays 0.

**Detection and scanning must be separate steps.** GitHub's default shell is `bash -e` and every scanner
here is expected to exit non-zero under D-04, so a second command in the same `run:` block would be
silently dropped. The workflow's own comment at lines 168-172 records this; Phase 15 already hit it with
Gitleaks.

**No new `uses:` is required.** Both tools install directly. If you reach for
`terraform-linters/setup-tflint` or `pypa/gh-action-pip-audit`, STOP and REPORT — that is a user decision,
not an executor choice. 16-RESEARCH records both SHAs but recommends against both.

**`--audit-level=high` gates the exit code only.** It does NOT filter the npm report — the opposite of
Trivy's `--severity`, which does filter its report file. Keep `--audit-level=high` for exit-code
consistency with the repo's existing pre-commit hook; the full-severity report is retained regardless,
which is what Phase 17 will want.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pip-audit and tflint, and add the three detect steps</name>
  <files>repos/security-platform/.github/workflows/security.yml</files>
  <read_first>
    - repos/security-platform/.github/workflows/security.yml (all 188 lines — the file being modified; the `sca` job at 76-106, the pip install analog at 29-32, the Gitleaks checksum install at 158-166)
    - .planning/phases/16-sca-ecosystem-coverage/16-02-SUMMARY.md (the frozen detector contract — argument, list-file names, log prefixes, exit code)
    - .planning/phases/16-sca-ecosystem-coverage/16-RESEARCH.md lines 84-108 (install block, measured checksum, runner preinstall evidence)
    - .planning/phases/16-sca-ecosystem-coverage/16-PATTERNS.md lines 90-141 (Analogs A, B and C, and the three details of the `cicd/` analog that must change)
  </read_first>
  <action>
    Work only inside the `sca` job. First rewrite the banner comment above it so it names all four tools
    with their requirement ids — SCA-04 Trivy filesystem sweep, SCA-01 npm audit, SCA-02 pip-audit, SCA-03
    tflint — in the existing box-drawn style. Leave `name: SCA — Trivy Filesystem` byte-identical, and add
    a comment line immediately above that `name:` stating it is deliberately frozen because Phase 18's
    branch-protection required-check list hard-codes it.

    Add two install steps after the existing `setup-trivy` step. First `pip install pip-audit==2.10.1`,
    following the `sast` job's line-29 shape including a two-line Assumption A7 fallback comment ladder
    adapted to pip-audit (pipx first, then `--break-system-packages`). Add no `actions/setup-python` and no
    `actions/setup-node` — Python, pip and npm are all preinstalled on the runner. Second, a named
    `Install tflint` step following the Gitleaks download-then-verify-then-extract shape at lines 158-166,
    keeping its "never curl | sh" comment and its stated reason: `curl -sSfL -o tflint.zip` against the
    v0.64.0 `tflint_linux_amd64.zip` release asset, then `sha256sum -c -` against
    `cca9d13e2e1d7a2c627af60ff899a3c9b74212899416aeb96ec764d2ef954537`, then
    `sudo unzip -o -d /usr/local/bin tflint.zip`. That last line is the one place the Gitleaks analog does
    not transfer — tflint ships a `.zip`, so `tar xzf` is wrong.

    Add three detect steps, each with an `id` — `npm`, `py`, `tf` — each a single `run:` line invoking the
    shared script with its list-file argument: `bash scripts/detect-npm.sh npm-lockfiles.txt`,
    `bash scripts/detect-python.sh py-reqs.txt`, `bash scripts/detect-terraform.sh tf-files.txt`. Do not
    inline the detection logic: the point of the extracted scripts is that CI and the smoke gate exercise
    the same implementation, so the Criterion 4 negative test proves the real code path. Do not put
    `continue-on-error` on a detect step — a detector that fails is an infrastructure failure, and it is
    contractually guaranteed to exit 0 on both branches.

    Add a comment above the detect steps explaining why detection is a step rather than a bare
    `if: hashFiles(...) != ''` on the scan step: a step skipped by `if:` produces no log output at all, and
    Criterion 4 requires a clear message on the skip path.
  </action>
  <verify>
    <automated>cd repos/security-platform && python3 - <<'PY'
import yaml
d = yaml.safe_load(open('.github/workflows/security.yml'))
jobs = d['jobs']
assert len(jobs) == 5, 'job count changed: %d' % len(jobs)
assert jobs['sca']['name'] == 'SCA — Trivy Filesystem', repr(jobs['sca']['name'])
assert not any('needs' in j for j in jobs.values()), 'a needs: was introduced'
ids = {s.get('id') for s in jobs['sca']['steps']}
assert {'npm', 'py', 'tf'} <= ids, 'missing detect step ids: %r' % (ids,)
calls = [s for s in jobs['sca']['steps'] if 'bash scripts/detect-' in str(s.get('run', ''))]
assert len(calls) == 3, 'expected 3 detector invocations in run: blocks, got %d' % len(calls)
print('job count 5, sca name frozen, zero needs, 3 detect steps present')
PY</automated>
    <automated>cd repos/security-platform && grep -q 'pip install pip-audit==2.10.1' .github/workflows/security.yml && grep -q 'cca9d13e2e1d7a2c627af60ff899a3c9b74212899416aeb96ec764d2ef954537' .github/workflows/security.yml && grep -q 'unzip -o -d /usr/local/bin tflint.zip' .github/workflows/security.yml && ! grep -q 'tar xzf tflint' .github/workflows/security.yml && ! grep -q 'setup-tflint' .github/workflows/security.yml && ! grep -q 'setup-python' .github/workflows/security.yml && ! grep -q 'setup-node' .github/workflows/security.yml</automated>
    <automated>cd repos/security-platform && yamllint -d relaxed .github/workflows/security.yml</automated>
  </verify>
  <acceptance_criteria>
    - `yaml.safe_load` reports exactly 5 jobs, no job has a `needs:` key, and `jobs['sca']['name']` is the byte-exact string `SCA — Trivy Filesystem` (em-dash U+2014)
    - A comment immediately above that `name:` states it is frozen for Phase 18's required-check list
    - The `sca` banner comment names all four tools with their requirement ids (SCA-01, SCA-02, SCA-03, SCA-04)
    - The workflow contains `pip install pip-audit==2.10.1` with a two-line pipx / `--break-system-packages` fallback comment
    - The tflint install performs `curl -sSfL -o`, `sha256sum -c -` against the literal checksum `cca9d13e…4537`, and `unzip -o -d /usr/local/bin`; no `tar xzf` for tflint, and no `curl … | sh` anywhere in the file
    - `setup-tflint`, `gh-action-pip-audit`, `setup-python` and `setup-node` appear nowhere in the file
    - Exactly three steps invoke `bash scripts/detect-…` in their `run:` block (counted through the YAML parse, not `grep -c`, because the explanatory comment this task adds also contains that string), with step ids `npm`, `py`, `tf`
    - No detect step carries `continue-on-error`
    - `yamllint -d relaxed .github/workflows/security.yml` exits 0
  </acceptance_criteria>
  <done>The sca job installs both new tools safely and resolves the three ecosystems through the same detector scripts the smoke gate tests, with the check-run name untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the three guarded sub-scan groups, their report-verification steps, and guarded evidence steps</name>
  <files>repos/security-platform/.github/workflows/security.yml</files>
  <read_first>
    - repos/security-platform/.github/workflows/security.yml (the file being modified, as left by Task 1)
    - repos/security-platform/scripts/smoke-scans.sh (the npm/pip/tflint sections added in 16-03 — the CI steps run the same commands and the same assertions)
    - .planning/phases/16-sca-ecosystem-coverage/16-RESEARCH.md lines 442-566 (Code Examples — per-input iteration, exit-code capture, the two-step tflint form)
    - .planning/phases/16-sca-ecosystem-coverage/16-PATTERNS.md lines 143-173 (Analogs D and E) and lines 402-411 (Shared Patterns 4 and 5)
  </read_first>
  <action>
    Add three scan groups, each gated `if: steps.<id>.outputs.found == 'true'` and each carrying
    `continue-on-error: true` with an inline `# D-04` comment on the same line, matching the convention on
    the existing Gitleaks and Trivy steps.

    SCA-01: a step that reads `npm-lockfiles.txt` with a `while read -r` loop, `cd`s into each lockfile's
    directory (npm audit operates on a working directory, not a named file), runs
    `npm audit --audit-level=high --json` into a numbered `npm-audit-<n>.json` at the workspace root with
    explicit `rc=$?` capture, echoes the directory and exit code per iteration, and exits with the last
    captured code so `continue-on-error` is what tolerates it. Comment `--audit-level=high` with the fact
    that it gates the exit code only and does not filter the report.

    SCA-02: the same iteration shape over `py-reqs.txt`, invoking
    `pip-audit -r "$req" --format json --progress-spinner=off -o pip-audit-<n>.json`. No `--no-deps`, no
    `--disable-pip`, no `--require-hashes` — the resolving default is a locked decision. Comment that
    choice and its ~11.5 s cost so a later reader does not "optimise" it into a hard error on unpinned
    consumer requirements.

    SCA-03: two steps, following the Gitleaks precedent rather than RESEARCH's single chained block, because
    tflint has no `-o` flag and `--format sarif` writes to stdout. Step one redirects
    `tflint --recursive --format sarif` into `tflint.sarif`. Step two, gated
    `if: always() && steps.tf.outputs.found == 'true'`, runs `tflint --recursive --format default` so the
    findings are human-readable in the job log. **Both** tflint steps carry `continue-on-error: true  # D-04`,
    exactly as both Gitleaks steps do — the second one exits 2 on findings just as the first does, and an
    untolerated step would fail the job on a successful scan. Comment that `--recursive` is mandatory — a
    repo-root tflint sees zero `.tf` files and exits 0, a silent false pass — and that tflint exits 2 on
    findings, not 1. Do not add `--force`.

    Then add three report-verification steps, one per sub-scan, each gated
    `if: always() && steps.<id>.outputs.found == 'true'` and each deliberately WITHOUT
    `continue-on-error`. Carry a comment saying so and why: an error-object or missing report is an
    infrastructure failure, not a finding, and it must turn the step red. Each runs `python3 -c` over the
    matching report file(s) and prints what a human needs to see in the log:
    - npm: fail if the report's top-level key is `error` (the ENOLOCK shape, which returns the same rc=1 as
      a real finding); otherwise print `metadata.vulnerabilities`, so the severity levels Criterion 1
      requires are visible in the log rather than only inside a file;
    - pip-audit: fail if the file is missing or has no `dependencies` key (pip-audit writes no file at all
      on its error paths); otherwise print per-package advisory counts, with a comment noting pip-audit
      emits no severity field;
    - tflint: fail if the SARIF has no `runs` key; otherwise print the set of `runs[].results[].ruleId`.

    Finally, fix the evidence step. The existing `Show scan output files` at the end of the `sca` job runs
    `ls -l trivy-fs.json trivy-fs.sarif` under a bare `if: always()`. Leave that step as-is — both Trivy
    files are produced unconditionally — and add three NEW guarded evidence steps, each
    `if: always() && steps.<id>.outputs.found == 'true'`, listing that sub-scan's reports
    (`npm-audit-*.json`, `pip-audit-*.json`, `tflint.sarif`). Do not fold the numbered per-input globs into
    the existing unconditional `ls`: in a consumer repo where a sub-scan skipped, the file does not exist,
    `ls` exits 2, and the job goes red on a clean skip — the failure mode Criterion 4 forbids from the
    other direction.
  </action>
  <verify>
    <automated>cd repos/security-platform && python3 - <<'PY'
import yaml
d = yaml.safe_load(open('.github/workflows/security.yml'))
steps = d['jobs']['sca']['steps']
guards = {"steps.npm.outputs.found == 'true'",
          "steps.py.outputs.found == 'true'",
          "steps.tf.outputs.found == 'true'"}
scans = [s for s in steps if s.get('if') in guards]
assert len(scans) >= 3, 'expected >=3 guarded scan steps, got %d' % len(scans)
for s in scans:
    assert s.get('continue-on-error') is True, 'scan step missing continue-on-error: %r' % s.get('name')
verif = [s for s in steps
         if str(s.get('if', '')).startswith('always()')
         and 'outputs.found' in str(s.get('if', ''))
         and 'python3' in str(s.get('run', ''))]
assert len(verif) == 3, 'expected 3 report-verification steps, got %d' % len(verif)
for s in verif:
    assert 'continue-on-error' not in s, 'verification step must not be tolerated: %r' % s.get('name')
tf_default = [s for s in steps if '--format default' in str(s.get('run', ''))]
assert len(tf_default) == 1, 'expected one tflint --format default step, got %d' % len(tf_default)
assert tf_default[0].get('continue-on-error') is True, 'tflint --format default step must be continue-on-error (it exits 2 on findings)'
ev = [s for s in steps
      if str(s.get('if', '')).startswith('always()')
      and 'outputs.found' in str(s.get('if', ''))
      and str(s.get('run', '')).strip().startswith('ls ')]
assert len(ev) == 3, 'expected 3 guarded evidence steps, got %d' % len(ev)
print('3 guarded scans + tflint step 2 tolerated, 3 verifications not tolerated, 3 guarded evidence steps')
PY</automated>
    <automated>cd repos/security-platform && grep -q 'npm audit --audit-level=high --json' .github/workflows/security.yml && grep -q 'progress-spinner=off' .github/workflows/security.yml && ! grep -q 'disable-pip' .github/workflows/security.yml && ! grep -q 'require-hashes' .github/workflows/security.yml && grep -q 'tflint --recursive --format sarif' .github/workflows/security.yml && grep -q 'tflint --recursive --format default' .github/workflows/security.yml && ! grep -q 'tflint --force' .github/workflows/security.yml</automated>
    <automated>cd repos/security-platform && [ "$(grep -c '# D-04' .github/workflows/security.yml)" -ge 11 ] && yamllint -d relaxed .github/workflows/security.yml</automated>
  </verify>
  <acceptance_criteria>
    - Three scan steps are gated on `steps.npm|py|tf.outputs.found == 'true'` and each has `continue-on-error: true` with an inline `# D-04` comment
    - The npm step iterates `npm-lockfiles.txt`, writes numbered `npm-audit-<n>.json`, and captures `rc` explicitly rather than relying on `bash -e`
    - The pip-audit invocation is exactly `-r "$req" --format json --progress-spinner=off -o pip-audit-<n>.json`; `--no-deps`, `--disable-pip` and `--require-hashes` appear nowhere in the file
    - tflint runs as two steps, both with `--recursive` and both carrying `continue-on-error: true  # D-04`, SARIF via shell redirection, the second gated `always() && steps.tf.outputs.found == 'true'`; `--force` appears nowhere
    - Exactly three report-verification steps exist, each gated `always() && steps.<id>.outputs.found == 'true'`, each running `python3`, and NONE carrying `continue-on-error`
    - The npm verification fails on a top-level `error` key and prints `metadata.vulnerabilities`; the pip-audit verification fails on a missing file or missing `dependencies` key; the tflint verification prints the `ruleId` set
    - Exactly three new guarded `ls`-based evidence steps exist; the pre-existing unconditional `ls -l trivy-fs.json trivy-fs.sarif` step is unchanged
    - `yamllint -d relaxed .github/workflows/security.yml` exits 0
  </acceptance_criteria>
  <done>All four SCA sub-scans run in the one job, each guarded by its detector, each tolerated at the step level under D-04, and each followed by a report-content check that is not tolerated.</done>
</task>

<task type="auto">
  <name>Task 3: Statically validate the whole workflow against the Phase 15 contract and commit</name>
  <files>repos/security-platform/.github/workflows/security.yml</files>
  <read_first>
    - repos/security-platform/.github/workflows/security.yml (the file being modified, as left by Task 2 — read it end to end before asserting on it)
    - .planning/phases/15-five-parallel-scan-jobs/15-05-SUMMARY.md (the five verbatim check-run names recorded for Phase 18)
    - .planning/phases/16-sca-ecosystem-coverage/16-RESEARCH.md lines 680-692 (the static assertions the phase's test map calls for)
  </read_first>
  <action>
    Assert the Phase 15 contract still holds, using a YAML parse rather than line greps — `grep -c 'needs:'`
    counts comment text and would be self-invalidating, since this plan adds comments that discuss `needs:`
    and job counts.

    Write the checks as a single `python3` invocation over `yaml.safe_load`, asserting: exactly five jobs
    with ids `sast`, `iac`, `sca`, `container`, `secrets`; the list of `name:` values equal to the five
    verbatim Phase 15 strings including the em-dash U+2014 in each; no job carrying a `needs:` key; the
    workflow's top-level `on:` still `workflow_call`; and `permissions:` still exactly `contents: read` —
    no sub-scan needs more, and `security-events: write` is Phase 17's change to `pr-security.yml`, not
    this file's.

    Run `yamllint -d relaxed .github/workflows/security.yml` and `pre-commit run --all-files` in the target
    repo. Then commit on the phase branch with message
    `feat(16-04): add npm, Python and Terraform sub-scans to the SCA job`. If any gate is red, STOP and
    REPORT — never `--no-verify`, and never weaken an assertion to make it pass.

    Do not push. Do not open a PR. 16-05 owns both.
  </action>
  <verify>
    <automated>cd repos/security-platform && python3 - <<'PY'
import yaml
d = yaml.safe_load(open('.github/workflows/security.yml'))
jobs = d['jobs']
assert set(jobs) == {'sast', 'iac', 'sca', 'container', 'secrets'}, sorted(jobs)
expected = ['SAST — Semgrep CE', 'IaC — Checkov', 'SCA — Trivy Filesystem',
            'Container — Trivy Image', 'Secrets — Gitleaks']
actual = [jobs[k]['name'] for k in ['sast', 'iac', 'sca', 'container', 'secrets']]
assert actual == expected, actual
assert not any('needs' in j for j in jobs.values()), 'a needs: was introduced'
assert 'workflow_call' in (d.get('on') or d.get(True)), 'workflow_call trigger changed'
assert d['permissions'] == {'contents': 'read'}, d['permissions']
print('five jobs, five frozen names, zero needs, workflow_call, contents: read')
PY</automated>
    <automated>cd repos/security-platform && yamllint -d relaxed .github/workflows/security.yml && pre-commit run --all-files</automated>
    <automated>cd repos/security-platform && git log --oneline -1 | grep -q '16-04' && [ -z "$(git status --porcelain)" ] && [ -z "$(git log origin/main..HEAD --oneline --name-only -- cicd/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parse asserts pass: five job ids, five byte-exact names, zero `needs:`, `workflow_call` trigger, `permissions: {contents: read}`
    - Job-count and `needs:` assertions are made through `yaml.safe_load`, not through `grep -c` on the raw file
    - `yamllint -d relaxed .github/workflows/security.yml` exits 0
    - `pre-commit run --all-files` exits 0 in the target repo
    - `git diff origin/main..HEAD --name-only` shows no file under `cicd/` — the stale reference template is untouched
    - Working tree clean; `git log -1` subject is `feat(16-04): add npm, Python and Terraform sub-scans to the SCA job`
    - No push and no PR was created by this plan
  </acceptance_criteria>
  <done>The extended workflow still satisfies every Phase 15 structural guarantee Phase 18 depends on, proven by parse-based assertions, and is committed on the phase branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release CDN → runner `/usr/local/bin` | tflint binary crosses into an executable path |
| PyPI → runner Python environment | pip-audit installs from a public registry |
| checked-out repo content → scanners | Untrusted PR content is the scan input |
| detect step `$GITHUB_OUTPUT` → scan/evidence guards | A runner-side control value deciding whether a scan runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Tampering | tflint binary altered in transit | mitigate | `curl -sSfL -o` then `sha256sum -c -` against the checksum read from the official v0.64.0 `checksums.txt`, then `unzip`; never `curl \| sh` (the SAST job's own p/default ruleset flags `gha-curl-pipe-shell`) |
| T-16-15 | Information Disclosure (missed finding) | npm ENOLOCK / pip-audit invalid-input report tolerated by `continue-on-error` and read as clean | mitigate | Per-sub-scan report-verification steps that deliberately omit `continue-on-error`, asserting `auditReportVersion` / `dependencies` / `runs` and printing the severity histogram and rule-id set into the job log |
| T-16-16 | Denial of Service | Evidence `ls` on a non-existent numbered report turns the job red on a clean skip | mitigate | Three separate evidence steps, each gated `always() && steps.<id>.outputs.found == 'true'`; the unconditional Trivy `ls` is left alone |
| T-16-17 | Tampering | Script injection via `${{ }}` interpolation into a `run:` block | mitigate | No new interpolation is introduced by any sub-scan; the only existing one (`${{ github.sha }}` in the container job) is not attacker-controlled and is not touched |
| T-16-18 | Elevation of Privilege | Workflow permissions widened to satisfy a sub-scan | mitigate | `permissions: contents: read` asserted unchanged in Task 3; no sub-scan needs more. `security-events: write` is Phase 17's change to `pr-security.yml` |
| T-16-19 | Elevation of Privilege | npm lifecycle scripts executing during the audit | mitigate | `npm audit` reads the lockfile and queries the advisory endpoint; it does not install. No `npm ci` / `npm install` is added to the job |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Only `pip install pip-audit==2.10.1`, version-pinned. 16-RESEARCH's Package Legitimacy Audit records pip-audit as Approved (PyPA-maintained, slopcheck `[OK]`) and tflint's release assets as checksum- and cosign-signature-verified. No `[ASSUMED]` or `[SUS]` package in this phase, so no blocking legitimacy checkpoint is required |
</threat_model>

<verification>
- `yaml.safe_load` on `security.yml`: five jobs, five byte-exact names, zero `needs:`, `workflow_call`, `permissions: {contents: read}`
- Three guarded scan steps, all `continue-on-error: true`
- Three report-verification steps, none `continue-on-error`
- Three guarded evidence steps plus the untouched unconditional Trivy `ls`
- `yamllint -d relaxed` and `pre-commit run --all-files` both exit 0
- `cicd/` is untouched in the branch diff
</verification>

<success_criteria>
The `sca` job runs four scanners — Trivy fs, npm audit, pip-audit and tflint — each guarded by a shared
detector and each followed by an intolerant report check, while the workflow keeps the exact five-job,
five-name, zero-`needs`, `contents: read` shape Phase 18 depends on.
</success_criteria>

<output>
Create `.planning/phases/16-sca-ecosystem-coverage/16-04-SUMMARY.md` when done. Record the verbatim `sca`
job name as it stands after this plan (unchanged), the numbered report filenames each sub-scan produces,
and the fact that `cicd/.github/workflows/security.yml` was deliberately not modified — Phase 17 needs all
three.
</output>
