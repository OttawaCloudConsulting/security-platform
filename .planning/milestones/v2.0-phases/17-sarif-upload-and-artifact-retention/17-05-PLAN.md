---
phase: 17-sarif-upload-and-artifact-retention
plan: 05
type: execute
wave: 5
depends_on: [17-04]
files_modified:
  - repos/security-platform/fixtures/main.tf
autonomous: true
requirements: [CICD-02, CICD-03]

must_haves:
  truths:
    - "A real workflow run on a real pull request uploads six analyses and five artifacts"
    - "The six categories are present on the PR merge ref and are distinct, so no scanner's results overwrote another's"
    - "Every artifact is listed with a name, a size and an expiry date roughly 90 days out"
    - "The SCA artifact really contains the numbered npm and pip reports the glob was written for"
    - "The verification PR touches a line a scanner actually flags, so an absence of inline annotations means something"
  artifacts:
    - path: "repos/security-platform/fixtures/main.tf"
      provides: "a deliberate edit inside a Checkov-flagged line range that preserves the finding"
      contains: "resource"
  key_links:
    - from: "the phase branch's workflow run"
      to: "GitHub code scanning analyses for refs/pull/<N>/merge"
      via: "gh api code-scanning/analyses — the branch/PR filter D-02 requires"
      pattern: "code-scanning/analyses"
    - from: "the workflow run"
      to: "five downloadable artifacts with expires_at"
      via: "gh api actions/runs/<id>/artifacts"
      pattern: "expires_at"
---

<objective>
Push the phase branch, open the pull request, and collect the live evidence for Success Criteria 1 and 3 —
plus the raw material Criterion 2 is judged on in 17-07.

Purpose: everything before this plan is static. The repo's measured baseline is `total_count: 0` artifacts
and HTTP 404 `"no analysis found"` on code scanning; this plan is what changes both, and it is the first
time the permission grant, the categories and the retention keys meet the real GitHub API. RESEARCH
Pitfall 4 is the trap: GitHub only annotates "new alerts on lines of code changed in the pull request", so
a PR that does not touch a flagged line shows zero annotations even with a flawless upload. The
verification PR is therefore CONSTRUCTED, not assumed.

Output: a live PR, a completed run, and recorded evidence — category list, artifact list with expiries, SCA
artifact contents, and the byte-exact check-run names for Phase 18. No merge — that is 17-07.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-RESEARCH.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-PATTERNS.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-04-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md

<interfaces>
<!-- REPO: OttawaCloudConsulting/security-platform (PUBLIC). Local checkout: repos/security-platform.  -->
<!--                                                                                                   -->
<!-- MEASURED "BEFORE" BASELINE (17-RESEARCH §Runtime State Inventory / §Environment Availability):     -->
<!--   actions/artifacts        -> total_count: 0                                                       -->
<!--   code-scanning/analyses   -> HTTP 404 "no analysis found"  (healthy pre-upload state, NOT a 403)  -->
<!--   repo actions settings    -> enabled: true, allowed_actions: all, sha_pinning_required: false     -->
<!--                                                                                                   -->
<!-- THE THREE LIVE COMMANDS (17-RESEARCH §Phase Requirements → Test Map):                              -->
<!--   gh api repos/OttawaCloudConsulting/security-platform/code-scanning/analyses \                    -->
<!--     ?ref=refs/pull/<N>/merge --jq '[.[].category]|unique'                                          -->
<!--   gh api repos/OttawaCloudConsulting/security-platform/actions/runs/<RUN>/artifacts \              -->
<!--     --jq '.artifacts[]|{name,expires_at,size_in_bytes}'                                            -->
<!--   gh run download <RUN> -n sca-results -D <dir>   then list the directory                          -->
<!--                                                                                                   -->
<!-- gh SCOPE WARNING (17-RESEARCH §Environment Availability): the `security_events` scope on the local -->
<!--   gh token is UNVERIFIED. The pre-phase 404 proves the endpoint is reachable, but a post-upload    -->
<!--   read may still fail on scope. Fallback: gh auth refresh -h github.com -s security_events.        -->
<!--   Preflight `gh auth status` BEFORE the first analyses read and report what it shows.              -->
<!--                                                                                                   -->
<!-- ANNOTATION-CAPABLE SOURCES — measured in 17-RESEARCH Pattern 3 / Pitfall 4. Exactly three:         -->
<!--   fixtures/main.tf           Checkov CKV_AWS_23/24 (~L26-34), CKV_TF_1/2 (~L38-40); tflint 7/16/38 -->
<!--   fixtures/Dockerfile        Semgrep missing-user (L6)                                             -->
<!--   fixtures/package-lock.json Trivy fs (L15-20)                                                     -->
<!--   NOT annotation-capable, by construction — do not write any check expecting them:                 -->
<!--   the container scan (all 56 results point at `library/scan-fixture` 1:1, not a repo file) and     -->
<!--   Gitleaks (all 9 findings point at .planning/ and .claude/ paths ABSENT from the current tree).   -->
<!--                                                                                                   -->
<!-- D-02 CONSEQUENCE, stated so it is not rediscovered as a surprise: the caller triggers on           -->
<!--   pull_request ONLY, so `main` is never analysed and NO alert this phase produces is associated    -->
<!--   with the default branch. Criterion 1 is therefore verified through the branch/PR filter and the  -->
<!--   analyses API, never through the unfiltered Security tab. Whether PR diff annotations render AT   -->
<!--   ALL with no base analysis to diff against is genuinely UNVERIFIED (RESEARCH Q2) — the user       -->
<!--   accepted that when locking D-02.                                                                 -->
<!--                                                                                                   -->
<!-- PHASE 16 LIVE-RUN PRECEDENT (16-05-SUMMARY): the check-runs endpoint returned SIX checks on the    -->
<!--   head SHA — the five security/* jobs plus an external GitGuardian Security Checks app.            -->
</interfaces>

<notes>
**TARGET REPOSITORY.** All git and `gh` operations run against `repos/security-platform`. Never push from
the outer docs repo.

**Do not merge anything in this plan.** The PR stays open for human sign-off in 17-07.

**Do not add `push: branches: [main]`** to make the Security tab easier to read. D-02 is locked. If the
unfiltered view looks empty, that is the expected consequence, and the evidence is the filtered view and
the analyses API.

**If zero inline annotations appear, that is recorded — not fixed and not failed.** Criterion 2's own
wording is "wherever the tool reports a file and line". If the analyses API shows the six categories but
the PR diff shows no annotations, record Criterion 2 as **NOT OBSERVED** with the D-02 no-base-analysis
consequence named as the probable cause, and hand it to Phase 19 (VAL-01), which owns the end-to-end
proof. Do not add a trigger, do not fabricate a screenshot, and do not score it PASSED off the Security
tab — the Security tab is Criterion 1's evidence, and they are different things. This is the same
discipline 16-07 applied when it recorded Criterion 4 as met by a negative test rather than a live skip.

**If a verification step fails on the live run, that is the phase working.** An intolerant `.outcome`
assertion turning a job red means an upload did not land. STOP and REPORT with the raw step log; the most
likely cause is the caller-side permission grant. Do not relax the assertion.

**Do not delete a fixture to manufacture a skip.** The repo carries all three ecosystems and Criterion 4's
clean-skip behaviour is already evidenced by 16-03's empty-repo negative test.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct a verification edit on a line a scanner actually flags</name>
  <files>repos/security-platform/fixtures/main.tf</files>
  <read_first>
    - repos/security-platform/fixtures/main.tf (the file being modified — every resource block, so the edit lands inside a flagged range rather than near one)
    - repos/security-platform/fixtures/README.md (the measured finding counts recorded in 16-01 — the baseline this edit must not change)
    - .planning/phases/17-sarif-upload-and-artifact-retention/17-RESEARCH.md lines 513-528 (Pitfall 4 — why the PR must touch a flagged line, and the three annotation-capable sources)
    - .planning/phases/16-sca-ecosystem-coverage/16-01-SUMMARY.md (what the fixture currently contains and why — the unconstrained provider and the unpinned module are load-bearing for SCA-03)
  </read_first>
  <action>
    First MEASURE, do not assume. Run Checkov locally with the smoke gate's exact comma-mapped invocation
    (`checkov -d . --quiet --compact --output cli --output json --output sarif --output-file-path
    "console,<dir>/checkov-results.json,<dir>/checkov.sarif"`, `scripts/smoke-scans.sh` lines 244-248) —
    that form PINS the output filenames, whereas `--output sarif` alone lets Checkov choose the name. Then
    read the SARIF for `fixtures/main.tf`:
    record each result's `ruleId` and its `locations[0].physicalLocation.region.startLine` and `endLine`.
    Also run `tflint --recursive --format sarif` from the repo root and record its result line numbers.
    Print both sets — they are the evidence for choosing the edit, and they go in the SUMMARY.

    Then choose ONE line that sits inside a flagged region and edit it in a way that CANNOT remove the
    finding. A comment appended to a flagged resource line, or a `description`/`tags` string change inside
    a flagged resource block, are both safe; changing a `cidr_blocks` value, adding a version constraint,
    or pinning the module are NOT — each would delete the very finding the annotation depends on. Add a
    short comment in the fixture noting the line is deliberately touched to exercise PR diff annotations.

    Re-run BOTH tools and compare against the pre-edit measurement: the same rule ids must still fire and
    the count must not drop. If anything changed, revert the edit and pick a different line — do not
    proceed with a fixture whose findings moved.

    Note that `fixtures/` is excluded from four pre-commit hooks (`terraform_fmt`, `terraform_validate`,
    `hadolint`, `npm-audit`) by 15-01, so the commit will not be blocked by the repo's own Terraform hooks.
    Commit: `test(17-05): touch a Checkov-flagged fixture line to exercise PR diff annotations`.
  </action>
  <verify>
    <automated>cd repos/security-platform && git diff HEAD~1 --stat -- fixtures/main.tf | grep -q 'main.tf' && test "$(git diff HEAD~1 -- fixtures/main.tf | grep -c '^+')" -le 6 && echo "edit is small and confined to fixtures/main.tf"</automated>
    <automated>cd repos/security-platform && rm -rf /tmp/17-05-checkov && mkdir -p /tmp/17-05-checkov && checkov -d . --quiet --compact --output cli --output json --output sarif --output-file-path "console,/tmp/17-05-checkov/checkov-results.json,/tmp/17-05-checkov/checkov.sarif" >/dev/null 2>&1; python3 - /tmp/17-05-checkov/checkov.sarif <<'PY'
import json
import sys
d = json.load(open(sys.argv[1]))
res = [r for run in d['runs'] for r in run.get('results', [])]
tf = [r for r in res if 'main.tf' in r['locations'][0]['physicalLocation']['artifactLocation']['uri']]
ids = sorted({r['ruleId'] for r in tf})
lines = sorted({r['locations'][0]['physicalLocation']['region']['startLine'] for r in tf})
print('post-edit checkov main.tf rule ids:', ids)
print('post-edit checkov main.tf start lines:', lines)
assert tf, 'the edit removed every Checkov finding on fixtures/main.tf'
PY</automated>
    <automated>cd repos/security-platform && tflint --recursive --format sarif > /tmp/17-05-tflint.sarif; python3 - /tmp/17-05-tflint.sarif <<'PY'
import json
import sys
d = json.load(open(sys.argv[1]))
ids = sorted({r.get('ruleId') for run in d['runs'] for r in run.get('results', [])})
print('post-edit tflint rule ids:', ids)
assert ids, 'the edit removed every tflint finding'
PY</automated>
  </verify>
  <acceptance_criteria>
    - The pre-edit and post-edit Checkov rule-id sets for `fixtures/main.tf` are both printed and are IDENTICAL; the post-edit result count is not lower than the pre-edit count
    - The pre-edit and post-edit tflint rule-id sets are both printed and are identical (16-05 recorded three ids; a drift is reported, not absorbed)
    - The edited line number falls inside a `startLine`..`endLine` region of at least one recorded Checkov or tflint result — the SUMMARY names which rule id and which region
    - The diff touches only `fixtures/main.tf` and adds at most six lines
    - The fixture carries a comment explaining that the line is deliberately touched to exercise PR diff annotations
    - No `cidr_blocks` value, provider version constraint or module `ref` was changed
  </acceptance_criteria>
  <done>The PR about to be opened modifies a line that a scanner really flags, so the presence or absence of an inline annotation is informative rather than meaningless.</done>
</task>

<task type="auto">
  <name>Task 2: Push, open the PR, and collect the live upload, artifact and check-run evidence</name>
  <files></files>
  <read_first>
    - repos/security-platform/.github/workflows/security.yml (the committed workflow whose live behaviour is being observed — the 11 upload steps and their 11 assertions, so the job logs can be read against what was intended)
    - .planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md (the live-run procedure this task repeats: push, `gh pr create`, watch, evidence collection, and the six-checks-on-head-SHA finding)
    - .planning/phases/17-sarif-upload-and-artifact-retention/17-RESEARCH.md lines 888-903 (the exact live commands and what each proves)
    - .planning/phases/17-sarif-upload-and-artifact-retention/17-RESEARCH.md lines 798-822 (Phase 18 hand-forwards — what must be recorded byte-for-byte from the check-run list, and why nothing may be added to a required-check list yet)
  </read_first>
  <action>
    Preflight first: run `gh auth status` and record which scopes the token carries. If `security_events`
    is absent, run `gh auth refresh -h github.com -s security_events` before touching the analyses
    endpoint, and record that you did. Also re-record the "before" state —
    `gh api repos/OttawaCloudConsulting/security-platform/actions/artifacts --jq .total_count` and
    `gh api repos/OttawaCloudConsulting/security-platform/code-scanning/analyses` — so the phase has a
    measured before/after pair rather than a recalled one.

    Push the phase branch and open the PR against `main` with `gh pr create`, titling it for Phase 17 and
    describing in the body: the six SARIF categories, the five artifacts and their 90-day retention, the
    caller-side permission grant, the `trivy convert` → direct SARIF change in the `sca` job, and the
    deliberate fixture edit with the rule region it lands in. State in the body that the trigger is
    `pull_request`-only by decision D-02, so alerts are associated with the PR rather than the default
    branch.

    Watch the run to completion. Then collect, printing each result in full:

    1. Per-job conclusions for the five `security / *` checks, and the FULL check-run list on the head SHA
       from `gh api repos/.../commits/<sha>/check-runs --jq '[.check_runs[].name]'`. Record every name
       BYTE-FOR-BYTE — 16-05 saw six checks (five jobs plus an external GitGuardian app); code scanning adds
       more, and Phase 18 needs to know whether GitHub creates one check per `tool.driver.name` or one per
       `category`, because `Trivy` appears under two categories and the two hypotheses predict different
       counts. Record the observation; do NOT add anything to a required-check list — that is Phase 18's
       deliberate decision and a code-scanning check that is `neutral` when no alerts are found behaves
       differently from a job check.
    2. The eleven assertion steps' conclusions from the run log — all eleven must be green or skipped, and
       any skip must be explained by its own guard, not assumed.
    3. `gh api repos/.../code-scanning/analyses?ref=refs/pull/<N>/merge --jq '[.[].category]|unique'` —
       expect exactly the six categories. Also capture each analysis's `tool.name` so the measured driver
       names (`Semgrep OSS`, `Checkov`, `Trivy` twice, `tflint`, `gitleaks`) are confirmed LIVE for Phase 18
       rather than inherited from a local measurement.
    4. `gh api repos/.../actions/runs/<RUN>/artifacts --jq '.artifacts[]|{name,expires_at,size_in_bytes}'`
       — expect five artifacts, five distinct names, every `expires_at` roughly 90 days after the run, and
       every `size_in_bytes` greater than zero.
    5. `gh run download <RUN> -n sca-results -D <tmpdir>` then list the directory — assert at least one
       `npm-audit-*.json` and one `pip-audit-*.json` are present, proving the glob did what a literal
       filename could not, plus `trivy-fs.json`, `trivy-fs.sarif` and `tflint.sarif`.

    Leave the PR OPEN and unmerged. If any of the five checks is red, or the analyses list is missing a
    category, or fewer than five artifacts exist, STOP and REPORT with the raw output rather than re-running
    and hoping.
  </action>
  <verify>
    <automated>cd repos/security-platform && gh auth status 2>&1 | grep -i 'token scopes' || echo 'NOTE: scopes line not printed — record verbatim what gh reported'</automated>
    <automated>cd repos/security-platform && PR=$(gh pr view --json number -q .number) && echo "PR #$PR" && gh api "repos/OttawaCloudConsulting/security-platform/code-scanning/analyses?ref=refs/pull/$PR/merge" --jq '[.[].category]|unique' > /tmp/17-05-cats.json && python3 - /tmp/17-05-cats.json <<'PY'
import json
import sys
cats = json.load(open(sys.argv[1]))
want = {'semgrep', 'checkov', 'trivy-fs', 'tflint', 'trivy-image', 'gitleaks'}
missing = want - set(cats)
assert not missing, 'missing categories: %r (got %r)' % (sorted(missing), cats)
assert len(cats) == len(set(cats))
print('all six categories present and distinct:', sorted(cats))
PY</automated>
    <automated>cd repos/security-platform && RUN=$(gh run list --branch feature/phase-17-sarif-upload-and-artifact-retention --limit 1 --json databaseId -q '.[0].databaseId') && gh api "repos/OttawaCloudConsulting/security-platform/actions/runs/$RUN/artifacts" --jq '.artifacts[]|{name,expires_at,size_in_bytes}' > /tmp/17-05-artifacts.txt && cat /tmp/17-05-artifacts.txt && python3 - /tmp/17-05-artifacts.txt <<'PY'
import json
import sys
rows = [json.loads(ln) for ln in open(sys.argv[1]) if ln.strip()]
names = sorted(r['name'] for r in rows)
want = ['checkov-results', 'gitleaks-results', 'sca-results', 'semgrep-results', 'trivy-image-results']
assert names == want, names
assert all(r['size_in_bytes'] > 0 for r in rows), rows
assert all(r['expires_at'] for r in rows), 'an artifact has no expires_at — retention-days did not apply'
print('five artifacts, expected names, non-zero sizes, every one carrying an expiry')
PY</automated>
    <automated>cd repos/security-platform && RUN=$(gh run list --branch feature/phase-17-sarif-upload-and-artifact-retention --limit 1 --json databaseId -q '.[0].databaseId') && rm -rf /tmp/17-05-sca && gh run download "$RUN" -n sca-results -D /tmp/17-05-sca && ls -l /tmp/17-05-sca && ls /tmp/17-05-sca/npm-audit-*.json >/dev/null && ls /tmp/17-05-sca/pip-audit-*.json >/dev/null && test -s /tmp/17-05-sca/trivy-fs.json && test -s /tmp/17-05-sca/trivy-fs.sarif && test -s /tmp/17-05-sca/tflint.sarif && echo "SCA artifact carries the globbed npm/pip reports plus trivy-fs and tflint"</automated>
    <automated>cd repos/security-platform && SHA=$(git rev-parse HEAD) && gh api "repos/OttawaCloudConsulting/security-platform/commits/$SHA/check-runs" --jq '[.check_runs[].name]' | tee /tmp/17-05-checks.json && gh pr view --json state,mergeable -q '{state,mergeable}'</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status` output is recorded verbatim, including whether `security_events` was present or had to be refreshed
    - The "before" readings are re-taken and recorded: `actions/artifacts` total_count and the `code-scanning/analyses` response prior to the run
    - The PR is open against `main`, unmerged, and its body names the six categories, the five artifacts, the 90-day retention, the permission grant, the direct-SARIF change and the fixture edit
    - All five `security / *` checks conclude non-red; the five job names are byte-identical to the frozen strings
    - The full check-run name list on the head SHA is captured byte-for-byte and recorded, together with an explicit statement of how many checks code scanning added and whether that count matches one-per-tool or one-per-category — recorded as an observation, with nothing added to any required-check list
    - All eleven `.outcome` assertion steps conclude green or skipped, and every skip is attributed to a specific guard clause
    - `code-scanning/analyses?ref=refs/pull/<N>/merge` returns all six categories `semgrep, checkov, trivy-fs, tflint, trivy-image, gitleaks`, with no duplicates, and the live `tool.name` per analysis is recorded
    - Exactly five artifacts exist with names `semgrep-results`, `checkov-results`, `sca-results`, `trivy-image-results`, `gitleaks-results`, each with a non-zero size and a non-null `expires_at` roughly 90 days out
    - The downloaded `sca-results` artifact contains at least one `npm-audit-*.json`, at least one `pip-audit-*.json`, plus non-empty `trivy-fs.json`, `trivy-fs.sarif` and `tflint.sarif`
    - The PR remains OPEN (`gh pr view` state is OPEN) — nothing was merged
  </acceptance_criteria>
  <done>Criteria 1 and 3 have live, machine-read evidence, and the material Criterion 2 will be judged on in 17-07 exists on a real PR diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local workstation `gh` token → GitHub API | Evidence collection uses a human-held token; a missing `security_events` scope produces a read failure that must not be confused with an upload failure |
| Phase branch → `main` | This plan pushes a branch and opens a PR; it must not merge |
| PR head SHA → check-run surface | Successful uploads add new check runs that Phase 18 may later make merge-blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Repudiation | Evidence recalled rather than measured — scoring a criterion off a recollection or a green tick | mitigate | Every criterion is read from `gh api` output and pasted into the SUMMARY; before/after baselines are re-taken, not inherited |
| T-17-22 | Spoofing (of verification) | A 403 read failure on `code-scanning/analyses` being misread as "no analyses were created" | mitigate | `gh auth status` preflight with the documented `gh auth refresh -h github.com -s security_events` fallback; a 403 and a 404 are recorded distinctly |
| T-17-23 | Elevation of Privilege | Adding a code-scanning check to the required-check list before Phase 18 has evaluated its `neutral` behaviour | accept | Explicitly out of scope here: names are RECORDED only; Phase 18 owns the decision |
| T-17-14 | Elevation of Privilege | "Fixing" a missing annotation by widening the trigger to `push: main` | mitigate | D-02 is locked; the NOT OBSERVED outcome branch is pre-authorised in the notes so there is no incentive to widen scope |
| T-17-04 | Information Disclosure | The downloaded `gitleaks-results.json` artifact containing live secrets | mitigate | `--redact` is asserted present by the static gate and was verified in the measured output; the artifact is downloaded only to list filenames, never to publish content |
| T-17-24 | Tampering | Editing the fixture in a way that deletes the finding the annotation depends on | mitigate | Pre- and post-edit rule-id sets compared; a drift reverts the edit rather than being absorbed |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installed; this plan runs `gh`, `git`, `checkov` and `tflint` only |
</threat_model>

<verification>
- Six distinct categories on `refs/pull/<N>/merge`, read from the API, not the UI.
- Five artifacts with distinct names, non-zero sizes and non-null expiries.
- `sca-results` contains globbed `npm-audit-*.json` and `pip-audit-*.json`.
- Eleven `.outcome` assertions green or explicably skipped; five job checks non-red.
- Check-run name list captured byte-for-byte for Phase 18.
- PR open and unmerged.
</verification>

<success_criteria>
- Criterion 1 has live evidence under the branch/PR filter D-02 makes necessary.
- Criterion 3 has live evidence including the SCA sub-scan reports.
- Criterion 2's raw material exists on a PR diff that touches a genuinely flagged line, with the NOT
  OBSERVED outcome pre-authorised and bounded rather than improvised.
</success_criteria>

<output>
Create `.planning/phases/17-sarif-upload-and-artifact-retention/17-05-SUMMARY.md` when done.
Record: the PR number and run id; the before/after artifact and analyses readings; the six categories and
their live `tool.name` values; the five artifacts with `expires_at`; the `sca-results` file listing; the
complete byte-exact check-run name list with a count comparison against 16-05's six; and the exact
Checkov/tflint rule region the fixture edit landed in.
</output>
