---
phase: 20-template-packaging-and-adoption-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - repos/security-platform/scripts/check-detector-parity.sh
  - scripts/check-adoption-guide.sh
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "A standing offline gate proves the workflow's four ecosystem detect steps behave identically to scripts/detect-{npm,python,terraform}.sh on the same tree, and that the Dockerfile pathspec matches root, nested, dotted-suffix and .dockerfile forms"
    - "The gate was observed FAILING, with its failure shape predicted in advance, before the code that satisfies it existed"
    - "A second standing gate asserts the adoption guide's frozen strings (five check-run contexts, em dash U+2014, the corrected uses: path) without retyping them"
    - "The phase branch exists on the canonical host's remote so later plans can find it from a fresh clone"
  artifacts:
    - path: "repos/security-platform/scripts/check-detector-parity.sh"
      provides: "Wave 0 detector-parity and Dockerfile-pathspec gate; exit 0 pass / 1 assertion failure / 2 PyYAML missing"
      contains: "check-detector-parity.sh"
      min_lines: 120
    - path: "scripts/check-adoption-guide.sh"
      provides: "Wave 0 adoption-guide invariant gate; exit 0 pass / 1 guide defect / 2 canonical host clone absent"
      contains: "check-adoption-guide.sh"
      min_lines: 60
  key_links:
    - from: "repos/security-platform/scripts/check-detector-parity.sh"
      to: "the run bodies of the npm / py / tf / docker detect steps in .github/workflows/security.yml"
      via: "PyYAML step-id extraction, then execution under the runner's default shell semantics in a scratch git repo"
      pattern: "steps\\[.*'id'.*\\]"
    - from: "scripts/check-adoption-guide.sh"
      to: "the five job name: values in repos/security-platform/.github/workflows/security.yml"
      via: "derive the 'security / <name>' contexts from source rather than retyping them"
      pattern: "security / "
---

<objective>
Create the two Wave 0 gates 20-VALIDATION.md requires (tasks 20-01-01, 20-01-02 and the byte-exactness
requirement behind 20-04-02), and observe both failing before the deliverables they gate exist.

Purpose: every failure mode in this phase's domain fails *silently green*. A wrong pathspec produces a
`SKIP:` line on a repository that really does have dependencies; a hyphen typed where an em dash belongs
produces a permanently-pending required check. RESEARCH assumption A4 (the Dockerfile pathspec) is the one
detector pathspec this project has never measured, and it "must be measured before shipping, the same way
16-03 measured the other three." Writing the gate first, and watching it go red for the exact reasons
predicted, is what makes plan 03's green a signal instead of a claim.

Output: two committed gate scripts in two different git repositories, each observed red, plus the phase
branch pushed to the canonical host's remote.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-VALIDATION.md

**TWO REPOSITORIES, TWO COMMITS.** Task 1 commits to `repos/security-platform` (remote
`OttawaCloudConsulting/security-platform`). Task 2 commits to `security_solution` (this repo). `.gitignore`
here is one line, `repos/`, so no single commit can contain both. Never stage a path under `repos/` from
this repo.

**HOST PREFLIGHT — run before Task 1, every host-repo plan in this phase repeats it.**
1. If `repos/security-platform/.git` does not exist (the normal case in an isolated parallel worktree,
   18-05 precedent), clone it: `git clone https://github.com/OttawaCloudConsulting/security-platform.git repos/security-platform`.
2. `git -C repos/security-platform fetch origin --tags`.
3. The local clone is very likely on the ABANDONED branch `feature/phase-19-clean-pr` at `9483ba5`, one
   commit ahead of `origin/main` — that commit belongs to PR #12, which was CLOSED not merged (19-06). Do
   not build on it and do not delete it; plan 05 decides the fate of the README note it carries.
4. Create the phase branch from the remote main: `git -C repos/security-platform checkout -b feature/phase-20-template-packaging origin/main`.
   `origin/main` must be `b4cb207` (the Phase 19 merge). Record the SHA you actually observe.
5. Before finishing the task, `git -C repos/security-platform push -u origin feature/phase-20-template-packaging`.
   Later plans may run in a fresh worktree with a fresh clone and can only see PUSHED state.

**Project rule, restated in both existing gate scripts' headers:** never `chmod +x`; invoke every script as
`bash scripts/<name>.sh`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the detector-parity and Dockerfile-pathspec gate in the canonical host repo</name>
  <files>repos/security-platform/scripts/check-detector-parity.sh</files>
  <read_first>
    - `repos/security-platform/scripts/check-workflow-uploads.sh` lines 1-60 — the three-exit-code preamble (0 pass / 1 assertion failure / 2 PyYAML not importable, with NO regex fallback), the `REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` + `cd "$REPO_ROOT"` convention, and the no-`chmod` header note
    - `repos/security-platform/scripts/smoke-scans.sh` lines 38-137 and 395-420 — the `FAILURES=()` accumulator (report every failure, never stop at the first), and the convention of passing an explicit `$OUT` list-file path so nothing is left untracked in the checkout
    - `repos/security-platform/scripts/detect-npm.sh` (all 60 lines) — the reference implementation, its pathspec comment block, its byte-exact `SKIP:`/`FOUND` strings, its sanctioned single `|| true`, and the fact that it writes the LIST FILE only on the FOUND branch
    - `repos/security-platform/scripts/detect-python.sh` and `detect-terraform.sh` — the same shape with pathspecs `'*requirements*.txt'` and `'*.tf'`
    - `repos/security-platform/.github/workflows/security.yml` lines 385-405 (the four-line detect-step block and the comment above it) and lines 515-535 and 580-595 (the `done 3< npm-lockfiles.txt` and `done 3< py-reqs.txt` consumers — these filenames are a contract)
  </read_first>
  <action>
    Create `scripts/check-detector-parity.sh` in the canonical host repo. Copy the exit-code contract,
    `REPO_ROOT` cd, and no-fallback stance from `check-workflow-uploads.sh`; copy the `FAILURES` array
    accumulator from `smoke-scans.sh`. Exit codes: 0 all checks passed, 1 at least one assertion failed,
    2 preflight failed because `python3 -c 'import yaml'` fails (an infrastructure problem on this machine,
    never conflated with a workflow defect).

    Extract step `run` bodies from `.github/workflows/security.yml` with PyYAML by JOB and STEP ID — job
    `sca` step ids `npm`, `py`, `tf`; job `container` step id `docker`. This is the 16-04 technique:
    execute the body CI runs, not a copy of it. A step id that does not exist is an assertion failure with
    the message naming the missing id, not a crash.

    Execute each extracted body with `bash -e` and NOT `-o pipefail`. Measured: `security.yml` contains zero
    `shell:` keys and no `defaults:` block, so the runner uses its default `bash -e {0}`. Running the body
    under `bash -euo pipefail` would prove parity under semantics the runner does not use — and pipefail is
    exactly what makes the sanctioned `|| true` on the discovery pipeline necessary or not. Also assert that
    no `shell:` key exists on any of the four detect steps, so this harness cannot silently drift from the
    runner.

    Assertions, all accumulated:
    - SOURCE, per detect step: the body contains `git ls-files` and does NOT contain `bash scripts/detect-`.
      (At this plan's commit all four of these FAIL — that is the point.)
    - SOURCE, per detect step: the discovery pipeline carries `|| true`, and the `|| true` is not on any line
      that decides `found=true`/`found=false`.
    - SOURCE: the bodies still write the list files `npm-lockfiles.txt` and `py-reqs.txt` under exactly those
      names — `security.yml` lines 530 and 592 read them on fd 3, so a renamed list file breaks the npm and
      pip sub-scans without touching the detect step.
    - BEHAVIOUR, positive, per ecosystem: build a scratch git repo (in the session scratchpad, `git init`,
      one commit) containing that ecosystem's file at the ROOT and again in a nested directory; run the
      extracted body with `GITHUB_OUTPUT` pointed at a temp file; assert stdout contains `FOUND 2` and the
      output file contains `found=true`.
    - BEHAVIOUR, negative, per ecosystem: an EMPTY scratch git repo; assert rc=0, stdout contains the
      byte-exact `SKIP:` string copied from the corresponding `detect-*.sh` (em dash U+2014 included), and
      the output file contains `found=false`.
    - PARITY, per ecosystem npm/py/tf: on the same positive scratch tree, also run
      `bash <host>/scripts/detect-<x>.sh "$OUT/<list>"` and diff its stdout and its `found=` line against the
      extracted body's. Byte-equal stdout and equal `found=` value is the parity claim — it is what replaces
      the now-false "the logic lives in shared scripts so CI and the smoke gate exercise the same
      implementation" comment plan 03 deletes. Two differences are OUT of parity scope and must be stated in
      the script as deliberate: (i) on the SKIP branch `detect-npm.sh` writes no list file while an inlined
      `> npm-lockfiles.txt` redirect leaves an empty one — harmless because every consumer step is guarded on
      `steps.<id>.outputs.found == 'true'`; (ii) the scripts run under their own `set -euo pipefail` shebang
      while the inlined bodies run under `bash -e`. Where a parity run is impossible because the body still
      delegates to the script, record the failure as "not yet inlined", do not fabricate a pass.
    - NPM EXCLUSION: a scratch tree with `node_modules/x/package-lock.json` and `pkg/node_modules/y/package-lock.json`
      plus one real root lockfile yields `FOUND 1`.
    - A4 PATHSPEC (independent of the YAML, so it PASSES at this commit): in a scratch tree containing
      `Dockerfile`, `svc/Dockerfile`, `Dockerfile.dev`, `app.dockerfile` and a decoy `docs/notes-Dockerfile.md`,
      assert `git ls-files -- '*Dockerfile' '*Dockerfile.*' '*.dockerfile'` returns exactly those four paths
      and not the decoy. Print the measured list so the number lands in the SUMMARY. Also assert
      `dirname "$(head -1 …)"` yields `.` for a root Dockerfile — the build-context derivation plan 03 uses.

    Print a final `PASSED n / FAILED m` line and every accumulated failure with its check name. Header
    comment must state the no-`chmod` rule and `bash scripts/check-detector-parity.sh` as the invocation.

    Run the gate. PREDICT the failure shape in writing BEFORE running it, then compare: the four SOURCE
    "still delegates" / "step id docker not found" failures plus their dependent BEHAVIOUR/PARITY failures,
    zero A4 failures, exit 1. If the observed shape differs from the prediction, stop and report — a
    different shape means the extraction or the assertion is wrong, not the workflow.

    Commit to the phase branch in the host repo and push per the HOST PREFLIGHT step 5.
  </action>
  <verify>
    <automated>bash -c 'cd repos/security-platform && bash scripts/check-detector-parity.sh; rc=$?; [ "$rc" -eq 1 ] || { echo "expected exit 1 (gate red before plan 03), got $rc"; exit 1; }'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-detector-parity.sh` run from `repos/security-platform` exits 1, prints a `FAILED` count greater than zero, and every printed failure is one of: a detect step still delegating to `bash scripts/detect-`, step id `docker` absent, or a BEHAVIOUR/PARITY check blocked by one of those.
    - Zero failures reference the A4 Dockerfile pathspec; the four expected paths and the excluded decoy are printed.
    - `python3 -c 'import yaml'` failing makes the script exit 2, verified by running it with a PATH or PYTHONPATH that hides PyYAML (or by asserting the preflight branch exists and is reached before any assertion).
    - The script contains no `chmod` and the file mode is 100644 (`git ls-files -s scripts/check-detector-parity.sh` starts with `100644`).
    - `git -C repos/security-platform log --oneline -1` shows the new commit on `feature/phase-20-template-packaging`, and `git ls-remote --heads origin feature/phase-20-template-packaging` returns a SHA equal to local HEAD.
  </acceptance_criteria>
  <done>A standing parity/pathspec gate exists in the host repo, is red for exactly the predicted reasons, and the A4 assumption is measured.</done>
</task>

<task type="auto">
  <name>Task 2: Author the adoption-guide invariant gate in this repo</name>
  <files>scripts/check-adoption-guide.sh</files>
  <read_first>
    - `repos/security-platform/scripts/check-workflow-uploads.sh` lines 1-60 — the same exit-code and no-fallback preamble to mirror
    - `repos/security-platform/.github/workflows/security.yml` — the five job `name:` values (`SAST — Semgrep CE`, `IaC — Checkov`, `SCA — Trivy Filesystem`, `Container — Trivy Image`, `Secrets — Gitleaks`), which are the source the contexts are DERIVED from
    - `repos/security-platform/.github/workflows/pr-security.yml` — the frozen caller job id `security`, which is the `security / ` prefix
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md` §Shared Patterns → "Byte-exact frozen strings" and "Markdown lint gate"
    - `.markdownlint-cli2.yaml` — the rules still ENABLED (MD001, MD009, MD010, MD025, MD034) that the guide must pass
  </read_first>
  <action>
    Create `scripts/check-adoption-guide.sh` in this repo (a new top-level `scripts/` directory — this repo
    has none today; plan 12 adds the CLAUDE.md structure bullet for it). It takes an optional guide path,
    defaulting to `docs/adoption-guide.md`. Exit codes: 0 all checks passed, 1 at least one assertion failed
    (including "guide not found" — the guide is a pending deliverable, not an infrastructure problem),
    2 the canonical host clone `repos/security-platform/.github/workflows/` is absent, so the frozen strings
    cannot be derived (an infrastructure signal; never fall back to hard-coded strings).

    DERIVE the five required check-run contexts instead of retyping them: read the five job `name:` values
    from `repos/security-platform/.github/workflows/security.yml` and the caller job id from
    `pr-security.yml`, and compose `<caller job id> / <job name>`. Then assert each composed context appears
    in the guide, and that each occurrence's separator codepoint is U+2014 (dump the bytes; a hyphen or en
    dash is invisible in a renderer and produces a permanently-pending required check).

    Further assertions, all accumulated and reported together:
    - The guide contains exactly five such contexts and no sixth `security / …` string. A bare `security`
      check run does not exist (verified 14-02 and 18-04) and a sixth required context would sit pending.
    - The string `OCC-github` appears ZERO times. It is a dead path (RESEARCH C-1: the org 404s).
    - Every reusable-workflow reference in the guide is exactly
      `OttawaCloudConsulting/security-platform/.github/workflows/security.yml@v1` or `…@v1.0.0`; no `@main`,
      no bare `@<SHA>` placeholder, no other owner or repo.
    - Every `raw.githubusercontent.com` URL in the guide is pinned at `/v1/` (never `/main/`).
    - The banned patterns `|| true` and `--config auto` appear ZERO times outside an explicitly labelled
      anti-pattern block; count them with comment/prose lines excluded rather than a bare `grep -c`.
    - The guide never instructs a consumer to create anything named `fixtures/` — that is validation-only
      scaffolding local to `security-platform` (19-06), and a consumer that adds it inherits permanent
      findings and can never go blocking. Assert zero occurrences of `fixtures/` outside a sentence that
      names it as security-platform-only.
    - `markdownlint-cli2` is invoked on the guide and must report zero violations for the rules left enabled
      in `.markdownlint-cli2.yaml`.

    Run it. It must exit 1 with a "guide not found" assertion because `docs/adoption-guide.md` does not
    exist yet. Commit to this repo (a separate commit from Task 1's, in a different repository).
  </action>
  <verify>
    <automated>bash -c 'bash scripts/check-adoption-guide.sh; rc=$?; [ "$rc" -eq 1 ] || { echo "expected exit 1 (guide absent), got $rc"; exit 1; }'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-adoption-guide.sh` exits 1 and its first reported failure names `docs/adoption-guide.md` as not found.
    - Running it with `repos/security-platform/.github/workflows/security.yml` temporarily unreadable exits 2, and the message distinguishes "cannot derive the frozen strings" from "the guide is wrong".
    - The five contexts are printed as derived, and a byte dump shows `e2 80 94` (U+2014) as the separator in each.
    - The script contains no hard-coded copy of the five context strings for comparison purposes — `grep -c 'SAST — Semgrep CE' scripts/check-adoption-guide.sh` with comment lines excluded returns 0.
    - `git ls-files -s scripts/check-adoption-guide.sh` starts with `100644`, and `git log --oneline -1` in this repo shows the commit.
  </acceptance_criteria>
  <done>A standing guide gate exists in this repo, derives the frozen strings from source rather than retyping them, and is red because the guide has not been written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `security.yml` source → this harness | The harness must exercise the bytes CI runs, under the shell CI uses, or its green is meaningless |
| consumer repository tree → detector pathspec | An untrusted (unknown-shape) repository is the input; a missed manifest is a silent false pass |
| `security.yml` job names → adoption guide prose | A retyped string is a tampering vector with no runtime signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | inlined npm/python/terraform detect steps | mitigate | SOURCE + BEHAVIOUR + PARITY assertions per ecosystem, executed under `bash -e` (the runner's default), including the `node_modules` exclusion case |
| T-20-02 | Tampering | Dockerfile pathspec (assumption A4) | mitigate | Fixture tree with root / nested / `Dockerfile.dev` / `.dockerfile` / decoy; exact-set assertion, measured not assumed |
| T-20-12 | Tampering | the five frozen check-run contexts in the adoption guide | mitigate | Contexts derived from `security.yml` job names and the frozen caller job id, with a U+2014 byte dump; no hard-coded comparison copy permitted |
| T-20-03 | Tampering | the `npm-lockfiles.txt` / `py-reqs.txt` list-file contract | mitigate | SOURCE assertion that both names survive, because lines 530 and 592 read them on fd 3 |
| T-20-SC | Tampering | package-manager installs | accept | No package is installed; PyYAML and markdownlint-cli2 are pre-existing (RESEARCH §Environment Availability) and a missing PyYAML exits 2 rather than falling back |
</threat_model>

<verification>
- `bash repos/security-platform/scripts/check-detector-parity.sh` exits 1 with the predicted failure shape and zero A4 failures.
- `bash scripts/check-adoption-guide.sh` exits 1 on the absent guide.
- Two commits exist in two repositories; neither contains a path belonging to the other.
- `git ls-remote --heads origin feature/phase-20-template-packaging` (in the host repo) resolves.
</verification>

<success_criteria>
Both Wave 0 items in 20-VALIDATION.md are satisfied by committed, reusable gates that are red for stated
reasons, and A4 is no longer an assumption.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-02-SUMMARY.md` when done. Record the
observed `origin/main` SHA, the predicted-vs-observed failure shape, and the measured A4 path set.
`requirements.mark-complete` is deliberately NOT invoked here — plan 12 owns it.
</output>
