---
phase: 20-template-packaging-and-adoption-docs
plan: 05
type: execute
wave: 4
depends_on: [20-04]
files_modified:
  - repos/security-platform/cicd/README.md
  - repos/security-platform/cicd/.github/workflows/security.yml
  - repos/security-platform/cicd/renovate.json
  - repos/security-platform/README.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "The canonical host repository contains exactly ONE GitHub Actions security workflow that anyone could deploy — the stale pre-Phase-14 third copy is gone, not merely annotated"
    - "cicd/README.md no longer instructs a reader to deploy the stale template, no longer credits Grype or Renovate, and no longer lists the five required checks by job id"
    - "The host repository's front-page README names where the canonical workflow lives and points at the adoption guide"
    - "Nothing in the Azure DevOps or GitLab members of the cicd/ package is presented as validated when it never was"
  artifacts:
    - path: "repos/security-platform/cicd/README.md"
      provides: "M2 deployment guide corrected to the shipped pipeline, pointing at the canonical workflow and the adoption guide"
      contains: ".github/workflows/security.yml"
    - path: "repos/security-platform/README.md"
      provides: "Front-page pointer to the canonical reusable workflow and the adoption guide"
      contains: "adoption-guide"
  key_links:
    - from: "repos/security-platform/cicd/README.md deployment section"
      to: "the canonical .github/workflows/security.yml and the adoption guide in security_solution"
      via: "a pointer replacing the cp -r cicd/.github/ recipe"
      pattern: "adoption-guide"
---

<objective>
Remove the second stale copy-paste template — the one inside the canonical host repository itself — and
correct the deployment guide sitting next to it.

Purpose: 20-PATTERNS.md found what RESEARCH missed: `repos/security-platform/cicd/.github/workflows/security.yml`
is a 203-line pre-Phase-14 template carrying every defect RESEARCH attributes to the blueprint's illustrative
section (`actions/checkout@34e1148…  # v4` against the canonical v7.0.1, `pip install semgrep` unpinned,
`--config auto`, `|| true` silent fallbacks, two permission grants instead of three, no `workflow_call`, no
`gate_mode`, no verify steps, no `retention-days`, no SARIF `category`). `cicd/README.md` tells a reader to
deploy it with `cp -r cicd/.github/ <target-repo>/.github/`, credits Grype for the SCA job, credits Renovate
plus a Marketplace app for SHA currency, lists the required checks as `sast`/`iac`/`sca`/`container`/`secrets`,
and marks CICD-01 through CICD-05 "Planned". Two deliverables describing one procedure is the exact failure
18-06 had to repair between the blueprint and the milestone plan — and this one is a supply-chain hazard, not
just a documentation one, because the stale file is deployable as written.

Output: one commit in the canonical host repo removing the stale template and correcting both READMEs. The
deletion is surfaced for operator review at plan 06's merge checkpoint before anything reaches `main`.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-04-SUMMARY.md

**HOST REPO ONLY.** Repeat the HOST PREFLIGHT: clone if `repos/security-platform/.git` is absent,
`fetch origin`, `checkout feature/phase-20-template-packaging`, confirm plan 04's banner is present
(`grep -c "gh variable set GATE_MODE" .github/workflows/pr-security.yml` returns at least 1), and
`push origin HEAD` at the end.

**Chesterton's fence, articulated before removal.** `cicd/` is the Milestone 2 draft package, authored
2026-03-22, months before Phase 14 existed. Its GitHub member was the design sketch that Phases 14-19 then
built, validated live, and superseded. Its Azure DevOps and GitLab members were never built and never
validated — they stay in place, and this plan labels them as the drafts they are rather than deleting work
the project may still want.

**The host repo runs its own pre-commit hooks on commit** (markdownlint-cli2, shellcheck, yamllint and
others). A commit that fails them is a real failure, not a nuisance — do not use `--no-verify`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the stale third template and correct cicd/README.md to the shipped pipeline</name>
  <files>repos/security-platform/cicd/README.md, repos/security-platform/cicd/.github/workflows/security.yml, repos/security-platform/cicd/renovate.json</files>
  <read_first>
    - `repos/security-platform/cicd/README.md` in full (~8.5 KB) — in particular the What This Delivers table (lines ~16-23, the Grype row), the Requirements table (lines ~27-34, five "Planned" rows), the Deployment → GitHub Actions section (lines ~36-50, the `cp -r cicd/.github/` recipe and the Files deployed table), the Renovate section (lines ~55-66), the Branch Protection table (line ~125, the job-id check names and the "Settings → Branches" navigation), the Validation Checklist (lines ~178-189) and the Contents table (lines ~192-200)
    - `repos/security-platform/cicd/.github/workflows/security.yml` in full (203 lines) — read it before deleting it, and record in the SUMMARY which specific defects it carried, so the deletion rationale is evidence rather than assertion
    - `repos/security-platform/cicd/renovate.json` — the Renovate config that contradicts CICD-05 (Dependabot) and the project's "no external accounts" constraint
    - `repos/security-platform/.github/workflows/security.yml` — the canonical file that replaces it: the five job `name:` values, the four SCA tools actually used (Trivy filesystem, npm audit, pip-audit, tflint), the `retention-days: 90` artifact keys and the six SARIF categories
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md` §New Finding — the five measured contradictions, and the two reusable shapes (Files deployed table, Validation Checklist) the adoption guide will inherit
    - `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-06-SUMMARY.md` — the precedent: a negative-grep verify check can fail on its own prose, so a corrected passage must not contain the literal substring it forbids
  </read_first>
  <action>
    Delete `cicd/.github/workflows/security.yml` and `cicd/renovate.json` with `git rm`. Both are superseded
    by the canonical `.github/workflows/security.yml` and `.github/dependabot.yml` at the repository root, and
    both are actively misleading: the first is deployable as written and would give a consumer an unpinned
    Semgrep, `--config auto` (which the canonical file's own comment records as a hard error with
    `--metrics=off`), `|| true` silent fallbacks, and no `gate_mode`; the second points at a hosted Mend app,
    which contradicts CICD-05 and PROJECT.md's zero-external-accounts constraint.

    Rewrite the GitHub-facing parts of `cicd/README.md` so every statement matches the shipped pipeline:
    - Deployment → GitHub Actions: replace the `cp -r cicd/.github/` recipe with a pointer to the canonical
      pair at the repository root (`.github/workflows/security.yml`, `.github/workflows/pr-security.yml`) and
      to `docs/adoption-guide.md` in the `security_solution` documentation repository as the procedure of
      record for both consumption modes. Keep the Files deployed TABLE SHAPE — it is the analog the adoption
      guide reuses — but with the correct three files (`security.yml`, `pr-security.yml`, `dependabot.yml`).
    - What This Delivers: the `sca` row is Trivy filesystem plus npm audit, pip-audit and tflint, not Grype.
      State the per-tool failure semantics honestly: tflint signals findings with exit 2 while the others use
      exit 1, and pip-audit emits no severity field, so `blocking` is severity-agnostic and fails on ANY
      finding.
    - Requirements table: CICD-01 through CICD-05 are Complete (Phases 14-19), and CICD-05 is Dependabot, not
      Renovate. Add CICD-06 (`gate_mode`) as Complete.
    - Renovate section: replace with Dependabot (`.github/dependabot.yml`, `github-actions`, `directory: "/"`,
      weekly). No Marketplace app, no hosted service, no account.
    - Branch Protection row for GitHub: the enforcement surface is a repository RULESET read and written
      through the API (`/rulesets`, read back from `rules/branches/main`), not the classic branch-protection
      settings screen — whose `branches/main/protection` endpoint 404s by design on a rulesets-only repo and
      must never be cited as evidence (14-02). The five contexts are the `security / …` names with the em dash
      U+2014, never the bare job ids. Do not retype them: copy them from the canonical `security.yml` job
      `name:` values prefixed with `security / `, and state that this repository's own `main` deliberately
      leaves them unrequired (18-05, re-confirmed 19-07) because `fixtures/` guarantees findings.
    - Validation Checklist: keep the shape, drop the Renovate bullet, and replace "Confirm scanner failures
      block the merge" with the D-07 ordering — confirm the five contexts appear and conclude under
      `report-only` first, then flip to `blocking` and confirm they turn red, and only then make them required.
    - Add one short status note at the top of the Azure DevOps and GitLab sections: those members are
      unvalidated drafts; only the GitHub Actions pipeline has been built and live-proven (Phases 14-19).
    - Contents table: remove the two deleted rows.

    Because several corrected passages must NOT contain the strings they replace, verify each negative
    assertion with comments and fenced blocks excluded rather than a bare `grep -c`, and word the corrections
    so they do not reintroduce the forbidden substring as an example (the 18-06 lesson).
  </action>
  <verify>
    <automated>bash -c 'cd repos/security-platform && test ! -e cicd/.github/workflows/security.yml && test ! -e cicd/renovate.json && grep -v "^\s*#" cicd/README.md | grep -c "cp -r cicd/.github/" | grep -qx 0 && grep -c "Grype" cicd/README.md | grep -qx 0 && grep -c "Renovate" cicd/README.md | grep -qx 0 && markdownlint-cli2 cicd/README.md'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files cicd/` lists neither `.github/workflows/security.yml` nor `renovate.json`, and `git log --diff-filter=D --name-only -1` shows both as deleted in this commit.
    - `grep -c "cp -r cicd/.github/" cicd/README.md` returns 0; `grep -c "Grype" cicd/README.md` returns 0; `grep -c "Renovate" cicd/README.md` returns 0; `grep -c "renovate.json" cicd/README.md` returns 0.
    - `grep -c "adoption-guide" cicd/README.md` returns at least 1, and `grep -c "\.github/workflows/security.yml" cicd/README.md` returns at least 1.
    - The five `security / …` contexts in `cicd/README.md` byte-match the ones derived from the canonical `security.yml` job names, confirmed by the same derive-and-diff method plan 02's guide gate uses (em dash U+2014 verified by byte dump, not by eye).
    - `grep -Ec "sast.*iac.*sca.*container.*secrets" cicd/README.md` returns 0 for any line presenting bare job ids as required status checks.
    - Every `Planned` status in the Requirements table is gone: `grep -c "| Planned |" cicd/README.md` returns 0.
    - `markdownlint-cli2 cicd/README.md` reports zero violations, and the commit passes the repo's pre-commit hooks without `--no-verify`.
    - The SUMMARY lists, by line reference, the specific defects the deleted template carried — evidence for the deletion, not an assertion.
  </acceptance_criteria>
  <done>The host repository contains exactly one deployable GitHub Actions security workflow, and the guide beside it describes the pipeline that actually ships.</done>
</task>

<task type="auto">
  <name>Task 2: Point the host repository's front page at the canonical workflow and the adoption guide</name>
  <files>repos/security-platform/README.md</files>
  <read_first>
    - `repos/security-platform/README.md` in full — the Structure tree (it does not mention the root `.github/` at all), the Milestones table (`cicd/` is "In progress"), and the trailing paragraph about the report-only pipeline
    - `git show origin/main:README.md` — compare against the working tree. The local clone may be on the abandoned `feature/phase-19-clean-pr` branch whose commit `9483ba5` added the report-only paragraph; PR #12 was CLOSED, not merged, so that paragraph may be ABSENT from `origin/main`. Establish which before editing
    - `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-06-SUMMARY.md` — the wording and the measured claim behind that paragraph (five green checks is report-only tolerance, never zero findings)
  </read_first>
  <action>
    Determine whether `origin/main`'s `README.md` contains the report-only pipeline paragraph. If it does not,
    re-author it on this branch (do not cherry-pick from the abandoned branch; write it against the measured
    19-06 facts) so the note finally lands. Record which case applied.

    Add to the Structure tree an entry for the root `.github/` directory naming `workflows/security.yml` as
    the canonical callable workflow, `workflows/pr-security.yml` as the local caller, and `dependabot.yml`.
    Today the tree lists only `workstation/` and `cicd/`, which reads as though the pipeline lives under
    `cicd/` — the exact confusion Task 1 exists to remove.

    Add one short section stating that this repository is the canonical host for the reusable security
    workflow: other repositories adopt it either by calling
    `uses: OttawaCloudConsulting/security-platform/.github/workflows/security.yml@v1` or by copying the three
    files, and the procedure of record is `docs/adoption-guide.md` in the `security_solution` documentation
    repository. Wrap any URL per MD034 (no bare URLs in prose). Do NOT write a version comment beside the
    `@v1` reference: `v1` is a moving tag and a hand-written `# v1.0.0` beside it would rot silently —
    Dependabot maintains version comments on SHA pins, not tag refs.

    Update the Milestones table: `cicd/` M2 becomes Complete for the GitHub Actions pipeline, with the Azure
    DevOps and GitLab members named as unvalidated drafts in the description. Do not restructure the table.
  </action>
  <verify>
    <automated>bash -c 'cd repos/security-platform && grep -c "adoption-guide" README.md | grep -qvx 0 && grep -c "security.yml@v1" README.md | grep -qvx 0 && grep -c "\.github/" README.md | grep -qvx 0 && ! grep -nE "security\.yml@v1[[:space:]]+#" README.md && markdownlint-cli2 README.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adoption-guide" README.md` returns at least 1 and `grep -c "OttawaCloudConsulting/security-platform/.github/workflows/security.yml@v1" README.md` returns at least 1.
    - The Structure tree contains a `.github/` entry naming all three files; `grep -c "pr-security.yml" README.md` returns at least 1.
    - No `@v1` reference carries a trailing version comment: `grep -nE "security\.yml@v1[[:space:]]*#" README.md` returns nothing.
    - `grep -c "| In progress |" README.md` returns 0 for the `cicd/` row.
    - The report-only paragraph is present, and the SUMMARY states whether it was inherited from `origin/main` or re-authored because `origin/main` lacked it (with the `git show origin/main:README.md` evidence).
    - `markdownlint-cli2 README.md` reports zero violations; MD034 in particular passes (no bare URL in prose).
    - `git diff origin/main --name-only` for this branch now lists exactly: `.github/workflows/security.yml`, `.github/workflows/pr-security.yml`, `.github/dependabot.yml`, `scripts/check-detector-parity.sh`, `cicd/README.md`, `README.md`, and the two deletions.
  </acceptance_criteria>
  <done>A reader landing on the host repository's front page learns where the canonical workflow is and how to adopt it, in one screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stale deployable template → a consumer repository | Anyone following `cp -r cicd/.github/` installs an unpinned, `|| true`-tolerant, permission-short workflow |
| documentation claim → operator action | A wrong required-check name or a wrong settings screen produces either a permanently-pending check or a false belief that gating is in force |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Tampering / Supply chain | `cicd/.github/workflows/security.yml` (stale third copy) | mitigate | Deleted with `git rm`, with its specific defects recorded as the rationale; the deployment recipe that shipped it is replaced by a pointer, asserted by a negative grep with comments excluded |
| T-20-09 | Elevation of privilege | `cicd/README.md` branch-protection guidance | mitigate | Ruleset path and the five byte-exact `security / …` contexts derived from source; the classic-protection 404 explicitly named as a false negative; D-07 ordering replaces "failures block the merge" |
| T-20-12 | Tampering | the five frozen contexts in prose | mitigate | Derived from `security.yml` job names and byte-dumped for U+2014, never retyped |
| T-20-15 | Spoofing | `cicd/renovate.json` + Mend Marketplace app guidance | mitigate | Deleted; Dependabot (`directory: "/"`, weekly, no account) stated instead, consistent with CICD-05 and the zero-external-accounts constraint |
| T-20-SC | Tampering | package-manager installs | accept | None; the deleted template's `pip install semgrep` was the only unpinned install anywhere in the host repo and it is now gone |
</threat_model>

<verification>
- `markdownlint-cli2 cicd/README.md README.md` reports zero violations.
- All negative greps above return 0 with comment/fence lines excluded.
- `bash scripts/check-workflow-uploads.sh` and `bash scripts/check-detector-parity.sh` still exit 0 (this plan touches no workflow under `.github/`, so a failure here means something else moved).
- The host branch's diff against `origin/main` lists exactly the expected file set.
</verification>

<success_criteria>
Searching the canonical host repository for a GitHub Actions security workflow returns one answer, and the
two READMEs beside it agree with it.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-05-SUMMARY.md` when done. Record the
deleted files with the defects they carried, and flag both deletions explicitly for plan 06's merge
checkpoint. `requirements.mark-complete` is deliberately NOT invoked — plan 12 owns it.
</output>
