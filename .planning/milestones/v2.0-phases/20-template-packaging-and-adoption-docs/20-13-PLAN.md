---
phase: 20-template-packaging-and-adoption-docs
plan: 13
type: execute
wave: 12
depends_on: [20-12]
files_modified:
  - docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md
  - docs/adr/README.md
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DIST-06, DIST-07, DIST-08]

must_haves:
  truths:
    - "The packaging, host, versioning and portability decisions of this phase are recorded in one append-only ADR, with the corrections to earlier decisions stated as corrections"
    - "The dead uses: OCC-github/security_solution path no longer appears as a requirement or a success criterion"
    - "DIST-06, DIST-07 and DIST-08 are marked complete exactly once, in this plan, with the evidence for each named"
    - "What this phase did NOT verify is written down as plainly as what it did"
  artifacts:
    - path: "docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md"
      provides: "ADR-018: canonical host, single-copy packaging, dual-tag versioning, the portability pass, the private-repo capability disposition, and the stale-copy removals"
      contains: "DIST-06, DIST-07, DIST-08"
      min_lines: 80
    - path: "docs/adr/README.md"
      provides: "ADR index with exactly one new row appended after ADR-017"
      contains: "ADR-018"
  key_links:
    - from: ".planning/ROADMAP.md Phase 20 success criterion 2 and REQUIREMENTS.md DIST-07"
      to: "the real published reference"
      via: "a correction note citing the D-01 amendment and RESEARCH C-1"
      pattern: "OttawaCloudConsulting/security-platform"
---

<objective>
Record the phase's decisions in an ADR, correct the two planning records that still name a repository path which
does not exist, and mark the three requirements complete.

Purpose: this phase amended a locked decision (D-01) on measured evidence, inverted a research recommendation
(Q5), implemented a premise that was false (D-04's claim that the container job auto-detects Dockerfiles), and
deleted two artifacts. None of that is self-evident from a diff, and `docs/adr/` is append-only, so ADR-018 is the
only place those corrections can live. ROADMAP success criterion 2 and REQUIREMENTS DIST-07 both still specify
`uses: OCC-github/security_solution/...`, a path whose org returns 404 — left uncorrected, a verifier would check
this phase against a dead string and a future reader would believe the account exists.

Output: ADR-018, one index row, two corrected planning records, three requirements marked complete, and the fate of
three open pilot pull requests decided.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md

All twelve prior SUMMARYs in this phase directory are the evidence base for the ADR. Read the ones each decision
came from rather than reconstructing the decision from plan text.

**THIS REPO ONLY.** Nothing in `repos/security-platform` changes.

**`docs/adr/` is append-only.** ADR-001 through ADR-017 are untouchable. Corrections to earlier decisions are
recorded as `**Correction to D-NN: …**` bullets inside ADR-018, never as edits to the file being corrected. Append
exactly one row to `docs/adr/README.md` after the ADR-017 row. The index's intro line about ADR-001 through
ADR-012 coming out of the red-team findings is NOT stale — it correctly scopes which ADRs had that origin. Leave it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-018 and append its index row</name>
  <files>docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md, docs/adr/README.md, CLAUDE.md</files>
  <read_first>
    - `docs/adr/adr017-configurable-gate-mode-and-required-checks.md` in full — the skeleton to reuse (`## Context`, `## Decision`, `## Consequences`, `## What was NOT verified`; do not invent a fifth heading), the header block with `**Status:** Accepted` / `**Date:**` / `**Addresses:**`, the bold-claim-then-evidence context bullets, the decision bullets naming the rejected alternative inline, the `**Correction to D-NN: …**` form, the `**Improved:**` / `**Tradeoff — …**` consequences shape, and the "What WAS measured and must not be re-litigated" opening of the last section
    - `docs/adr/README.md` — the index table, its last row, and the `adrNNN-kebab-case-title.md` link convention with no separator between `adr` and the number
    - `20-01-SUMMARY.md`, `20-03-SUMMARY.md`, `20-04-SUMMARY.md`, `20-05-SUMMARY.md`, `20-06-SUMMARY.md`, `20-07-SUMMARY.md`, `20-10-SUMMARY.md`, `20-11-SUMMARY.md`, `20-12-SUMMARY.md` in this phase directory — the measured evidence behind each decision
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md` §Assumptions Log and §Open Questions — the source of the not-verified list and the questions this phase closed
    - `CLAUDE.md` — whether plan 12 left the ADR range parenthetical at ADR-017 for this plan to extend
  </read_first>
  <action>
    Create `docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md` with
    `**Addresses:** DIST-06, DIST-07, DIST-08` and ADR-017's four-heading skeleton.

    Decisions to record, each with its measurement and, where one exists, the rejected alternative:
    - **`OttawaCloudConsulting/security-platform` is the canonical host** (the D-01 amendment), stated as a
      correction: `OCC-github` is not a GitHub account (org and repo both 404), the account is a User not an
      Organization, and this repository's history is disjoint from any remote — so D-01's original "the canonical
      files move into THIS repo" was impossible, not merely inconvenient. Rejected alternative: RESEARCH Q1 Option
      A, creating a new public repository and publishing 320 commits of planning history that `gitleaks git .`
      measured as carrying 16 findings.
    - **Exactly one copy of the workflow exists.** No `templates/` directory here; Mode A is three files fetched at
      the published tag; the Mode B caller is documented inline in the adoption guide. Rejected: a checked-in
      template copy here, because the D-01 amendment forbids a second source of truth and a divergent copy cannot
      be diffed against the canonical.
    - **The host keeps `uses: ./.github/workflows/security.yml`** (RESEARCH Q5 inverted): it is the producer, so its
      own pull requests keep proving the canonical file BEFORE any tag is cut, which removes Pattern 5's
      tag-before-proof inversion and the temporary un-pin procedure it would have required.
    - **Dual tagging:** annotated immutable `v1.0.0` plus a LIGHTWEIGHT moving `v1`, with the reason the moving tag
      is lightweight (an annotated tag makes `git/ref/tags/v1` return a tag object, forcing a second dereference on
      every consumer-side check). Inline release notes rather than `--notes-file`, because the host has no `docs/`
      tree and a release-notes file there would have no second user. No version comment beside a tag ref.
    - **The portability pass implements D-04's premise rather than changing scanning behaviour** (the C-3 / C-4
      correction): the three detectors inlined, the container job made conditional on a discovered Dockerfile, six
      downstream steps compounded with `steps.docker.outputs.found == 'true'` per the 18-02 `always() &&` lesson.
      Cite plan 02's A4 pathspec measurement and plan 06's finding-count comparison showing that WHERE detection
      happens changed while WHAT is found did not.
    - **The private-repository capability disposition** from plans 01, 04 and 11, stated as measured: the probe run
      id, the guard expression or its deliberate absence, and why a capability guard beats deleting the verify
      assertions. Record the deliberate non-decision: no `expect_code_scanning` input, because it would be a second
      substitution point and would break D-04 for a consumer that does not yet exist.
    - **`gate_mode` remains the only per-repo substitution point and the only `workflow_call` input**, expressed as a
      banner plus a `gh variable set` command rather than a placeholder token, with the `""`-counts-as-provided
      mechanism that makes a passthrough forbidden.
    - **Two stale artifacts were deleted** (`cicd/.github/workflows/security.yml`, `cicd/renovate.json`) and the
      blueprint's illustrative section was retitled rather than replaced (RESEARCH Q3 option (a)), with the defects
      each carried. State the three-copies-become-one outcome.
    - **Inherited correction restated, not re-decided:** the required-check set is FIVE contexts, not six. ADR-017
      records it; ADR-018 cites it so a reader of this ADR alone does not reintroduce a sixth.

    Consequences: `**Improved:**` with run ids, PR numbers, tag SHAs and the pilot evidence; then one
    `**Tradeoff — …**` paragraph per accepted cost, at minimum: a moving `v1` is mutable by design; a Mode B
    consumer takes a run-time dependency on a repository containing deliberately vulnerable `fixtures/`; and the
    adoption guide and the canonical YAML are two artifacts that can drift, mitigated only by
    `scripts/check-adoption-guide.sh` deriving its frozen strings from the workflow instead of copying them.

    `## What was NOT verified` opens by fencing off what WAS measured and must not be re-litigated, then a numbered
    list: A3 (a fork pull request cannot read repository variables — one forum source, January 2023, never measured
    here); A5 (the account's plan is unknown, the token lacks the `user` scope); A7; whether a GHAS-bearing
    organisation consumer behaves differently; that the Dockerfile pathspec does not match `Containerfile`; that the
    `cicd/` Azure DevOps and GitLab members were never built or validated; and that no repository has required
    checks enabled — that step stays deferred per 18-05 and 19-07.

    Append exactly one row to `docs/adr/README.md` after the ADR-017 row, matching the existing link and date
    format. If plan 12 left `CLAUDE.md`'s ADR range at ADR-017, extend it to ADR-018 now.
  </action>
  <verify>
    <automated>bash -c 'set -eu; F=docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md; test -f "$F"; markdownlint-cli2 "$F" docs/adr/README.md CLAUDE.md; grep -c "^## " "$F" | grep -qx 4; grep -q "Addresses:.*DIST-06, DIST-07, DIST-08" "$F"; grep -c "ADR-018" docs/adr/README.md | grep -qx 1; grep -c "Correction to" "$F" | awk "{exit !(\$1>=2)}"'</automated>
  </verify>
  <acceptance_criteria>
    - The ADR has exactly four `## ` headings in ADR-017's order, and a header block with `**Status:** Accepted`, a date, and `**Addresses:** DIST-06, DIST-07, DIST-08`.
    - At least two `**Correction to …**` bullets exist (the D-01 amendment and the C-3/C-4 portability premise), each naming the measurement that falsified the original premise.
    - Every decision bullet that had a rejected alternative names it inline.
    - `## What was NOT verified` opens with the what-WAS-measured fence and lists at least seven numbered items, including A3, A5 and the `Containerfile` gap.
    - `docs/adr/README.md` gained exactly one line — `git diff docs/adr/README.md` shows one insertion and no other change — and `grep -c "ADR-018"` returns 1.
    - `markdownlint-cli2` reports zero violations on all three files.
    - `OCC-github` appears in the ADR only inside a correction explaining that it does not exist.
  </acceptance_criteria>
  <done>The phase's decisions, its corrections to earlier decisions, and its open items live in one append-only place.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the dead repository path, finalise the ROADMAP entry, and mark the three requirements complete</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 20 — the goal line, the four success criteria (criterion 2 carries the dead `uses: OCC-github/security_solution/...` string), the Requirements line, and the `**Plans**: TBD` placeholder this plan finalises; also the Phase 14-19 entries, for the plan-list format to match
    - `.planning/REQUIREMENTS.md` — DIST-06, DIST-07 (the dead path again) and DIST-08 in the Distribution section, and their three Traceability rows
    - `20-07-SUMMARY.md`, `20-10-SUMMARY.md`, `20-11-SUMMARY.md`, `20-12-SUMMARY.md` — the evidence cited for each requirement's completion
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, correct DIST-07's path to
    `uses: OttawaCloudConsulting/security-platform/.github/workflows/security.yml@<ref>` and append a short
    parenthetical naming the correction's authority: the D-01 amendment and RESEARCH C-1, where `OCC-github` was
    measured as a 404. Apply the same correction to ROADMAP Phase 20 success criterion 2. Correct the wording, not
    the substance — both still require a reusable workflow callable from another repository against a stable
    published ref. Touch no other requirement or criterion.

    Finalise the ROADMAP Phase 20 entry: the plan count (13) and a checklist of the thirteen plan files with a brief
    objective each, in the format Phases 14-19 use. Annotate each of the four success criteria with its evidence —
    PR numbers and run ids for SC1 and SC2, file paths for SC3 and SC4.

    Then mark the requirements complete, once, here. Read the SDK handler's expected argv before calling it rather
    than assuming a flag shape, then invoke the `requirements.mark-complete` handler for DIST-06, DIST-07 and
    DIST-08. Every earlier plan in this phase withheld this deliberately, following the 17-01 and 19-01 precedent
    where a premature `mark-complete` had to be reverted. Afterwards verify BOTH representations directly — the
    `- [x] **DIST-0N**` checkboxes and the `| DIST-0N | Phase 20 | Complete |` Traceability rows — rather than
    trusting the command's exit code.
  </action>
  <verify>
    <automated>bash -c 'set -eu; ! grep -q "OCC-github" .planning/ROADMAP.md; ! grep -q "OCC-github" .planning/REQUIREMENTS.md; [ "$(grep -cE "^- \[x\] \*\*DIST-0[678]\*\*" .planning/REQUIREMENTS.md)" = 3 ]; [ "$(grep -cE "^\| DIST-0[678] \| Phase 20 \| Complete" .planning/REQUIREMENTS.md)" = 3 ]; ! grep -q "^\*\*Plans\*\*: TBD" .planning/ROADMAP.md; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OCC-github"` returns 0 in both `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
    - DIST-07's corrected text names `OttawaCloudConsulting/security-platform` and carries the correction's authority (D-01 amendment / RESEARCH C-1).
    - All three DIST checkboxes are `[x]` AND all three Traceability rows read `Complete` — both verified by grep, not by the command's exit code.
    - The ROADMAP Phase 20 entry lists 13 plans with objectives and no `TBD` placeholder remains.
    - Each of the four success criteria carries its evidence reference.
    - No other requirement's status changed: `git diff .planning/REQUIREMENTS.md` shows changes only on the DIST-06/07/08 lines, their Traceability rows, and the DIST-07 wording.
  </acceptance_criteria>
  <done>The planning records name a repository that exists, and the three Distribution requirements are complete with evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase close-out — pilot pull request fate and requirement sign-off</name>
  <read_first>
    - `20-10-SUMMARY.md` and `20-11-SUMMARY.md` — the three open pilot pull requests, their repositories and their evidence
    - `.planning/REQUIREMENTS.md` — the three completions just recorded
    - `docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md` — the decisions and the not-verified list
  </read_first>
  <files>none — this task presents the close-out and records decisions; any merge it performs happens in a pilot repository</files>
  <action>Present the three open pilot pull requests with their repositories and evidence, the three requirement completions with the evidence behind each, and ADR-018's not-verified list, then STOP and wait. Perform a merge only for the pull requests the operator names, and for each merge read the resulting commit SHA back from that repository's `main` rather than from a local tree. Record every decision verbatim.</action>
  <verify>
    <automated>bash -c 'S=.planning/phases/20-template-packaging-and-adoption-docs/20-13-SUMMARY.md; grep -qiE "merge|close|leave open" "$S" && grep -qi "confirmed" "$S"'</automated>
  </verify>
  <done>Each pilot pull request has a recorded fate, and the three requirement completions are operator-confirmed.</done>
  <what-built>
    The phase's full deliverable set: a portable canonical workflow on the host's `main`, `v1` and `v1.0.0` with a
    release, `docs/adoption-guide.md` verified by execution against three live pilot runs, two stale templates
    removed, ADR-018, and DIST-06/07/08 marked complete.
  </what-built>
  <how-to-verify>
    1. Three pilot pull requests are OPEN and were deliberately left that way: two on the public pilot (Mode A and
       Mode B) and one on the private pilot. Decide each: merge to adopt the pipeline in that repository now, close
       to leave adoption for later, or leave open. Adopting is a real change to another project's CI, which is why
       no plan merged them.
    2. Confirm the three requirement completions read correctly against the evidence: SC1 and SC2 rest on the pilot
       run ids; SC3 and SC4 rest on the guide's sections and its standing gate.
    3. Read ADR-018's `## What was NOT verified` list and confirm nothing there should have been measured in this
       phase instead of deferred — in particular whether required-check adoption stays deferred, as it has been
       since 18-05.
  </how-to-verify>
  <acceptance_criteria>
    - `20-13-SUMMARY.md` records a per-pull-request decision (merge / close / leave open) with the operator's reply quoted verbatim, and any merge actually performed with the resulting commit SHA read back from that repository's `main`.
    - The SUMMARY confirms or corrects the three requirement completions, and states that `requirements.mark-complete` was invoked exactly once in this phase, in this plan.
    - Any item the operator moves from deferred to in-scope is recorded as a follow-up with an owner, not silently absorbed.
  </acceptance_criteria>
  <resume-signal>Reply with the decision for each of the three pull requests, and `confirmed` for the requirement completions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → verifier and future reader | A success criterion naming a nonexistent repository is checked against a dead string |
| append-only ADR set → the project's decision history | An edit to an accepted ADR destroys the record it exists to keep |
| pilot pull request → another project's CI | Merging adopts the pipeline in a repository this phase does not own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-26 | Repudiation | undocumented amendment of a locked decision | mitigate | ADR-018 records the D-01 amendment and the C-3/C-4 premise correction as explicit `**Correction to …**` bullets with the falsifying measurements |
| T-20-27 | Spoofing | the dead `OCC-github/security_solution` path | mitigate | Removed from both ROADMAP and REQUIREMENTS with the correction's authority cited; asserted absent by grep |
| T-20-28 | Tampering | the append-only ADR index | mitigate | Exactly one inserted line asserted by `git diff`; ADR-001..017 rows byte-unchanged |
| T-20-29 | Elevation of privilege | adopting the pipeline in another project | mitigate | All three pilot pull requests left OPEN by every plan; merge happens only on an explicit per-pull-request operator decision |
| T-20-30 | Repudiation | premature requirement completion | mitigate | `mark-complete` invoked once, in this plan, per the 17-01 / 19-01 precedent, and both representations verified by grep rather than by exit code |
| T-20-SC | Tampering | package-manager installs | accept | None; documentation and planning records only |
</threat_model>

<verification>
- `markdownlint-cli2` clean on ADR-018, `docs/adr/README.md` and `CLAUDE.md`.
- Zero `OCC-github` occurrences in `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
- Three `[x]` DIST checkboxes and three `Complete` Traceability rows.
- `git diff docs/adr/README.md` shows exactly one insertion.
- A per-pull-request fate decision on record with the operator's words.
</verification>

<success_criteria>
The phase is closed with its decisions recorded, its records accurate, its requirements complete with evidence, and
nothing left implicitly adopted in a repository the operator did not choose.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-13-SUMMARY.md` when done. This is the ONLY plan
in the phase that invokes `requirements.mark-complete`.
</output>
