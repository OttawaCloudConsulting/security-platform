---
phase: 22-branch-protection-apply-live-exercise-needs-explicit-target
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/
autonomous: false
requirements: [VAL-02]

must_haves:
  truths:
    - "The script's pre-network guards are re-proven at this commit — exit 4 without --verify-sha, exit 5 without the lockout acknowledgement, exit 2 on a document with no rules key"
    - "The live PUT is performed by the human operator, never by Claude, because the executor's Bash classifier has denied this exact script twice"
    - "Claude re-reads the live API after the write instead of accepting the operator's pasted report"
    - "The five pre-existing rule types survive the PUT — nothing was dropped and bypass_actors is still empty"
    - "A 422 halts the phase with its raw body recorded, and is treated as a finding rather than as a failure"
  artifacts:
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/guard-rehearsal.txt"
      provides: "Observed exit codes 4, 5 and 2 from the script's own guards at this commit"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/apply-transcript.txt"
      provides: "The operator's verbatim terminal output from the --apply invocation"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merged.json"
      provides: "The document the script PUT, written by --out"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/rules-after.txt"
      provides: "Rule types after the write, independently re-read by Claude"
  key_links:
    - from: "22-evidence/merged.json"
      to: ".planning/phases/20-template-packaging-and-adoption-docs/20-10-evidence/merged.json"
      via: "diff — the 2026-09-14 dry run produced the document this write is expected to reproduce"
      pattern: "required_status_checks"
    - from: "22-evidence/rules-after.txt"
      to: "22-evidence/rules-before.txt"
      via: "set comparison — after must equal before plus exactly required_status_checks and pull_request"
      pattern: "required_status_checks"
---

<objective>
Make the five `security / ...` contexts required on `OttawaCloudConsulting/terraform-pipelines`'
`main`, by having the OPERATOR run `set-required-checks.sh --apply` at a blocking checkpoint, and then
independently verify from the live API that the write did what it claimed — preserving every
pre-existing rule type and inventing no bypass actor.

Purpose: this is the one irreversible-ish write in the phase and the one command Claude must not run.
The executor's auto-mode Bash classifier has denied this exact script twice (20-07, 20-10, both for a
harmless `--out` dry run); planning around that denial by trying harder would stall the phase
mid-flight with a partially-applied ruleset. The plan routes through it instead: the human writes,
Claude measures.

A successful PUT is NOT the deliverable. It is the precondition for plan 04, which witnesses the
refusal. Do not celebrate a 200.

Output: three observed guard exit codes, the operator's verbatim apply transcript, the merged
document, and Claude's own post-write read-back.

Target and end state are OPERATOR-DECIDED and closed: target is `terraform-pipelines`, and the
ruleset is RESTORED in plan 05. This checkpoint is not a decision point about either.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md

Read before assembling the checkpoint:

- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-02-SUMMARY.md`
  — the blocking head SHA, the red check name, the control verdict, and the absolute path of
  `exit2-fixture.json`. The checkpoint's command text is built from these.
- `repos/security-platform/scripts/set-required-checks.sh` lines 1–110 (header, exit-code table,
  flag list) and lines 205–260 (the six-key body and the drop guard). Read the script before writing
  a checkpoint that tells a human to run it.
- `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-PLAN.md` lines 196–315 —
  the element skeleton for both checkpoint task types, and the two sentences that are carried forward
  verbatim into this plan.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-PATTERNS.md`,
  the `checkpoint:human-verify` pattern assignment — why `--out` must be an ABSOLUTE path and why
  both `--repo` and `--ruleset` are mandatory even when one equals its default.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-RESEARCH.md`
  lines 499–528 (Pitfalls 3 and 4).

<interfaces>
  REPO = OttawaCloudConsulting/terraform-pipelines
  RID  = 12760793
  Script (in the vendored host clone, invoked with bash, never ./ and never chmod +x):
    repos/security-platform/scripts/set-required-checks.sh

Script exit contract, from its own header:
  0  dry run produced its output, or --apply's PUT AND read-back both succeeded
  1  generic failure surfaced from gh api or python3
  2  the fetched/input document has no `rules` key (missing is not empty)
  3  the merged document would drop a pre-existing rule type — never written anywhere
  4  --apply passed without --verify-sha
  5  --apply passed without --yes-i-understand-lockout
  6  --verify-sha found a context missing, or carrying the wrong app id

Flag defaults are INDEPENDENT: --repo defaults to OttawaCloudConsulting/security-platform and
--ruleset defaults to 14243983. Passing --repo alone silently points at the other repository's
ruleset id and fails with a bare 404 that gives no hint about the mismatch. Every invocation in this
plan passes BOTH.

The script does `cd "$(dirname "$0")/.."` at line 67, so a relative --out lands under
repos/security-platform/, not under the caller's cwd. --out must be an ABSOLUTE path.
</interfaces>

<scope_boundaries>
- `set-required-checks.sh` is INVOKED, never edited. A `--restore` flag is a deferred item, not this
  phase's scope: it would be a commit in a separate git repository, an adoption-guide §8 update here,
  and a standing-gate re-run.
- Claude does NOT run the script. Not the guards, not the preflight, not the apply. If Claude believes
  it could get away with running the guard rehearsal because it writes nothing, that belief is the
  failure mode Pitfall 6 describes; the denials were measured on a `--out` dry run.
- Do NOT work around any exit code. The guards encode failures this project has already reasoned
  through; a plan that routes around one has misunderstood the phase.
- Do NOT set `GATE_MODE`. It was deleted at the end of plan 02 and stays deleted — the red conclusions
  already live on the commit.
- Do NOT attempt the merge here. Plan 04 owns the witness, behind its own hard precondition.
</scope_boundaries>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator rehearses the guards, then performs the live --apply</name>
  <files></files>
  <read_first>
    - repos/security-platform/scripts/set-required-checks.sh — the whole header block and the flag parser, so the command text presented to the operator matches what the script actually accepts.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-02-SUMMARY.md — the blocking head SHA to pass to --verify-sha, and the absolute path of exit2-fixture.json.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/rules-before.txt and variables-before.txt — the state the operator is being asked to change, quoted back to them.
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-PLAN.md lines 252–261 — the two sentences carried forward verbatim below.
  </read_first>
  <action>
    Present the operator with four commands to run, in order, in their own terminal, from this
    repository's root. Give each one in full, with every flag written out and the real SHA substituted
    — not a placeholder — and state the expected exit code beside each. Then stop and wait.

    Carried forward verbatim from 18-05 and binding on this task:
    "Do NOT run `--apply` on your own initiative under any circumstance."
    "If the PUT returns 422, report the raw error body and stop: the `pull_request` parameter set is
    an unverified assumption and a 422 changes no state."

    Commands 1 through 3 are the guard rehearsal. They re-prove at THIS commit what 18-03 proved at
    its own, rather than inheriting the claim. Commands 1 and 2 are checked before any network call,
    so they touch nothing; command 3 reads a local fixture. All three pass both `--repo` and
    `--ruleset` explicitly:
      1. the script with `--repo`, `--ruleset` and `--apply`, and NO `--verify-sha` — expect exit 4
      2. the script with `--repo`, `--ruleset`, `--apply` and `--verify-sha <the blocking head SHA>`,
         and NO `--yes-i-understand-lockout` — expect exit 5
      3. the script with `--repo`, `--ruleset` and `--input <absolute path to exit2-fixture.json>`
         and an absolute `--out` under the scratchpad — expect exit 2
    Tell the operator explicitly: if any of these three produces an exit code other than the one
    stated, STOP and report it — do not proceed to command 4. A guard that has stopped behaving as
    documented invalidates the safety argument the fourth command rests on.

    Command 4 is the live write: the script with `--repo OttawaCloudConsulting/terraform-pipelines`,
    `--ruleset 12760793`, `--verify-sha <the blocking head SHA>`, `--out` pointing at the ABSOLUTE
    path of `22-evidence/merged.json`, and both `--apply` and `--yes-i-understand-lockout`. Expect
    exit 0. Ask the operator to paste the complete terminal output of all four commands, including
    exit codes, which Claude records into `guard-rehearsal.txt` (commands 1–3) and
    `apply-transcript.txt` (command 4).

    State the consequences plainly before asking, using the measured values from the evidence
    directory rather than generalities: this repository's `main` currently carries
    `copilot_code_review,deletion,non_fast_forward` and no `.github/workflows/` at all (HTTP 404), so
    while the checks are required, any future pull request that does not itself carry the workflow
    will show five permanently-pending checks and cannot merge; and the script always adds a
    `pull_request` rule alongside the required checks, which removes the direct-push shortcut this
    repository's `main` currently sees in use. Both conditions last only until plan 05's restore,
    which is unconditional.

    If command 4 returns exit 6, the `--verify-sha` preflight found a context missing or with the
    wrong app id — nothing was written; report it and stop. If it returns 422, capture the raw body,
    record that the ruleset was NEVER modified, and stop: plan 04 is skipped entirely and plan 05 takes
    its no-forward-write branch. A 422 naming `rules[N].type` for `copilot_code_review` is a genuine
    finding about read-modify-write on a repository carrying a preview rule type, and belongs in
    ADR-019 as a result, not as a defeat.
  </action>
  <what-built>
    An exercise pull request on `OttawaCloudConsulting/terraform-pipelines` carrying one red check
    (`security / SAST — Semgrep CE`) among five, produced by a repository-variable flip on a
    byte-identical git tree, with the variable already deleted and the repository already back to its
    default mode. Its merge verdict with that check red but NOT required has been measured and
    recorded. Everything is in place except the one write Claude is structurally unable to perform.
  </what-built>
  <how-to-verify>
    1. Read the four commands Claude has printed. Confirm each one names BOTH `--repo` and
       `--ruleset` — the script's two defaults are independent, and `--repo` alone points at
       `security-platform`'s ruleset id.
    2. Confirm the `--out` path in command 4 is ABSOLUTE. The script changes directory to its own
       repository root before doing anything, so a relative path lands somewhere you did not intend.
    3. Run commands 1, 2 and 3. Confirm the exit codes are 4, 5 and 2 respectively
       (`echo $?` after each). If any differs, stop here and tell Claude — do not run command 4.
    4. Run command 4. Paste its complete output, including the script's own read-back section and the
       final exit code.
    5. If it exits 0, confirm for yourself before replying: run
       `gh api repos/OttawaCloudConsulting/terraform-pipelines/rules/branches/main --jq '.[].type'`
       and check you see five types — the three that were there plus `required_status_checks` and
       `pull_request`. Never read `branches/main/protection`; it 404s by design on a ruleset-governed
       repository and that 404 is a false negative.
    6. If it exits non-zero, paste the raw error body unedited. Do not retry, do not edit flags, do
       not hand-write a `gh api --method PUT`.
  </how-to-verify>
  <verify>
    <automated>grep -qE 'exit(ed)? code[: ]*4' .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/guard-rehearsal.txt &amp;&amp; grep -qE 'exit(ed)? code[: ]*5' .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/guard-rehearsal.txt &amp;&amp; grep -qE 'exit(ed)? code[: ]*2' .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/guard-rehearsal.txt &amp;&amp; test -s .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/apply-transcript.txt</automated>
  </verify>
  <resume-signal>Type "applied" and paste the four transcripts, or paste the failing output and type "halt". Nothing else in this phase proceeds until one of those two arrives.</resume-signal>
  <acceptance_criteria>
    - All four commands are presented with real substituted values — a real 40-hex SHA, a real absolute `--out` path, the literal repo slug and ruleset id 12760793 — and no placeholder tokens
    - Each command is presented with its expected exit code, and the instruction to halt before command 4 if any of 1–3 deviates is stated explicitly
    - The measured consequences (the `.github/workflows` 404, the `pull_request` rule removing direct push, both bounded by plan 05's unconditional restore) are stated with their evidence-file sources, not as generalities
    - Claude ran no `set-required-checks.sh` invocation of any kind during this task
    - `guard-rehearsal.txt` records three observed exit codes: 4, 5, 2
    - `apply-transcript.txt` records command 4's full output verbatim, including its exit code
    - If any exit code other than 0 came back from command 4, the raw body is recorded, the phase halted, and the SUMMARY names which downstream plans are now skipped or re-branched
  </acceptance_criteria>
  <done>The one write in this phase was made by the person who owns the repository, after its guards were re-proven at this commit and its consequences were stated in measured terms.</done>
</task>

<task type="auto">
  <name>Task 2: Independently verify the write from the live API</name>
  <files>.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/rules-after.txt, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/required-contexts-after.json, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/ruleset-after.json</files>
  <read_first>
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/rules-before.txt and ruleset-before.json — the state being compared against.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merged.json — the document the script says it PUT.
    - .planning/phases/20-template-packaging-and-adoption-docs/20-10-evidence/merged.json — the 2026-09-14 dry run's output, an independent reference for what the merged document should contain.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-PATTERNS.md, Shared Pattern 2 — 20-10 did not accept the orchestrator's report of its own dry run at face value; this task is that discipline applied to the operator's transcript.
  </read_first>
  <action>
    Do not treat the operator's pasted transcript as the measurement. Re-read the live API.

    Capture `rules-after.txt` from
    `gh api "repos/$REPO/rules/branches/main" --jq '[.[].type]|sort|join(",")'` and assert its set is
    exactly the `rules-before.txt` set plus `required_status_checks` and `pull_request` — five types,
    nothing dropped. A dropped `deletion` or `non_fast_forward` would be a security regression dressed
    as a security improvement, which is the specific failure the script's exit-3 guard exists to
    prevent; asserting it independently is what confirms the guard worked rather than assuming it.

    Capture `ruleset-after.json` from `gh api "repos/$REPO/rulesets/$RID"` and `required-contexts-after.json`
    from the same endpoint filtered to
    `.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[]`.
    Assert five contexts, each with `integration_id` 15368, each name carrying the EM DASH U+2014 —
    dump the codepoints rather than eyeballing the dash. Assert `bypass_actors` is still `[]` and
    `current_user_can_bypass` is still `"never"`: a bypass actor appearing here would quietly convert
    the whole exercise into a no-op, because an operator who can bypass makes the refusal advisory.

    Then diff `22-evidence/merged.json` against
    `.planning/phases/20-template-packaging-and-adoption-docs/20-10-evidence/merged.json`. They were
    produced two days apart from the same ruleset by the same code path, so a difference means
    something changed on the pilot's ruleset in between. Report any difference with its content rather
    than noting that one exists.

    Also read back the `pull_request` rule's parameters and record them verbatim. Plan 04's third
    verdict expects `CLEAN` with all five checks green, and that expectation only holds if
    `required_approving_review_count` is 0 and `require_last_push_approval`,
    `require_code_owner_review`, `dismiss_stale_reviews_on_push` and
    `required_review_thread_resolution` are all false. Measured from the script's source at plan time
    they are; confirm it from the live document, because plan 04's acceptance criteria depend on it
    and a review requirement would hold the PR at `BLOCKED` for a reason unrelated to the required
    checks.

    Finally re-read `gh api "repos/$REPO/commits/main" --jq .sha` and assert it still equals
    `main-head-before.txt`. Nothing has been merged; this is the check that says so definitively.
  </action>
  <verify>
    <automated>python3 -c "
import json
E='.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/'
before=set(open(E+'rules-before.txt').read().strip().split(','))
after=set(open(E+'rules-after.txt').read().strip().split(','))
assert after == before | {'required_status_checks','pull_request'}, (sorted(before), sorted(after))
c=json.load(open(E+'required-contexts-after.json'))
assert len(c)==5, len(c)
assert all(x['integration_id']==15368 for x in c)
assert all('—' in x['context'] for x in c), 'EM DASH missing'
d=json.load(open(E+'ruleset-after.json'))
assert d.get('bypass_actors')==[], d.get('bypass_actors')
pr=[r for r in d['rules'] if r['type']=='pull_request'][0]['parameters']
assert pr['required_approving_review_count']==0, pr
print('write verified:', sorted(after))
"</automated>
  </verify>
  <acceptance_criteria>
    - `rules-after.txt` set equals the `rules-before.txt` set union `{required_status_checks, pull_request}` exactly — five types, nothing dropped, asserted by the automated command above rather than by reading
    - `required-contexts-after.json` holds exactly 5 contexts, all `integration_id` 15368, em-dash codepoints dumped and recorded as U+2014
    - `bypass_actors` reads `[]` and `current_user_can_bypass` reads `"never"` after the write; both quoted in the SUMMARY
    - The `pull_request` rule's five parameters are quoted verbatim in the SUMMARY, and the SUMMARY states whether plan 04's `CLEAN` expectation survives them
    - `22-evidence/merged.json` diffed against `20-10-evidence/merged.json`, with any difference reported by content
    - `gh api repos/$REPO/commits/main --jq .sha` still equals `main-head-before.txt`
    - Every assertion in this task comes from a fresh API read, not from the operator's transcript; the SUMMARY says so explicitly
  </acceptance_criteria>
  <done>The live ruleset has been re-read by the agent that did not perform the write, and it says the five contexts are required with every pre-existing rule intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal → GitHub ruleset PUT | The one privileged write in the phase; crosses out of Claude's control by design |
| operator's pasted transcript → the project's evidence record | An untrusted report until Claude re-reads the API itself |
| `PUT /rulesets/{id}` semantics | The endpoint replaces the WHOLE document; a partial body deletes rules silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Tampering | `main`'s existing rules (`deletion`, `non_fast_forward`, `copilot_code_review`) | mitigate | Read-modify-write via `set-required-checks.sh` only, never a hand-written PUT; the script's exit-3 drop guard plus Task 2's independent set comparison of `rules-after.txt` against `rules-before.txt` |
| T-22-12 | Elevation of Privilege | an agent performing the irreversible-ish write unauthorised | mitigate | The write is a `gate="blocking"` `checkpoint:human-verify`; 18-05's "Do NOT run `--apply` on your own initiative under any circumstance" carried verbatim; Claude runs no script invocation in this plan |
| T-22-13 | Elevation of Privilege | `bypass_actors` | mitigate | Asserted `[]` before and after; never synthesised. A non-empty value would make the plan-04 refusal advisory and is treated as a halt condition |
| T-22-14 | Denial of Service | a one-codepoint context typo creating a permanently-pending required check | mitigate | `--verify-sha` preflight (exit 6) against the live check runs; contexts derived by the script from its own frozen heredoc, never retyped; em-dash codepoints dumped in Task 2 |
| T-22-15 | Repudiation | the claim that the write succeeded | mitigate | Claude re-reads `rules/branches/main` and `rulesets/{id}` from the API rather than parsing the operator's paste (20-10's precedent, Shared Pattern 2) |
| T-22-16 | Denial of Service | a partially-applied ruleset after a 422 | accept | The PUT replaces the document atomically, so a 422 writes nothing; capture-first design means rollback is possible either way, and plan 05 branches explicitly on whether the forward PUT returned 200 |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs nothing |
</threat_model>

<verification>
- `guard-rehearsal.txt` records exit codes 4, 5, 2 — observed, not inherited from 18-03
- `apply-transcript.txt` records the operator's verbatim output including the exit code
- `rules-after.txt` = `rules-before.txt` ∪ `{required_status_checks, pull_request}`
- Five required contexts, `integration_id` 15368, U+2014 separators confirmed by codepoint
- `bypass_actors` still `[]`
- `gh api repos/OttawaCloudConsulting/terraform-pipelines/commits/main --jq .sha` unchanged
- `bash scripts/check-adoption-guide.sh` still 15 passed / 0 failed
</verification>

<success_criteria>
- Three guard exit codes observed at this commit by the operator
- The `--apply` performed by the operator, its transcript captured verbatim
- The five contexts required on `main` with every pre-existing rule type intact, verified by Claude from the live API rather than from the transcript
- The `pull_request` rule's parameters recorded, and plan 04's `CLEAN` expectation confirmed or corrected against them
- Any non-zero exit halted the phase with its raw body recorded and the downstream branch named
</success_criteria>

<output>
Create `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-03-SUMMARY.md` when done.
Record: the four commands as presented, the three guard exit codes, the apply exit code, the operator's
reply verbatim, the five required contexts, the `pull_request` parameter set, the `bypass_actors` value,
the `merged.json` diff result against 20-10's, and — explicitly — whether the forward PUT returned 200.
Plan 05 branches on that last fact and must not have to infer it.
</output>
