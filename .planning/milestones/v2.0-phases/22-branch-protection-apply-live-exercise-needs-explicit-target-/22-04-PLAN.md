---
phase: 22-branch-protection-apply-live-exercise-needs-explicit-target
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-assert-verdicts.py
  - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/
autonomous: true
requirements: [VAL-02]

must_haves:
  truths:
    - "The same pull request whose red check read UNSTABLE while unrequired reads BLOCKED once that check is required — the transition, not either value alone, is the phase deliverable"
    - "GitHub's literal refusal text for gh pr merge on a ruleset-blocked pull request is captured from stderr rather than predicted"
    - "The merge attempt is hard-preconditioned on a fresh BLOCKED read, so a stale read cannot turn a probe into a merge"
    - "A third verdict with all five checks green and still required isolates the red required check as the cause of the refusal"
    - "Nothing landed on the target repository's main — its HEAD is unchanged from the plan-01 capture"
  artifacts:
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-state-blocked.json"
      provides: "The deliverable verdict — red check, now required"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-attempt.txt"
      provides: "GitHub's verbatim refusal, stdout and stderr combined"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-state-clean.json"
      provides: "The third verdict — green and still required"
    - path: ".planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/check-runs-clean.json"
      provides: "Five green check runs at the third-verdict head SHA"
  key_links:
    - from: "22-evidence/merge-state-unstable.json"
      to: "22-evidence/merge-state-blocked.json"
      via: "the only change between them is the ruleset write — same head SHA, same red check"
      pattern: "mergeStateStatus"
    - from: "22-evidence/merge-attempt.txt"
      to: "the required_status_checks rule on ruleset 12760793"
      via: "GitHub's refusal, captured with stderr redirected into stdout from gh pr merge"
      pattern: "gh pr merge"
---

<objective>
Witness the refusal. Read the exercise pull request's merge verdict now that its red check is
required, confirm the transition away from the control verdict recorded in plan 02, then attempt the
merge and capture GitHub's decline verbatim. Finally re-run the same tree with all five checks green
while the contexts remain required, so a third verdict isolates the red required check as the cause
rather than the `pull_request` rule or `copilot_code_review`.

Purpose: this is the one observation the v2.0 milestone audit lists as never made — "Blocking gate,
required check, merge blocked — UNVERIFIED — dry-run only, never applied". Everything around it is
already proven. A successful PUT proved GitHub accepts the body; only a refusal proves the gate works.

Output: three merge verdicts on one tree hash, and the literal text GitHub emits when it declines.

Precondition from plan 03: the forward PUT returned exit 0. If `22-03-SUMMARY.md` records a 422, a
403, or exit 6, THIS PLAN DOES NOT RUN — the ruleset was never modified, there is nothing to witness,
and plan 05 takes its no-forward-write branch. Check that first and stop if it is not satisfied.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read before acting:

- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-03-SUMMARY.md`
  — whether the forward PUT returned 200, the five required contexts, and the `pull_request` rule's
  measured parameters. The third verdict's `CLEAN` expectation depends on that parameter set.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-02-SUMMARY.md`
  — the control merge state, the blocking head SHA, the tree hash, the red check's name.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/repo-config-before.json`
  — which merge methods the target repository actually allows. A disabled method produces an error
  that reads like a refusal and is not one.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-RESEARCH.md`
  lines 556–606 — Pitfall 7 (`gh pr merge` is a write, not a probe) and Pitfall 9 (asynchronous
  mergeability). Both apply to Task 1 simultaneously.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-PATTERNS.md`,
  the "No Analog Found" row on `gh pr merge` refusal text — capture it, do not predict it.

<interfaces>
  REPO = OttawaCloudConsulting/terraform-pipelines
  RID  = 12760793
  BRANCH = chore/phase-22-required-check-exercise
  POLL = bash .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-poll-merge-state.sh REPO PR PREV
  E    = .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence

Merge methods measured on the target 2026-09-16: allow_squash_merge true, allow_merge_commit true,
allow_rebase_merge true, allow_auto_merge false. Re-confirm from repo-config-before.json before
choosing the flag.

mergeStateStatus semantics, treated as a PREDICTION to be confirmed and not as a fact to cite:
  UNSTABLE = mergeable, with a non-passing, non-required check
  BLOCKED  = an unmet required review or status check
  CLEAN    = all green
The phase does not depend on this wording; it measures the transition.

`gh pr view --json merged` is NOT a valid field in gh 2.101.0. Use `state` and `mergedAt`.
</interfaces>

<scope_boundaries>
- NEVER pass `--admin` to `gh pr merge`. That flag exists specifically to bypass branch protection and
  would merge straight through the gate this plan exists to witness.
- Do NOT invoke `set-required-checks.sh`. The ruleset is already applied; this plan only reads it.
- Do NOT restore the ruleset here. Plan 05 owns the restore, and it must run whether this plan
  succeeded or not.
- Do NOT use `gh run rerun`. The third verdict uses an empty commit, for the same reason plan 02 did:
  upload-artifact v4 names must be unique per run id and a rerun reuses the run id, which would turn
  jobs red for a reason unrelated to the gate and poison a verdict that requires all five green.
- Do NOT set `GATE_MODE`. It is already deleted; the third verdict's green run is what an absent
  variable produces.
- If any merge-state read comes back `UNKNOWN`, or the settle-poll exits non-zero, STOP. An artifact
  containing `UNKNOWN` is not evidence, and a stale `BLOCKED` is precisely what would let the merge
  attempt succeed.
</scope_boundaries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the BLOCKED verdict and capture GitHub's refusal</name>
  <files>.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-assert-verdicts.py, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-state-blocked.json, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-attempt.txt, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/pr-after-attempt.json, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/main-head-after-attempt.txt</files>
  <read_first>
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-03-SUMMARY.md — confirm the forward PUT returned exit 0 before doing anything in this task.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-state-unstable.json — the previous value the settle-poll is told about, so a stale read cannot pass as a transition.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/repo-config-before.json — the allowed merge methods.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-RESEARCH.md lines 556–582 — Pitfall 7's four simultaneous mitigations, all of which belong in this one task.
  </read_first>
  <action>
    Write the assertion helper `22-assert-verdicts.py` in the phase directory FIRST, before any live
    read — both tasks' verify commands call it, so it must exist before this task's own verification
    runs. It takes one argument naming which verdict to check (`blocked` or `clean`), loads the
    relevant evidence files from `22-evidence/`, and exits 0 on success or 1 with the failing assertion
    printed. For `blocked` it asserts: the blocked and unstable records share a `headRefOid`; the
    unstable record is not itself `BLOCKED` (without that, there is no discrimination to report); the
    blocked record is `BLOCKED`; `merge-attempt.txt` is non-empty and contains no `--admin`;
    `pr-after-attempt.json` is `OPEN` with a null `mergedAt`; and `main-head-before.txt` matches
    `main-head-after-attempt.txt`. For `clean` it asserts: the three tree-hash files are byte-identical;
    `check-runs-clean.json` holds five entries all `success`; `required-contexts-still-set.json` holds
    five contexts at `integration_id` 15368; and no file under `22-evidence/` contains the string
    `UNKNOWN`. Never set its executable bit; it is invoked as `python3 <path> <verdict>`.

    Then confirm plan 03's forward PUT returned exit 0. If it did not, stop here and record that this
    plan is skipped; do not attempt to witness a gate that was never applied.

    Read the verdict through the settle-poll, passing the control value from
    `merge-state-unstable.json` as the previous value so the helper's corrected two-condition
    assertion can reject a stale read. Capture the full record with `gh pr view` requesting the json
    fields `number,state,headRefOid,mergeable,mergeStateStatus,baseRefName` into
    `merge-state-blocked.json`. Assert `headRefOid` is the SAME SHA plan 02 measured the control
    verdict on — nothing was pushed between the two reads, so the only difference between the control
    and this reading is the ruleset write.

    `BLOCKED` is the expectation. If the poll exits non-zero, or the value is `UNKNOWN`, or it is
    anything other than `BLOCKED`, STOP and report the raw JSON with `rules-after.txt` beside it. Do
    not proceed to the merge attempt on an unexpected value — the merge attempt's safety rests
    entirely on the verdict being what it says it is.

    Then attempt the merge, with all four of Pitfall 7's mitigations in this same task and in this
    order:

    1. Take a FRESH single read of the merge state immediately before the attempt and hard-abort
       unless it is exactly `BLOCKED` — not the polled value from a minute ago, a new one. `gh pr merge`
       has no dry-run mode; there is no way to ask "would this be refused?", only to ask GitHub for the
       state and then attempt it. If that state is stale or wrong, the command does not probe the gate,
       it merges the pull request and lands the workflow file on a real repository's default branch.
    2. Pass an explicit merge method that `repo-config-before.json` shows is enabled — `--squash` on
       the measured configuration — so the command cannot block waiting on a TTY prompt.
    3. NEVER pass `--admin`.
    4. Redirect stderr into stdout and tee the whole thing into `merge-attempt.txt`. The refusal
       arrives on stderr and it IS the deliverable. Do not summarise it, do not reformat it, do not
       predict its wording: this project has never exercised it.

    The `gh pr merge` command is expected to exit non-zero. Record the exit code. Then assert three
    things from fresh reads, not from the merge output: `gh pr view` requesting `state,mergedAt` into
    `pr-after-attempt.json` still shows `OPEN` with a null `mergedAt`; `gh api` on
    `repos/OttawaCloudConsulting/terraform-pipelines/commits/main` with `--jq .sha` into
    `main-head-after-attempt.txt` still equals `main-head-before.txt`, which is the definitive proof
    that nothing landed; and `merge-attempt.txt` does not mention a merge method being disabled, which
    would mean the command failed for a configuration reason rather than being refused by the gate.
  </action>
  <verify>
    <automated>python3 .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-assert-verdicts.py blocked</automated>
  </verify>
  <acceptance_criteria>
    - `merge-state-blocked.json` reads `BLOCKED`, contains no `UNKNOWN`, and its `headRefOid` is byte-identical to the one in `merge-state-unstable.json`
    - The control value from `merge-state-unstable.json` was passed to the settle-poll as its `prev` argument and the poll exited 0; the invocation is quoted in the SUMMARY
    - `merge-attempt.txt` is non-empty, contains the command's stderr, and does not contain the string `--admin`
    - The `gh pr merge` exit code is recorded, and it is non-zero
    - `merge-attempt.txt` contains no text about a disabled or unavailable merge method; the SUMMARY states which method was passed and cites `repo-config-before.json` as showing it enabled
    - `pr-after-attempt.json` shows `state: OPEN` and a null `mergedAt`
    - `diff main-head-before.txt main-head-after-attempt.txt` produces no output
    - `22-assert-verdicts.py` exists, is not executable (`test ! -x`), and `python3 22-assert-verdicts.py blocked` exits 0
    - The SUMMARY quotes the refusal text verbatim and states explicitly that its wording was captured, never predicted
  </acceptance_criteria>
  <done>GitHub has been observed refusing a merge because a required check was red, and the refusal is on disk in GitHub's own words.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the third verdict — green, still required, CLEAN</name>
  <files>.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/check-runs-clean.json, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/merge-state-clean.json, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/tree-hash-clean.txt, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/required-contexts-still-set.json</files>
  <read_first>
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-evidence/tree-hash-baseline.txt and tree-hash-blocking.txt — the invariant the third verdict must also satisfy.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-03-SUMMARY.md — the `pull_request` rule's parameters. If `required_approving_review_count` is not 0, or any of the review booleans is true, `CLEAN` is not reachable and this task records `BLOCKED` with `reviewDecision` as the explanation instead of failing.
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-SUMMARY.md lines 115–125 — three commits, one tree hash, three verdicts. This task completes the Phase 22 analogue of that proof.
  </read_first>
  <action>
    Confirm `gh variable list` on the target repository is still empty — no `GATE_MODE`. The third
    verdict's green run is what an absent variable produces, and re-confirming it costs one read.

    In the scratchpad clone, push a second empty commit to the exercise branch. Write
    `git rev-parse HEAD^{tree}` to `tree-hash-clean.txt` and assert it equals both
    `tree-hash-baseline.txt` and `tree-hash-blocking.txt`. Three commits, one tree: that is the
    invariant this phase asserts, and it is what makes three merge verdicts comparable.

    Wait for the run with the same bounded poll over `gh run list` that plan 02 used — never
    `gh run watch --exit-status`. Capture `check-runs-clean.json` from the check-runs endpoint at the
    new SHA filtered to `app.id == 15368`, and assert five entries, all `success`. Then confirm the
    contexts are STILL required by re-reading the ruleset into `required-contexts-still-set.json` —
    the point of the third verdict is that the requirement did not go away, only the redness did. A
    green verdict with the requirement silently removed would prove nothing.

    Read the merge state through the settle-poll with `BLOCKED` as the previous value, and capture
    `merge-state-clean.json`. `CLEAN` is the expectation and it is the observation that isolates the
    red required check as the sole cause of the refusal: same tree, same ruleset, same `pull_request`
    rule, same `copilot_code_review` — only the check conclusions differ, and the verdict moves.

    The helper's two-condition guard needs one deliberate accommodation here, and skipping it would
    strand this task. That guard exists because GitHub can serve a cached value on a RE-READ of the
    same SHA, which is the situation in Task 1. This read is on a NEW SHA, where GitHub recomputes
    from scratch and the real risk is `UNKNOWN`, not staleness — so an exit 1 carrying the
    settled-but-unchanged message does not mean the read was stale, it means the verdict genuinely did
    not move. When that happens: wait at least 60 seconds, re-invoke the helper with an EMPTY previous
    value, and take that reading as authoritative. Only then decide which branch below applies. Do not
    halt on the first exit 1; do halt on a never-settled timeout or an `UNKNOWN` value.

    If the authoritative reading is `BLOCKED` with all five green, do not treat that as a task
    failure. Capture
    `gh pr view` with the json field `reviewDecision` alongside, record both, and state in the SUMMARY
    that the residual block comes from a review requirement or from `copilot_code_review` rather than
    from the required status checks — that is a genuine finding for ADR-019 about what else the
    `pull_request` and Copilot rules contribute, and it narrows rather than destroys the Task 1 result.

    Leave the pull request OPEN and unmerged. It is now mergeable, which is a safer resting point than
    a blocked one for plan 05 to close — but it must be closed, never merged, and plan 05 owns that.
  </action>
  <verify>
    <automated>python3 .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-assert-verdicts.py clean</automated>
  </verify>
  <acceptance_criteria>
    - `tree-hash-clean.txt` is byte-identical to both `tree-hash-baseline.txt` and `tree-hash-blocking.txt`; the three head SHAs are all different and all three are listed in the SUMMARY
    - `check-runs-clean.json` holds exactly 5 entries, every `conclusion` is `success`
    - `required-contexts-still-set.json` holds exactly 5 contexts at `integration_id` 15368 — the requirement survived into the third verdict
    - `merge-state-clean.json` contains no `UNKNOWN`, and its value is recorded as read; if it is not `CLEAN`, `reviewDecision` is captured beside it and the SUMMARY names the residual cause
    - If the first poll exited 1 with the settled-but-unchanged message, the SUMMARY records that, the 60-second wait, and the empty-`prev` re-poll whose result was taken as authoritative — an exit 1 on a new SHA is not a stale read and must not halt the task
    - `gh variable list` on the target repository was confirmed empty before the re-trigger
    - The three verdicts are presented in the SUMMARY as one table with the shared tree hash as a row, following 18-05's form
    - The pull request is still OPEN and unmerged; `main`'s HEAD is still unchanged
    - `python3 22-assert-verdicts.py clean` exits 0; both modes of the helper have now been run and both exited 0
  </acceptance_criteria>
  <done>One tree, three commits, three merge verdicts — and the middle one is a refusal that the third one proves was caused by the red required check and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gh pr merge` → the target repository's `main` | The command is a write with no dry-run mode; if the gate is not actually engaged it lands a commit |
| GitHub's asynchronous mergeability computation → this plan's assertions | The first read after a change is frequently `UNKNOWN` or stale |
| exercise branch → `main` | The only path by which anything from this phase could enter the pilot repository's default branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-17 | Tampering | `terraform-pipelines` `main` | mitigate | `gh pr merge` is hard-preconditioned on a FRESH read equal to `BLOCKED`, `--admin` is never passed, an explicit enabled merge method is passed so no TTY prompt can hang, and `main`'s HEAD is diffed against the plan-01 capture afterwards |
| T-22-18 | Repudiation | a stale merge-state read presented as a measurement | mitigate | Every read goes through the settle-poll whose corrected two-condition assertion rejects a value equal to the previous one; any artifact containing `UNKNOWN` fails the verify command |
| T-22-19 | Spoofing | a refusal that is actually a merge-method configuration error | mitigate | Allowed methods captured in plan 01 before the attempt; `merge-attempt.txt` asserted free of merge-method error text; the passed method is named in the SUMMARY with its evidence source |
| T-22-20 | Repudiation | attributing `BLOCKED` to the required status checks when `copilot_code_review` or the `pull_request` rule caused it | mitigate | The three-verdict structure on one tree hash — the third verdict holds the ruleset constant and changes only the check conclusions; if it does not settle to `CLEAN`, `reviewDecision` is captured and the residual cause named rather than glossed |
| T-22-21 | Denial of Service | the third verdict poisoned by artifact-name collisions | mitigate | Re-trigger by empty commit, never `gh run rerun` — upload-artifact v4 requires unique names per run id and a rerun reuses it (18-05's measured reason) |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs nothing |
</threat_model>

<verification>
- `python3 22-assert-verdicts.py blocked` exits 0
- `python3 22-assert-verdicts.py clean` exits 0
- Three tree-hash files byte-identical; three head SHAs distinct
- `merge-attempt.txt` non-empty, carries stderr, no `--admin`, no merge-method error
- `diff 22-evidence/main-head-before.txt 22-evidence/main-head-after-attempt.txt` empty
- `! grep -rq UNKNOWN .planning/phases/22-*/22-evidence/`
- `bash scripts/check-adoption-guide.sh` still 15 passed / 0 failed
</verification>

<success_criteria>
- A merge refusal captured in GitHub's own words on a pull request whose only change since the control reading was the ruleset write
- Three merge verdicts on one tree hash, presented as a single comparison table
- The required contexts confirmed still in place at the third verdict
- Nothing merged; the target repository's `main` HEAD unchanged from the plan-01 capture
</success_criteria>

<output>
Create `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-04-SUMMARY.md` when done.
Record: the three head SHAs, the shared tree hash, the three merge states in order, the `gh pr merge`
exit code, the refusal text VERBATIM, the merge method passed, the five green conclusions at the third
verdict, and the confirmation that the contexts were still required when it settled. ADR-019 in plan 06
is written from these values and quotes the refusal inline.
</output>
