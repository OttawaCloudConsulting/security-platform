---
phase: 21-docs-cleanup-close-remaining-phase-20-deferred-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/milestone-plan/milestone-2-cicd-gate.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "A reader of milestone-2-cicd-gate.md sees only scanners the live pipeline actually runs — Grype appears nowhere in the file"
    - "The M2-F3 JSON output list names the five real JSON filenames the live workflow writes"
    - "The M2-F3 DefectDojo parser row names the parsers the live SCA and container jobs actually feed, with no bare duplicated Trivy Scan"
    - "The document introduces no markdownlint violation (baseline was 0 errors)"
  artifacts:
    - path: "docs/milestone-plan/milestone-2-cicd-gate.md"
      provides: "M2-F1/M2-F3 descriptions reconciled to the live security.yml"
      contains: "trivy-fs.json"
  key_links:
    - from: "docs/milestone-plan/milestone-2-cicd-gate.md"
      to: "OttawaCloudConsulting/security-platform .github/workflows/security.yml"
      via: "prose restating the live job composition, artifact names and emitted filenames"
      pattern: "trivy-fs\\.json|trivy-image\\.json"
---

<objective>
Reconcile `docs/milestone-plan/milestone-2-cicd-gate.md` to the live callable workflow by removing all
five Grype references (D-01, D-02) and correcting the two wrong JSON filenames on the M2-F3 output line
(D-03 as amended, plus the approved `trivy-results.json` fix).

Purpose: closes Phase 17 deferred item #4 and DIST-06. The milestone doc is the requirements-level
description a manual adopter reads before copying the template; naming a scanner the template does not
contain (Grype was removed from the pipeline in Phases 15/16) makes the packaged description untrue.
Fixing these five sites also resolves an existing internal contradiction — M2-F4 (line ~92) already
names `security / SCA — Trivy Filesystem` correctly while M2-F1/M2-F3 still say Grype.

Output: one edited markdown file, `grep -c -i grype` = 0, markdownlint still 0 errors.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read these ranges only (RESEARCH.md is 1,097 lines — do not read it whole):

- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-RESEARCH.md` lines 128–297
  (Constraint Conflicts, Live Pipeline Ground Truth, the L78 token table, SCA job composition)
- same file lines 414–486 (DefectDojo parser names from source; Second-Order Effects SE-1/SE-2/SE-3)
- same file lines 577–660 (milestone-plan lint expectation; Pitfalls 1, 2, 4)
- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-CONTEXT.md` (locked
  decisions D-01 through D-04 — note D-03 and D-04 are AMENDED below; the amendments win)

<amendments>
<!-- The orchestrator obtained explicit user decisions on three conflicts RESEARCH.md found between
     CONTEXT.md's locked decisions and primary-source evidence. These OVERRIDE CONTEXT.md's literal text. -->

- **D-03 AMENDED — `sca-results.json` does not exist.** `sca-results` is the *artifact* name on the SCA
  job's `actions/upload-artifact` step (`with: name:`), not a filename. The SCA job's real JSON outputs
  are `trivy-fs.json`, `npm-audit-<N>.json` and `pip-audit-<N>.json`. tflint emits SARIF only and
  contributes no JSON — do not invent a tflint JSON filename. Never write `sca-results.json`.
- **D-04 AMENDED — per-job qualification required.** `Trivy Scan` is the correct DefectDojo parser
  string, but it already occupies the container slot on the same line, so a bare substitution yields
  `Trivy Scan, Trivy Scan` and reads as a copy-paste error. Qualify each slot. The verified API strings
  are exactly `Trivy Scan`, `NPM Audit v7+ Scan` (NOT "NPM Audit Version 7+", which is a docs-site
  display label) and `pip-audit Scan` (lowercase p, hyphen).
- **L78 second filename APPROVED IN SCOPE.** `trivy-results.json` is also wrong; the live container job
  writes `trivy-image.json`. Fix it on the same line, same pass.
</amendments>

<live_ground_truth>
<!-- Verified 2026-09-15 from OttawaCloudConsulting/security-platform .github/workflows/security.yml via
     gh api, diff-confirmed byte-identical to repos/security-platform/.github/workflows/security.yml,
     and corroborated by measured artifact downloads in .planning/STATE.md. Use these, not docs/. -->

Five jobs, artifact names, emitted files:

- `sast` / name `SAST — Semgrep CE` / artifact `semgrep-results` / `semgrep-results.json`, `semgrep.sarif`
- `iac` / name `IaC — Checkov` / artifact `checkov-results` / `checkov-results.json`, `checkov.sarif`
- `sca` / name `SCA — Trivy Filesystem` / artifact `sca-results` / `trivy-fs.json`,
  `npm-audit-<N>.json`, `pip-audit-<N>.json`, `trivy-fs.sarif`, `tflint.sarif`
- `container` / name `Container — Trivy Image` / artifact `trivy-image-results` / `trivy-image.json`,
  `trivy-image.sarif`
- `secrets` / name `Secrets — Gitleaks` / artifact `gitleaks-results` / `gitleaks-results.json`,
  `gitleaks.sarif`

SCA job step commands (live): `trivy fs . --scanners vuln --format json --output trivy-fs.json
--exit-code 1 --severity HIGH,CRITICAL`; a second identical-flag run with `--format sarif --output
trivy-fs.sarif`; `npm audit --audit-level=high --json` per discovered `package-lock.json`;
`pip-audit -r <req> --format json --progress-spinner=off -o pip-audit-<N>.json`; tflint SARIF only.

Every separator in a `name:` value is U+2014 EM DASH. This plan adds no `security / …` context string;
do not introduce one.
</live_ground_truth>

<tree_state_note>
The repository working tree currently carries 367 unrelated modified files under `.claude/` (an in-flight
GSD tooling update, measured 2026-09-15). `docs/` and `scripts/` are clean. Every "only this file changed"
assertion in this plan is therefore path-scoped with `-- docs/ scripts/`; a bare `git diff --name-only`
would list the `.claude/` churn and fail a correct edit. Those `.claude/` modifications are outside this
phase and are not a finding.
</tree_state_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the three Grype prose sites in M2-F1 (lines 16, 33, 43) and disambiguate the Trivy image bullet</name>
  <files>docs/milestone-plan/milestone-2-cicd-gate.md</files>
  <read_first>
    - `docs/milestone-plan/milestone-2-cicd-gate.md` lines 1–50 (Features table and the whole M2-F1 section)
    - `21-RESEARCH.md` lines 223–297 (Live Pipeline Ground Truth, SCA job composition table)
    - `21-RESEARCH.md` lines 601–645 (Pitfall 1: docs are not ground truth; Pitfall 4: never bulk-sed grype)
    - `docs/adoption-guide.md` lines 218–246 (§6) — use ONLY as a phrasing/style reference, never as a
      source of fact
  </read_first>
  <action>
Run `git status --porcelain docs/milestone-plan/milestone-2-cicd-gate.md` first and confirm it is clean;
editing a file with uncommitted changes is a high-risk action per the project epistemology rule. If it is
dirty, STOP and report.

Make exactly four single-line edits with the Edit tool. Edit by explicit content match — never
`grep -rl grype | xargs sed`, which would wreck the M1 workstation docs (where Grype is legitimately
installed) and the append-only ADRs.

1. Line 16, the M2-F1 row of the Features table. Current third cell reads
`5 parallel scan jobs: Semgrep CE, Checkov, Trivy, Grype, Gitleaks`. Replace that cell text with
`5 parallel scan jobs: Semgrep CE (SAST), Checkov (IaC), Trivy filesystem with npm audit, pip-audit and tflint (SCA), Trivy image (container), Gitleaks (secrets)`.
The job count stays five because the SCA job is one job running four tools. Keep the row on one line,
keep the leading and trailing `|` and the exact `| M2-F1 | GitHub Actions security workflow | ` prefix,
and introduce no additional `|` character inside the cell (it would split the table column).

2. Line 33, currently `- Grype with `--fail-on high` (fails on high/critical)`. Replace the whole bullet
with
`- Trivy filesystem with `--scanners vuln --exit-code 1 --severity HIGH,CRITICAL`, plus the ecosystem sub-scans npm audit (`--audit-level=high`), pip-audit and tflint (SARIF only)`.
These flags are the live ones; do not paraphrase them.

3. Line 34, currently `- Trivy with `exit-code: '1'` and `severity: 'HIGH,CRITICAL'``. Once line 33 names
Trivy filesystem, an unqualified "Trivy" on line 34 is ambiguous. Change the leading noun to
`Trivy image` so the bullet reads `- Trivy image with `exit-code: '1'` and `severity: 'HIGH,CRITICAL'``.
This disambiguation is exercised under Claude's Discretion ("consistency is the goal, not a rewrite") and
is a deliberate part of this plan, not scope creep.

4. Line 43, currently `- A PR introducing a known vulnerable dependency is flagged by Grype`. Replace the
whole bullet with
`- A PR introducing a known vulnerable dependency is flagged by the SCA job — Trivy filesystem, or npm audit / pip-audit when the advisory is ecosystem-specific`.
Use U+2014 EM DASH for the dash, matching the file's existing style.


Note on the replacement strings quoted in this action: the outermost backticks delimit the replacement text
in this plan and are NOT part of what you write into the file. Backticks appearing inside the replacement
(around filenames, flags and parser strings) ARE literal and must be typed into the document.

Touch nothing else in the M2-F1 section. Lines 78 and 85 belong to Task 2. Do not modify line 83's
"all 5 JSON artifacts" claim, the M2-F4 required-context list, or any other section — they are out of
this phase's scope.
  </action>
  <verify>
    <automated>test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 2 &amp;&amp; grep -qF 'Trivy image with' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; grep -qF '--audit-level=high' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; test "$(grep -c '^| M2-F' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 5 &amp;&amp; markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md 2>&amp;1 | grep -q 'Summary: 0 error'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 2` passes (lines 16, 33, 43 cleared; only lines 78 and 85 remain, and they are Task 2's)
    - `grep -qF 'Semgrep CE (SAST), Checkov (IaC)' docs/milestone-plan/milestone-2-cicd-gate.md` passes
    - `test "$(grep -c '^| M2-F' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 5` passes (feature table still has its 5 rows, columns unbroken)
    - `grep -qF 'Trivy image with' docs/milestone-plan/milestone-2-cicd-gate.md` passes (line 34 disambiguated)
    - `grep -qF 'tflint' docs/milestone-plan/milestone-2-cicd-gate.md` and `grep -qF 'pip-audit' docs/milestone-plan/milestone-2-cicd-gate.md` both pass
    - `test "$(grep -c 'sca-results.json' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0` passes (the amended-away filename was never written)
    - `markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff --name-only -- docs/ scripts/` lists only `docs/milestone-plan/milestone-2-cicd-gate.md` (path-scoped on purpose — see `<tree_state_note>`)
  </acceptance_criteria>
  <done>Lines 16, 33, 34 and 43 name only live scanners; the feature table structure is intact; lint is still clean; only the intended file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the M2-F3 JSON filename list (line 78) and the DefectDojo parser row (line 85)</name>
  <files>docs/milestone-plan/milestone-2-cicd-gate.md</files>
  <read_first>
    - `docs/milestone-plan/milestone-2-cicd-gate.md` lines 70–90 (the whole M2-F3 section)
    - `21-RESEARCH.md` lines 134–222 (CONFLICT-1 `sca-results.json` does not exist; CONFLICT-2 the
      Trivy Scan collision and the recommended D-04′ wording)
    - `21-RESEARCH.md` lines 245–267 (the L78 token-by-token live-status table: three of five tokens are
      already correct — do not rewrite all five)
    - `21-RESEARCH.md` lines 414–434 (DefectDojo `get_scan_types()` strings, primary source)
    - `21-RESEARCH.md` lines 611–620 (Pitfall 2: artifact name is not a filename)
  </read_first>
  <action>
Two single-line edits with the Edit tool. Both lines stay one line each so no line numbers shift
(MD013 line-length is disabled in this repo, so long lines are acceptable).

1. Line 78 currently reads
`- JSON output: `semgrep-results.json`, `checkov-results.json`, `grype-results.json`, `trivy-results.json`, `gitleaks-results.json``.
Only two of its five tokens are wrong. Keep `semgrep-results.json`, `checkov-results.json` and
`gitleaks-results.json` verbatim — they match the live workflow exactly. Replace `grype-results.json`
with the SCA job's three real JSON filenames and replace `trivy-results.json` with `trivy-image.json`,
producing:
`- JSON output: `semgrep-results.json`, `checkov-results.json`, `trivy-fs.json` plus `npm-audit-<N>.json` and `pip-audit-<N>.json` where the matching ecosystem sub-scan fires, `trivy-image.json`, `gitleaks-results.json``.
Use `<N>` (the workflow's own loop counter, `npm-audit-${i}.json` / `pip-audit-${i}.json`), not `*`.
Keep every filename inside backticks. Do NOT write `sca-results.json` — no such file exists. Do NOT add
`tflint.sarif` or `trivy-fs.sarif`: this is a JSON-output list and tflint contributes no JSON.

2. Line 85 currently reads
`- Artifact names match the expected DefectDojo parser input: Semgrep JSON Report, Checkov Scan, Anchore Grype, Trivy Scan, Gitleaks Scan`.
Replace the list after the colon with the per-job-qualified form:
`Semgrep JSON Report, Checkov Scan, Trivy Scan (SCA filesystem — plus NPM Audit v7+ Scan and pip-audit Scan where the ecosystem sub-scans fire), Trivy Scan (container image), Gitleaks Scan`.
Use U+2014 EM DASH inside the parenthetical. The three strings `NPM Audit v7+ Scan`, `pip-audit Scan` and
`Trivy Scan` are DefectDojo's literal `get_scan_types()` API values — type them exactly, including the
lowercase `p` and hyphen in `pip-audit Scan` and the `v7+` in `NPM Audit v7+ Scan`. Do not write
`NPM Audit Scan`, `NPM Audit Version 7+`, or `Pip-Audit Scan`. Leave `Semgrep JSON Report`,
`Checkov Scan` and `Gitleaks Scan` untouched — all three already match DefectDojo source.

Rationale to record in the SUMMARY: `Anchore Grype` is still a real DefectDojo parser. What changed is
that the pipeline no longer produces input for it — this is a pipeline-output fix, not a parser-name fix.

Leave line 83 ("all 5 JSON artifacts are downloadable") and every other line in M2-F3 unchanged.

Note on the replacement strings quoted in this action: the outermost backticks delimit the replacement text
in this plan and are NOT part of what you write into the file. Backticks appearing inside the replacement
(around filenames, flags and parser strings) ARE literal and must be typed into the document.

  </action>
  <verify>
    <automated>test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0 &amp;&amp; grep -qF 'trivy-fs.json' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; grep -qF 'trivy-image.json' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; grep -qF 'NPM Audit v7+ Scan' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; grep -qF 'pip-audit Scan' docs/milestone-plan/milestone-2-cicd-gate.md &amp;&amp; test "$(grep -c 'trivy-results.json' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0 &amp;&amp; markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md 2>&amp;1 | grep -q 'Summary: 0 error'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0` passes (all five sites cleared; DIST-06 gap closed)
    - `test "$(grep -c 'trivy-results.json' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0` passes
    - `test "$(grep -c 'sca-results.json' docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0` passes
    - `grep -qF 'trivy-fs.json'`, `grep -qF 'npm-audit-<N>.json'`, `grep -qF 'pip-audit-<N>.json'` and `grep -qF 'trivy-image.json'` all pass against the file
    - `grep -qF 'NPM Audit v7+ Scan'` and `grep -qF 'pip-audit Scan'` both pass
    - `test "$(grep -o 'Trivy Scan' docs/milestone-plan/milestone-2-cicd-gate.md | wc -l | tr -d ' ')" -eq 2` passes (grep -c counts lines, not occurrences — both hits are on line 85, so -o plus wc -l is required) and neither occurrence is unqualified: `grep -qF 'Trivy Scan (SCA filesystem'` and `grep -qF 'Trivy Scan (container image)'` both pass
    - `grep -qF 'semgrep-results.json'`, `grep -qF 'checkov-results.json'`, `grep -qF 'gitleaks-results.json'` still pass (correct tokens preserved)
    - `markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff --name-only -- docs/ scripts/` lists only `docs/milestone-plan/milestone-2-cicd-gate.md`
  </acceptance_criteria>
  <done>Line 78 names the five real JSON files; line 85 names the real parsers with both Trivy slots qualified; zero Grype references remain in the file; lint clean.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only change. No code path, no input handling, no credential, no runtime behaviour is
introduced or modified, so a STRIDE register over this plan's surface would have no components to
enumerate. The one relevant control is documentation integrity (ASVS V14, documentation sense): a doc
naming a scanner the template does not contain degrades a real security control by misleading adopters.
Mitigated by every value in this plan being copied from the live workflow / DefectDojo source rather than
composed, and by the grep acceptance criteria above. Related hard rule carried from RESEARCH: no task in
this phase may modify `scripts/check-adoption-guide.sh` (the gate's own header identifies editing it as
tampering vector T-20-12).
</threat_model>

<verification>
```bash
test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0 && echo "GRYPE-CLEAR"
markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md    # expect: Summary: 0 error(s)
git diff --name-only -- docs/ scripts/                            # expect: only the milestone-2 doc
```
</verification>

<success_criteria>
- `grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md` returns 0
- Neither `grype-results.json`, `trivy-results.json` nor `sca-results.json` appears in the file
- `trivy-fs.json`, `npm-audit-<N>.json`, `pip-audit-<N>.json`, `trivy-image.json` all appear
- `NPM Audit v7+ Scan` and `pip-audit Scan` appear; both `Trivy Scan` occurrences are job-qualified
- markdownlint-cli2 reports `Summary: 0 error(s)` (baseline preserved)
- `git diff --name-only -- docs/ scripts/` lists exactly one file

Note on the `grep -c -i grype … -eq 0` gates: zero means zero anywhere in the file, headings and comments
included. That is the intended semantics here — the word Grype must not survive in any form — so these
assertions are deliberately unfiltered.
</success_criteria>

<output>
Create `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-01-SUMMARY.md` when done.
Record: the measured `grep -c -i grype` before/after, the markdownlint summary line, and the rationale
that `Anchore Grype` remains a valid DefectDojo parser (the pipeline output changed, not the parser).
</output>
