---
phase: 21-docs-cleanup-close-remaining-phase-20-deferred-items
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adoption-guide.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "An adopter reading §6 learns that a large run can lose findings silently — only the top 5,000 results per run are displayed, prioritized by severity, while the rest are accepted and never shown"
    - "An adopter reading §6 learns the four hard ceilings that cause outright rejection: 10 MB gzipped per SARIF file, 20 runs per file, 25,000 results per run, 25,000 rules per run"
    - "An adopter reading §6 learns that a repository reaching 1,000,000 alerts has all further uploads blocked with no self-service recovery"
    - "The limits are attributed to GitHub with current, non-redirecting docs URLs written as markdown links"
    - "The standing documentation gate still reports PASSED 15 / FAILED 0 and markdownlint still reports zero violations"
    - "No section was renumbered — the guide still ends at ## 13. and every existing 'section N' cross-reference remains valid"
  artifacts:
    - path: "docs/adoption-guide.md"
      provides: "New ### subsection inside ## 6. documenting GitHub code-scanning SARIF upload limits"
      contains: "top 5,000"
  key_links:
    - from: "docs/adoption-guide.md §6"
      to: "docs.github.com SARIF support reference"
      via: "markdown link citing the platform as the owner of the limits"
      pattern: "reference/code-scanning/sarif-files/sarif-support"
---

<objective>
Add one new `###` subsection inside `## 6. First Run — What to Expect` of `docs/adoption-guide.md`
documenting GitHub's code-scanning SARIF upload limits (D-05, D-06 as verified, D-07 as amended), framed
around the failure mode that actually bites first: silent display truncation at 5,000 results per run,
not the 25,000 rejection ceiling.

Purpose: closes Phase 17 deferred item #6 and DIST-08. §6 currently tells an adopter what a first run
produces — artifact counts, analysis counts, "green does not mean clean" — but states no upload ceiling at
all, so a consumer repo running Semgrep `p/default` across a large monorepo gets no warning before it
starts losing findings or having uploads rejected.

Output: one edited markdown file; `bash scripts/check-adoption-guide.sh` still PASSED 15 / FAILED 0;
`markdownlint-cli2 docs/adoption-guide.md` still zero violations.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read these ranges only (RESEARCH.md is 1,097 lines — do not read it whole):

- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-RESEARCH.md` lines 299–412
  (GitHub SARIF Upload Limits: file size, the objects table, maximum-vs-truncation distinction, exact
  error strings, the negative finding on per-hour limits, the canonical 200 URLs)
- same file lines 211–222 (CONFLICT-3 — why the subsection must centre on 5,000, not 25,000)
- same file lines 488–583 (Editing Constraints: forced `###` heading level, insertion point, the standing
  gate's 15 assertions, the markdownlint rules that actually bite)
- same file lines 631–663 (Pitfalls 5, 6, 7)
- same file lines 286–297 (measured finding counts — why the number 56 must not be written)
- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-CONTEXT.md` (D-05, D-06,
  D-07 — D-06's four numbers are CONFIRMED correct; D-07 is amended below)

<amendments>
<!-- User-approved amendments from the orchestrator. These OVERRIDE CONTEXT.md's literal text. -->

- **D-06 CONFIRMED, D-05/D-07 SCOPE EXPANDED.** All four candidate numbers are verified correct against
  `github/docs` source: 10 MB gzipped per file, 20 runs per file, 25,000 results per run, 25,000 rules per
  run. The subsection must ALSO state (a) the 5,000-results-per-run **display truncation** — silent data
  loss well below the rejection ceiling, and the framing the whole subsection centres on — and (b)
  GitHub's **1,000,000 total alert limit per repository**, which blocks all further SARIF uploads with no
  self-service recovery.
- **Do not write "soft limit" against 25,000.** Exceeding any *Maximum value* rejects the file outright.
  The truncation column is an additional display ceiling that applies *below* the maximum. A rendered
  GitHub page summarised this backwards; the docs source does not.
- **D-07 AMENDED — do not write the number 56.** deferred-items.md's "largest run: 56 results" comes from
  a different run than the figures §6 already publishes three paragraphs above the insertion point (8
  Semgrep, 11 Gitleaks, 14 Checkov, 58 Trivy image, 6 Trivy filesystem, 3 tflint). Printing 56 next to 58
  invites a reader to think one is wrong. Cite the already-published scale instead.
- **Citation URL AMENDED.** Do not cite
  `https://docs.github.com/en/code-security/code-scanning/integrating-with-code-scanning/sarif-support-for-code-scanning`
  — it returns 301. Use the canonical paths given in the task action.
</amendments>

<verified_values>
<!-- Read from github/docs source (content/code-security/reference/code-scanning/sarif-files/sarif-support.md
     and data/reusables/code-scanning/sarif-limits.md, last modified 2024-10-18) via gh api on 2026-09-15.
     Every number below is a transcription, not a recollection. Do not compose or round any of them. -->

Hard maximums — exceeding any one of these rejects the entire SARIF file, so no alerts appear at all:

- 10 MB per gzip-compressed SARIF file
- 20 runs per file
- 25,000 results per run
- 25,000 rules per run
- 1,000,000 alerts per repository (the "Alert Limit")

Display truncation — the upload succeeds and the data is accepted, but not all of it is shown:

- Results per run: only the top 5,000 are displayed, prioritized by severity

Outcome table for results per run: ≤ 5,000 accepted and fully displayed; 5,001–25,000 accepted, upload
succeeds, only the top 5,000 displayed (soft-limit warning emitted); > 25,000 rejected.

Verbatim GitHub error strings, if the subsection quotes one: `Analysis SARIF file rejected due to result
limits`, `Analysis SARIF file rejected due to rule limits`, `Analysis SARIF file rejected due to run
limits`, `All analysis uploads blocked due to alert limit`, and the soft-limit warning `Analysis SARIF
file exceeded alert limits`.

Alert-limit consequence, verbatim from `results-exceed-limit.md`: "There is no self-service method for
deleting alerts at this time, so contacting customer support is necessary before code-scanning can be
re-enabled."

No per-hour or per-day SARIF upload rate limit is published. A code search across the `github/docs`
source repo for an hourly ceiling returned zero results. Do not state or imply one exists.

Canonical URLs (all return HTTP 200, verified by curl 2026-09-15):

- `https://docs.github.com/en/code-security/reference/code-scanning/sarif-files/sarif-support`
- `https://docs.github.com/en/code-security/reference/code-scanning/sarif-files/troubleshoot-sarif-uploads/results-exceed-limit`

Measured baselines for this file, taken 2026-09-15 before any edit — these are the numbers your
verification must still produce afterwards:

- `bash scripts/check-adoption-guide.sh` → `check-adoption-guide: PASSED 15 / FAILED 0`, exit 0
- `markdownlint-cli2 docs/adoption-guide.md` → `Summary: 0 error(s)`, exit 0
- 596 lines; 20 `## ` headings; 9 `### ` headings; 5 lines containing `security / `; zero occurrences of
  `soft limit`, `25,000`, `5,000`, `10 MB`, `1,000,000`, `per hour`, `docs.github.com`
</verified_values>

<insertion_point>
<!-- Verified by reading the file 2026-09-15. Use content anchors, not line numbers, when editing. -->

`## 6. First Run — What to Expect` starts at line 218. Inside it: the `gh run view` / `gh run download`
fenced bash block closes with its ``` fence on line 257, line 258 is blank, lines 259–262 are the
"Before you consider switching to `blocking`, internalise this corollary…" paragraph, line 263 is blank,
and `## 7. Gate-Mode Selection` starts at line 264.

Insert the new subsection between line 258 and line 259 — i.e. after the fenced block and its blank line,
immediately before the "Before you consider switching to `blocking`" paragraph. That paragraph is a
deliberate bridge into §7 and must remain the last prose in §6.
</insertion_point>

<tree_state_note>
The repository working tree currently carries 367 unrelated modified files under `.claude/` (an in-flight
GSD tooling update, measured 2026-09-15). `docs/`, `scripts/` and the Phase 17 planning directory are clean.
Every "only this file changed" assertion in this plan is therefore path-scoped; a bare
`git diff --name-only` would list the `.claude/` churn and fail a correct edit. Those `.claude/`
modifications are outside this phase and are NOT a finding — do not report them and do not clean them up.
</tree_state_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SARIF upload limits subsection into §6</name>
  <files>docs/adoption-guide.md</files>
  <read_first>
    - `docs/adoption-guide.md` lines 218–264 (all of §6 plus the §7 heading — the exact insertion seam)
    - `docs/adoption-guide.md` lines 44–52 (the `### Prerequisites` subsection — the file's unnumbered
      `###` heading style, which the new heading must match)
    - `21-RESEARCH.md` lines 299–412 (the limits, both columns, error strings, canonical URLs)
    - `21-RESEARCH.md` lines 488–583 (forced heading level, insertion point, the gate's 15 assertions, the
      markdownlint rules that bite: MD034 bare URLs, MD022 blanks-around-headings, MD031
      blanks-around-fences, MD032 blanks-around-lists)
    - `21-RESEARCH.md` lines 863–884 (the vetted example paragraph showing an MD034-safe citation, and the
      anti-pattern list)
    - `scripts/check-adoption-guide.sh` — READ ONLY, to understand what the 15 assertions check. This file
      MUST NOT be modified by any task in this phase; its own header identifies editing it as tampering
      vector T-20-12.
  </read_first>
  <action>
Confirm `git status --porcelain docs/adoption-guide.md` is clean before editing; if dirty, STOP and report.
Capture the pre-edit baseline by running `bash scripts/check-adoption-guide.sh` and
`markdownlint-cli2 docs/adoption-guide.md` once, and record both outputs — you need the before/after pair
for the summary.

Insert a single new subsection at the seam described in `<insertion_point>`: after the blank line that
follows the `gh run download` fenced block, and immediately before the paragraph beginning "Before you
consider switching to `blocking`". Add no other content anywhere in the file.

Heading: `### SARIF Upload Limits at Consumer Scale` — three hashes, no section number. A new `## 7.`
is forbidden: it would force renumbering §7–§13 and invalidate 16 prose "section N" cross-references.
Put one blank line above the heading and one blank line below it (MD022 is enabled in this repo despite
`.markdownlint-cli2.yaml` appearing to disable it — the repo-root `.markdownlint.jsonc` overrides that
file and re-enables MD018, MD022, MD029, MD031 and MD032).

The subsection must make these five points, in this order, in prose and at most one bullet list. Lead with
the truncation case — it is the failure mode this subsection exists to warn about, and it is invisible
without documentation:

1. **Silent truncation first.** GitHub displays only the top 5,000 results per run, prioritized by
   severity. A run that produces more than that still uploads successfully and still shows a green check;
   the results past 5,000 are accepted but never displayed. Spell out the consequence concretely: a
   Semgrep `p/default` run on a large monorepo producing, say, 8,000 findings shows 5,000 alerts and
   silently drops the other 3,000. Nothing in the run's own output looks like an error.
2. **Hard ceilings second, with the opposite consequence.** Exceeding any one of these rejects the whole
   SARIF file, so no alerts appear at all: 10 MB per gzip-compressed SARIF file; 20 runs per file; 25,000
   results per run; 25,000 rules per run. Name what an adopter would see — the code-scanning upload fails
   with `Analysis SARIF file rejected due to result limits` (and the analogous rule- and run-limit
   variants). Do NOT describe 25,000 as a "soft limit"; exceeding a maximum rejects the file.
3. **The repository-wide cap third.** A repository that accumulates 1,000,000 code-scanning alerts has all
   further analysis uploads blocked. State plainly that there is no self-service way to delete alerts, so
   re-enabling code scanning requires contacting GitHub support. This is the only limit with no
   self-service recovery, which is why it is worth naming despite being far above any plausible first-run
   scale.
4. **This project's measured scale, for calibration.** Say that nothing this pipeline has run comes close:
   the largest single-tool count measured on this project's own validation pull request is 58 findings
   (Trivy image — the figures quoted earlier in this section), i.e. fewer than 60 from any one tool, three
   orders of magnitude below the display ceiling. Then say which consumer repositories plausibly do cross
   it: a large monorepo scanned with Semgrep `p/default`, or a container image with a large base layer.
   Do NOT write the number 56 anywhere — §6 already publishes 58 three paragraphs above, and printing both
   makes one look wrong.
5. **Attribution.** Close by pointing at GitHub as the owner of these numbers, using markdown links, not
   bare URLs (MD034 is enabled and is the single likeliest lint failure here). Use link text
   `SARIF support for code scanning` for
   `https://docs.github.com/en/code-security/reference/code-scanning/sarif-files/sarif-support`
   and link text `troubleshooting SARIF results that exceed limits` for
   `https://docs.github.com/en/code-security/reference/code-scanning/sarif-files/troubleshoot-sarif-uploads/results-exceed-limit`.
   Both URLs return 200; the older `integrating-with-code-scanning/sarif-support-for-code-scanning` path
   301-redirects and must not appear.

Style and formatting constraints:

- Match §6's existing voice and hand-wrapping at roughly 100 columns (MD013 line-length is disabled, so
  this is convention, not enforcement). Use `-` for any bullet, matching the file.
- If you use a bullet list, put a blank line above and below it (MD032).
- Do not use a markdown table here — MD055 and MD056 are enabled and also fire when a non-table line
  follows a table row without a blank line. §6 is prose plus bullets today; stay with that.
- Add no fenced code block. If you quote a GitHub error string, put it in an inline code span. Any fence
  you did add would need blank lines above and below (MD031).
- Do not put a trailing space inside a code span (MD038) and leave no trailing whitespace (MD009).

Line-wrapping constraint that interacts with this plan's verification: the following phrases are asserted
as contiguous strings by the acceptance criteria and by plan 21-04's suite, so each must sit unbroken on a
single line — `top 5,000`, `25,000 results per run`, `25,000 rules per run`, `20 runs per file`, `10 MB`,
`1,000,000`, and the words `no self-service`. Wrap the surrounding prose anywhere else you like; do not let
a line break fall inside one of those phrases.


Hard prohibitions — each one maps to an assertion in the standing gate or to a verified fact:

- Do not introduce any new string matching `security / …`. The gate's SIXTH-CONTEXT assertion fails on any
  such string outside the derived five, and CONTEXT-EM-DASH requires U+2014 in the existing five. Leave all
  five untouched.
- Do not write `OCC-github` anywhere (NO-OCC-GITHUB assertion — it is this working directory's path and is
  trivially pasted in by accident).
- Do not add a `raw.githubusercontent.com` URL or a reusable-workflow ref; both have pinning assertions.
- Do not write `|| true` or `--config auto` (BANNED-PATTERNS assertion).
- Do not state or imply a per-hour or per-day upload rate limit. None is published; a code search of the
  `github/docs` source repo returned zero results.
- Do not modify `scripts/check-adoption-guide.sh`, any `##` heading line, the §6 text that already exists,
  or any other file.
  </action>
  <verify>
    <automated>grep -qF 'top 5,000' docs/adoption-guide.md &amp;&amp; grep -qF '25,000 results per run' docs/adoption-guide.md &amp;&amp; grep -qF '25,000 rules per run' docs/adoption-guide.md &amp;&amp; grep -qF '20 runs per file' docs/adoption-guide.md &amp;&amp; grep -qF '10 MB' docs/adoption-guide.md &amp;&amp; grep -qF '1,000,000' docs/adoption-guide.md &amp;&amp; grep -qF 'reference/code-scanning/sarif-files/sarif-support' docs/adoption-guide.md &amp;&amp; grep -qF 'results-exceed-limit' docs/adoption-guide.md &amp;&amp; test "$(grep -c '^### ' docs/adoption-guide.md)" -eq 10 &amp;&amp; markdownlint-cli2 docs/adoption-guide.md 2>&amp;1 | grep -q 'Summary: 0 error'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'top 5,000' docs/adoption-guide.md` passes (display truncation stated, and the phrase is anchored so it cannot be satisfied by the substring inside `25,000`)
    - `grep -qF '25,000 results per run'` and `grep -qF '25,000 rules per run'` both pass
    - `grep -qF '20 runs per file'` and `grep -qF '10 MB'` both pass
    - `grep -qF '1,000,000'` passes and `grep -qiF 'no self-service'` passes (alert cap and its recovery consequence both stated)
    - `test "$(grep -c -i 'soft limit' docs/adoption-guide.md)" -eq 0` passes (25,000 is never mislabelled; baseline was 0)
    - `test "$(grep -c -i 'per hour\|per day\|hourly' docs/adoption-guide.md)" -eq 0` passes (no invented rate limit; baseline was 0)
    - `test "$(grep -c -F ' 56 ' docs/adoption-guide.md)" -eq 0` passes and `grep -qF '58' docs/adoption-guide.md` still passes (D-07 amended: cite 58, never 56)
    - `grep -qF 'reference/code-scanning/sarif-files/sarif-support'` and `grep -qF 'troubleshoot-sarif-uploads/results-exceed-limit'` both pass
    - `test "$(grep -c -F 'integrating-with-code-scanning' docs/adoption-guide.md)" -eq 0` passes (the 301 path is absent)
    - `test "$(grep -cE '(^|[^[(<`])https://docs\.github\.com' docs/adoption-guide.md)" -eq 0` passes (no bare URL; MD034)
    - `test "$(grep -c '^### ' docs/adoption-guide.md)" -eq 10` passes (exactly one new `###`; baseline was 9)
    - `test "$(grep -c '^## ' docs/adoption-guide.md)" -eq 20` passes, `test "$(grep -c '^## 13\. ' docs/adoption-guide.md)" -eq 1` passes, and `test "$(grep -c '^## 14' docs/adoption-guide.md)" -eq 0` passes (no renumbering, no new numbered section)
    - `test "$(grep -c -F 'security / ' docs/adoption-guide.md)" -eq 5` passes (baseline preserved; no sixth context)
    - `test "$(grep -c -F 'OCC-github' docs/adoption-guide.md)" -eq 0` passes
    - `markdownlint-cli2 docs/adoption-guide.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff --name-only -- docs/ scripts/` lists only `docs/adoption-guide.md` (path-scoped on purpose — see `<tree_state_note>`); `git diff docs/adoption-guide.md` shows only added lines at the §6 seam and no deletions or modifications of existing lines
  </acceptance_criteria>
  <done>One new `### SARIF Upload Limits at Consumer Scale` subsection sits inside §6 between the artifact-download fence and the `blocking` bridge paragraph, states the 5,000 truncation first, the four rejection ceilings second, the 1,000,000 cap third, this project's 58-finding scale fourth, and cites two current GitHub docs URLs as markdown links; markdownlint is clean; no existing line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the standing documentation gate is still fully green</name>
  <files>docs/adoption-guide.md</files>
  <read_first>
    - `scripts/check-adoption-guide.sh` — READ ONLY; understand each of the 15 assertions well enough to
      diagnose a failure. Modifying this file is forbidden (tampering vector T-20-12).
    - `21-RESEARCH.md` lines 512–531 (the seven assertions the new text could plausibly break, and the
      preflight exit-2 condition)
    - `21-RESEARCH.md` lines 645–659 (Pitfall 6 — read the markdownlint output from the tool directly, not
      through the gate's single FAIL line)
  </read_first>
  <action>
Run `markdownlint-cli2 docs/adoption-guide.md` FIRST and read its own output; the gate collapses a lint
failure into one `FAIL: MARKDOWNLINT` line that is much harder to diagnose. Expect `Summary: 0 error(s)`.

Then run `bash scripts/check-adoption-guide.sh` — with `bash` explicitly, never `./`, and never after
`chmod +x`; the project rule forbids setting the executable bit on scripts. Expect the final line
`check-adoption-guide: PASSED 15 / FAILED 0` and exit code 0. Note that exit code 2 means an
infrastructure problem (a missing `repos/security-platform/.github/workflows/` clone, or `python3 -c
'import yaml'` failing), not a documentation defect; both were confirmed working on 2026-09-15, so a 2
here means something in the environment changed — report it rather than working around it.

If any assertion fails: STOP, report the raw failing assertion output, your theory, and the proposed fix,
per the project's STOP → REPORT → WAIT rule. Then fix the new subsection only. Under no circumstances
modify `scripts/check-adoption-guide.sh`, relax a markdownlint config file, or add an inline lint-disable
comment to make the check pass — the gate's own header identifies weakening it as the tampering vector it
exists to prevent.

Finally, since Task 1's edit is committed by the time this task runs (executor commits per task), do NOT
check the working-tree diff — it will be clean. Instead check the last commit's file list against
`HEAD~1`: run `git diff HEAD~1 --name-only -- docs/ scripts/ .markdownlint.jsonc .markdownlint-cli2.yaml`
and confirm it lists exactly one file, `docs/adoption-guide.md`. Scope the check to those paths: the tree
carries 367 unrelated `.claude/` modifications that an unscoped `git diff` would list, and this scoped
form also proves the gate script and lint configs were not touched by the same commit. Record the measured
gate line, the markdownlint summary line, and the commit diffstat in the plan summary as evidence — a
checkpoint is "I ran it, here is what happened," not "I believe this works."
  </action>
  <verify>
    <automated>markdownlint-cli2 docs/adoption-guide.md 2>&amp;1 | grep -q 'Summary: 0 error' &amp;&amp; bash scripts/check-adoption-guide.sh | grep -q 'PASSED 15 / FAILED 0' &amp;&amp; test "$(git diff HEAD~1 --name-only -- docs/ scripts/ .markdownlint.jsonc .markdownlint-cli2.yaml | wc -l | tr -d ' ')" -eq 1 &amp;&amp; git diff HEAD~1 --name-only -- docs/ scripts/ .markdownlint.jsonc .markdownlint-cli2.yaml | grep -qF 'docs/adoption-guide.md'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-adoption-guide.sh` exits 0 and its last line is `check-adoption-guide: PASSED 15 / FAILED 0`
    - `markdownlint-cli2 docs/adoption-guide.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff HEAD~1 --numstat -- docs/adoption-guide.md` shows 0 deleted lines (purely additive edit)
    - `git diff HEAD~1 --name-only -- docs/ scripts/ .markdownlint.jsonc .markdownlint-cli2.yaml` lists exactly one file, `docs/adoption-guide.md` (proves the gate script and lint configs were not touched by the same commit)
    - `test "$(grep -c -F 'markdownlint-disable' docs/adoption-guide.md)" -eq 0` passes (no inline suppression added)
  </acceptance_criteria>
  <done>Both verification commands were executed and their actual output recorded: gate at PASSED 15 / FAILED 0 exit 0, markdownlint at 0 errors, diff purely additive in one file, gate script and lint configs unmodified.</done>
</task>

</tasks>

<threat_model>
N/A as a code threat model — this plan writes prose into one markdown file and introduces no code path,
input handler, credential, or runtime behaviour. Two documentation-integrity risks from RESEARCH's security
domain are nonetheless in force and are mitigated by this plan's own constraints:

- **False assurance by omission (Repudiation / Information disclosure of findings).** A silently truncated
  SARIF upload yields green checks while thousands of findings are never displayed. Mitigated by requiring
  the subsection to lead with the 5,000-result display truncation and to state the 25,000 rejection
  ceiling separately, with its different consequence.
- **Tampering with the verification gate.** Editing `scripts/check-adoption-guide.sh` (or relaxing a
  markdownlint config, or adding an inline disable) to make a failing document pass is the exact vector the
  gate's header names as T-20-12. Prohibited by Task 1 and asserted against in Task 2's acceptance
  criteria.
- **Spoofing of authority.** Publishing an invented platform limit a consumer then designs around.
  Mitigated by transcribing every number from `github/docs` source and by the explicit prohibition on
  stating a per-hour upload limit, which is not published.
</threat_model>

<verification>
```bash
markdownlint-cli2 docs/adoption-guide.md        # expect: Summary: 0 error(s)
bash scripts/check-adoption-guide.sh            # expect: PASSED 15 / FAILED 0, exit 0
git diff --numstat docs/adoption-guide.md       # expect: 0 in the deletions column
git diff --name-only -- docs/ scripts/          # expect: only docs/adoption-guide.md
```
</verification>

<success_criteria>
- A `### SARIF Upload Limits at Consumer Scale` subsection exists inside `## 6.`, with the `## 7.` heading
  and all 13 numbered sections unchanged
- The subsection states: top 5,000 results per run displayed (truncation, accepted); 25,000 results per
  run, 25,000 rules per run, 20 runs per file, 10 MB gzipped per file (rejection); 1,000,000 alerts per
  repository with no self-service recovery
- The number 56 does not appear; §6's existing 58 figure is referenced instead
- Both GitHub docs URLs are present as markdown links; the 301 path is absent; no bare `docs.github.com` URL
- `bash scripts/check-adoption-guide.sh` → PASSED 15 / FAILED 0, exit 0
- `markdownlint-cli2 docs/adoption-guide.md` → Summary: 0 error(s)
- The edit is purely additive and touches exactly one file
</success_criteria>

<output>
Create `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-03-SUMMARY.md` when done.
Record verbatim: the pre-edit and post-edit gate lines, the markdownlint summary lines, the diffstat, and
the exact heading text used (plan 21-04 needs the heading text as closure evidence for deferred item #6).
</output>
