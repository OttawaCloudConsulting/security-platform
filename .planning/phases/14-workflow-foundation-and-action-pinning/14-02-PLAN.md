---
phase: 14-workflow-foundation-and-action-pinning
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: []
autonomous: true
requirements: [CICD-05]

must_haves:
  truths:
    - "Pushing the product-repo branch and opening a pull request produces a workflow run named 'PR Security' triggered by the pull_request event"
    - "The run completes with conclusion success"
    - "The checkout step actually executed inside the called workflow's placeholder job — the run is not a hollow pass"
    - "The pull request is mergeable and no required status check blocks it"
    - "The exact check-run name is captured verbatim for Phase 18 branch protection"
  artifacts:
    - path: ".planning/phases/14-workflow-foundation-and-action-pinning/14-02-SUMMARY.md"
      provides: "Recorded PR number, run ID, verbatim check-run name (assumption A3), and the ruleset evidence that nothing blocks the merge"
      contains: "check-run name"
  key_links:
    - from: "pull_request event on the PR head branch"
      to: "workflow run named 'PR Security'"
      via: "on: pull_request in pr-security.yml, read from the PR merge commit"
      pattern: "workflowName == \"PR Security\" && event == \"pull_request\""
    - from: "caller run 'PR Security'"
      to: "called job 'Placeholder' in security.yml"
      via: "relative reusable-workflow invocation"
      pattern: "checkout step present in gh run view --log"
---

<objective>
Push the Phase 14 branch in the product repository, open a pull request, and observe the resulting workflow run end to end — producing live evidence for ROADMAP criterion #1 (a PR triggers a security workflow run that completes without blocking the merge) and confirming the caller→called wiring of criterion #2 actually executes.

Purpose: Criteria #1 and #2 are claims about GitHub-side behaviour. Static linting cannot prove them. This plan converts them from "should work" to "observed working", and captures the exact check-run name that Phase 18 needs verbatim to configure branch protection.

Output: An open pull request on `OttawaCloudConsulting/security-platform`, an observed-green workflow run, and `14-02-SUMMARY.md` recording the PR number, run ID, verbatim check-run name, and merge-blocking evidence. No merge — Plan 03 owns that.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-01-SUMMARY.md

<notes>
**Target repository.** All git and `gh` operations in this plan act on the PRODUCT repository,
`OttawaCloudConsulting/security-platform`, checked out at `repos/security-platform/`. Resolve the slug
once and reuse it:
`SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##')`
Pass it as `gh pr … -R "$SLUG"`, `gh run … -R "$SLUG"`, `gh api "repos/$SLUG/…"`. Note `gh repo view`
takes the slug POSITIONALLY, not via `-R`. The `cd` working directory resets between calls — always
use `git -C repos/security-platform`, never a bare `git` that would hit the outer repo.

**NEVER run `git push` from the outer documentation repo.** Its `origin` points at the same URL but
its history is disjoint (different root commit, no merge base, 147 local commits). A push from there
would attempt to dump unrelated history onto `security-platform`.

**Why the workflow runs on the very PR that adds it.** The `pull_request` event reads workflow files
from the PR's *merge commit* (`GITHUB_SHA` = base ⊕ head, `GITHUB_REF` = `refs/pull/<N>/merge`), so a
workflow introduced by a PR runs on that same PR. This is why criteria #1-#3 are observable before any
merge — and why criterion #4 (Dependabot) is not: Dependabot reads config only from the default branch.

**Look for the run under the CALLER's name, not the callable file's.** A run initiated by the caller
appears in the Actions tab as `PR Security` (from `pr-security.yml`), with the called workflow's jobs
nested inside. Searching for `Security Scans` will find nothing and would falsely fail criterion #1.

**Do not gate on the `actions/workflows` API listing BOTH files.** RESEARCH suggests asserting both
workflows are registered server-side. Pre-merge this will likely show only `pr-security.yml`: the
endpoint lists default-branch workflows plus those with runs, and a callable workflow has no runs of
its own. Assert only `pr-security.yml` here. The both-registered assertion belongs in Plan 03, post-merge.

**Other activity already exists on this repo.** VERIFIED at planning time: `gh run list` shows
"Copilot code review" runs (a GitHub App, not a workflow file), and PR #3 "feat: phases 11-12 …" is
already open from the `feature/phase-12-repo-setup-script` branch. Filter runs by
`workflowName == "PR Security"` and match the PR by its head branch — do not assume "the most recent
run" or "the only open PR" is yours.

**Assumption A3 is being tested here.** RESEARCH assumes the check surfaces as
`<caller-job-name> / <called-job-name>`. That is unverified. Capture whatever string GitHub actually
emits, verbatim, and do not normalise or reformat it — Phase 18 will reference it exactly.

**Failure handling.** If the run fails, or no run appears at all, STOP → REPORT → WAIT per
`.claude/rules/defensive-protocol-v2-anti-slop.md`. Do not re-push hopefully. "No run appeared at all"
specifically means GitHub could not parse a workflow file — check the repo's Actions tab for a red
"workflow file issue" banner and report the raw message.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the product-repo branch, open the pull request, confirm the caller workflow registered</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-01-SUMMARY.md
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Code Examples, §Pitfall 7)
    - repos/security-platform/.github/workflows/pr-security.yml
    - repos/security-platform/.pre-commit-config.yaml (the gitleaks pre-push hook that will fire)
    - .claude/rules/defensive-protocol-v2-anti-slop.md (Failure Response)
  </read_first>
  <files></files>
  <action>
1. Confirm Plan 01's work is committed and the product-repo tree is clean. Use commit-granularity-independent
   checks, not `git log -1` (the executor may have committed per task):
   - `git -C repos/security-platform ls-files .github | wc -l` returns 3
   - `git -C repos/security-platform status --porcelain` is empty
   - `git -C repos/security-platform diff --quiet` and `--cached --quiet` both pass
   If any fails, STOP and REPORT — do not re-commit.

2. Push the product repo's current branch with upstream tracking:
   `git -C repos/security-platform push -u origin HEAD`.
   That repo's gitleaks pre-push hook will run. If it blocks the push, STOP and REPORT the raw finding —
   do NOT use `--no-verify`. Nothing in this phase's three files contains a credential, so a block here
   is genuine signal about something else on the branch.
   Never push from the outer documentation repo.

3. Open the pull request against `main`:
   `gh pr create -R "$SLUG" --base main --head "$(git -C repos/security-platform branch --show-current)" --fill`
   (or supply an explicit `--title`/`--body`; title should identify the phase, e.g.
   "feat(14): workflow foundation and action pinning"). Capture the PR number from the output and record it.
   Do not touch the pre-existing PR #3.

4. Confirm the caller workflow registered server-side:
   `gh api "repos/$SLUG/actions/workflows" --jq '.workflows[] | "\(.name)\t\(.path)\t\(.state)"'`
   Assert `.github/workflows/pr-security.yml` appears with state `active`. Do NOT require `security.yml`
   to appear — a callable workflow with no runs of its own and not yet on the default branch is
   legitimately absent from this listing pre-merge. Record whichever entries do appear.

5. If the Actions tab shows a "workflow file issue" banner, or step 4 returns neither new workflow,
   STOP and REPORT with the raw API output — that means a parse failure, not a timing issue.
  </action>
  <verify>
    <automated>R=repos/security-platform && SLUG=$(git -C $R remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##') && BR=$(git -C $R branch --show-current) && test "$(git -C $R ls-files .github | wc -l | tr -d ' ')" = "3" && test -z "$(git -C $R status --porcelain)" && git -C $R rev-parse --abbrev-ref --symbolic-full-name @{u} >/dev/null && N=$(gh pr list -R "$SLUG" --head "$BR" --state open --json number --jq '.[0].number') && test -n "$N" && gh pr view "$N" -R "$SLUG" --json state,baseRefName --jq '.state+" "+.baseRefName' | grep -qx 'OPEN main' && gh api "repos/$SLUG/actions/workflows" --jq '.workflows[].path' | grep -qx '.github/workflows/pr-security.yml'</automated>
  </verify>
  <acceptance_criteria>
    - The product repo's current branch has an upstream tracking ref on `origin`
    - `git push` was run with `git -C repos/security-platform` and without `--no-verify`
    - No git or `gh` command in this task acted on the outer documentation repo
    - A pull request exists whose head is the product repo's current branch, with state `OPEN` and base `main`
    - The PR number is recorded in the SUMMARY for use by Plan 03, and is NOT the pre-existing PR #3
    - `gh api repos/<slug>/actions/workflows --jq '.workflows[].path'` includes `.github/workflows/pr-security.yml` with state `active`
    - No "workflow file issue" banner / parse error is reported
    - `gh pr diff <N> -R "$SLUG" --name-only` lists exactly `.github/dependabot.yml`, `.github/workflows/pr-security.yml`, `.github/workflows/security.yml` — no other paths
  </acceptance_criteria>
  <done>Product-repo branch pushed, PR open against `main` with a three-file diff, caller workflow registered and active server-side, PR number recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the PR-triggered run to completion and capture the check-run name and merge-blocking evidence</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Code Examples, §Pitfall 7, §Assumptions Log A3, §Open Questions 4)
    - repos/security-platform/.github/workflows/security.yml
    - repos/security-platform/.github/workflows/pr-security.yml
  </read_first>
  <files></files>
  <action>
1. Locate the run. PRIMARY method — filter by branch and workflow *name*, because `--workflow=<file>`
   may not resolve until GitHub considers the workflow "known", and this repo already has unrelated
   "Copilot code review" runs:
   `gh run list -R "$SLUG" --branch "$(git -C repos/security-platform branch --show-current)" --json databaseId,workflowName,event,status,conclusion`
   Select the entry where `workflowName == "PR Security"` AND `event == "pull_request"`. Capture its
   `databaseId`. SECONDARY method if that returns nothing: `gh run list -R "$SLUG" --workflow=pr-security.yml --limit 5`.
   If neither finds a run after a reasonable wait, STOP and REPORT — do not assume it is still queueing.

2. Watch it to completion and let the exit status carry the result:
   `gh run watch "$RID" -R "$SLUG" --exit-status`. A non-zero exit means the run failed — STOP and
   REPORT the raw failure, do not re-run.

3. Prove the run is not a hollow pass. `gh run view "$RID" -R "$SLUG" --log` must show the checkout step
   actually executing inside the called workflow's `Placeholder` job. Record the job name(s) that appear
   nested under the caller — this is direct evidence the relative `uses:` resolved and criterion #2's
   wiring is live, not merely well-formed YAML.

4. Capture the check-run name VERBATIM (this tests assumption A3 and is required by Phase 18):
   `gh pr view <N> -R "$SLUG" --json statusCheckRollup --jq '.statusCheckRollup[].name'`
   Write the exact string(s) into the SUMMARY without reformatting, and note whether the shape matched
   RESEARCH's predicted `<caller-job-name> / <called-job-name>` or differed. Ignore any "Copilot code
   review" entry — record it as present but not ours.

5. Prove the run does not block the merge (criterion #1, second half). Two checks, both required:
   - `gh pr view <N> -R "$SLUG" --json mergeable,mergeStateStatus,statusCheckRollup` — expect
     `mergeable: MERGEABLE`. GitHub computes mergeability asynchronously; if it returns `UNKNOWN`,
     wait briefly and re-query once before treating it as a result.
   - Ruleset check, which is the AUTHORITATIVE one on this repo:
     `gh api "repos/$SLUG/rules/branches/main" --jq '[.[].type] | join(",")'`
     Assert no `required_status_checks` entry appears. VERIFIED at planning time: this returns exactly
     `deletion,non_fast_forward`.
   - Do NOT rely on `branches/main/protection` returning 404. That endpoint covers *classic* branch
     protection only; a 404 is a FALSE NEGATIVE when rulesets are in use, which is exactly this repo's case.

6. Record all captured values in the SUMMARY: PR number, run databaseId, verbatim check-run name(s),
   nested job names, `mergeable` value, and the ruleset rule types observed.
  </action>
  <verify>
    <automated>R=repos/security-platform && SLUG=$(git -C $R remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##') && BR=$(git -C $R branch --show-current) && N=$(gh pr list -R "$SLUG" --head "$BR" --state open --json number --jq '.[0].number') && RID=$(gh run list -R "$SLUG" --branch "$BR" --json databaseId,workflowName,event --jq '[.[] | select(.workflowName=="PR Security" and .event=="pull_request")][0].databaseId') && test -n "$RID" && gh run view "$RID" -R "$SLUG" --json status,conclusion --jq '.status+"/"+.conclusion' | grep -qx 'completed/success' && gh pr view "$N" -R "$SLUG" --json mergeable --jq '.mergeable' | grep -qx MERGEABLE && ! gh api "repos/$SLUG/rules/branches/main" --jq '.[].type' | grep -qx required_status_checks && test -n "$(gh pr view "$N" -R "$SLUG" --json statusCheckRollup --jq '.statusCheckRollup[].name')"</automated>
  </verify>
  <acceptance_criteria>
    - A run exists on the PR head branch with `workflowName == "PR Security"` and `event == "pull_request"`
    - That run has `status: completed` and `conclusion: success`
    - `gh run view <id> --log` shows the checkout step executing inside the called workflow's placeholder job (the run is not empty)
    - `gh pr view <N> --json statusCheckRollup --jq '.statusCheckRollup[].name'` returns at least one non-empty name, and every returned string is recorded verbatim in `14-02-SUMMARY.md`
    - `gh pr view <N> --json mergeable` returns `MERGEABLE` (a first `UNKNOWN` was re-queried, not accepted)
    - `gh api repos/<slug>/rules/branches/main` returns no `required_status_checks` type
    - The 404-on-`branches/main/protection` result was NOT used as the merge-blocking evidence
    - `14-02-SUMMARY.md` records the PR number and run databaseId so Plan 03 can reference them
    - The PR was NOT merged by this task, and PR #3 was not touched
  </acceptance_criteria>
  <done>The PR-triggered run is observed green with the checkout step actually executed, the check-run name is captured verbatim for Phase 18, and ruleset evidence confirms nothing blocks the merge. ROADMAP criteria #1 and #2 are witnessed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local product-repo checkout → remote `origin` | Committed content leaves the workstation; the gitleaks pre-push hook is the last local control |
| outer documentation repo → same remote URL | Disjoint history sharing one remote; a misdirected push is destructive |
| PR event payload → workflow run | Contributor-controlled event data enters the run context |
| GitHub Actions runner → repository | The run holds a `GITHUB_TOKEN` scoped by the caller's `permissions` block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Tampering | Pushing the outer repo's unrelated 147-commit history onto `security-platform` | mitigate | Every push is scoped `git -C repos/security-platform`; `<notes>` and the acceptance criteria forbid any git command against the outer repo. |
| T-14-08 | Information Disclosure | `git push` of the Phase 14 branch to a PUBLIC repo | mitigate | Push runs the product repo's gitleaks pre-push hook; `--no-verify` is explicitly forbidden, so a block must be reported, not bypassed. |
| T-14-19 | Tampering | Acting on the wrong PR or run (PR #3 and "Copilot code review" already exist) | mitigate | The PR is matched by head branch and the run by `workflowName` + `event`; "most recent" is never used as a selector. |
| T-14-03 | Elevation of Privilege | `GITHUB_TOKEN` scope on the live run | mitigate | Verified live: the run executes under the `permissions: contents: read` block set in both workflow files. |
| T-14-10 | Spoofing | Misreading which workflow produced the run | mitigate | Runs are matched on `workflowName == "PR Security"` AND `event == "pull_request"`, not on a filename guess (Pitfall 7). |
| T-14-11 | Denial of Service | A failing run being re-pushed repeatedly to "see if it passes" | mitigate | STOP → REPORT → WAIT is mandated in `<notes>` and Task 2 step 2; `gh run watch --exit-status` makes failure non-silent. |
| T-14-06 | Information Disclosure | Fork-PR secret exposure | accept | Plain `pull_request` trigger only (D-04): read-only token, no secret access on fork PRs. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No registry packages are installed by this plan — it performs only `git` and `gh` operations. No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- A PR whose head is the product repo's Phase 14 branch is `OPEN` against base `main`
- `gh pr diff <N> --name-only` lists exactly the three `.github/` files
- A run with `workflowName == "PR Security"`, `event == "pull_request"` reached `completed`/`success`
- `gh run view <id> --log` contains the checkout step from the called `Placeholder` job
- `gh pr view <N> --json statusCheckRollup --jq '.statusCheckRollup[].name'` is non-empty and recorded verbatim
- `gh pr view <N> --json mergeable` returns `MERGEABLE`
- `gh api repos/<slug>/rules/branches/main` contains no `required_status_checks` rule
- `.github/workflows/pr-security.yml` is listed `active` by the workflows API
</verification>

<success_criteria>
ROADMAP criterion #1 is witnessed: opening the pull request triggered a visible security workflow run that completed successfully and does not block the merge. Criterion #2's wiring is witnessed live: the caller's relative `uses:` resolved and the called workflow's placeholder job executed checkout. The verbatim check-run name is recorded for Phase 18. The PR remains open and unmerged.
</success_criteria>

<output>
Create `.planning/phases/14-workflow-foundation-and-action-pinning/14-02-SUMMARY.md` when done
(in the OUTER documentation repo). It MUST record, verbatim and unreformatted: the PR number, the run
`databaseId`, the check-run name(s) from `statusCheckRollup` (with a note on whether assumption A3's
predicted `<caller-job> / <called-job>` shape held), the nested job names observed in the run, the
`mergeable` value, and the rule `type`s returned by the `main` ruleset endpoint.
</output>
