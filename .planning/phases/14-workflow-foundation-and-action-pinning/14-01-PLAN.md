---
phase: 14-workflow-foundation-and-action-pinning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .yamllint
  - .github/workflows/security.yml
  - .github/workflows/pr-security.yml
  - .github/dependabot.yml
autonomous: true
requirements: [CICD-05]

must_haves:
  truths:
    - "Both workflow files parse under actionlint with zero errors"
    - "Every third-party `uses:` reference in .github/workflows/ is a full 40-character commit SHA with a same-line version comment"
    - "The callable workflow declares `on: workflow_call` and has no other trigger"
    - "The caller workflow triggers on `pull_request` only and invokes the callable workflow by relative path"
    - ".github/dependabot.yml declares exactly one github-actions ecosystem entry on a weekly schedule"
    - "Both workflow files set a top-level `permissions: contents: read` block"
  artifacts:
    - path: ".github/workflows/security.yml"
      provides: "Callable security-scan workflow (on: workflow_call) with the checkout-only placeholder job"
      contains: "workflow_call"
    - path: ".github/workflows/pr-security.yml"
      provides: "Thin pull_request caller that invokes security.yml locally"
      contains: "uses: ./.github/workflows/security.yml"
    - path: ".github/dependabot.yml"
      provides: "Dependabot version updates for the github-actions ecosystem (CICD-05)"
      contains: "github-actions"
    - path: ".yamllint"
      provides: "yamllint overrides so Actions YAML lints clean (truthy on:, document-start, line-length)"
      contains: "check-keys: false"
  key_links:
    - from: ".github/workflows/pr-security.yml"
      to: ".github/workflows/security.yml"
      via: "relative reusable-workflow reference (no @ref)"
      pattern: "uses: \\./\\.github/workflows/security\\.yml"
    - from: ".github/workflows/security.yml"
      to: "actions/checkout v7.0.0"
      via: "SHA-pinned uses: with same-line version comment"
      pattern: "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0\\s+# v7\\.0\\.0"
    - from: ".github/dependabot.yml"
      to: ".github/workflows/*.yml"
      via: "github-actions ecosystem scanning at directory \"/\""
      pattern: "package-ecosystem: \"github-actions\""
---

<objective>
Create the repository's `.github/` tree from scratch: a callable security-scan workflow (`on: workflow_call`), a thin `pull_request` caller that invokes it by relative path, and a Dependabot config for the `github-actions` ecosystem — all with `actions/checkout` pinned to the full v7.0.0 commit SHA. Validate everything statically before anything reaches GitHub.

Purpose: This is the wiring foundation for the entire v2.0 CI/CD milestone. Authoring `security.yml` as `workflow_call` from day one means Phase 20's cross-repo publish (DIST-07) is a publish step, not a restructure. The SHA pin plus `.github/dependabot.yml` is CICD-05 itself.

Output: `.yamllint`, `.github/workflows/security.yml`, `.github/workflows/pr-security.yml`, `.github/dependabot.yml` — committed on the current feature branch, statically validated, not yet pushed.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md

<interfaces>
<!-- Literal YAML shapes for all three files are in 14-RESEARCH.md: -->
<!--   §Architecture Patterns > Pattern 1 — callable workflow (security.yml)      -->
<!--   §Architecture Patterns > Pattern 2 — thin caller (pr-security.yml)         -->
<!--   §Architecture Patterns > Pattern 3 — dependabot.yml                        -->
<!-- Use those as the structural source of truth. Two deliberate deltas from      -->
<!-- RESEARCH, both binding:                                                      -->
<!--   1. The version comment is `# v7.0.0`, NOT `# v7` (see <notes> below).       -->
<!--   2. `.yamllint` must also raise line-length (see <notes> below).            -->

<!-- SHA-pin comment style precedent, from the untracked reference implementation -->
<!-- repos/security-platform/cicd/.github/workflows/security.yml:                 -->
<!--   uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4       -->
<!--   uses: aquasecurity/trivy-action@57a97c7e...  # v0.35.0                     -->
<!-- Two spaces before the `#`. Comment on the SAME line as `uses:` — Dependabot  -->
<!-- only rewrites the version comment when it is on the same line.               -->
</interfaces>

<notes>
**Manual precondition (NOT this plan's job to fix).** The repo is currently checked out on
`feature/phase-12-repo-setup-script`, a stale v1.1 branch. Phase 14's work must sit on a
fresh branch cut from `origin/main`. Branch creation/switching is the user's git workflow —
Task 1 only *checks* the precondition and stops if unmet. Never run `git checkout`/`git switch`.

**Dirty working tree.** `git status` currently shows dozens of modified/deleted files under
`.claude/`. Never use `git add .` or `git add -A` in this plan. Stage explicit paths only.

**Version comment granularity: `# v7.0.0` is binding.** D-02 locks "reuse the existing
convention from the reference workflow: full SHA + trailing human-readable version comment".
That reference file already carries both `# v4` and `# v0.35.0`, so the convention is
"human-readable version tag", not major-only. `# v7.0.0` is required here because the whole
criterion-#4 strategy turns on a v7.0.0 → v7.0.1 bump being legible by eye; `# v7` cannot
distinguish them. RESEARCH §Pitfall 4 recommends `# v7` — it is superseded by this decision.

**The v7.0.0 pin is deliberate — do NOT re-resolve to latest.** `actions/checkout` v7.0.1
exists. Pinning one patch behind means Dependabot's first post-merge run has a real upgrade
available and opens an observable bump PR, which is the direct evidence for ROADMAP criterion
#4. RESEARCH's "re-resolve if >30 days old" note does NOT apply — verify the v7.0.0 SHA is
still the canonical tag commit, do not replace it with v7.0.1.

**Do not hardcode a repo slug anywhere.** REQUIREMENTS DIST-07 says `OCC-github/security_solution`;
the actual remote is `OttawaCloudConsulting/security-platform`. This phase needs neither — the
caller uses a relative reference with no org/repo in it. In shell commands, resolve the slug at
runtime with `gh repo view --json nameWithOwner -q .nameWithOwner`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify branch preconditions and install the Actions linting toolchain</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Environment Availability, §Validation Architecture > Wave 0 Gaps, §Pitfall 5)
    - .claude/rules/defensive-protocol-v2-anti-slop.md (Failure Response, Script Safety)
    - CLAUDE.md
  </read_first>
  <files>.yamllint</files>
  <action>
Preflight, in this order. If ANY check fails, STOP → REPORT → WAIT per the anti-slop rule. Do not
attempt to fix a failing branch precondition — branch switching is owned by the user's git workflow.

1. `git fetch origin main`
2. Assert the current HEAD descends from `origin/main`: `git merge-base --is-ancestor origin/main HEAD` must exit 0.
3. Assert `git branch --show-current` is neither `main` nor `feature/phase-12-repo-setup-script`.
4. Assert this plan file is present on the current branch: `test -f .planning/phases/14-workflow-foundation-and-action-pinning/14-01-PLAN.md`. This matters because the Phase 14 planning docs were committed on the stale branch — if they are absent, the branch was cut from `main` before those commits landed and the phase artifacts need to be brought over first.
5. Assert `.github/` does not already exist: `test ! -e .github`.

Then install and configure the linting toolchain:

6. Install actionlint: `brew install actionlint`. Confirm with `actionlint --version`. If Homebrew is unavailable or the install fails, STOP and REPORT — the fallback (push-and-observe GitHub's own parser) is slower and must be a user decision, not a silent substitution.

7. Create `.yamllint` at the repository root with `extends: default` and a `rules:` mapping containing exactly three overrides:
   - `truthy:` with `check-keys: false` — YAML 1.1 reads the bare `on:` key found in every Actions workflow as a boolean; without this, every workflow file fails.
   - `document-start: disable` — Actions workflows do not begin with `---`.
   - `line-length:` with `max: 120` — VERIFIED EMPIRICALLY during planning: the pinned checkout line is 81 characters and default yamllint errors at 80. RESEARCH §Pitfall 5 omits this override and is therefore incomplete; without it the lint gate fails on a correctly-written file.

   Do not add any other rule overrides. Do not set the executable bit on anything.

Do not commit yet — Task 3 makes the single staged commit for this plan.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor origin/main HEAD && test "$(git branch --show-current)" != "main" && test "$(git branch --show-current)" != "feature/phase-12-repo-setup-script" && actionlint --version && grep -q 'check-keys: false' .yamllint && grep -q 'document-start: disable' .yamllint && grep -q 'max: 120' .yamllint</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor origin/main HEAD` exits 0
    - `git branch --show-current` returns a branch that is neither `main` nor `feature/phase-12-repo-setup-script`
    - `.planning/phases/14-workflow-foundation-and-action-pinning/14-01-PLAN.md` exists on the current branch
    - `actionlint --version` exits 0 and prints a version string
    - `.yamllint` exists at the repository root and contains `extends: default`
    - `.yamllint` contains `check-keys: false`, `document-start: disable`, and `max: 120`
    - `.yamllint` has mode 644 (no executable bit): `test ! -x .yamllint`
    - No `git checkout` or `git switch` command was run during this task
  </acceptance_criteria>
  <done>Branch preconditions verified without altering git state, actionlint installed and on PATH, `.yamllint` present with the three required overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Author the callable workflow and the thin pull_request caller</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Architecture Patterns Pattern 1 and Pattern 2, §Pitfall 6, §Pitfall 8, §Anti-Patterns to Avoid)
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md (D-01, D-02, D-04)
    - repos/security-platform/cicd/.github/workflows/security.yml (SHA-pin comment style ONLY — do not copy its structure; it is Renovate-managed and push/pull_request-triggered)
    - .yamllint (created in Task 1)
  </read_first>
  <files>.github/workflows/security.yml, .github/workflows/pr-security.yml</files>
  <action>
Create `.github/workflows/` and both files. Follow RESEARCH Pattern 1 and Pattern 2 literally, with
the `# v7.0.0` comment delta noted in `<context><notes>`.

**`.github/workflows/security.yml` — the CALLABLE workflow (the reusable artifact):**
- `name: Security Scans`
- Trigger: `on:` with a single key `workflow_call: {}` — the map form, not the scalar form. Phase 18 adds an `inputs:` block directly under it, so starting in map form avoids a structural edit later. Add NO other trigger. Putting `pull_request` here would make the file non-reusable and would double-trigger once the caller exists.
- Top-level `permissions:` with `contents: read` (Pitfall 8 — Phase 15's Checkov job will scan these very files).
- One job, id `placeholder`, with `name: Placeholder`, `runs-on: ubuntu-latest`, and a single step: `- uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0` — exactly that 40-character SHA, two spaces before the `#`, comment on the same line. Per D-01 this job runs checkout and nothing else: no `echo` no-op, no `run:` steps, and no pre-named sast/iac/sca/container/secrets stub jobs.

**`.github/workflows/pr-security.yml` — the CALLER (repo-local trigger policy):**
- `name: PR Security`
- Trigger: `on:` with `pull_request: {}` only. No `branches:` filter (D-04 — all branches), no `push` trigger (explicitly deferred).
- Top-level `permissions:` with `contents: read`.
- One job, id `security`, with `name: security` and `uses: ./.github/workflows/security.yml`.
  - The relative form takes NO `@ref` — it is only valid without one and resolves to the caller's own commit. This is why a PR exercises its own changes to `security.yml`, and why criterion #3 must not flag it as unpinned.
  - The allowed-key list for a `uses:` job is CLOSED. `runs-on` and `steps` are illegal here and are a parse error; GitHub silently drops workflows it cannot parse, so the symptom is "no run appears at all". `runs-on` belongs only in the callable file.
  - Do not add a `secrets:` block — the called workflow is automatically granted `secrets.GITHUB_TOKEN`.
  - Do not add `continue-on-error:` anywhere. ADR-001 (Accepted) rejects it, and non-blocking behaviour in this phase comes for free from the absence of required status checks.

Run `actionlint .github/workflows/*.yml` after writing and fix any reported error before moving on.
Do not commit yet.
  </action>
  <verify>
    <automated>actionlint .github/workflows/security.yml .github/workflows/pr-security.yml && grep -q 'workflow_call' .github/workflows/security.yml && grep -q 'uses: \./\.github/workflows/security\.yml' .github/workflows/pr-security.yml && grep -qE 'uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0[[:space:]]+# v7\.0\.0' .github/workflows/security.yml && grep -q '^permissions:' .github/workflows/security.yml && grep -q '^permissions:' .github/workflows/pr-security.yml && ! grep -q 'pull_request' .github/workflows/security.yml && ! grep -qE '^\s+(runs-on|steps):' .github/workflows/pr-security.yml && ! grep -rq 'continue-on-error' .github/workflows/</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/*.yml` exits 0 with no output
    - `.github/workflows/security.yml` contains `workflow_call` and does NOT contain the string `pull_request`
    - `.github/workflows/security.yml` contains a line matching `uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0` (SHA is exactly 40 hex chars, comment on the same line)
    - `.github/workflows/security.yml` contains `runs-on: ubuntu-latest`
    - `.github/workflows/pr-security.yml` contains `on:` / `pull_request` and does NOT contain a `branches:` filter or a `push:` trigger
    - `.github/workflows/pr-security.yml` contains `uses: ./.github/workflows/security.yml` with no `@` after `.yml`
    - `.github/workflows/pr-security.yml` contains neither `runs-on:` nor `steps:`
    - Both files contain a top-level `permissions:` block whose value includes `contents: read`
    - Neither file contains `continue-on-error`
    - No `uses:` line in `.github/workflows/` references a moving tag (no `@v7`, `@main`, or abbreviated SHA)
  </acceptance_criteria>
  <done>Both workflow files exist, parse cleanly under actionlint, and encode D-01 (checkout-only placeholder), D-02 (SHA + same-line version comment), and D-04 (pull_request-only caller).</done>
</task>

<task type="auto">
  <name>Task 3: Author the Dependabot config, run the full static gate, and commit explicit paths</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Architecture Patterns Pattern 3, §Pitfall 1, §Pitfall 3, §Validation Architecture > Phase Requirements → Test Map)
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md (D-03)
    - .github/workflows/security.yml (created in Task 2)
    - .github/workflows/pr-security.yml (created in Task 2)
  </read_first>
  <files>.github/dependabot.yml</files>
  <action>
1. Create `.github/dependabot.yml` per RESEARCH Pattern 3: top-level `version: 2`, then `updates:` as a
   list with exactly ONE entry containing the three required keys — `package-ecosystem: "github-actions"`,
   `directory: "/"`, and `schedule:` with `interval: "weekly"`.
   - `directory: "/"` is correct and is NOT `/.github/workflows`. Dependabot searches `/.github/workflows`
     plus the root `action.yml`/`action.yaml` when given `/`.
   - Per D-03, add no other ecosystem entries. An npm/pip/terraform entry pointing at a directory with no
     manifests produces noisy failing Dependabot jobs.
   - Add no optional keys (`groups`, `labels`, `open-pull-requests-limit`, `target-branch`, `commit-message`).
   - Record in the SUMMARY, so it is not later rediscovered as a defect: Dependabot ignores locally
     referenced reusable workflows by design, so `uses: ./.github/workflows/security.yml` will never appear
     in a Dependabot PR. `actions/checkout` is the only Dependabot-tracked dependency in this phase.

2. Run the full static gate. All five must pass:
   - `actionlint .github/workflows/*.yml`
   - `yamllint .github/` (picks up the root `.yamllint` from Task 1)
   - SHA-pin gate: list every `uses:` line under `.github/workflows/`, drop the relative reusable-workflow
     reference, and assert nothing remains that is not `@<40 hex>` followed by a same-line `# v` comment.
     Filter comment lines out before counting so the gate cannot be satisfied or invalidated by prose.
   - Canonical-tag verification, handling annotated tags correctly: run
     `gh api repos/actions/checkout/git/ref/tags/v7.0.0 --jq '"\(.object.type) \(.object.sha)"'`.
     Expect `commit 9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`. If the type comes back as `tag` (annotated),
     dereference with `gh api repos/actions/checkout/git/tags/<sha> --jq '.object.sha'` and compare that
     value instead. RESEARCH's `refs/tags | select(.object.sha==...)` form only works for lightweight tags —
     do not rely on it. This proves the SHA came from the canonical `actions` org repo and not a fork.
     Do NOT re-resolve to `v7`/latest; the v7.0.0 pin is deliberate.
   - `git check-ignore .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml`
     must find NO matches (exit 1) — confirming nothing in `.gitignore` swallows the new tree.

3. Commit with EXPLICIT paths only — the working tree is dirty with unrelated `.claude/` changes and
   `git add .` / `git add -A` would sweep them into the Phase 14 PR:
   `git add .yamllint .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml`
   Before committing, assert `git diff --cached --name-only` lists exactly those four paths and nothing else.
   Commit message: `feat(14): add callable security workflow, PR caller, and Dependabot config`.
   Do not push — Plan 02 owns pushing and PR creation.
  </action>
  <verify>
    <automated>yamllint .github/ && actionlint .github/workflows/*.yml && grep -q 'package-ecosystem: "github-actions"' .github/dependabot.yml && grep -q 'directory: "/"' .github/dependabot.yml && grep -q 'interval: "weekly"' .github/dependabot.yml && test "$(grep -v '^\s*#' .github/dependabot.yml | grep -c 'package-ecosystem:')" = "1" && test -z "$(grep -rhE '^[[:space:]]*(-[[:space:]]*)?uses:' .github/workflows/ | grep -v 'uses: \./' | grep -vE '@[0-9a-f]{40}[[:space:]]+# v')" && test "$(gh api repos/actions/checkout/git/ref/tags/v7.0.0 --jq '.object.sha')" = "9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0" && ! git check-ignore -q .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml && git log -1 --name-only --pretty=format: | sort | tr -d ' ' | grep -c . | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` contains `version: 2`, `package-ecosystem: "github-actions"`, `directory: "/"`, `interval: "weekly"`
    - `.github/dependabot.yml` has exactly ONE non-comment `package-ecosystem:` line (no npm/pip/terraform entries)
    - `yamllint .github/` exits 0 with no output
    - `actionlint .github/workflows/*.yml` exits 0 with no output
    - The SHA-pin gate returns an empty result set: no `uses:` line under `.github/workflows/` other than the relative `./` reference fails the `@<40-hex>` + same-line `# v` pattern
    - `gh api repos/actions/checkout/git/ref/tags/v7.0.0` resolves (after annotated-tag dereference if needed) to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`, and the object type is `commit`
    - The pinned SHA is NOT `3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1) — the one-patch-behind pin is deliberate
    - `git check-ignore` matches none of the three `.github/` files
    - The commit contains exactly 4 files: `.yamllint`, `.github/dependabot.yml`, `.github/workflows/security.yml`, `.github/workflows/pr-security.yml` — verified with `git log -1 --name-only`
    - No file under `.claude/` appears in the commit
    - Nothing was pushed to the remote
  </acceptance_criteria>
  <done>`.github/dependabot.yml` created per D-03, the full static gate passes, the pinned SHA is verified as a canonical tagged commit, and exactly four files are committed locally with no unrelated working-tree noise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream action repo → CI runner | `actions/checkout` code executes with the workflow's `GITHUB_TOKEN` on a GitHub-hosted runner |
| caller workflow → called workflow | `GITHUB_TOKEN` permissions cross a workflow boundary |
| PR event payload → workflow | Untrusted, contributor-controlled event data enters the run context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `actions/checkout` reference in `security.yml` | mitigate | Pin the full 40-character commit SHA `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`; never a moving tag (`@v7`) or an abbreviated SHA. ADR-004. |
| T-14-02 | Tampering | Provenance of the pinned SHA | mitigate | Task 3 resolves the tag via `gh api repos/actions/checkout/git/ref/tags/v7.0.0` against the canonical `actions` org, with annotated-tag dereference — proving the SHA is not from a fork. |
| T-14-03 | Elevation of Privilege | `GITHUB_TOKEN` scope in both workflow files | mitigate | Explicit top-level `permissions: contents: read` in `security.yml` and `pr-security.yml`. |
| T-14-04 | Tampering | Pin goes stale and misses upstream security fixes | mitigate | `.github/dependabot.yml`, `github-actions` ecosystem, weekly interval (CICD-05). |
| T-14-05 | Elevation of Privilege | Called workflow attempting to elevate caller permissions | accept | Platform-enforced: a called workflow can only downgrade, never elevate, the caller's `GITHUB_TOKEN` grant. No repo-side control needed. |
| T-14-06 | Information Disclosure | Fork-PR code execution / secret exposure ("pwn request") | accept | D-04 locks the plain `pull_request` trigger, which yields a read-only token and no secret access on fork PRs. `pull_request_target` is not used. Revisit if any later phase reaches for it. |
| T-14-07 | Injection / Tampering | Script injection via `${{ github.event.* }}` in `run:` | accept | Not reachable this phase — D-01 forbids `run:` steps; the placeholder job is checkout-only. Becomes live in Phase 15. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No registry packages are installed by this phase — no `npm install`, `pip install`, or `cargo add` occurs (RESEARCH §Package Legitimacy Audit). The only tooling is `actionlint` from `homebrew-core` (bottled formula, not a registry artifact), so registry slopsquatting does not apply. No `[ASSUMED]` or `[SUS]` packages exist; no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `actionlint .github/workflows/*.yml` exits 0
- `yamllint .github/` exits 0
- SHA-pin gate over `.github/workflows/` returns zero offending rows
- `gh api repos/actions/checkout/git/ref/tags/v7.0.0` dereferences to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`, type `commit`
- `git log -1 --name-only` lists exactly the four intended files
- `git status --porcelain .github .yamllint` is empty (everything committed)
</verification>

<success_criteria>
The `.github/` tree exists and is locally valid: `security.yml` is callable (`on: workflow_call`), `pr-security.yml` triggers on `pull_request` only and invokes it by relative path, `actions/checkout` is pinned to the canonical v7.0.0 SHA with a same-line `# v7.0.0` comment, both files declare `permissions: contents: read`, and `.github/dependabot.yml` declares a single weekly `github-actions` ecosystem entry. Static structural evidence for ROADMAP criteria #2 and #3 is in place; criteria #1 and #4 require GitHub-side observation (Plans 02 and 03).
</success_criteria>

<output>
Create `.planning/phases/14-workflow-foundation-and-action-pinning/14-01-SUMMARY.md` when done.
Record in it: the exact pinned SHA and comment string, the `.yamllint` overrides applied (including
the empirically-required `line-length: max: 120` that RESEARCH omitted), and the note that Dependabot
by design ignores the local `./.github/workflows/security.yml` reference.
</output>
