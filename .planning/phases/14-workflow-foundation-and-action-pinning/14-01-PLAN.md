---
phase: 14-workflow-foundation-and-action-pinning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repos/security-platform/.github/workflows/security.yml
  - repos/security-platform/.github/workflows/pr-security.yml
  - repos/security-platform/.github/dependabot.yml
autonomous: true
requirements: [CICD-05]

must_haves:
  truths:
    - "Both workflow files parse under actionlint with zero errors"
    - "Every third-party `uses:` reference in the new .github/workflows/ is a full 40-character commit SHA with a same-line version comment"
    - "The callable workflow declares `on: workflow_call` and has no other trigger"
    - "The caller workflow triggers on `pull_request` only and invokes the callable workflow by relative path"
    - "dependabot.yml declares exactly one github-actions ecosystem entry on a weekly schedule"
    - "Both workflow files set a top-level `permissions: contents: read` block"
  artifacts:
    - path: "repos/security-platform/.github/workflows/security.yml"
      provides: "Callable security-scan workflow (on: workflow_call) with the checkout-only placeholder job"
      contains: "workflow_call"
    - path: "repos/security-platform/.github/workflows/pr-security.yml"
      provides: "Thin pull_request caller that invokes security.yml locally"
      contains: "uses: ./.github/workflows/security.yml"
    - path: "repos/security-platform/.github/dependabot.yml"
      provides: "Dependabot version updates for the github-actions ecosystem (CICD-05)"
      contains: "github-actions"
  key_links:
    - from: "repos/security-platform/.github/workflows/pr-security.yml"
      to: "repos/security-platform/.github/workflows/security.yml"
      via: "relative reusable-workflow reference (no @ref)"
      pattern: "uses: \\./\\.github/workflows/security\\.yml"
    - from: "repos/security-platform/.github/workflows/security.yml"
      to: "actions/checkout v7.0.0"
      via: "SHA-pinned uses: with same-line version comment"
      pattern: "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0[[:space:]]+# v7\\.0\\.0"
    - from: "repos/security-platform/.github/dependabot.yml"
      to: "repos/security-platform/.github/workflows/*.yml"
      via: "github-actions ecosystem scanning at directory \"/\""
      pattern: "package-ecosystem: \"github-actions\""
---

<objective>
Create the root `.github/` tree in the product repository `OttawaCloudConsulting/security-platform` (checked out at `repos/security-platform/`): a callable security-scan workflow (`on: workflow_call`), a thin `pull_request` caller that invokes it by relative path, and a Dependabot config for the `github-actions` ecosystem — with `actions/checkout` pinned to the full v7.0.0 commit SHA. Validate everything statically before anything reaches GitHub.

Purpose: This is the wiring foundation for the entire v2.0 CI/CD milestone. Authoring `security.yml` as `workflow_call` from day one means Phase 20's cross-repo publish (DIST-07) is a publish step, not a restructure. The SHA pin plus `dependabot.yml` is CICD-05 itself.

Output: `repos/security-platform/.github/workflows/security.yml`, `.../pr-security.yml`, `.../dependabot.yml` — committed on a fresh feature branch inside the product repo, statically validated, not yet pushed.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md

<interfaces>
<!-- Literal YAML shapes for all three files are in 14-RESEARCH.md:                -->
<!--   §Architecture Patterns > Pattern 1 — callable workflow (security.yml)      -->
<!--   §Architecture Patterns > Pattern 2 — thin caller (pr-security.yml)         -->
<!--   §Architecture Patterns > Pattern 3 — dependabot.yml                        -->
<!-- Use those as the structural source of truth, with the two deltas in <notes>. -->

<!-- SHA-pin comment style precedent, from the product repo's own existing        -->
<!-- reference file cicd/.github/workflows/security.yml (a non-active path —      -->
<!-- GitHub only executes workflows under the ROOT .github/workflows/):           -->
<!--   uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4       -->
<!--   uses: aquasecurity/trivy-action@57a97c7e...  # v0.35.0                     -->
<!-- Two spaces before the `#`. Comment on the SAME line as `uses:` — Dependabot  -->
<!-- only rewrites the version comment when it is on the same line.               -->
</interfaces>

<notes>
**TARGET REPOSITORY — read this before anything else.**
All three deliverable files go into the product repository, checked out at `repos/security-platform/`
(remote `https://github.com/OttawaCloudConsulting/security-platform.git`). They do NOT go into the
outer documentation repo you are running from. This follows the established project convention:
archived plans such as `05-01-PLAN.md` carry `files_modified: repos/security-platform/...`, and
Phases 12/13 shipped `workstation/` into that same repo, while `.planning/` documents stay in the
outer repo. CLAUDE.md is explicit that the outer repo is reference documentation, not buildable
software.

**NEVER run `git push` from the outer repository.** VERIFIED during planning: the outer repo's
`origin` points at the SAME URL as the product repo, but their histories are DISJOINT — different
root commits (`fdfac5e` vs `c5b6100`), no merge base, 147 local commits. A push from the outer repo
would attempt to dump unrelated history onto `security-platform`. Every git operation in this phase
is scoped with `git -C repos/security-platform`. The `cd` working directory resets between calls, so
always use `-C`; never rely on a prior `cd`.

**RESEARCH's environment facts describe the WRONG repo — these notes supersede them.**
`14-RESEARCH.md` §Environment Availability and §Wave 0 Gaps were inventoried against the OUTER
documentation repo (it reports no `.pre-commit-config.yaml` at root, no `.yamllint`, and
"`.github/**.yml` will be the repo's first tracked workflow YAML"). None of that is true of the product
repo, which HAS a root `.pre-commit-config.yaml` with active yamllint/gitleaks hooks and other tracked
YAML. RESEARCH's GitHub-side facts (PUBLIC visibility, default branch `main`, ruleset `Default` id
14243983 enforcing only `deletion`+`non_fast_forward`, Actions permissions, the `actions/checkout`
SHAs) were resolved via `gh` and ARE product-repo facts — those remain valid. Where the two conflict,
these notes win.

**Manual precondition (NOT this plan's job to fix).** VERIFIED during planning: the product repo is
on branch `feature/phase-12-repo-setup-script`, **1 commit ahead and 28 behind `origin/main`** — it
has diverged, and there is an open PR #3 carrying that Phase 11-12 work. Phase 14 needs its OWN fresh
branch cut from `origin/main` inside `repos/security-platform/`. Branch creation and switching is the
user's git workflow — Task 1 only CHECKS the precondition and stops if unmet. Never run
`git checkout` / `git switch` / `git branch`.

**Version comment granularity: `# v7.0.0` is binding.** D-02 locks "reuse the existing convention from
the reference workflow: full SHA + trailing human-readable version comment". That reference file
already carries both `# v4` and `# v0.35.0`, so the convention is "human-readable version tag", not
major-only. `# v7.0.0` is required here because the criterion-#4 strategy turns on a v7.0.0 → v7.0.1
bump being legible by eye; `# v7` cannot distinguish them. RESEARCH §Pitfall 4 recommends `# v7` and
is superseded by this decision.

**The v7.0.0 pin is deliberate — do NOT re-resolve to latest.** `actions/checkout` v7.0.1 exists.
Pinning one patch behind means Dependabot's first post-merge run has a real upgrade available and
opens an observable bump PR, which is the direct evidence for ROADMAP criterion #4. RESEARCH's
"re-resolve if >30 days old" note does NOT apply — verify the v7.0.0 SHA is still the canonical tag
commit, do not replace it with v7.0.1.

**No `.yamllint` file is added.** RESEARCH §Pitfall 5 offers two options (commit a `.yamllint`, or use
an inline `-d` override) based on the OUTER repo, which has neither a yamllint config nor a
`.pre-commit-config.yaml`. The product repo is different and already has a convention: VERIFIED — its
`.pre-commit-config.yaml` runs the yamllint hook with `args: [-d, relaxed]`, which overrides any
`.yamllint` file anyway. Match that convention: use `yamllint -d relaxed` in the gate and add no new
config file. Also VERIFIED empirically: RESEARCH's suggested inline override
(`truthy` + `document-start` only) FAILS on these files — the pinned checkout line is 81 characters
and default yamllint errors at 80. `-d relaxed` downgrades that to a warning and exits 0.

**Do not hardcode a repo slug in any workflow file.** REQUIREMENTS DIST-07 says
`OCC-github/security_solution`; the actual remote is `OttawaCloudConsulting/security-platform`. This
phase needs neither — the caller uses a relative reference with no org/repo in it. In shell commands,
resolve the slug at runtime:
`SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##')`
Note that `gh repo view` takes the slug POSITIONALLY (`gh repo view "$SLUG"`), while `gh pr` / `gh run`
use `-R "$SLUG"`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify product-repo preconditions and install actionlint</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Environment Availability, §Validation Architecture > Wave 0 Gaps, §Pitfall 5)
    - repos/security-platform/.pre-commit-config.yaml (the yamllint and gitleaks hook entries that will run on commit/push)
    - .claude/rules/defensive-protocol-v2-anti-slop.md (Failure Response, Script Safety)
    - CLAUDE.md
  </read_first>
  <files></files>
  <action>
Preflight against the PRODUCT repo. Every command uses `git -C repos/security-platform`. If ANY check
fails, STOP → REPORT → WAIT per the anti-slop rule. Do not attempt to fix a failing branch
precondition — branch switching is owned by the user's git workflow.

1. `git -C repos/security-platform fetch origin`
2. Assert the checkout is clean: `git -C repos/security-platform status --porcelain` produces no output.
   A dirty product repo means unrelated work is in flight; stop rather than commit around it.
3. Assert the current branch is a fresh Phase 14 branch: `git -C repos/security-platform branch --show-current`
   must be neither `main` nor `feature/phase-12-repo-setup-script`.
4. Assert it descends from the remote default branch:
   `git -C repos/security-platform merge-base --is-ancestor origin/main HEAD` must exit 0.
   KNOWN STATE AT PLANNING TIME: this check WILL fail until the user cuts a fresh branch — the repo
   sits on `feature/phase-12-repo-setup-script`, 1 ahead / 28 behind `origin/main`. That is expected;
   report it and wait.
5. Assert the root `.github/` tree does not already exist:
   `git -C repos/security-platform ls-files .github` returns nothing, and `test ! -e repos/security-platform/.github`.
   Note: `cicd/.github/workflows/security.yml` DOES exist in that repo — it is a non-active reference
   path (GitHub only executes workflows under the ROOT `.github/workflows/`) and must be left untouched.

Then install the linting toolchain:

6. Install actionlint: `brew install actionlint`. Confirm with `actionlint --version`. If Homebrew is
   unavailable or the install fails, STOP and REPORT — the fallback (push-and-observe GitHub's own
   parser) is slower and must be a user decision, not a silent substitution.

7. Confirm the yamllint gate this phase will use matches the repo's own convention: read the yamllint
   hook in `repos/security-platform/.pre-commit-config.yaml` and confirm it carries `args: [-d, relaxed]`.
   Record the finding. Do NOT create a `.yamllint` file and do NOT modify `.pre-commit-config.yaml`.
   Do not set the executable bit on anything.
  </action>
  <verify>
    <automated>git -C repos/security-platform fetch origin -q && test -z "$(git -C repos/security-platform status --porcelain)" && B=$(git -C repos/security-platform branch --show-current) && test "$B" != "main" && test "$B" != "feature/phase-12-repo-setup-script" && git -C repos/security-platform merge-base --is-ancestor origin/main HEAD && test -z "$(git -C repos/security-platform ls-files .github)" && actionlint --version && grep -q 'relaxed' repos/security-platform/.pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `git -C repos/security-platform status --porcelain` produces no output (clean checkout)
    - `git -C repos/security-platform branch --show-current` returns a branch that is neither `main` nor `feature/phase-12-repo-setup-script`
    - `git -C repos/security-platform merge-base --is-ancestor origin/main HEAD` exits 0
    - `git -C repos/security-platform ls-files .github` returns nothing, and `repos/security-platform/.github` does not exist on disk
    - `repos/security-platform/cicd/.github/workflows/security.yml` is unmodified
    - `actionlint --version` exits 0 and prints a version string
    - `repos/security-platform/.pre-commit-config.yaml` is confirmed to run yamllint with `-d relaxed`, and is NOT modified by this task
    - No `.yamllint` file was created in either repository
    - No `git checkout`, `git switch`, or `git branch` command was run
    - No git command was run against the OUTER documentation repo
  </acceptance_criteria>
  <done>Product-repo preconditions verified without altering git state, actionlint installed and on PATH, and the repo's existing `yamllint -d relaxed` convention confirmed as the gate to use.</done>
</task>

<task type="auto">
  <name>Task 2: Author the callable workflow and the thin pull_request caller</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Architecture Patterns Pattern 1 and Pattern 2, §Pitfall 6, §Pitfall 8, §Anti-Patterns to Avoid)
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md (D-01, D-02, D-04)
    - repos/security-platform/cicd/.github/workflows/security.yml (SHA-pin comment style ONLY — do not copy its structure; it is Renovate-managed and push/pull_request-triggered, and lives in a non-active path)
  </read_first>
  <files>repos/security-platform/.github/workflows/security.yml, repos/security-platform/.github/workflows/pr-security.yml</files>
  <action>
Create `repos/security-platform/.github/workflows/` and both files. Follow RESEARCH Pattern 1 and
Pattern 2 literally, with the `# v7.0.0` comment delta noted in `<context><notes>`.

**`.github/workflows/security.yml` — the CALLABLE workflow (the reusable artifact):**
- `name: Security Scans`
- Trigger: `on:` with a single key `workflow_call: {}` — the map form, not the scalar form. Phase 18 adds an `inputs:` block directly under it, so starting in map form avoids a structural edit later. Add NO other trigger. Putting `pull_request` here would make the file non-reusable and would double-trigger once the caller exists.
- Top-level `permissions:` with `contents: read` (Pitfall 8 — Phase 15's Checkov job will scan these very files).
- One job, id `placeholder`, with `name: Placeholder`, `runs-on: ubuntu-latest`, and a single step: `- uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0` — exactly that 40-character SHA, two spaces before the `#`, comment on the same line. Per D-01 this job runs checkout and nothing else: no `echo` no-op, no `run:` steps, and no pre-named sast/iac/sca/container/secrets stub jobs.

**`.github/workflows/pr-security.yml` — the CALLER (repo-local trigger policy):**
- `name: PR Security`
- Trigger: `on:` with `pull_request: {}` only. No `branches:` filter (D-04 — all branches), no `push` trigger (explicitly deferred).
- Top-level `permissions:` with `contents: read`.
- One job, id `security`, with `name: security` and `uses: ./.github/workflows/security.yml`.
  - The relative form takes NO `@ref` — it is only valid without one and resolves to the caller's own commit. This is why a PR exercises its own changes to `security.yml`, and why criterion #3 must not flag it as unpinned.
  - The allowed-key list for a `uses:` job is CLOSED. `runs-on` and `steps` are illegal here and are a parse error; GitHub silently drops workflows it cannot parse, so the symptom is "no run appears at all". `runs-on` belongs only in the callable file.
  - Do not add a `secrets:` block — the called workflow is automatically granted `secrets.GITHUB_TOKEN`.
  - Do not add `continue-on-error:` anywhere. ADR-001 (Accepted) rejects it, and non-blocking behaviour in this phase comes for free from the absence of required status checks.

Run `actionlint repos/security-platform/.github/workflows/*.yml` after writing and fix any reported
error before moving on. If the executor's convention is to commit per task, commit here — but stage
ONLY these two explicit paths, never `git add .` or `-A`, and use `git -C repos/security-platform`.
  </action>
  <verify>
    <automated>W=repos/security-platform/.github/workflows && actionlint $W/security.yml $W/pr-security.yml && grep -q 'workflow_call' $W/security.yml && grep -q 'uses: \./\.github/workflows/security\.yml' $W/pr-security.yml && grep -qE 'uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0[[:space:]]+# v7\.0\.0' $W/security.yml && grep -q '^permissions:' $W/security.yml && grep -q '^permissions:' $W/pr-security.yml && grep -q 'runs-on: ubuntu-latest' $W/security.yml && ! grep -q 'pull_request' $W/security.yml && ! grep -qE '^[[:space:]]+(runs-on|steps):' $W/pr-security.yml && ! grep -rq 'continue-on-error' $W/</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint repos/security-platform/.github/workflows/*.yml` exits 0 with no output
    - `security.yml` contains `workflow_call` and does NOT contain the string `pull_request`
    - `security.yml` contains a line matching `uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0` (SHA exactly 40 hex chars, comment on the same line)
    - `security.yml` contains `runs-on: ubuntu-latest`
    - `pr-security.yml` contains a `pull_request` trigger with no `branches:` filter and no `push:` trigger
    - `pr-security.yml` contains `uses: ./.github/workflows/security.yml` with no `@` after `.yml`
    - `pr-security.yml` contains neither `runs-on:` nor `steps:`
    - Both files contain a top-level `permissions:` block whose value includes `contents: read`
    - Neither file contains `continue-on-error`
    - No `uses:` line references a moving tag (no `@v7`, `@main`, or abbreviated SHA)
    - `repos/security-platform/cicd/.github/workflows/security.yml` is unchanged: `git -C repos/security-platform diff --quiet -- cicd/` exits 0
  </acceptance_criteria>
  <done>Both workflow files exist under the product repo's root `.github/workflows/`, parse cleanly under actionlint, and encode D-01 (checkout-only placeholder), D-02 (SHA + same-line version comment), and D-04 (pull_request-only caller).</done>
</task>

<task type="auto">
  <name>Task 3: Author the Dependabot config, run the full static gate, and commit explicit paths</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Architecture Patterns Pattern 3, §Pitfall 1, §Pitfall 3, §Validation Architecture > Phase Requirements → Test Map)
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-CONTEXT.md (D-03)
    - repos/security-platform/.github/workflows/security.yml (created in Task 2)
    - repos/security-platform/.github/workflows/pr-security.yml (created in Task 2)
    - repos/security-platform/.pre-commit-config.yaml (yamllint `-d relaxed` and gitleaks hooks that fire on commit)
  </read_first>
  <files>repos/security-platform/.github/dependabot.yml</files>
  <action>
1. Create `repos/security-platform/.github/dependabot.yml` per RESEARCH Pattern 3: top-level `version: 2`,
   then `updates:` as a list with exactly ONE entry containing the three required keys —
   `package-ecosystem: "github-actions"`, `directory: "/"`, and `schedule:` with `interval: "weekly"`.
   - `directory: "/"` is correct and is NOT `/.github/workflows`. Dependabot searches `/.github/workflows`
     plus the root `action.yml`/`action.yaml` when given `/`.
   - Per D-03, add no other ecosystem entries. An npm/pip/terraform entry pointing at a directory with no
     manifests produces noisy failing Dependabot jobs.
   - Add no optional keys (`groups`, `labels`, `open-pull-requests-limit`, `target-branch`, `commit-message`).
   - Record in the SUMMARY, so it is not later rediscovered as a defect: Dependabot ignores locally
     referenced reusable workflows by design, so `uses: ./.github/workflows/security.yml` will never appear
     in a Dependabot PR. `actions/checkout` is the only Dependabot-tracked dependency in this phase.

2. Run the full static gate. All five must pass:
   - `actionlint repos/security-platform/.github/workflows/*.yml`
   - `yamllint -d relaxed repos/security-platform/.github/` — matching the repo's own pre-commit hook
     convention. Exit 0 is required; a `line-length` warning on the 81-character pin line is expected
     and acceptable. Do NOT substitute RESEARCH's `truthy`+`document-start` inline override — VERIFIED
     during planning that it errors on the pin line.
   - SHA-pin gate: list every `uses:` line under the new `.github/workflows/`, drop the relative
     reusable-workflow reference, and assert nothing remains that is not `@<40 hex>` followed by a
     same-line `# v` comment. Filter comment lines out first so the gate cannot be satisfied or
     invalidated by prose.
   - Canonical-tag verification, handling annotated tags correctly. Query
     `gh api repos/actions/checkout/git/ref/tags/v7.0.0 --jq '.object.type + " " + .object.sha'`.
     If the type is `commit`, that SHA is the answer. If the type is `tag` (annotated), dereference with
     `gh api repos/actions/checkout/git/tags/<sha> --jq '.object.sha'` and use that instead. Either way
     the result must equal `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`. RESEARCH's
     `refs/tags | select(.object.sha==...)` form only works for lightweight tags — do not rely on it.
     This proves the SHA came from the canonical `actions` org repo and not a fork. Do NOT re-resolve to
     `v7`/latest; the v7.0.0 pin is deliberate.
   - `git -C repos/security-platform check-ignore .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml`
     must find NO matches (exit 1) — confirming nothing in that repo's `.gitignore` swallows the new tree.

3. Commit inside the PRODUCT repo with EXPLICIT paths only. Never `git add .` or `git add -A`:
   `git -C repos/security-platform add .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml`
   (omit any path already committed by Task 2). Before committing, assert
   `git -C repos/security-platform diff --cached --name-only` lists only paths under `.github/`.
   Commit message: `feat(14): add callable security workflow, PR caller, and Dependabot config`.
   The repo's pre-commit hooks will run — yamllint with `-d relaxed` (VERIFIED to exit 0 on these files)
   and the other hooks. If any hook blocks the commit, STOP and REPORT the raw output; do NOT use
   `--no-verify`.
   Do not push — Plan 02 owns pushing and PR creation. Never push from the outer repository.
  </action>
  <verify>
    <automated>R=repos/security-platform && W=$R/.github/workflows && yamllint -d relaxed $R/.github/ && actionlint $W/*.yml && grep -q 'package-ecosystem: "github-actions"' $R/.github/dependabot.yml && grep -q 'directory: "/"' $R/.github/dependabot.yml && grep -q 'interval: "weekly"' $R/.github/dependabot.yml && test "$(grep -v '^[[:space:]]*#' $R/.github/dependabot.yml | grep -c 'package-ecosystem:')" = "1" && test -z "$(grep -rhE '^[[:space:]]*(-[[:space:]]*)?uses:' $W/ | grep -v 'uses: \./' | grep -vE '@[0-9a-f]{40}[[:space:]]+# v')" && T=$(gh api repos/actions/checkout/git/ref/tags/v7.0.0 --jq '.object.type+" "+.object.sha') && S=${T#* } && { [ "${T%% *}" = "commit" ] || S=$(gh api repos/actions/checkout/git/tags/$S --jq '.object.sha'); } && test "$S" = "9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0" && ! git -C $R check-ignore -q .github/dependabot.yml .github/workflows/security.yml .github/workflows/pr-security.yml && test "$(git -C $R ls-files .github | wc -l | tr -d ' ')" = "3" && git -C $R diff --quiet && git -C $R diff --cached --quiet && test -z "$(git -C $R show --name-only --pretty=format: HEAD | grep -v '^$' | grep -v '^\.github/')"</automated>
  </verify>
  <acceptance_criteria>
    - `repos/security-platform/.github/dependabot.yml` contains `version: 2`, `package-ecosystem: "github-actions"`, `directory: "/"`, `interval: "weekly"`
    - It has exactly ONE non-comment `package-ecosystem:` line (no npm/pip/terraform entries)
    - `yamllint -d relaxed repos/security-platform/.github/` exits 0
    - `actionlint repos/security-platform/.github/workflows/*.yml` exits 0 with no output
    - The SHA-pin gate returns an empty result set: no `uses:` line other than the relative `./` reference fails the `@<40-hex>` + same-line `# v` pattern
    - The `v7.0.0` tag (after annotated-tag dereference if needed) resolves to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`
    - The pinned SHA is NOT `3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1) — the one-patch-behind pin is deliberate
    - `git -C repos/security-platform check-ignore` matches none of the three files
    - `git -C repos/security-platform ls-files .github` lists exactly 3 files
    - `git -C repos/security-platform diff --quiet` and `--cached --quiet` both pass (everything committed, nothing stray)
    - Every commit made by this plan touches only paths under `.github/` — verified with `git show --name-only`
    - `--no-verify` was not used, and `cicd/` was not modified
    - Nothing was pushed to any remote, and no git command ran against the outer documentation repo
  </acceptance_criteria>
  <done>`.github/dependabot.yml` created per D-03, the full static gate passes, the pinned SHA is verified as a canonical tagged commit, and exactly three `.github/` files are committed inside the product repo with no unrelated changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream action repo → CI runner | `actions/checkout` code executes with the workflow's `GITHUB_TOKEN` on a GitHub-hosted runner |
| caller workflow → called workflow | `GITHUB_TOKEN` permissions cross a workflow boundary |
| PR event payload → workflow | Untrusted, contributor-controlled event data enters the run context |
| outer documentation repo → product repo remote | Two disjoint histories share one remote URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `actions/checkout` reference in `security.yml` | mitigate | Pin the full 40-character commit SHA `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`; never a moving tag (`@v7`) or an abbreviated SHA. ADR-004. |
| T-14-02 | Tampering | Provenance of the pinned SHA | mitigate | Task 3 resolves the tag via `gh api repos/actions/checkout/git/ref/tags/v7.0.0` against the canonical `actions` org, with annotated-tag dereference — proving the SHA is not from a fork. |
| T-14-03 | Elevation of Privilege | `GITHUB_TOKEN` scope in both workflow files | mitigate | Explicit top-level `permissions: contents: read` in `security.yml` and `pr-security.yml`. |
| T-14-04 | Tampering | Pin goes stale and misses upstream security fixes | mitigate | `.github/dependabot.yml`, `github-actions` ecosystem, weekly interval (CICD-05). |
| T-14-05 | Elevation of Privilege | Called workflow attempting to elevate caller permissions | accept | Platform-enforced: a called workflow can only downgrade, never elevate, the caller's `GITHUB_TOKEN` grant. |
| T-14-06 | Information Disclosure | Fork-PR code execution / secret exposure ("pwn request") | accept | D-04 locks the plain `pull_request` trigger — read-only token and no secret access on fork PRs. `pull_request_target` is not used. |
| T-14-07 | Injection / Tampering | Script injection via `${{ github.event.* }}` in `run:` | accept | Not reachable this phase — D-01 forbids `run:` steps; the placeholder job is checkout-only. Becomes live in Phase 15. |
| T-14-17 | Tampering | Pushing the outer repo's 147 commits of unrelated history onto `security-platform` | mitigate | Hard prohibition in `<notes>` and Task 1/3 acceptance criteria: no git command runs against the outer repo, and this plan pushes nothing at all. All operations use `git -C repos/security-platform`. |
| T-14-18 | Tampering | Accidentally modifying the product repo's existing `cicd/` reference tree or `.pre-commit-config.yaml` | mitigate | Explicit staging paths only; Task 2 and Task 3 acceptance criteria assert `cicd/` is unchanged and every commit touches only `.github/`. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No registry packages are installed by this phase — no `npm install`, `pip install`, or `cargo add` occurs (RESEARCH §Package Legitimacy Audit). The only tooling is `actionlint` from `homebrew-core` (bottled formula, not a registry artifact), so registry slopsquatting does not apply. No `[ASSUMED]`/`[SUS]` packages exist; no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `actionlint repos/security-platform/.github/workflows/*.yml` exits 0
- `yamllint -d relaxed repos/security-platform/.github/` exits 0
- SHA-pin gate over the new `.github/workflows/` returns zero offending rows
- The `v7.0.0` tag dereferences to `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`
- `git -C repos/security-platform ls-files .github` lists exactly the three intended files
- `git -C repos/security-platform status --porcelain` is empty
- `git -C repos/security-platform diff --quiet -- cicd/ .pre-commit-config.yaml` exits 0
</verification>

<success_criteria>
The product repository's root `.github/` tree exists and is locally valid: `security.yml` is callable (`on: workflow_call`), `pr-security.yml` triggers on `pull_request` only and invokes it by relative path, `actions/checkout` is pinned to the canonical v7.0.0 SHA with a same-line `# v7.0.0` comment, both files declare `permissions: contents: read`, and `dependabot.yml` declares a single weekly `github-actions` ecosystem entry. Static structural evidence for ROADMAP criteria #2 and #3 is in place; criteria #1 and #4 require GitHub-side observation (Plans 02 and 03).
</success_criteria>

<output>
Create `.planning/phases/14-workflow-foundation-and-action-pinning/14-01-SUMMARY.md` when done
(in the OUTER documentation repo — planning documents never enter the product repo).
Record in it: the target repository and branch used; the exact pinned SHA and comment string; the
decision to use `yamllint -d relaxed` rather than a new `.yamllint` (with the empirical finding that
RESEARCH's suggested override errors on the 81-character pin line); and the note that Dependabot by
design ignores the local `./.github/workflows/security.yml` reference.
</output>
