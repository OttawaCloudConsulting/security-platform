---
phase: 14-workflow-foundation-and-action-pinning
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: []
autonomous: false
requirements: [CICD-05]

must_haves:
  truths:
    - "`.github/dependabot.yml` is present on the default branch `main`"
    - "Both workflow files are registered server-side after the merge"
    - "A Dependabot update job has run against the repository"
    - "The criterion #4 outcome is classified with evidence as witnessed, inferred, or broken — a missing PR is diagnosed, never silently read as failure"
    - "If a Dependabot pull request exists, it bumps the pinned `actions/checkout` SHA away from 9c091bb… and rewrites the same-line version comment"
  artifacts:
    - path: ".planning/phases/14-workflow-foundation-and-action-pinning/14-03-SUMMARY.md"
      provides: "Merge confirmation, Dependabot observation evidence, and the explicit witnessed/inferred/broken verdict for CICD-05"
      contains: "witnessed"
  key_links:
    - from: ".github/dependabot.yml on main"
      to: "Dependabot update job"
      via: "default-branch config read (Dependabot ignores non-default branches)"
      pattern: "dependabot.yml reachable at ?ref=main"
    - from: "Dependabot update job"
      to: "bump pull request against main"
      via: "github-actions ecosystem scan of .github/workflows/"
      pattern: "gh pr list --author app/dependabot"
---

<objective>
Merge the Phase 14 pull request to `main` (with explicit human confirmation, because merging is irreversible), then observe Dependabot's first run and classify the outcome — producing the evidence for ROADMAP criterion #4 and closing requirement CICD-05.

Purpose: Dependabot reads its configuration only from the default branch. Until the PR is merged, `.github/dependabot.yml` is completely inert and criterion #4 cannot be observed at all. The `actions/checkout` pin was deliberately set one patch behind (v7.0.0, not v7.0.1) so that Dependabot's first post-merge run has a genuine upgrade available and opens a real bump PR — that PR is the direct evidence.

Output: Phase 14 merged to `main`; an explicit, evidence-backed verdict on criterion #4 recorded in `14-03-SUMMARY.md`.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-01-SUMMARY.md
@.planning/phases/14-workflow-foundation-and-action-pinning/14-02-SUMMARY.md

<notes>
**Why the merge is a human checkpoint, not an autonomous step.**
`.claude/rules/defensive-protocol-v2-session-management.md` (Irreversible Actions) requires explicit
user confirmation before merges and architectural commitments. This merge is both: it puts the repo's
first executable CI configuration on the default branch, where Phases 15-20 build on it.

**A missing Dependabot PR does NOT mean failure.** Assumption A7 in RESEARCH is unverified: the
deliberate-v7.0.0-pin strategy assumes Dependabot resolves the pinned SHA back to its tag
(`9c091bb…` → `v7.0.0`) and compares against the latest release. If it instead reads the trailing
`# v7.0.0` comment, or if upstream state differs, no PR appears. Three distinct outcomes must be
told apart, because only the third is a defect:
  - a bump PR appears                                  → criterion #4 WITNESSED
  - no PR, job log says "up to date / no updates"      → wiring PROVEN, criterion #4 INFERRED
  - no PR, job log shows a config/parse error          → wiring BROKEN — STOP and REPORT
  - no job ran at all                                  → config is not on the default branch (re-check the merge)
A missing PR examined in isolation proves nothing either way. The job log is what turns a null
result into information.

**Dependabot's job log is UI-only.** No REST endpoint triggers or exposes a Dependabot version-update
job; the log lives at Insights → Dependency graph → Dependabot, and the only manual trigger is the
"Check for updates" button there. That is why the classification step is a human checkpoint rather
than something the executor can resolve alone.

**Don't over-specify the expected bump target.** Assert the bump moves *away from*
`9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` to some other 40-hex SHA with an updated same-line version
comment. Do NOT hard-require `3d3c42e5aac5ba805825da76410c181273ba90b1` / `v7.0.1` — a newer patch
may have shipped since research on 2026-09-10.

**Dependabot will never touch the local caller reference.** `uses: ./.github/workflows/security.yml`
is ignored by design (local refs have no version to update). `actions/checkout` is the only
Dependabot-tracked dependency in this repo. Its absence from any Dependabot PR is correct, not a bug.

**Slug at runtime.** `gh repo view --json nameWithOwner -q .nameWithOwner`. Never hardcode.
</notes>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-confirmed merge of the Phase 14 pull request to main</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-02-SUMMARY.md (PR number, run ID, check-run name, mergeable evidence)
    - .claude/rules/defensive-protocol-v2-session-management.md (Irreversible Actions)
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Pitfall 2)
  </read_first>
  <files></files>
  <what-built>
Plans 01 and 02 created and proved the wiring: `.github/workflows/security.yml` (callable,
`on: workflow_call`, checkout-only placeholder job pinned to `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0`),
`.github/workflows/pr-security.yml` (thin `pull_request` caller invoking it by relative path),
`.github/dependabot.yml` (single weekly `github-actions` ecosystem entry), and `.yamllint`.
The PR-triggered run was observed green, the checkout step actually executed, and the `main`
ruleset was confirmed to contain no `required_status_checks` rule — so nothing blocks the merge.
ROADMAP criteria #1, #2 and #3 are already satisfied and evidenced in `14-02-SUMMARY.md`.

What is NOT yet possible: criterion #4. `.github/dependabot.yml` is inert on a feature branch —
Dependabot reads its configuration only from the default branch. The merge is the unlock.
  </what-built>
  <how-to-verify>
1. Review the PR before merging. Confirm the diff contains only the four Phase 14 files plus this
   phase's `.planning/` documents — no stray `.claude/` changes from the dirty working tree:
   `gh pr diff <PR> --name-only`
2. Re-read the green run and check name recorded in `14-02-SUMMARY.md`, and confirm
   `gh pr view <PR> --json mergeable` still returns `MERGEABLE`.
3. Confirm the deliberate pin is what you expect to land on `main`: the checkout SHA is
   `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` (v7.0.0), one patch behind v7.0.1 ON PURPOSE, so
   Dependabot has something real to bump. If you would rather pin the latest SHA immediately,
   say so now instead of approving — criterion #4 would then be recorded as *inferred* rather
   than *witnessed*, and the plan changes.
4. Merge, either way:
   - CLI: `gh pr merge <PR> --merge` (the `main` ruleset forbids deletion and non-fast-forward
     pushes only; a normal merge commit is permitted)
   - or the GitHub UI "Merge pull request" button
5. After merging, confirm the config actually landed on the default branch:
   `gh api "repos/$(gh repo view --json nameWithOwner -q .nameWithOwner)/contents/.github/dependabot.yml?ref=main" --jq '.path'`
   must return `.github/dependabot.yml` (HTTP 200). If this 404s, the merge did not land — stop.

Merging normally triggers an initial Dependabot check within a few minutes; the next task polls for it.
  </how-to-verify>
  <resume-signal>Type "approved" once the PR is merged and `.github/dependabot.yml` is confirmed present on `main`. If you decided against the deliberate v7.0.0 pin, or the merge was blocked, describe what happened instead.</resume-signal>
  <action>
This is a BLOCKING human checkpoint. Present the `<what-built>` and `<how-to-verify>` content below to
the user and HALT. Do NOT run `gh pr merge` yourself — merging to the default branch is an irreversible
action reserved for the user under `.claude/rules/defensive-protocol-v2-session-management.md`.
After the user replies with the resume signal, run the post-merge assertions in `<acceptance_criteria>`
(PR state `MERGED`; contents API 200 for `.github/dependabot.yml`, `security.yml` and `pr-security.yml`
at `?ref=main`; both workflow paths now listed by the `actions/workflows` endpoint; the pin on `main`
still `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`). If any assertion fails, STOP and REPORT — do not
proceed to Task 2 on an unconfirmed merge.
  </action>
  <verify>
    <automated>SLUG=$(gh repo view --json nameWithOwner -q .nameWithOwner) && gh api "repos/$SLUG/contents/.github/dependabot.yml?ref=main" --jq '.path' | grep -qx '.github/dependabot.yml' && gh api "repos/$SLUG/contents/.github/workflows/security.yml?ref=main" --jq '.content' | base64 -d | grep -q 'actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0' && gh api "repos/$SLUG/actions/workflows" --jq '.workflows[].path' | grep -qx '.github/workflows/security.yml' && gh api "repos/$SLUG/actions/workflows" --jq '.workflows[].path' | grep -qx '.github/workflows/pr-security.yml'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view <PR> --json state` returns `MERGED`
    - `gh api repos/<slug>/contents/.github/dependabot.yml?ref=main` returns HTTP 200 with path `.github/dependabot.yml`
    - `gh api repos/<slug>/contents/.github/workflows/security.yml?ref=main` returns HTTP 200
    - `gh api repos/<slug>/contents/.github/workflows/pr-security.yml?ref=main` returns HTTP 200
    - `gh api repos/<slug>/actions/workflows --jq '.workflows[].path'` now lists BOTH `.github/workflows/security.yml` and `.github/workflows/pr-security.yml` (the callable file becomes visible once it is on the default branch)
    - The merged commit on `main` still pins `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` — the pin was not silently changed to v7.0.1 before merge
    - The merge was performed by the user (CLI or UI), not autonomously by the executor
  </acceptance_criteria>
  <done>Phase 14's `.github/` tree is on `main`, both workflows are registered server-side, and `.github/dependabot.yml` is live on the default branch where Dependabot can read it.</done>
</task>

<task type="auto">
  <name>Task 2: Poll for the Dependabot bump pull request and assert its content</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Validation Architecture > the criterion #4 observability gap, §Pitfall 1, §Pitfall 3, §Assumptions Log A1 and A7)
    - .github/dependabot.yml
    - .github/workflows/security.yml
  </read_first>
  <files></files>
  <action>
1. Poll for a Dependabot-authored pull request at roughly 60-second intervals for up to ~15 minutes.
   Use a background or monitored loop — do not block the foreground with `sleep`.
   `gh pr list --author "app/dependabot" --state open --json number,title,headRefName,files`
   Stop polling as soon as a PR appears. Record the raw JSON either way.

2. If a PR appeared, assert its content rather than trusting the title:
   - `gh pr diff <N>` must show a changed line in `.github/workflows/security.yml` whose new value
     matches `uses: actions/checkout@<40 hex chars>` followed on the SAME line by an updated `# v…` comment.
   - The new SHA must NOT be `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` (that is the old pin).
   - Do NOT hard-require `3d3c42e5aac5ba805825da76410c181273ba90b1` / `# v7.0.1` — assert only that the
     SHA moved and the version comment was rewritten. A newer patch may exist by now.
   - Confirm the diff does NOT touch `uses: ./.github/workflows/security.yml`; local reusable-workflow
     references are ignored by Dependabot by design and their absence is correct.
   - Record the old SHA, the new SHA, the old comment, the new comment, and the PR number.
   - Also record whether the comment rewrite happened. That is the concrete proof that D-02's
     same-line `# v…` format is Dependabot-compatible, which is the load-bearing claim behind CICD-05.

3. If NO PR appeared within the window, do not conclude failure and do not extend the poll indefinitely.
   Record the null result plus the supporting facts the executor CAN gather:
   - `gh api "repos/$(gh repo view --json nameWithOwner -q .nameWithOwner)/contents/.github/workflows/security.yml?ref=main"`
     decoded — confirm `main` really carries the v7.0.0 pin (if it somehow carries v7.0.1, there was
     legitimately nothing to bump and the outcome is "inferred", not "broken").
   - `gh api repos/actions/checkout/releases/latest --jq '"\(.tag_name) prerelease=\(.prerelease)"'` —
     confirm a newer release than v7.0.0 genuinely exists upstream.
   Then hand off to Task 3, which requires the user to read the UI-only Dependabot job log.
   Do NOT mark criterion #4 failed from the executor's side.
  </action>
  <verify>
    <automated>SLUG=$(gh repo view --json nameWithOwner -q .nameWithOwner) && gh api "repos/$SLUG/contents/.github/dependabot.yml?ref=main" --jq '.path' | grep -qx '.github/dependabot.yml' && gh api "repos/$SLUG/contents/.github/workflows/security.yml?ref=main" --jq '.content' | base64 -d | grep -q 'actions/checkout@' && gh pr list --author "app/dependabot" --state open --json number,title > /tmp/dependabot-prs.json && test -f /tmp/dependabot-prs.json</automated>
  </verify>
  <acceptance_criteria>
    - The Dependabot PR query was run and its raw JSON output is recorded in the SUMMARY (empty array is an acceptable, recorded result)
    - `.github/dependabot.yml` is confirmed present on `main` via the contents API
    - The `security.yml` on `main` is confirmed to contain an `actions/checkout@` pin, and the SHA it carries is recorded
    - IF a Dependabot PR exists: `gh pr diff <N>` shows the `actions/checkout` `uses:` line changing to a different 40-hex SHA, with the same-line `# v…` comment rewritten; old SHA, new SHA, old comment and new comment are all recorded
    - IF a Dependabot PR exists: the diff does not modify the relative `uses: ./.github/workflows/security.yml` reference
    - IF no PR exists: the null result is recorded along with `actions/checkout` latest-release output and the on-`main` pin, and NO failure verdict is issued by this task
    - The poll did not exceed ~15 minutes and did not use a blocking foreground `sleep`
  </acceptance_criteria>
  <done>Dependabot's post-merge behaviour is observed and recorded with evidence — either a content-asserted bump PR, or a documented null result with the upstream and on-`main` facts needed to classify it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Classify the criterion #4 outcome and decide on the bump PR</name>
  <read_first>
    - .planning/phases/14-workflow-foundation-and-action-pinning/14-RESEARCH.md (§Validation Architecture > the criterion #4 observability gap, §Assumptions Log A1 and A7)
    - .planning/ROADMAP.md (§Phase 14 success criteria)
    - .planning/REQUIREMENTS.md (CICD-05)
    - .claude/rules/defensive-protocol-v2-anti-slop.md (Evidence Standards)
  </read_first>
  <files></files>
  <what-built>
Task 2 polled for a Dependabot-authored pull request after `.github/dependabot.yml` landed on `main`,
and recorded either an asserted bump diff or a documented null result. Dependabot's own job log is
UI-only — there is no REST endpoint for it — so the final classification needs a human to look.
  </what-built>
  <how-to-verify>
**If Task 2 found a bump PR:** review it. Confirm it changes `actions/checkout` in
`.github/workflows/security.yml` from `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` to a newer 40-hex
SHA and that the trailing `# v…` comment was rewritten on the same line. That comment rewrite is the
observable proof that the project's SHA-pin convention (D-02) is Dependabot-compatible. Criterion #4
is WITNESSED. Then decide whether to merge it — merging leaves the repo on the current upstream SHA,
which is the intended end state; declining leaves the deliberate one-patch-behind pin in place. Either
is acceptable for phase completion; criterion #4 asks only that Dependabot *opens* a PR.

**If Task 2 found no PR:** open Insights → Dependency graph → Dependabot in the GitHub UI, click into
the most recent update job for the `github-actions` ecosystem, and read the log. Report which of these
you see — the distinction is the whole point:
  a. "up to date" / "no updates needed" → wiring PROVEN, criterion #4 recorded as INFERRED, not
     witnessed. Worth also confirming the pin on `main` really is v7.0.0.
  b. any error, or a config parse failure → wiring BROKEN. Paste the raw error. Per
     `.claude/rules/defensive-protocol-v2-anti-slop.md` this is STOP → REPORT → WAIT; the phase does
     not close and CICD-05 stays open.
  c. no job listed at all → Dependabot never read the config; re-check that `.github/dependabot.yml`
     is on `main` (Task 1's contents-API check) before concluding anything.
If you want to force a check rather than wait for the weekly schedule, use the "Check for updates"
button on that same page — it is the only manual trigger available.
  </how-to-verify>
  <resume-signal>Reply with the verdict: "witnessed" (with the bump PR number, and whether you merged it), "inferred" (paste the up-to-date job log line), or "broken" (paste the raw error). If "broken", do not approve — the phase stays open.</resume-signal>
  <action>
This is a BLOCKING human checkpoint. Present the `<what-built>` and `<how-to-verify>` content below to
the user and HALT. The Dependabot job log is reachable only through the GitHub UI (Insights → Dependency
graph → Dependabot), so the executor cannot classify a null result on its own — that is precisely why
this is a checkpoint and not an auto task.
After the user replies with the resume signal, record their verdict verbatim as exactly one of
`witnessed`, `inferred`, or `broken`, together with the primary evidence they supplied (PR number and
diff excerpt, or the quoted job-log line, or the raw error). Do not soften a `broken` verdict into
`inferred`, and do not upgrade an `inferred` verdict into `witnessed`. On `broken`, make no completion
claim and leave CICD-05 as `Pending` in `.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>SLUG=$(gh repo view --json nameWithOwner -q .nameWithOwner) && gh api "repos/$SLUG/contents/.github/dependabot.yml?ref=main" --jq '.path' | grep -qx '.github/dependabot.yml' && gh api "repos/$SLUG/contents/.github/workflows/security.yml?ref=main" --jq '.content' | base64 -d | grep -qE 'uses: actions/checkout@[0-9a-f]{40}[[:space:]]+# v' && gh pr list --author "app/dependabot" --state all --json number >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one of `witnessed`, `inferred`, or `broken` is recorded in `14-03-SUMMARY.md` as the criterion #4 verdict
    - The verdict is accompanied by primary evidence — a PR number and diff excerpt, or a quoted Dependabot job-log line, or a raw error — never an unsupported assertion
    - IF `witnessed`: the bump PR number is recorded, along with whether it was merged and the resulting SHA now pinned on `main`
    - IF `inferred`: the SUMMARY states explicitly that criterion #4 was inferred and NOT directly observed, and notes that assumption A7 (Dependabot resolving SHA→tag) did not hold as predicted
    - IF `broken`: no completion claim is made, CICD-05 remains `Pending` in `.planning/REQUIREMENTS.md`, and the raw error is reported to the user
    - The SUMMARY distinguishes verified facts from beliefs per the project's Evidence Standards rule
    - The merge decision on any Dependabot PR was made by the user, not autonomously
  </acceptance_criteria>
  <done>Criterion #4 has an explicit, evidence-backed classification and CICD-05's status reflects it honestly — a null result is diagnosed rather than guessed at in either direction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch → default branch `main` | Executable CI configuration crosses into the branch every later phase builds on; irreversible |
| Dependabot service → repository | An automated bot opens branches and pull requests against the repo |
| upstream `actions/checkout` releases → repo pins | Version currency depends on an external release feed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Tampering | Merge of executable CI config to `main` | mitigate | Task 1 is a blocking `checkpoint:human-verify`; the merge is performed by the user after diff review, per the Irreversible Actions rule. Never auto-merged. |
| T-14-13 | Tampering | Unrelated dirty working-tree content reaching `main` | mitigate | Task 1's verification step requires `gh pr diff --name-only` review before merge; Plan 01 staged explicit paths only. |
| T-14-04 | Tampering | Action pin drifting stale after this phase | mitigate | Weekly `github-actions` Dependabot schedule now live on the default branch (CICD-05). Task 2/3 verify it functions rather than assuming it. |
| T-14-14 | Tampering | Blindly merging a bot-authored PR | mitigate | Task 2 asserts the diff content (SHA changes, same-line comment rewritten, local ref untouched) before Task 3 lets the user decide; the merge decision is the user's. |
| T-14-15 | Repudiation | Claiming CICD-05 complete without evidence | mitigate | Task 3 forces an explicit witnessed/inferred/broken verdict backed by a PR diff or a quoted job log; `broken` blocks phase closure and keeps CICD-05 `Pending`. |
| T-14-16 | Elevation of Privilege | Dependabot PRs running the workflow with a read-only token and no secrets | accept | Harmless this phase — the placeholder job only runs checkout. Becomes a real failure mode in Phase 17 when SARIF upload needs `security-events: write`; recorded there, not mitigated here. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No registry packages are installed by this plan — it performs only `gh` API operations and human review. No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `gh pr view <PR> --json state` returns `MERGED`
- `gh api repos/<slug>/contents/.github/dependabot.yml?ref=main` returns 200
- `gh api repos/<slug>/actions/workflows --jq '.workflows[].path'` lists both workflow files
- `gh pr list --author "app/dependabot" --state open` output is captured (empty or not)
- A verdict of `witnessed`, `inferred`, or `broken` is recorded with primary evidence
- `.planning/REQUIREMENTS.md` CICD-05 status matches the verdict (Complete only on witnessed or inferred)
</verification>

<success_criteria>
Phase 14's `.github/` tree is on `main` via a human-confirmed merge, both workflows are registered
server-side, and ROADMAP criterion #4 has an explicit evidence-backed classification. On `witnessed`,
a real Dependabot pull request bumped the pinned `actions/checkout` SHA and rewrote its same-line
version comment — CICD-05 is satisfied by observation. On `inferred`, a clean Dependabot job log proves
the wiring while assumption A7 is recorded as not having held. On `broken`, the phase does not close.
</success_criteria>

<output>
Create `.planning/phases/14-workflow-foundation-and-action-pinning/14-03-SUMMARY.md` when done.
It MUST record: the merge commit and merged PR number; the raw Dependabot PR query output; the
criterion #4 verdict (`witnessed` / `inferred` / `broken`) with its primary evidence; the old and new
`actions/checkout` SHAs and version comments if a bump PR existed; whether that PR was merged and what
SHA `main` now pins; and the resolution status of RESEARCH assumptions A1, A3 and A7.
</output>
