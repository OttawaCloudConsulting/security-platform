---
phase: 18-configurable-gate-mode-and-branch-protection
plan: 06
type: execute
wave: 6
depends_on: [18-05]
files_modified:
  - docs/development-security-stack-option-1.md
  - docs/milestone-plan/milestone-2-cicd-gate.md
autonomous: true
requirements: [CICD-04, CICD-06]

must_haves:
  truths:
    - "A reader following the blueprint's branch-protection steps ends up with required checks that actually match real check runs"
    - "The doc routes the reader to rulesets, the mechanism this stack really uses, not to classic branch protection"
    - "The doc states which severity threshold triggers a failure, and answers it honestly"
    - "The doc explains how to set the gate flag in both consumption modes, and in which order to adopt it"
    - "The same defect in the milestone plan is corrected too, and the one place those job ids are correct is left alone"
  artifacts:
    - path: "docs/development-security-stack-option-1.md"
      provides: "a corrected Phase 2 branch-protection passage: five byte-exact contexts, ruleset navigation, gate_mode in both modes, D-07 ordering, severity answer"
      contains: "security / SAST — Semgrep CE"
    - path: "docs/milestone-plan/milestone-2-cicd-gate.md"
      provides: "the same correction in the M2-F4 section"
      contains: "security / IaC — Checkov"
  key_links:
    - from: "the blueprint's required-check list"
      to: "the live check-run names"
      via: "byte-exact contexts copied from commits/{sha}/check-runs, em dash U+2014"
      pattern: "security / (SAST|IaC|SCA|Container|Secrets)"
    - from: "the blueprint's adoption steps"
      to: "the gate flag"
      via: "gate_mode as a workflow_call input and as the repo variable GATE_MODE"
      pattern: "GATE_MODE"
---

<objective>
Correct the two user-facing documents that currently tell a reader to require five check names that do not
exist, and give Success Criteria 2 and 4 a written home. This is CICD-04's guidance deliverable.

Purpose: the blueprint's Phase 2 passage instructs the reader to add `sast`, `iac`, `sca`, `container`,
`secrets` as required checks. Those are JOB IDS. GitHub treats a required context with no matching check
run as permanently PENDING, not passing — so following the document verbatim produces five checks stuck at
"Expected — Waiting for status to be reported" and a branch nobody can merge into. The same passage routes
the reader to Settings then Branches (classic protection), which returns 404 on the reference repository by
design because it is governed by a ruleset. `docs/milestone-plan/milestone-2-cicd-gate.md` repeats both
defects, found by grepping `docs/` rather than trusting a single pointer.

Output: two corrected passages carrying the five byte-exact contexts, the ruleset navigation, the severity
answer, the flag's two consumption modes, and the adoption ordering — with every adjacent inaccuracy the
phase did not scope left exactly as it was.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-CONTEXT.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-RESEARCH.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-PATTERNS.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-04-SUMMARY.md
@.planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-SUMMARY.md

<interfaces>
<!-- THE FIVE CONTEXTS — byte-exact, em dash U+2014, app github-actions (integration_id 15368):            -->
<!--   security / SAST — Semgrep CE                                                                        -->
<!--   security / IaC — Checkov                                                                            -->
<!--   security / SCA — Trivy Filesystem                                                                   -->
<!--   security / Container — Trivy Image                                                                  -->
<!--   security / Secrets — Gitleaks                                                                       -->
<!-- FIVE, not six: the caller job `security` emits NO check run of its own — it is the PREFIX. Verified    -->
<!--   twice live (14-02's one-job callee produced only `security / Placeholder`; the Phase 18 research     -->
<!--   read of the Phase 17 head SHA returned 12 names, none a bare `security`), and re-confirmed by        -->
<!--   18-04's own run. CONTEXT.md's D-06 says six; the doc must say five.                                  -->
<!--                                                                                                       -->
<!-- THE SEVERITY ANSWER (Success Criterion 4, D-04/D-05): "any finding, per each tool's existing native    -->
<!--   detection configuration". There is no severity knob, and the reason is concrete: pip-audit's JSON    -->
<!--   carries no severity or CVSS field at all, so a cutoff could not be applied consistently across the   -->
<!--   five jobs. Per-tool native mechanisms as configured today:                                           -->
<!--     Semgrep   --error                          -> exit 1 on findings                                   -->
<!--     Checkov   soft_fail: false                 -> native failing exit code                             -->
<!--     Trivy     --exit-code 1 --severity HIGH,CRITICAL  (fs and image)                                    -->
<!--     tflint    exit 2 means findings (exit 1 is reserved for tool errors)                                -->
<!--     Gitleaks  exit 1 on findings                                                                       -->
<!--     npm audit native --audit-level exit code; pip-audit exit 1 on findings, NO severity field           -->
<!--                                                                                                       -->
<!-- THE BLUEPRINT PASSAGE (~L2019-2028, inside the Phase 2 deliverables block L2008-2045). L2021-2028 are  -->
<!--   indented TWO SPACES, nested under the L2019 deliverable bullet — preserve that nesting. L2028's      -->
<!--   closing paragraph (branch protection is what gives the gate any enforcement at all) is CORRECT and   -->
<!--   must survive the rewrite.                                                                            -->
<!--                                                                                                       -->
<!-- THE MILESTONE-PLAN PASSAGE (M2-F4, ~L98-109): L99 lists the same job ids as "Required status checks";  -->
<!--   L98 and L109 route to Settings > Branches. All three need the same correction.                       -->
<!--   L30 of that file lists `sast`, `iac`, `sca`, `container`, `secrets` as JOB IDS, which is CORRECT      -->
<!--   there. DO NOT TOUCH L30.                                                                             -->
<!--                                                                                                       -->
<!-- ADJACENT PRE-EXISTING DRIFT — OUT OF SCOPE, DO NOT "TIDY" (18-PATTERNS):                                -->
<!--   blueprint L2012 lists `Grype + Syft`, which the implemented workflow does not run                    -->
<!--   blueprint L2016 says "and direct pushes to `main`", though the workflow is pull_request-only (D-02)   -->
<!--   blueprint L2039 shows a `grype dir:.` command in the verify-locally block                             -->
<!--   Each is real and each belongs to another phase. Leave all three byte-identical.                       -->
<!--                                                                                                       -->
<!-- CLAUDE.md CONSTRAINTS: preserve the ASCII architecture diagrams, the 4-phase layered structure, the    -->
<!--   tool coverage matrices and the security tools comparison table. `docs/adr/` is append-only — this     -->
<!--   plan touches no ADR (18-07 adds ADR-017 as a new file).                                               -->
</interfaces>

<notes>
**These edits are in the OUTER docs repository**, not `repos/security-platform`. It carries roughly a
hundred unrelated uncommitted `.claude/` changes: commit with explicit pathspecs only, and verify diffs
with `git diff HEAD~1 HEAD --name-only` (the working-tree form reported 139 files and a false FAIL in
17-06).

**Write five contexts, not six.** If the number six appears anywhere in the new text, it is wrong.

**Do not soften the pending-check warning.** A reader who mistypes a context does not get an error — they
get a branch that can never merge. That consequence belongs in the text, not just in an ADR.

**Do not add a live 40-hex commit SHA to the blueprint.** ADR-004's placeholder convention governs every
`uses:` example in that file; this plan adds no action reference, and the count of live SHAs must stay 0.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the blueprint's Phase 2 branch-protection passage</name>
  <files>docs/development-security-stack-option-1.md</files>
  <read_first>
    - docs/development-security-stack-option-1.md lines 2005-2050 (the file being modified: the whole Phase 2 deliverables block, so the rewrite matches the surrounding bullet style, the two-space nesting, and the validation paragraph that follows)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-RESEARCH.md lines 556-578 (P-08 and P-09 — the two defects and why the PR rule must be presented alongside)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-RESEARCH.md lines 675-713 (the UI path, the strict-policy recommendation, the picker caveat, and the copy-paste `gh variable set` mode)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-PATTERNS.md lines 300-360 (the passage as it stands, the exact nesting, and the do-not-tidy list for L2012/L2016/L2039)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-05-SUMMARY.md (the measured blocking/report-only pair the guidance can now state as observed rather than expected)
  </read_first>
  <action>
    Rewrite the deliverable bullet at ~L2019 and its nested block (~L2021-2028) in place, preserving the
    two-space nesting and the surrounding bullet voice. Change nothing above L2019 and nothing below
    L2028's closing paragraph, which stays because it is correct.

    The rewritten block must carry all of the following, in an order that reads as adoption steps:

    (a) Navigation to the mechanism actually in use: Settings, then Rules, then Rulesets, then the ruleset
    covering the default branch, then Edit, then Branch rules. State that classic "Settings then Branches"
    protection is a different, older mechanism and that a repository governed by a ruleset returns 404 from
    the classic protection API, so its absence there proves nothing.

    (b) The gate flag, both consumption modes (Criterion 2): `gate_mode` is a `workflow_call` input taking
    `blocking` or `report-only`; a copy-paste consumer instead sets a repository variable named `GATE_MODE`
    with `gh variable set GATE_MODE --body blocking -R OWNER/REPO`, because a called workflow reads the
    CALLER repository's variables. Unset in both modes means `report-only`. State that switching modes
    requires no YAML edit, and that this was measured: the same commit concluded five green checks and then
    five red ones with only the variable changing.

    (c) The severity answer Criterion 4 asks for: any finding fails the job, per each tool's own native
    detection configuration. List the per-tool mechanism from the interfaces block above, and give the
    reason there is no severity knob — pip-audit reports no severity field, so a cutoff could not apply
    uniformly across the five jobs. Note that Trivy's own `--severity HIGH,CRITICAL` already filters what
    Trivy reports, so the threshold is per-tool configuration rather than a pipeline-level switch.

    (d) The five required contexts, byte-exact, each on its own line, with the source named: they come from
    `gh api repos/OWNER/REPO/commits/SHA/check-runs`, never from the code-scanning analyses endpoint, which
    disagrees on case. State the naming rule (`<caller job id> / <called workflow job name>`) and state
    that the caller job itself emits no check run, so there are five contexts and not six. Warn that a
    context GitHub has never seen sits permanently pending rather than failing, so a mistyped name — wrong
    dash, wrong case — produces an unmergeable branch, and that the separator here is an em dash.

    (e) The pull-request rule alongside the status checks, with the reason: required status checks alone do
    not require a PR, so a developer with write access bypasses the entire gate with one direct push. Note
    that `deletion` and `non_fast_forward` should stay enabled, and that a scripted update must be
    read-modify-write because the ruleset API replaces the whole document — a body carrying only the new
    rule silently deletes the others. Mention that the reference implementation ships this as
    `scripts/set-required-checks.sh`, run with `bash`, dry-run by default.

    (f) The adoption ORDER (D-07), with its correction: first confirm in report-only that all five checks
    appear on a PR and conclude green; then set the flag to blocking and confirm the same PR turns red;
    only then add the checks as required. Say plainly that red-on-findings is NOT observable in report-only
    — by definition report-only is green whatever the scanners find — so the confirmation available at step
    one is that the checks appear and are green, and that doing step three before step two leaves merges
    un-gated while the settings claim otherwise.

    (g) Two caveats, stated as caveats: leave the "require branches to be up to date" (strict) policy off
    unless re-run churn is acceptable, because five jobs re-run on every base-branch advance; and whether a
    fork PR can read repository variables at all is unverified, so a PUBLIC repository that intends to run
    blocking should pass a literal `with: gate_mode: blocking` from its caller rather than relying on the
    variable, since a fork resolving the variable to empty would degrade to report-only while a required
    check still reports green.

    Do not touch L2012, L2016 or L2039. Do not restructure any table, diagram or heading. Commit with an
    explicit pathspec: `docs(18-06): correct the blueprint's required-check names and ruleset guidance`.
  </action>
  <verify>
    <automated>python3 - docs/development-security-stack-option-1.md <<'PY'
import sys
src = open(sys.argv[1], encoding="utf-8").read()
for c in ["security / SAST — Semgrep CE", "security / IaC — Checkov",
          "security / SCA — Trivy Filesystem", "security / Container — Trivy Image",
          "security / Secrets — Gitleaks"]:
    n = src.count(c)
    assert n >= 1, "missing context: %r" % c
    print("ok  x%d  %r" % (n, c))
assert "add each security workflow job as a required check" not in src, "the P-08 defect text is still present"
assert "`sast`, `iac`, `sca`, `container`, `secrets`" not in src, "job ids still presented as check names"
assert "GATE_MODE" in src and "gate_mode" in src, "the flag must be documented in both consumption modes"
assert "Rulesets" in src, "ruleset navigation missing"
for keep in ["Grype + Syft", "and direct pushes to `main`", "grype dir:."]:
    assert keep in src, "out-of-scope adjacent line was 'tidied': %r" % keep
print("blueprint assertions pass")
PY</automated>
    <automated>python3 - docs/development-security-stack-option-1.md <<'PY'
import re, sys
src = open(sys.argv[1], encoding="utf-8").read()
assert "–" not in src.split("## ")[0] or True, ""
bad = [m.start() for m in re.finditer("security / [A-Za-z]+ – ", src)]
assert not bad, "en dash U+2013 used in a context name at offsets %r" % bad
print("code fences:", src.count("```"))
print("live 40-hex SHAs:", len(re.findall(r"@[0-9a-f]{40}\b", src)))
PY</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git diff HEAD~1 HEAD --name-only</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git diff HEAD~1 HEAD --stat -- docs/development-security-stack-option-1.md</automated>
  </verify>
  <acceptance_criteria>
    - All five contexts appear byte-exactly with U+2014; the regex check finds no en dash in any context name
    - Neither `add each security workflow job as a required check` nor the backticked job-id list survives anywhere in the file
    - The passage names all seven elements: ruleset navigation (with the classic-404 note), both consumption modes of the flag, the severity answer with its pip-audit reason, the five contexts with their source endpoint and the pending-check warning, the `pull_request` rule with the read-modify-write warning, the D-07 ordering with the report-only correction, and the two caveats (strict policy, fork `vars`)
    - The number six is not used to describe the required-check set anywhere
    - `Grype + Syft`, `and direct pushes to `main`` and `grype dir:.` are all still present byte-identical
    - The code-fence count is unchanged from before the edit and the live 40-hex SHA count is 0 (report both before and after)
    - `git diff HEAD~1 HEAD --name-only` lists exactly `docs/development-security-stack-option-1.md`
  </acceptance_criteria>
  <done>A reader following the blueprint gets required checks that match real check runs, in the settings screen their repository actually uses, in an order that does not leave them un-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the same defect in the milestone plan's M2-F4 section</name>
  <files>docs/milestone-plan/milestone-2-cicd-gate.md</files>
  <read_first>
    - docs/milestone-plan/milestone-2-cicd-gate.md lines 20-40 and 90-115 (the file being modified: the M2-F4 section that carries the defect, and the earlier line 30 that uses the same five job ids CORRECTLY and must not change)
    - docs/development-security-stack-option-1.md (the passage just rewritten — the milestone plan must agree with it rather than invent a second wording)
    - .planning/phases/18-configurable-gate-mode-and-branch-protection/18-PATTERNS.md lines 362-395 (the exact lines at fault, and the explicit instruction to leave line 30 alone)
  </read_first>
  <action>
    In the M2-F4 section only: replace the `Settings > Branches > Branch protection rules` key component
    with the ruleset path (Settings, Rules, Rulesets, the default-branch ruleset); replace the
    `Required status checks: sast, iac, sca, container, secrets` line with the five byte-exact
    `security / …` contexts and a note that they come from the check-runs API and that the caller job
    contributes the prefix rather than a sixth check; and correct the Done Criteria line that says
    `Settings > Branches shows the protection rule active` so it reads against
    `gh api repos/OWNER/REPO/rules/branches/main` (and states that the classic `branches/main/protection`
    endpoint 404s on a ruleset-governed repository and is not evidence of anything).

    Keep the section's existing bullet structure, its Delivers/Key Components/Done Criteria/Dependencies
    shape, and its terse voice — this is a milestone plan, not the blueprint, so do not paste the long
    explanation from Task 1. Add one line pointing to the blueprint passage for the full procedure, and one
    Done Criteria line covering the gate flag: the same PR observed green in report-only and red in
    blocking, switched by the repository variable alone.

    Leave line 30's job-id list exactly as it is: there the strings genuinely are job ids and they are
    correct. Change nothing in any other section. Commit with an explicit pathspec:
    `docs(18-06): correct the milestone plan's required-check names and protection path`.
  </action>
  <verify>
    <automated>python3 - docs/milestone-plan/milestone-2-cicd-gate.md <<'PY'
import sys
lines = open(sys.argv[1], encoding="utf-8").read().splitlines()
src = "\n".join(lines)
for c in ["security / SAST — Semgrep CE", "security / IaC — Checkov",
          "security / SCA — Trivy Filesystem", "security / Container — Trivy Image",
          "security / Secrets — Gitleaks"]:
    assert src.count(c) >= 1, "missing context: %r" % c
assert "Required status checks: `sast`" not in src, "the defect line survives"
assert "Settings > Branches" not in src, "classic branch-protection path still routed to"
assert "rules/branches/main" in src, "the authoritative read-back path is missing"
jobid = [i + 1 for i, l in enumerate(lines) if "`sast`" in l]
print("lines still mentioning `sast`:", jobid, "-> must be the job-id line only (originally L30)")
assert len(jobid) == 1, jobid
print(lines[jobid[0] - 1])
PY</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && git diff HEAD~1 HEAD --name-only</automated>
    <automated>cd /Users/christian/git-repos/OCC-github/development_environment/security_solution && grep -rn 'Required status checks: `sast`' docs/ || echo "no remaining occurrence of the defect anywhere under docs/"</automated>
  </verify>
  <acceptance_criteria>
    - The five contexts appear byte-exactly in M2-F4; the defect line `Required status checks: \`sast\`…` is gone
    - Exactly one line in the file still mentions `` `sast` `` — the original line 30 job-id list — and the check prints it for confirmation
    - `Settings > Branches` appears nowhere; the Done Criteria reference `rules/branches/main` and name the classic-endpoint 404 as a non-evidence
    - A Done Criteria line covers the gate flag: same PR green in report-only, red in blocking, switched by repository variable alone
    - A grep across all of `docs/` finds no remaining instance of the job-ids-as-required-checks defect
    - `git diff HEAD~1 HEAD --name-only` lists exactly `docs/milestone-plan/milestone-2-cicd-gate.md`
  </acceptance_criteria>
  <done>The second copy of the same misleading instruction is corrected, and the only place those job ids are genuinely job ids is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Written guidance → an operator's live repository settings | Every instruction here is executed by a human against production branch protection; a wrong context name is a self-inflicted denial of service |
| Documentation → other documentation | Two files describe the same procedure; a correction applied to one and not the other leaves a reader following the stale copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Denial of Service | Required contexts documented as job ids (P-08) | mitigate | Five byte-exact contexts with their source endpoint named; the defect string asserted absent across all of `docs/`; the permanently-pending consequence spelled out in the text |
| T-18-06 | Elevation of Privilege | A reader enabling status checks without a PR requirement (P-09 / ADR-002) | mitigate | The `pull_request` rule is presented in the same step, with the direct-push bypass named as the reason |
| T-18-04 | Tampering | A reader scripting a ruleset update that replaces the document | mitigate | The read-modify-write requirement and the "PUT replaces everything" fact are stated in the guidance, with the shipped helper named |
| T-18-07 | Elevation of Privilege | A public repo going blocking while fork PRs silently degrade to report-only (Q3) | mitigate | Documented as an explicit caveat with the literal `with: gate_mode: blocking` mitigation; flagged as unverified rather than asserted either way |
| T-18-20 | Repudiation | Classic-protection 404 being read as "no protection exists" | mitigate | The guidance states the 404 is expected on a ruleset-governed repo and names `rules/branches/main` as the authoritative read |
| T-18-21 | Tampering | Scope creep into adjacent inaccurate lines producing an unreviewable diff | mitigate | The three out-of-scope lines are asserted still present byte-identical after the edit |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installed; documentation edits only |
</threat_model>

<verification>
- Five byte-exact contexts in both files, U+2014, no en dash, no sixth context.
- The job-ids-as-required-checks defect absent from all of `docs/`.
- `Settings > Branches` / classic-protection routing removed from the corrected passages.
- The three out-of-scope blueprint lines still present byte-identical; code-fence count unchanged; zero live 40-hex SHAs.
- Exactly one surviving `` `sast` `` mention in the milestone plan, on the correct job-id line.
- Each commit touches exactly one file.
</verification>

<success_criteria>
- Success Criterion 4 has its written artifact, including the severity answer.
- Success Criterion 2's two consumption modes are documented where an adopter will find them.
- Following the documentation produces a working gate rather than a locked branch.
</success_criteria>

<output>
Create `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-06-SUMMARY.md` when done.
Record: the before and after text of both corrected passages; the before/after code-fence and live-SHA
counts for the blueprint; the confirmation that the three out-of-scope lines are unchanged; the surviving
`` `sast` `` line in the milestone plan; and the `docs/`-wide grep proving the defect appears nowhere else.
</output>
