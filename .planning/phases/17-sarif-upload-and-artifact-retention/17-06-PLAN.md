---
phase: 17-sarif-upload-and-artifact-retention
plan: 06
type: execute
wave: 6
depends_on: [17-05]
files_modified:
  - docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md
  - docs/adr/README.md
  - docs/development-security-stack-option-1.md
  - .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md
autonomous: true
requirements: [CICD-02, CICD-03]

must_haves:
  truths:
    - "Every decision this phase made — including the two the user locked — is written down in a new, append-only ADR rather than living only in a plan file"
    - "The blueprint no longer teaches a copy-paste workflow that 403s: it shows the permissions block, the category input and the retention-days input"
    - "The blueprint's action version comments match the majors this phase actually pinned"
    - "The tool matrix states which tools can and cannot emit SARIF, so Criterion 4's resolution is discoverable from the primary artifact"
    - "Every documentation surface deliberately left stale is written down as a deferral with an owner, not silently skipped"
  artifacts:
    - path: "docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md"
      provides: "the phase's decision record — categories, retention, Criterion 4 resolution, trigger policy, direct-SARIF choice"
      contains: "Accepted"
      min_lines: 60
    - path: "docs/adr/README.md"
      provides: "index row for ADR-016"
      contains: "adr016-"
    - path: "docs/development-security-stack-option-1.md"
      provides: "CI/CD template showing permissions, category and retention-days, with current major comments"
      contains: "retention-days"
    - path: ".planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md"
      provides: "explicit deferral list with owning phase per item"
      contains: "DIST-06"
  key_links:
    - from: "docs/adr/README.md index table"
      to: "docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md"
      via: "markdown link in the appended row"
      pattern: "adr016-sarif-upload-attribution-and-artifact-retention.md"
    - from: "blueprint CI/CD workflow template"
      to: "the live workflow in repos/security-platform"
      via: "same keys (permissions, category, retention-days) with <SHA> placeholders instead of live pins"
      pattern: "category:"
---

<objective>
Write the phase's decisions into the documentation repo: a new ADR-016, its index row, the blueprint's CI/CD
template and tool matrix, and an explicit deferral list for the surfaces this phase deliberately leaves
alone.

Purpose: CLAUDE.md makes `docs/development-security-stack-option-1.md` the primary artifact, and today it
teaches a workflow that cannot work — it shows **no** `permissions:` block, **no** `category:` on any
`upload-sarif` example and **no** `retention-days` on any `upload-artifact` example (all three verified by
grep: zero hits). Anyone copy-pasting it gets a 403 on every SARIF upload and no stated retention. The two
user-locked decisions and the five planner decisions this phase made also need a home that survives the
plan files: `docs/adr/` is append-only, so that home is a new ADR-016 — never an edit to ADR-001, ADR-004
or ADR-015.

Output: one new ADR, one index row, surgical blueprint insertions, and a deferred-items file. All in the
OUTER repo. Nothing under `repos/` is touched.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-RESEARCH.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-PATTERNS.md
@.planning/phases/17-sarif-upload-and-artifact-retention/17-05-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-06-SUMMARY.md

<interfaces>
<!-- OUTER REPO ONLY. Nothing under repos/security-platform is modified by this plan.                  -->
<!--                                                                                                   -->
<!-- WHAT ADR-016 MUST RECORD (the complete decision set — all of it, none implied):                    -->
<!--   D-01 (USER)  Criterion 4 for npm audit / pip-audit is satisfied via the ARTIFACT SET plus the    -->
<!--                documented conversion step `trivy convert`, which after this phase lives in the     -->
<!--                CONTAINER job (the sca job now emits SARIF directly — see below). No converter is   -->
<!--                hand-rolled and no third-party converter action is introduced. Measured support:    -->
<!--                pip-audit -f accepts only columns|json|cyclonedx-json|cyclonedx-xml|markdown, and   -->
<!--                npm audit offers only --json; pip-audit's JSON has NO severity field at all, so a   -->
<!--                hand-rolled converter would emit `level: warning` for every result — a real         -->
<!--                fidelity loss, not a cosmetic one. Every retained format has a native DefectDojo    -->
<!--                parser (dojo/tools: checkov, gitleaks, npm_audit_7_plus, pip_audit, sarif,          -->
<!--                semgrep, tflint, trivy), which is why native JSON is retained unnormalised.        -->
<!--   D-02 (USER)  Trigger stays `pull_request`-only; `push: branches: [main]` is NOT added.           -->
<!--                Consequence, stated plainly: `main` is never analysed, so no alert this phase       -->
<!--                produces is associated with the default branch, and Criterion 1 is read under the   -->
<!--                Security tab's branch/PR filter and the analyses API. Revisit in Phase 19 (VAL-01). -->
<!--   OD-1         `security-events: write` granted at the CALLING JOB in pr-security.yml (least       -->
<!--                privilege) and re-declared at workflow level in security.yml, because a called      -->
<!--                workflow can only downgrade an inherited set. `actions: read` carried for private-  -->
<!--                repo portability. No other write scope.                                             -->
<!--   OD-2         Upload success is asserted on `steps.<id>.outcome`, not on `sarif-id` presence,     -->
<!--                because sarif-id is populated before wait-for-processing can reject the SARIF.      -->
<!--   OD-4         Categories: semgrep, checkov, trivy-fs, tflint, trivy-image, gitleaks — sticky by   -->
<!--                design: changing one later ORPHANS its historical alerts rather than updating them. -->
<!--                Artifact names: semgrep-results, checkov-results, sca-results, trivy-image-results, -->
<!--                gitleaks-results — a future DEFECT-01 importer will hard-code these.                -->
<!--                retention-days: 90, the maximum available without a repository-settings change.     -->
<!--   OD-7         The `sca` job emits SARIF from a second direct `trivy fs --format sarif` run rather -->
<!--                than `trivy convert`, with the measured reason: convert sets                        -->
<!--                originalUriBaseIds.ROOTPATH to the INPUT JSON FILE path, direct output sets it to   -->
<!--                the SCAN ROOT (measured 2026-09-11, trivy 0.74.0, byte-exact CI invocation).        -->
<!--   ADR-001 pairing — `continue-on-error: true` stays on every upload step AND each is paired with   -->
<!--                an intolerant assertion, so tolerance never hides a silent 403.                     -->
<!--   UNVERIFIED, to be stated as unverified: whether Dependabot runs can obtain `security-events:     -->
<!--                write` (RESEARCH Q3/A8) — the verify steps SKIP on Dependabot and fork PRs rather   -->
<!--                than fail, and the observation is handed to Phase 18; and whether PR diff           -->
<!--                annotations render with no base analysis (RESEARCH Q2), handed to Phase 19.         -->
<!--                                                                                                   -->
<!-- ADR FORMAT — analog docs/adr/adr015-tflint-terraform-pin-checking.md (61 lines, written last       -->
<!--   phase, same author). Header: `# ADR-016: <Title>` then `**Status:** Accepted`, `**Date:**`,      -->
<!--   `**Addresses:**`. Sections: Context, Decision (bold lead-in per bullet), Consequences split into -->
<!--   **Improved:** and one or more **Tradeoff — …** paragraphs, a measured table where a claim is     -->
<!--   measured, and an explicit statement of what was NOT verified.                                    -->
<!--   docs/adr/README.md is 25 lines with ONE `## Index` table; the ADR-015 row is the LAST line of    -->
<!--   the file. Row shape:                                                                             -->
<!--   | [ADR-015](adr015-tflint-terraform-pin-checking.md) | <Title> | 2026-09-11 | Accepted |         -->
<!--                                                                                                   -->
<!-- BLUEPRINT TARGETS (line numbers verified 2026-09-11; they SHIFT as edits land — anchor on text):   -->
<!--   ~L1462-1471  workflow header (`name: Security Scans`, the two-trigger comment, `on:`) — insert a -->
<!--                workflow-level `permissions:` block. The file's only existing permissions block is  -->
<!--                the `sign` job's at ~L1615-1619; copy its one-scope-per-line + `# Required for …`   -->
<!--                comment idiom.                                                                      -->
<!--   ~L1515, 1536, 1583   `upload-sarif@<SHA>  # v3 — pin to current SHA: …`   -> `# v4`              -->
<!--   ~L1519, 1540, 1555, 1587, 1603  `upload-artifact@<SHA>  # v4 — …`         -> `# v7`              -->
<!--   ~L1518, 1539, 1586   `with: { sarif_file: <x>.sarif }`   -> add `category:`                      -->
<!--   ~L1522, 1543, 1558, 1590, 1606  `with: { name: …, path: … }` -> add `retention-days:`            -->
<!--   ~L1700-1707  `## Cross-Platform CI Compatibility` table `| Tool | Install Method | Output        -->
<!--                Formats |` — currently 6 rows (Semgrep CE, Checkov, Trivy, Grype, Syft, Gitleaks).  -->
<!--                ADD rows for npm audit, pip-audit and tflint. This is the table CLAUDE.md says to   -->
<!--                preserve: add rows, never restructure.                                              -->
<!--   KEEP the `@<SHA>` placeholders — ADR-004's deliberate scope split: the blueprint stays           -->
<!--   placeholder-based so it cannot go stale; only the product repo carries live SHAs. Do NOT paste   -->
<!--   b96794f0… or 043fb46d… into the blueprint.                                                       -->
<!--                                                                                                   -->
<!-- ALREADY DONE BY 16-06 — do not duplicate: pip-audit and tflint rows exist in the tool-selection    -->
<!--   table (~L145-146) and in the language matrix (~L1720) with its note (~L1734). The Cross-Platform -->
<!--   CI Compatibility table at ~L1700 is a THIRD, different table and does NOT have them.             -->
</interfaces>

<notes>
**OUTER REPOSITORY.** Every file here is in this docs repo. Do not touch `repos/security-platform` — the
PR opened in 17-05 stays exactly as it is until 17-07.

**`docs/adr/` is append-only.** ADR-001 (continue-on-error), ADR-004 (SHA pinning) and ADR-015 (tflint) are
Accepted and immutable. ADR-016 may CITE them; it may not edit them. If you find yourself wanting to
"correct" an accepted ADR, stop — that is what a new ADR is for.

**Surgical insertions only in the blueprint.** ASCII architecture diagrams, the 4-phase layered structure,
the tool coverage matrices and the security tools comparison table are all preserved. 16-06 verified its
edits by invariant rather than by eye (fence count unchanged, per-row pipe-count parity); do the same here.

**Scope boundary — do NOT fix these here:** the blueprint's Grype-based `sca:` job (~L1545-1558), the
blueprint example's own `push: branches: [main]` trigger, `repos/security-platform/cicd/.github/workflows/security.yml`
(stale since 16-04), and `docs/milestone-plan/milestone-2-cicd-gate.md` M2-F3's `grype-results.json`. Each
goes in `deferred-items.md` with its owning phase. An unbounded blueprint rewrite is this plan's failure
mode, and 16-06 set the precedent that reconciliation is scoped explicitly rather than allowed to sprawl.

**Write ADR-016 from the recorded evidence, not from memory.** Every measured claim in it must be
traceable to 17-RESEARCH or to a 17-0x-SUMMARY. Where the phase did not verify something — Dependabot
grantability, annotation rendering with no base analysis — say so in those words.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-016 and append its index row</name>
  <files>docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md, docs/adr/README.md</files>
  <read_first>
    - docs/adr/adr015-tflint-terraform-pin-checking.md (all 61 lines — the format analog: header block, bold lead-in per Decision bullet, Consequences split into Improved/Tradeoff, a measured table, and an explicit "not verified" statement)
    - docs/adr/README.md (all 25 lines — the single Index table and the ADR-015 row that is the file's last line)
    - docs/adr/adr001-remove-continue-on-error.md (the constraint ADR-016 must reconcile with, not contradict — uploads stay tolerated)
    - docs/adr/adr004-pin-actions-to-sha-digest.md (the placeholder-vs-live-SHA scope split ADR-016 relies on)
    - .planning/phases/17-sarif-upload-and-artifact-retention/17-05-SUMMARY.md (the live evidence ADR-016 cites: categories, artifact expiries, check-run names, what was and was not observed)
  </read_first>
  <action>
    Create `docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md`, titled
    `# ADR-016: SARIF Upload Attribution and Scan Artifact Retention`, with
    `**Status:** Accepted`, `**Date:** 2026-09-11` (or the actual execution date) and
    `**Addresses:** CICD-02, CICD-03`.

    **Context** states the problem in the project's own terms: five jobs already produced six SARIF files
    and seven-plus JSON reports that nothing consumed; GitHub keys analyses on tool name + category and two
    of the six files carry the identical driver name `Trivy`; the workflow is a reusable `workflow_call`
    whose permission set is capped by its caller; and ADR-001 requires upload steps to be tolerated.

    **Decision** carries one bold lead-in bullet per decision, covering ALL of: the per-file upload with a
    unique category and the six category strings, with the note that categories are sticky — changing one
    later orphans its historical alerts; the caller-job-level `security-events: write` grant plus the
    callee re-declaration, and the fact that `actions: read` is carried only for private-repo portability;
    the `outcome`-not-`sarif-id` assertion and why; the tolerated-upload/intolerant-assertion pairing that
    reconciles ADR-001 with the project's no-silent-fallback rule; the five artifact names, the explicit
    `retention-days: 90` and why an implicit repo default does not satisfy Criterion 3; retaining each
    tool's native JSON unnormalised, citing the measured DefectDojo parser inventory and the
    `npm_audit_7_plus` detail; the `sca` job's direct `trivy fs --format sarif` with the measured ROOTPATH
    comparison, and that `trivy convert` is retained in the CONTAINER job where it remains the phase's
    documented conversion step; the user's D-01 resolution of Criterion 4 through the artifact set, with
    pip-audit's missing severity field given as the concrete fidelity cost of the alternative; and the
    user's D-02 trigger decision with its default-branch consequence spelled out.

    **Consequences** splits into **Improved:** and at least two **Tradeoff — …** paragraphs. Tradeoffs to
    state honestly: the `pull_request`-only trigger means no alert is ever associated with the default
    branch, so the unfiltered Security tab reads empty and Criterion 1 is verified under a filter; npm and
    pip findings reach the artifact set but NOT the Security tab; 90 days is the ceiling without a
    repo-settings change, and a DefectDojo landing later than that will find these artifacts expired;
    successful uploads add check runs to the head SHA that Phase 18 must evaluate deliberately before
    making anything required; and code scanning on a PRIVATE consumer repo requires a paid GitHub Code
    Security licence, so the zero-cost claim holds for this public repo but does not transfer (a Phase 20
    adoption-doc obligation).

    Include one measured table — the six-row SARIF inventory (job, file, driver name, runs, category,
    whether locations resolve to repo files) — and close with an explicit paragraph naming what was NOT
    verified: whether Dependabot runs can obtain `security-events: write`, and whether PR diff annotations
    render with no base analysis on the default branch. Use those words; do not soften them.

    Then append ONE row to `docs/adr/README.md`'s single Index table, after the ADR-015 row that is
    currently the file's last line, in the exact column shape of that row. Do not create a second table and
    do not modify any other row.

    Commit both files together: `docs(17-06): record SARIF attribution and artifact retention decisions in ADR-016`.
  </action>
  <verify>
    <automated>test -f docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md && head -5 docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md | grep -q '^\*\*Status:\*\* Accepted' && grep -q '^\*\*Addresses:\*\*.*CICD-02.*CICD-03' docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md</automated>
    <automated>for s in semgrep checkov trivy-fs tflint trivy-image gitleaks semgrep-results checkov-results sca-results trivy-image-results gitleaks-results retention-days 'pull_request' 'security-events' ROOTPATH outcome npm_audit_7_plus; do grep -q -- "$s" docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md || { echo "ADR-016 missing: $s"; exit 1; }; done; echo "ADR-016 covers every decided value"</automated>
    <automated>grep -qi 'not verified\|unverified' docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md && grep -qi 'dependabot' docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md && grep -q 'Tradeoff' docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md</automated>
    <automated>test "$(grep -c '^## Index' docs/adr/README.md)" -eq 1 && test "$(grep -c 'adr016-sarif-upload-attribution-and-artifact-retention.md' docs/adr/README.md)" -eq 1 && tail -1 docs/adr/README.md | grep -q 'ADR-016' && git diff HEAD~1 --stat -- docs/adr/ | grep -q 'README.md'</automated>
    <automated>git diff HEAD~1 --name-only | grep -v '^docs/adr/' | grep -q . && { echo "FAIL: files outside docs/adr/ in this commit"; exit 1; } || echo "commit confined to docs/adr/"</automated>
    <automated>git status --porcelain docs/adr/adr001-remove-continue-on-error.md docs/adr/adr004-pin-actions-to-sha-digest.md docs/adr/adr015-tflint-terraform-pin-checking.md | grep -q . && { echo "FAIL: an accepted ADR was modified"; exit 1; } || echo "accepted ADRs untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md` exists, is at least 60 lines, and opens with `# ADR-016:`, `**Status:** Accepted`, a `**Date:**` and `**Addresses:** CICD-02, CICD-03`
    - The Decision section contains a bold lead-in bullet for each of: category attribution, the caller-job permission grant, the `outcome` assertion, the ADR-001 tolerated/intolerant pairing, artifact names and `retention-days: 90`, native-format retention, the `sca` direct-SARIF change, D-01's Criterion 4 resolution, and D-02's trigger decision
    - All six category strings, all five artifact names, `retention-days`, `ROOTPATH`, `outcome`, `npm_audit_7_plus` and `security-events` appear literally in the file
    - The file contains a measured six-row SARIF inventory table
    - Consequences contains an **Improved:** paragraph and at least two **Tradeoff — …** paragraphs, including the default-branch consequence of D-02 and the private-repo licence limitation
    - A closing paragraph names, in those words, the two things NOT verified: Dependabot `security-events` grantability, and annotation rendering with no base analysis
    - `docs/adr/README.md` has exactly one `## Index` heading, exactly one ADR-016 row, and that row is the file's last line; no other row changed
    - `docs/adr/adr001-*.md`, `adr004-*.md` and `adr015-*.md` are unmodified in git status
    - The commit contains only files under `docs/adr/`
  </acceptance_criteria>
  <done>Every decision this phase made — user-locked and planner-made — is recorded in an append-only ADR that also states what the phase did not establish.</done>
</task>

<task type="auto">
  <name>Task 2: Make the blueprint's CI/CD template copy-pasteable, and record the deferrals</name>
  <files>docs/development-security-stack-option-1.md, .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md</files>
  <read_first>
    - docs/development-security-stack-option-1.md lines 1456-1480 (the file being modified — the workflow header inside the fenced yaml block that starts at ~L1462)
    - docs/development-security-stack-option-1.md lines 1490-1625 (the sast/iac/sca/container/secrets job examples with their `upload-sarif` and `upload-artifact` lines, and the `sign` job's job-level permissions block at ~L1615-1619 — the comment idiom to copy)
    - docs/development-security-stack-option-1.md lines 1690-1740 (the `## Cross-Platform CI Compatibility` table to extend, and the language matrix + note 16-06 already updated — do not touch those)
    - .planning/phases/16-sca-ecosystem-coverage/16-06-SUMMARY.md (the precedent: which tables were already updated, and the invariant-based verification style — fence count unchanged, per-row pipe parity)
    - .planning/phases/16-sca-ecosystem-coverage/deferred-items.md (the deferral file format to copy)
  </read_first>
  <action>
    Work by ANCHOR TEXT, not by line number — every edit shifts the numbers below it. After each edit,
    re-grep to confirm the expected count of remaining occurrences.

    1. Insert a workflow-level `permissions:` block into the blueprint's `security.yml` example, after the
       `on:` trigger block and before `jobs:`, granting `contents: read`, `security-events: write` and
       `actions: read`, each with a `# Required for …` trailing comment in the `sign` job's idiom. Add one
       comment line above it stating that a copy-pasted caller must grant `security-events: write` on its
       CALLING JOB too, because a called workflow can only downgrade an inherited permission set — this is
       the single most likely adoption failure and it produces a green run with an empty Security tab.
    2. Change every `upload-sarif@<SHA>  # v3` comment to `# v4`, and every
       `upload-artifact@<SHA>  # v4` comment to `# v7`. Keep the `@<SHA>` placeholder and the trailing
       "pin to current SHA: <releases url>" text exactly as they are — ADR-004 keeps the blueprint
       placeholder-based on purpose. Verify by grep that zero `upload-sarif` lines still say `# v3` and
       zero `upload-artifact` lines still say `# v4`.
    3. Add a `category:` to every `upload-sarif` example's `with:` mapping, using values consistent with the
       live workflow (`semgrep`, `checkov`, `trivy-image` — match each example to the tool it shows). The
       blueprint uses inline-flow mappings (`with: { sarif_file: x.sarif }`); pick ONE rendering — flow with
       the extra key, or expanded block style — and apply it consistently to all of them. Add a short
       comment on the first one stating that a distinct category per analysis is mandatory, not stylistic,
       because GitHub keys analyses on tool name + category and two Trivy scans otherwise collide.
    4. Add `retention-days:` to every `upload-artifact` example's `with:` mapping with the same value the
       product repo uses, plus a comment on the first one stating that an explicit period is required and
       that 90 is the maximum without a repository-settings change.
    5. In the `## Cross-Platform CI Compatibility` table only, append rows for **npm audit**, **pip-audit**
       and **tflint** with their install method and output formats, making the SARIF truth explicit: npm
       audit is JSON only, pip-audit is JSON/CycloneDX/Markdown with no SARIF, tflint does emit SARIF. Add
       or extend a note immediately below the table recording that the two tools without SARIF reach the
       retained artifact set rather than the Security tab, and link ADR-016. Add rows only — do not
       reorder, rename or remove any existing row, and do not touch the language matrix at ~L1720 or its
       note, which 16-06 already updated.

    Then create `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` in the format
    used by `.planning/phases/16-sca-ecosystem-coverage/deferred-items.md`, one row per deferral with a
    reason and an owning phase: the `cicd/.github/workflows/security.yml` mirror template (Phase 20 /
    DIST-06 — stale since 16-04, and it already carries a `security-events` line the live workflow was
    missing); the blueprint's Grype-based `sca:` job example (pre-existing drift versus the live
    Trivy/npm/pip/tflint stack); the blueprint example's own `push: branches: [main]` trigger, which
    diverges from the live workflow's D-02 `pull_request`-only policy (documentation surface — flag, do not
    change, since D-02 governs the live workflow); `docs/milestone-plan/milestone-2-cicd-gate.md` M2-F3's
    `grype-results.json` naming; the private-repo Code Security licence limitation for consumer adoption
    (Phase 20 / DIST-08); and the SARIF size/result limits at consumer scale (10 MB gzipped per file,
    25,000 results per run — Phase 20).

    Commit: `docs(17-06): show permissions, category and retention in the blueprint CI/CD template`.
  </action>
  <verify>
    <automated>grep -n 'upload-sarif@<SHA>' docs/development-security-stack-option-1.md | grep -c '# v4' | grep -qx 3 && ! grep 'upload-sarif@<SHA>' docs/development-security-stack-option-1.md | grep -q '# v3' && echo "upload-sarif comments now v4"</automated>
    <automated>grep -n 'upload-artifact@<SHA>' docs/development-security-stack-option-1.md | grep -c '# v7' | grep -qx 5 && ! grep 'upload-artifact@<SHA>' docs/development-security-stack-option-1.md | grep -q '# v4' && echo "upload-artifact comments now v7"</automated>
    <automated>test "$(grep -c 'retention-days' docs/development-security-stack-option-1.md)" -ge 5 && test "$(grep -c 'category:' docs/development-security-stack-option-1.md)" -ge 3 && grep -q 'security-events: write' docs/development-security-stack-option-1.md && echo "permissions, category and retention now modelled"</automated>
    <automated>python3 - <<'PY'
import re
p = 'docs/development-security-stack-option-1.md'
src = open(p).read()
lines = src.splitlines()
fences = sum(1 for ln in lines if ln.startswith('```'))
assert fences % 2 == 0, 'odd number of code fences (%d) — a block was broken' % fences
i = next(n for n, ln in enumerate(lines) if ln.startswith('## Cross-Platform CI Compatibility'))
tbl = []
for ln in lines[i:]:
    if ln.startswith('|'):
        tbl.append(ln)
    elif tbl and not ln.strip():
        break
pipes = {ln.count('|') for ln in tbl}
assert len(pipes) == 1, 'ragged table: pipe counts %r' % pipes
rows = [ln for ln in tbl[2:]]
names = [ln.split('|')[1].strip() for ln in rows]
for want in ('npm audit', 'pip-audit', 'tflint'):
    assert any(want.lower() in n.lower() for n in names), 'missing row: %s (%r)' % (want, names)
for keep in ('Semgrep CE', 'Checkov', 'Trivy', 'Grype', 'Syft', 'Gitleaks'):
    assert any(keep.lower() in n.lower() for n in names), 'row removed: %s' % keep
assert len(rows) == 9, 'expected 6 original + 3 new rows, got %d' % len(rows)
print('fences balanced (%d), CI-compatibility table has 9 rows with uniform pipe count' % fences)
PY</automated>
    <automated>grep -q 'ADR-016\|adr016' docs/development-security-stack-option-1.md && test -f .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md && for s in DIST-06 'cicd/' Grype 'milestone-2' 'push: branches'; do grep -q -- "$s" .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md || { echo "deferred-items missing: $s"; exit 1; }; done; echo "deferrals recorded with owners"</automated>
    <automated>git diff HEAD~1 --name-only | sort | grep -vE '^(docs/development-security-stack-option-1.md|.planning/)' | grep -q . && { echo "FAIL: unexpected file in commit"; exit 1; } || echo "commit scoped to the blueprint and planning docs"</automated>
    <automated>markdownlint docs/development-security-stack-option-1.md docs/adr/adr016-sarif-upload-attribution-and-artifact-retention.md 2>/dev/null || echo "NOTE: markdownlint not available or reported issues — record its exact output"</automated>
  </verify>
  <acceptance_criteria>
    - The blueprint's `security.yml` example contains a workflow-level `permissions:` block with `contents: read`, `security-events: write` and `actions: read`, each carrying a `# Required for …` comment, plus a comment warning that a copy-pasted CALLER must grant `security-events: write` on its calling job
    - All three `upload-sarif@<SHA>` lines now read `# v4` and none reads `# v3`; all five `upload-artifact@<SHA>` lines now read `# v7` and none reads `# v4`; every `<SHA>` placeholder is preserved and no live 40-hex SHA was pasted into the blueprint
    - Every `upload-sarif` example carries a `category:`, rendered consistently across all of them, with a comment stating why a distinct category is mandatory
    - Every `upload-artifact` example carries `retention-days:` with a comment on the first stating the 90-day ceiling
    - The `## Cross-Platform CI Compatibility` table has exactly 9 rows — the 6 originals unchanged plus npm audit, pip-audit and tflint — with a uniform pipe count across every table line, and a note below it stating that the two SARIF-less tools reach the artifact set rather than the Security tab, linking ADR-016
    - The count of code fences in the blueprint is even (no fenced block was broken); the language matrix at ~L1720 and its note are untouched
    - `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` exists and names all six deferrals with an owning phase each: the `cicd/` mirror, the blueprint Grype `sca` job, the blueprint's `push: branches: [main]` divergence, milestone-2 M2-F3, the private-repo licence limitation, and the SARIF size limits
    - The commit touches only `docs/development-security-stack-option-1.md` and files under `.planning/`
  </acceptance_criteria>
  <done>The primary artifact now teaches a workflow that would actually work when copy-pasted, and every surface left stale on purpose is named with an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blueprint → any repo that copy-pastes it | The document is the adoption path; a permission block missing here becomes a broken control in someone else's repo |
| ADR record → future phases | Accepted ADRs are immutable inputs to Phases 18-20; a wrong claim here propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-25 | Tampering (of guidance) | A live 40-hex SHA pasted into the blueprint, which then goes stale and teaches a stale pin (ASVS V14) | mitigate | `@<SHA>` placeholders preserved per ADR-004; only the version comments change, asserted by grep counts |
| T-17-26 | Repudiation | The blueprint teaching a workflow that 403s, with no visible failure — the adoption-scale form of Pitfalls 1+2 | mitigate | Workflow-level `permissions:` block added plus an explicit caller-side warning comment |
| T-17-27 | Tampering | An unbounded blueprint rewrite silently dropping a matrix row or breaking a fenced block | mitigate | Invariant checks: even fence count, uniform pipe count, exact 9-row table, all six original rows asserted present |
| T-17-28 | Information Disclosure | ADR-016 overstating what was verified, so Phase 18/19 build on a false premise | mitigate | Explicit "not verified" paragraph naming Dependabot grantability and annotation rendering; every measured claim traceable to RESEARCH or a SUMMARY |
| T-17-29 | Tampering | Modifying an accepted, immutable ADR | mitigate | `git status` assertion that ADR-001, ADR-004 and ADR-015 are unmodified; ADR-016 is a new file |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Documentation only — zero installs, zero executable content |
</threat_model>

<verification>
- ADR-016 exists, is Accepted, covers every decided value, and names what was not verified.
- `docs/adr/README.md` has one Index table with exactly one new row, appended last.
- Blueprint: 3 × `# v4` on `upload-sarif`, 5 × `# v7` on `upload-artifact`, zero stale comments, placeholders intact.
- Blueprint: `permissions:` block present; `category:` on every SARIF example; `retention-days:` on every artifact example.
- CI-compatibility table at exactly 9 rows with uniform pipe count; fence count even.
- `deferred-items.md` names all six deferrals with owners.
- Two commits, both `docs(17-06):`.
</verification>

<success_criteria>
- The project's primary artifact no longer contradicts what CI does, and no longer teaches a workflow that
  cannot upload.
- The phase's decisions — including both user-locked ones — survive outside the plan files, in an
  append-only record that also states its own limits.
- Nothing was silently left stale.
</success_criteria>

<output>
Create `.planning/phases/17-sarif-upload-and-artifact-retention/17-06-SUMMARY.md` when done.
Record: the ADR-016 filename and its Decision bullet list; the exact grep counts before and after for the
version comments; the CI-compatibility table's row count before and after; and the full deferral list with
owning phases.
</output>
