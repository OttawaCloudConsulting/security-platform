---
phase: 03-web-and-config-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repos/aws-zabbix-monitoring-solution/eslint.config.mjs
  - repos/aws-zabbix-monitoring-solution/package.json
  - repos/aws-zabbix-monitoring-solution/package-lock.json
  - repos/aws-zabbix-monitoring-solution/lib/**/*.ts
  - repos/aws-zabbix-monitoring-solution/lambda/**/*.ts
  - repos/aws-zabbix-monitoring-solution/test/**/*.ts
  - repos/aws-zabbix-monitoring-solution/bin/cdk-zabbix.ts
autonomous: true
requirements: [LINT-03]

must_haves:
  truths:
    - "ESLint is installed and configured with typescript-eslint recommended preset"
    - "All 36 TypeScript files pass ESLint with zero violations"
    - "pre-commit run eslint --all-files exits with code 0"
    - "Committing a TS file with a deliberate violation (e.g. const x: any = 1) triggers an ESLint error"
  artifacts:
    - path: "repos/aws-zabbix-monitoring-solution/eslint.config.mjs"
      provides: "ESLint flat config with recommended presets and ignores"
      contains: "tseslint.configs.recommended"
    - path: "repos/aws-zabbix-monitoring-solution/package.json"
      provides: "ESLint devDependencies"
      contains: "eslint"
  key_links:
    - from: ".pre-commit-config.yaml (eslint local hook)"
      to: "eslint.config.mjs"
      via: "npx eslint reads flat config from project root"
      pattern: "entry: npx eslint"
---

<objective>
Install ESLint with typescript-eslint, create flat config, and fix all existing violations across 36 TypeScript files so the ESLint pre-commit hook passes cleanly on all files.

Purpose: Satisfies LINT-03 — ESLint flags TS/JS linting issues on every commit.
Output: Working ESLint hook with zero violations on --all-files.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-and-config-hooks/03-CONTEXT.md
@.planning/phases/03-web-and-config-hooks/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ESLint dependencies and create flat config</name>
  <files>
    repos/aws-zabbix-monitoring-solution/eslint.config.mjs
    repos/aws-zabbix-monitoring-solution/package.json
  </files>
  <read_first>
    repos/aws-zabbix-monitoring-solution/package.json
    repos/aws-zabbix-monitoring-solution/tsconfig.json
    repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml
  </read_first>
  <action>
1. Install ESLint devDependencies in the target repo:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   npm install --save-dev eslint @eslint/js typescript-eslint
   ```

2. Create `eslint.config.mjs` in the project root (alongside `package.json`) with this exact content:
   ```javascript
   // @ts-check
   import eslint from '@eslint/js';
   import tseslint from 'typescript-eslint';

   export default tseslint.config(
     eslint.configs.recommended,
     tseslint.configs.recommended,
     {
       ignores: ['cdk.out/**', 'jest.config.js'],
     },
   );
   ```

   Key points per user decisions:
   - Use flat config format (`eslint.config.mjs`) — NOT legacy `.eslintrc`
   - Use `@typescript-eslint/recommended` preset via `tseslint.configs.recommended`
   - Ignore `cdk.out/**` (generated CDK output) and `jest.config.js` (CommonJS file)
   - Do NOT ignore `node_modules` (ESLint ignores it by default in flat config)
   - Lint ALL files including test files — no exclusions per user decision

3. Verify ESLint can load the config and run:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   npx eslint --version
   npx eslint lib/config.ts 2>&1 | head -20
   ```
   This confirms the config is found and ESLint can parse TypeScript files.
  </action>
  <verify>
    <automated>cd repos/aws-zabbix-monitoring-solution && npx eslint --version && test -f eslint.config.mjs && node -e "const p = require('./package.json'); if (!p.devDependencies.eslint || !p.devDependencies['@eslint/js'] || !p.devDependencies['typescript-eslint']) { process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - repos/aws-zabbix-monitoring-solution/eslint.config.mjs exists
    - eslint.config.mjs contains "tseslint.configs.recommended"
    - eslint.config.mjs contains "eslint.configs.recommended"
    - eslint.config.mjs contains "'cdk.out/**'" in ignores
    - eslint.config.mjs contains "'jest.config.js'" in ignores
    - package.json devDependencies contains "eslint"
    - package.json devDependencies contains "@eslint/js"
    - package.json devDependencies contains "typescript-eslint"
    - `npx eslint --version` exits 0
  </acceptance_criteria>
  <done>ESLint installed, flat config created, ESLint can parse TypeScript files</done>
</task>

<task type="auto">
  <name>Task 2: Fix all ESLint violations and verify hook passes</name>
  <files>
    repos/aws-zabbix-monitoring-solution/lib/**/*.ts
    repos/aws-zabbix-monitoring-solution/lambda/**/*.ts
    repos/aws-zabbix-monitoring-solution/test/**/*.ts
    repos/aws-zabbix-monitoring-solution/bin/cdk-zabbix.ts
  </files>
  <read_first>
    repos/aws-zabbix-monitoring-solution/eslint.config.mjs
    .planning/phases/03-web-and-config-hooks/03-RESEARCH.md
  </read_first>
  <action>
1. Run ESLint across all files to get the full violation list:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   npx eslint . 2>&1
   ```

2. Fix all violations systematically. Expected violation types from research:
   - `@typescript-eslint/no-explicit-any`: Replace `any` with proper types, or suppress with `// eslint-disable-next-line @typescript-eslint/no-explicit-any` where the type is genuinely unknown (e.g., CloudFormation construct props, Lambda event payloads)
   - `@typescript-eslint/no-unused-vars`: Remove unused imports/variables, or prefix unused function parameters with underscore (`_param`)
   - `@typescript-eslint/no-require-imports`: Should not appear in TS files (jest.config.js is excluded)
   - `no-undef`: Should be handled by typescript-eslint overrides; if it fires on TypeScript globals, investigate

3. Follow the Phase 2 pattern for suppressions:
   - Fix violations where possible (add types, remove unused imports, prefix params with `_`)
   - Suppress with inline `// eslint-disable-next-line <rule-name>` ONLY where fixing would change meaningful behavior
   - Do NOT use file-level or block-level disables

4. After fixing, verify zero violations:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   npx eslint . 2>&1
   ```

5. Run the ESLint pre-commit hook to confirm it passes:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   pre-commit run eslint --all-files
   ```

6. Run existing tests to confirm fixes did not break anything:
   ```bash
   cd repos/aws-zabbix-monitoring-solution
   npm test
   ```
  </action>
  <verify>
    <automated>cd repos/aws-zabbix-monitoring-solution && npx eslint . && pre-commit run eslint --all-files && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint .` exits with code 0 (zero violations)
    - `pre-commit run eslint --all-files` exits with code 0 (hook passes)
    - `npm test` exits with code 0 (existing tests still pass)
    - Any suppression comments use the format `// eslint-disable-next-line <rule-name>`
  </acceptance_criteria>
  <done>All TypeScript files pass ESLint with zero violations, pre-commit eslint hook passes on --all-files, existing tests unbroken</done>
</task>

</tasks>

<verification>
1. `cd repos/aws-zabbix-monitoring-solution && npx eslint . && echo "ESLint clean"` — zero violations
2. `cd repos/aws-zabbix-monitoring-solution && pre-commit run eslint --all-files` — hook passes
3. `cd repos/aws-zabbix-monitoring-solution && npm test` — existing tests pass
</verification>

<success_criteria>
- LINT-03 satisfied: ESLint hook flags TS/JS linting issues on every commit
- eslint.config.mjs exists with typescript-eslint recommended preset
- All 36 TS files pass ESLint cleanly
- pre-commit run eslint --all-files exits 0
- npm test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-and-config-hooks/03-01-SUMMARY.md`
</output>
