---
phase: 01-pre-commit-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repos/security-platform/.pre-commit-config.yaml
  - repos/security-platform/.gitignore
  - repos/security-platform/README.md
  - repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml
autonomous: false
requirements: [PCOM-01, PCOM-02, PCOM-03]

must_haves:
  truths:
    - "pre-commit --version returns 4.5.0 or later"
    - ".pre-commit-config.yaml exists in security-platform repo on feature branch and is committed"
    - ".pre-commit-config.yaml exists in aws-zabbix-monitoring-solution repo on feature branch and is committed"
    - "pre-commit validate-config succeeds against the config file in both repos"
    - "git commit in target repo triggers pre-commit hook execution"
  artifacts:
    - path: "repos/security-platform/.pre-commit-config.yaml"
      provides: "Canonical pre-commit config with all Tier 1 + Tier 2 hooks"
      contains: "repos:"
    - path: "repos/security-platform/.gitignore"
      provides: "Standard gitignore for implementation repo"
    - path: "repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml"
      provides: "Deployed copy of canonical pre-commit config"
      contains: "repos:"
  key_links:
    - from: "repos/aws-zabbix-monitoring-solution/.git/hooks/pre-commit"
      to: "pre-commit framework"
      via: "pre-commit install"
      pattern: "pre-commit"
    - from: "repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml"
      to: "repos/security-platform/.pre-commit-config.yaml"
      via: "identical copy"
      pattern: "repos:"
---

<objective>
Install the pre-commit framework configuration in both the canonical source repo (security-platform) and the first target repo (aws-zabbix-monitoring-solution), activate hooks, and verify that git commit triggers hook execution.

Purpose: Establishes the git hook framework that all subsequent phases (2-5, 9) depend on. Without this, no automated quality or security checks run on commit.
Output: Two repos with identical .pre-commit-config.yaml committed on feature branches, hooks activated in target repo, verified by test commit.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-commit-framework/01-CONTEXT.md
@.planning/phases/01-pre-commit-framework/01-RESEARCH.md

Key facts from research:
- pre-commit 4.5.0 is already installed on the workstation (verified)
- security-platform repo is on branch `feature/m1-workstation-foundation`, has only `.git` (empty repo, one init commit)
- aws-zabbix-monitoring-solution repo is on `main`, needs a feature branch created
- Target repo has `package.json` and `package-lock.json` -- run `npm install` before hook activation so ESLint/npm-audit local hooks can resolve
- The complete .pre-commit-config.yaml content is in `docs/development-security-stack-option-1.md` lines 1302-1384
- Do NOT run `pre-commit run --all-files` in Phase 1 -- individual hook validation is Phases 2-5
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize security-platform repo with canonical pre-commit config</name>
  <files>
    repos/security-platform/.pre-commit-config.yaml
    repos/security-platform/.gitignore
    repos/security-platform/README.md
  </files>
  <read_first>
    docs/development-security-stack-option-1.md (lines 1302-1384 for exact config content)
    .planning/phases/01-pre-commit-framework/01-CONTEXT.md (locked decisions about repo structure)
  </read_first>
  <action>
Working in `repos/security-platform/` on the existing `feature/m1-workstation-foundation` branch:

1. Create `.gitignore` with standard entries:
```
# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# pre-commit
.pre-commit-cache/
```

2. Create `.pre-commit-config.yaml` with the EXACT content from `docs/development-security-stack-option-1.md` lines 1302-1384. The file must contain:
   - Tier 1 header comment block
   - 8 repo entries for Tier 1: pre-commit-terraform (terraform_fmt, terraform_validate), ruff-pre-commit (ruff --fix, ruff-format), shellcheck-py (shellcheck), hadolint (hadolint-docker), yamllint (yamllint -d relaxed), markdownlint-cli (markdownlint), local eslint hook, local npm-audit hook
   - Tier 2 header comment block
   - 1 repo entry for Tier 2: gitleaks (gitleaks)
   - Version pins: terraform v1.96.0, ruff v0.8.4, shellcheck v0.10.0.1, hadolint v2.12.0, yamllint v1.35.1, markdownlint v0.43.0, gitleaks v8.21.2

3. Create `README.md` with brief description:
```markdown
# security-platform

Canonical security configuration for OttawaCloudConsulting repositories.

## Contents

- `.pre-commit-config.yaml` — Pre-commit hook configuration (Tier 1 quality + Tier 2 secrets)

## Usage

Copy `.pre-commit-config.yaml` to target repository root, then:

    cd <target-repo>
    pre-commit install
    pre-commit run --all-files
```

4. Validate config syntax: `pre-commit validate-config .pre-commit-config.yaml`

5. Git add and commit all three files on `feature/m1-workstation-foundation`:
   `git add .gitignore .pre-commit-config.yaml README.md`
   `git commit -m "feat: add canonical pre-commit config with Tier 1 and Tier 2 hooks"`
  </action>
  <verify>
    <automated>cd repos/security-platform && pre-commit validate-config .pre-commit-config.yaml && git log --oneline -1 | grep -q "pre-commit" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - repos/security-platform/.pre-commit-config.yaml contains "repos:" as first non-comment YAML key
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v1.96.0" (terraform)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v0.8.4" (ruff)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v0.10.0.1" (shellcheck)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v2.12.0" (hadolint)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v1.35.1" (yamllint)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v0.43.0" (markdownlint)
    - repos/security-platform/.pre-commit-config.yaml contains "rev: v8.21.2" (gitleaks)
    - repos/security-platform/.pre-commit-config.yaml contains "id: eslint" (local hook)
    - repos/security-platform/.pre-commit-config.yaml contains "id: npm-audit" (local hook)
    - repos/security-platform/.gitignore contains ".DS_Store"
    - repos/security-platform/README.md contains "security-platform"
    - `pre-commit validate-config repos/security-platform/.pre-commit-config.yaml` exits 0
    - `git -C repos/security-platform log --oneline -1` contains "pre-commit"
  </acceptance_criteria>
  <done>
    security-platform repo initialized on feature/m1-workstation-foundation with .gitignore, README.md, and .pre-commit-config.yaml committed. Config validates cleanly with pre-commit validate-config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy config to target repo, install hooks, and verify execution</name>
  <files>
    repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml
  </files>
  <read_first>
    repos/security-platform/.pre-commit-config.yaml (canonical source just created in Task 1)
    repos/aws-zabbix-monitoring-solution/package.json (to verify npm install target)
    .planning/phases/01-pre-commit-framework/01-RESEARCH.md (pitfalls about npm install prerequisite)
  </read_first>
  <action>
Working in `repos/aws-zabbix-monitoring-solution/`:

1. Create feature branch: `git checkout -b feature/add-pre-commit`

2. Run `npm install` to populate node_modules (required for ESLint and npm-audit local hooks to resolve). Verify `node_modules/.bin/eslint` exists after install.

3. Copy the IDENTICAL `.pre-commit-config.yaml` from `repos/security-platform/.pre-commit-config.yaml` to `repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml`. Use `cp` to ensure byte-identical copy.

4. Validate config: `pre-commit validate-config .pre-commit-config.yaml`

5. Install hooks: `pre-commit install` -- this creates `.git/hooks/pre-commit`

6. Verify hook installation: `ls -la .git/hooks/pre-commit` should show the hook file exists

7. Git add and commit the config file:
   `git add .pre-commit-config.yaml`
   `git commit -m "feat: add pre-commit hook configuration"`
   This commit itself will trigger pre-commit hooks -- some hooks may fail (expected, validation is Phases 2-5). If hooks fail on this commit, use `git commit --no-verify -m "feat: add pre-commit hook configuration (hooks validated in Phases 2-5)"` and note which hooks failed in the summary.

8. Verify the commit exists: `git log --oneline -1` should show the commit message
  </action>
  <verify>
    <automated>cd repos/aws-zabbix-monitoring-solution && pre-commit validate-config .pre-commit-config.yaml && test -f .git/hooks/pre-commit && git log --oneline -1 | grep -q "pre-commit" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml exists and is identical to repos/security-platform/.pre-commit-config.yaml
    - `pre-commit validate-config repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml` exits 0
    - repos/aws-zabbix-monitoring-solution/.git/hooks/pre-commit exists (created by `pre-commit install`)
    - `git -C repos/aws-zabbix-monitoring-solution log --oneline -1` contains "pre-commit"
    - `git -C repos/aws-zabbix-monitoring-solution branch --show-current` returns "feature/add-pre-commit"
    - repos/aws-zabbix-monitoring-solution/node_modules/.bin/eslint exists (npm install completed)
  </acceptance_criteria>
  <done>
    Target repo has .pre-commit-config.yaml committed on feature/add-pre-commit branch. Hooks are installed via pre-commit install. Git commit triggered hook execution (some hooks may fail -- that is expected and validated in Phases 2-5).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify hooks trigger on commit</name>
  <what-built>
    Pre-commit framework installed in both repos:
    1. security-platform repo: canonical .pre-commit-config.yaml committed on feature/m1-workstation-foundation
    2. aws-zabbix-monitoring-solution repo: config deployed, hooks installed, committed on feature/add-pre-commit
  </what-built>
  <how-to-verify>
    1. Run `pre-commit --version` -- should show 4.5.0 or later (PCOM-01)
    2. Run `cat repos/security-platform/.pre-commit-config.yaml | head -5` -- should show the config header (PCOM-02)
    3. Run `cat repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml | head -5` -- should show identical config (PCOM-02)
    4. Run `cd repos/aws-zabbix-monitoring-solution && ls .git/hooks/pre-commit` -- should show hook file exists (PCOM-03)
    5. Optionally: create a trivial file change, stage it, and run `pre-commit run` to see hooks execute (PCOM-03)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 1 requirements verification:
- PCOM-01: `pre-commit --version` returns version >= 4.5.0
- PCOM-02: `.pre-commit-config.yaml` exists and is committed in both repos, contains all 9 Tier 1 hooks + 1 Tier 2 hook
- PCOM-03: `pre-commit install` has been run in target repo, `.git/hooks/pre-commit` exists, git commit triggers hooks
</verification>

<success_criteria>
1. `pre-commit --version` succeeds (PCOM-01)
2. `.pre-commit-config.yaml` is committed in both repos with all 10 hook entries (PCOM-02)
3. `pre-commit install` activated hooks in target repo -- `.git/hooks/pre-commit` exists (PCOM-03)
4. A git commit in the target repo triggers pre-commit hook output (PCOM-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-commit-framework/01-01-SUMMARY.md`
</output>
