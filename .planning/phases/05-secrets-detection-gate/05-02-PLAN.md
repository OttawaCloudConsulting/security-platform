---
phase: 05-secrets-detection-gate
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docs/development-security-stack-option-1.md
  - repos/security-platform/README.md
autonomous: true
requirements: [SECR-02, SECR-03]

must_haves:
  truths:
    - "A commit containing dummy AWS key AKIAIOSFODNN7EXAMPLE is blocked on push by Gitleaks pre-push hook"
    - "Developer can find --no-verify bypass documentation in three locations: main doc, README, inline config"
    - "CI is identified as the compensating control per ADR-011"
  artifacts:
    - path: "docs/development-security-stack-option-1.md"
      provides: "Bypass and compensating controls documentation"
      contains: "--no-verify"
    - path: "repos/security-platform/README.md"
      provides: "Developer-facing bypass guidance"
      contains: "--no-verify"
  key_links:
    - from: "docs/development-security-stack-option-1.md"
      to: "docs/adr/adr011-precommit-bypass-warning.md"
      via: "ADR-011 reference"
      pattern: "ADR-011"
---

<objective>
Verify end-to-end that Gitleaks blocks a dummy AWS key on push (SECR-02), and document the --no-verify bypass with CI as compensating control in three locations (SECR-03).

Purpose: SECR-02 proves the pre-push hook actually blocks secrets. SECR-03 ensures the developer understands the hook can be bypassed and what compensates for that bypass. These are the remaining two requirements for the secrets detection gate.

Output: Verified SECR-02 test result, bypass documentation in main doc + README + inline config comments.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-secrets-detection-gate/05-CONTEXT.md
@.planning/phases/05-secrets-detection-gate/05-RESEARCH.md
@.planning/phases/05-secrets-detection-gate/05-01-SUMMARY.md
@docs/adr/adr011-precommit-bypass-warning.md

<interfaces>
<!-- ADR-011 key content for bypass documentation -->
From docs/adr/adr011-precommit-bypass-warning.md:
- `git commit --no-verify` and `git push --no-verify` bypass all hooks including Gitleaks
- CI/CD layer (Phase 2 / M2) is the compensating server-side control
- Gitleaks runs in GitHub Actions against full git history on every PR and push
- Branch protection + required CI status checks = non-bypassable enforcement

<!-- Current Gitleaks section in main doc (lines 1372-1384) -->
From docs/development-security-stack-option-1.md:
```
# TIER 2: Secrets Gate
# Secrets are the only pre-commit security check. A credential pushed to any
# branch is a potential exposure regardless of context. SAST (Semgrep CE) and
# IaC scanning (Checkov) run at the Pull Request gate in GitHub Actions instead.

  # --- Secrets detection: Gitleaks ---
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
```

<!-- Gitleaks CLI section (lines 358-378) -->
From docs/development-security-stack-option-1.md:
```
### 5. Gitleaks — Secrets Detection
Purpose-built secrets scanner. Scans current files and full git history. No account, CLI-only.
Setup:
  brew install gitleaks
  gitleaks detect --source .
  gitleaks detect --source . --log-opts="--all"
  gitleaks detect --source . --report-path gitleaks-report.json --report-format json
  gitleaks protect --staged
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end dummy AWS key test (SECR-02)</name>
  <read_first>
    - repos/security-platform/.pre-commit-config.yaml (verify stages/args from Plan 01)
    - repos/security-platform/.git/hooks/pre-push (verify hook exists)
    - .planning/phases/05-secrets-detection-gate/05-RESEARCH.md (SECR-02 test pattern section)
  </read_first>
  <files>none (test-only, all artifacts cleaned up)</files>
  <action>
Perform the SECR-02 verification in the security-platform repo (or aws-zabbix -- whichever has a remote configured for push). The test follows the Phase 3 hadolint temp-file pattern:

1. Create a temp test file with a dummy AWS key:
   ```bash
   cd repos/security-platform
   echo 'AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"' > test-secret.txt
   ```

2. Stage and commit the file:
   ```bash
   git add test-secret.txt
   git commit -m "test: dummy secret for SECR-02 verification"
   ```

3. Attempt to push. The Gitleaks pre-push hook MUST block this:
   ```bash
   git push 2>&1
   ```
   Expected: Non-zero exit code, output containing "gitleaks" and indication of a detected secret (rule: `aws-access-token` or similar). The push must NOT succeed.

   IMPORTANT: If there is no remote configured, use `pre-commit run gitleaks --hook-stage pre-push` as an alternative verification that gitleaks detects the staged dummy key.

4. Clean up regardless of result:
   ```bash
   git reset --soft HEAD~1
   rm -f test-secret.txt
   git checkout -- . 2>/dev/null
   ```

5. Verify cleanup:
   ```bash
   git log --oneline -3  # test commit must NOT appear
   ls test-secret.txt 2>/dev/null  # must not exist
   ```

Record the exact gitleaks output showing the block for the SUMMARY.
  </action>
  <verify>
    <automated>cd repos/security-platform && test ! -f test-secret.txt && git log --oneline -1 | grep -v "SECR-02"</automated>
  </verify>
  <acceptance_criteria>
    - Gitleaks detected the dummy AWS key `AKIAIOSFODNN7EXAMPLE` and blocked the push (or flagged it in pre-commit run)
    - `test-secret.txt` does not exist after cleanup
    - The test commit does not appear in `git log`
    - The gitleaks output referenced rule `aws-access-token` or similar AWS key pattern
  </acceptance_criteria>
  <done>SECR-02 verified: dummy AWS key AKIAIOSFODNN7EXAMPLE is blocked by Gitleaks pre-push hook, test artifacts fully cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: Document --no-verify bypass and CI compensating control (SECR-03)</name>
  <read_first>
    - docs/development-security-stack-option-1.md (full file -- need to find Gitleaks sections and identify insertion points)
    - docs/adr/adr011-precommit-bypass-warning.md
    - repos/security-platform/README.md
    - repos/security-platform/.pre-commit-config.yaml (verify inline comments from Plan 01)
  </read_first>
  <files>docs/development-security-stack-option-1.md, repos/security-platform/README.md</files>
  <action>
Document the `--no-verify` bypass and CI compensating control in three locations per CONTEXT.md locked decision:

**Location 1: `docs/development-security-stack-option-1.md`**

Find the Tier 2 Secrets Gate section (around line 1372). After the `.pre-commit-config.yaml` code block for Gitleaks (which should now show `stages: [pre-push]` and `args: [protect, --staged]`), add a "Bypass and Compensating Controls" subsection. Update the code block to match the reconfigured hook (add `stages: [pre-push]` and `args: [protect, --staged]`, update rev to match the autoupdated version from Plan 01).

Add this content after the Gitleaks hook code block:

```markdown
#### Bypass and Compensating Controls

> **Warning:** `git push --no-verify` bypasses the Gitleaks pre-push hook entirely. This is a Git feature — no special permissions required.

The pre-commit/pre-push framework is defense-in-depth: it catches secrets before they reach the remote, but it is not the sole enforcement mechanism. The CI/CD pipeline (Milestone 2) runs Gitleaks against the full git history on every pull request and push to protected branches. Because CI runs server-side in GitHub Actions, it cannot be bypassed from the developer workstation.

The enforcement chain:
1. **Pre-push hook (this section)** — client-side, bypassable with `--no-verify`
2. **CI/CD Gitleaks scan (M2)** — server-side, non-bypassable
3. **Branch protection + required status checks** — prevents merge without passing CI

See [ADR-011](docs/adr/adr011-precommit-bypass-warning.md) for the full rationale.
```

Also update the Gitleaks CLI section (around line 358-378) to add a note about `protect --staged` being used in the pre-push hook.

**Location 2: `repos/security-platform/README.md`**

Add a "Secrets Detection" section to the README with developer-facing bypass guidance:

```markdown
## Secrets Detection

Gitleaks runs as a **pre-push hook** — it scans staged changes for credentials and secrets before they reach the remote repository.

**If Gitleaks blocks your push:**
1. Review the finding — is it a real secret or a false positive?
2. Real secret: remove it, rotate the credential, then push again
3. False positive: add the fingerprint to `.gitleaksignore` (see format in that file)

**Bypass:** `git push --no-verify` skips the hook. Use only when you've verified the finding is a false positive and can't immediately update `.gitleaksignore`. CI will re-scan server-side (see ADR-011).
```

**Location 3: Inline config comments (verify from Plan 01)**

Verify that `repos/security-platform/.pre-commit-config.yaml` already has the bypass comment from Plan 01:
```yaml
  # Bypass: git push --no-verify skips this hook — CI is the compensating control (see ADR-011)
```
If missing, add it. Also verify the same comment exists in `repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml` and `repos/terraform-pipelines/.pre-commit-config.yaml`. If missing in target repos, add it.

Commit documentation changes. For `docs/development-security-stack-option-1.md`, commit in the security_solution repo. For `repos/security-platform/README.md`, commit in the security-platform repo.
  </action>
  <verify>
    <automated>grep -c "no-verify" docs/development-security-stack-option-1.md && grep -c "ADR-011" docs/development-security-stack-option-1.md && grep -c "no-verify" repos/security-platform/README.md && grep "no-verify" repos/security-platform/.pre-commit-config.yaml && grep "no-verify" repos/aws-zabbix-monitoring-solution/.pre-commit-config.yaml && grep "no-verify" repos/terraform-pipelines/.pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `docs/development-security-stack-option-1.md` contains `--no-verify` bypass warning text
    - `docs/development-security-stack-option-1.md` contains `ADR-011` reference in the bypass section
    - `docs/development-security-stack-option-1.md` Gitleaks hook code block shows `stages: [pre-push]` and `args: [protect, --staged]`
    - `repos/security-platform/README.md` contains `## Secrets Detection` section
    - `repos/security-platform/README.md` contains `--no-verify` bypass guidance
    - All three `.pre-commit-config.yaml` files contain inline comment mentioning `--no-verify` and `ADR-011`
    - `docs/development-security-stack-option-1.md` Gitleaks hook code block rev matches the autoupdated version (not v8.21.2)
  </acceptance_criteria>
  <done>SECR-03 complete: --no-verify bypass and CI compensating control documented in main doc (with ADR-011 reference), security-platform README, and inline config comments in all three repos</done>
</task>

</tasks>

<verification>
- SECR-02: Gitleaks blocked a dummy AWS key `AKIAIOSFODNN7EXAMPLE` on push attempt (recorded in summary)
- SECR-03: `grep --no-verify` finds matches in all three documentation locations:
  1. `docs/development-security-stack-option-1.md` — bypass section with ADR-011 reference
  2. `repos/security-platform/README.md` — developer-facing guidance
  3. All three `.pre-commit-config.yaml` files — inline comments
</verification>

<success_criteria>
SECR-02 proven: dummy AWS key blocked on push. SECR-03 satisfied: bypass documentation exists in main doc, README, and inline config comments, all referencing ADR-011 and identifying CI as the compensating control.
</success_criteria>

<output>
After completion, create `.planning/phases/05-secrets-detection-gate/05-02-SUMMARY.md`
</output>
