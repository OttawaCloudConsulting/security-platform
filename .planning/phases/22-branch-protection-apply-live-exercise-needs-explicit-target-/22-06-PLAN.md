---
phase: 22-branch-protection-apply-live-exercise-needs-explicit-target
plan: 06
type: execute
wave: 6
depends_on: [22-05]
files_modified:
  - docs/adr/adr019-required-check-enforcement-live-exercise.md
  - docs/adr/README.md
  - docs/adoption-guide.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-VALIDATION.md
autonomous: true
requirements: [VAL-02, DIST-08]

must_haves:
  truths:
    - "A new ADR-019 records the measured exercise, closing ADR-017 item 4 and ADR-018 item 7 by reference only — never by editing them"
    - "ADR-019 states that the closure is partial, because those two items are scoped to security-platform's own main and the exercise was run on terraform-pipelines"
    - "The adoption guide's branch-protection section reports the apply path as measured rather than as unexercised guidance, and the standing gate stays green"
    - "VAL-02 is marked complete only after the refusal was witnessed and the ruleset restored"
  artifacts:
    - path: "docs/adr/adr019-required-check-enforcement-live-exercise.md"
      provides: "The architectural record of the first live --apply and the witnessed refusal"
      contains: "## What was NOT verified"
    - path: "docs/adr/README.md"
      provides: "ADR index with the ADR-019 row appended"
      contains: "ADR-019"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VAL-02 marked complete and mapped to Phase 22"
      contains: "VAL-02"
  key_links:
    - from: "docs/adr/adr019-required-check-enforcement-live-exercise.md"
      to: "docs/adr/adr017-configurable-gate-mode-and-required-checks.md and docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md"
      via: "reference by identifier and item number — append-only, never an edit to either file"
      pattern: "ADR-017|ADR-018"
    - from: "docs/adoption-guide.md section 8"
      to: "scripts/check-adoption-guide.sh"
      via: "the standing gate that derives the five contexts from the host clone's YAML"
      pattern: "PASSED"
---

<objective>
Record what was measured. Write ADR-019 as a NEW file carrying the exercise's three merge verdicts and
GitHub's verbatim refusal, index it, strengthen the adoption guide's branch-protection section from
guidance to measurement where the exercise earned it, and mark VAL-02 complete.

Purpose: the v2.0 milestone audit's one UNVERIFIED flow is only closed when the record says so in the
place the project keeps its decisions. ADR-017's "What was NOT verified" item 4 and ADR-018's item 7
both state that `set-required-checks.sh --apply` has never been run. Those files are append-only per
this repository's CLAUDE.md, so they are closed by reference from a new record — and the closure is
partial, because both items are scoped to `security-platform`'s own `main`, which this exercise did
not touch.

Output: ADR-019, one index row, a measured adoption-guide section, VAL-02 ticked, and the phase's
validation map populated.

Precondition: plan 05 recorded `diff rules-before.txt rules-restored.txt` as empty. Do not mark VAL-02
complete against a target repository that is still in an exercise state.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md

Read before writing:

- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-03-SUMMARY.md`,
  `22-04-SUMMARY.md` and `22-05-SUMMARY.md` — every number, SHA, run id and quoted string in ADR-019
  comes from these. Do not restate a measurement from memory; copy it from the summary that measured
  it, or re-run the command.
- `docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md` — the shape convention: a
  four-line header with no frontmatter, then exactly four level-two headings in fixed order.
- `docs/adr/adr017-configurable-gate-mode-and-required-checks.md` — its Consequences voice, and item 4
  of its "What was NOT verified" section, quoted in ADR-019 rather than edited.
- `docs/adr/README.md` — the index table. Append one row; never reorder, never edit a prior row.
- `docs/adoption-guide.md` section 8 (lines 340–416) — the section this plan may strengthen, including
  the self-lockout warning and the exit-code list.
- `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-PATTERNS.md`,
  the ADR and REQUIREMENTS pattern assignments — the four REQUIREMENTS edit points and the exact ADR
  section order and voice.

<interfaces>
ADR-019 file name: docs/adr/adr019-required-check-enforcement-live-exercise.md

Header block, four lines, no frontmatter, matching adr018 lines 1-6:
  a level-one heading naming the ADR number and title
  a Status line reading Accepted
  a Date line
  an Addresses line naming VAL-02 followed by an em-dash gloss

Section order, exactly four level-two headings and no others:
  Context
  Decision
  Consequences
  What was NOT verified

Index row columns in docs/adr/README.md: ADR | Title | Date | Status. Link text is the ADR number,
target is the bare filename with no leading ./, Status is Accepted.
</interfaces>

<scope_boundaries>
- ADRs in `docs/adr/` are APPEND-ONLY per this repository's CLAUDE.md. Do NOT edit
  `adr017-configurable-gate-mode-and-required-checks.md` or
  `adr018-workflow-packaging-canonical-host-and-versioning.md`. Not to add a "superseded" note, not to
  tick an item. Quote them; reference them; leave them alone.
- Do NOT relitigate ADR-017's `leave-unrequired` decision for `security-platform`. It was made on
  measured grounds. This exercise ran on a different repository and narrows those items rather than
  overturning them.
- Preserve the blueprint's ASCII diagrams, four-phase layered structure and tool coverage matrices. If
  `development-security-stack-option-1.md` needs no change, do not invent one.
- Any adoption-guide edit is surgical and gated: `bash scripts/check-adoption-guide.sh` must still
  report 15 passed / 0 failed afterwards. That gate derives the five contexts from the host clone's
  YAML, so a mangled em dash fails it rather than shipping.
- Do NOT claim the exercise closed ADR-017 item 4 or ADR-018 item 7 in full. Both are written about
  `security-platform`'s own `main`.
</scope_boundaries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-019 and index it</name>
  <files>docs/adr/adr019-required-check-enforcement-live-exercise.md, docs/adr/README.md</files>
  <read_first>
    - docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md — the whole file. It is the most recent ADR and therefore the current form of the convention: header block, four section headings, the bolded-lead-clause voice in Context, the Improved/Tradeoff shape in Consequences, and the numbered list under What was NOT verified.
    - docs/adr/adr017-configurable-gate-mode-and-required-checks.md lines 49–58 — item 4, the text this ADR closes by reference.
    - docs/adr/README.md — the index table and its last two rows.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-04-SUMMARY.md — the three verdicts, the refusal text, the run ids, the SHAs and the tree hash that go inline.
  </read_first>
  <action>
    Create `docs/adr/adr019-required-check-enforcement-live-exercise.md` as a NEW file. Status
    Accepted, dated the day the exercise ran, addressing VAL-02.

    Context carries measured facts with their measurement attached, each bullet leading with a bolded
    clause, in adr018's voice: the milestone audit's single UNVERIFIED flow; that `--apply` had never
    been run against any repository before this phase; that `terraform-pipelines` was chosen because it
    produces a natural red check from its own Semgrep findings and needs no seeded vulnerability; that
    its `main` carries no `.github/workflows/` at all, which is why the required checks could not be
    left in place.

    Consequences leads with an Improved paragraph carrying the whole measurement inline, in ADR-017's
    sentence shape but with three MERGE verdicts substituted for three check verdicts: one pull request,
    three commits sharing one tree hash, the three head SHAs, the three run ids, and the three
    `mergeStateStatus` values in order. Then the refusal itself, quoted verbatim — GitHub's own words
    are the deliverable and they belong in the ADR, not in a linked file. Then Tradeoff paragraphs: the
    checks were required only for a bounded window and were restored, so the pilot repository's steady
    state is unchanged; the code-scanning analyses and the closed pull request persist and no git revert
    touches them; and the blocking window was repository-wide for its duration, which ADR-017 already
    records as a structural tradeoff of the `GATE_MODE` mechanism.

    Enumerate in Decision, or in Consequences where it reads better, the four assumptions this exercise
    retires by measurement rather than by argument, each with the observation that retired it: the
    exact `pull_request` parameter set GitHub accepted on the PUT (ADR-017's item 4); whether
    `copilot_code_review` round-tripped through PUT on a repository carrying that preview rule type;
    whether the existing token scope sufficed for a ruleset write; and the literal text `gh pr merge`
    emits when it declines. If the `--apply` had returned a 422 instead, that is a finding about
    read-modify-write on a repository carrying a preview rule type and belongs here in exactly the same
    place — the ADR records what happened, not what was hoped for.

    What was NOT verified opens with a must-not-be-relitigated paragraph and then a numbered list. Its
    first item is the scoping statement this ADR exists to be honest about: ADR-017 item 4 and ADR-018
    item 7 are both written about `security-platform`'s own `main`, which this exercise did not touch,
    so this record NARROWS them — it proves the mechanism works and that the script's `--apply` path is
    live-exercised, while `security-platform`'s own `main` remains un-required, which is still ADR-017's
    standing decision on measured grounds. Quote item 4's text so a reader does not have to open the
    other file to see what was closed. Carry forward whatever the exercise still could not answer: the
    end state on `security-platform`, whether a `--restore` flag belongs in the script (deferred, with
    its cost stated — a commit in a separate repository, an adoption-guide update, a gate re-run), and
    anything plan 04 recorded as unresolved.

    Then append exactly one row to `docs/adr/README.md`, matching the existing column shape. Never
    reorder, never edit a prior row.
  </action>
  <verify>
    <automated>test -f docs/adr/adr019-required-check-enforcement-live-exercise.md &amp;&amp; grep -c '^## ' docs/adr/adr019-required-check-enforcement-live-exercise.md &amp;&amp; grep -q 'ADR-019' docs/adr/README.md &amp;&amp; git diff --stat -- docs/adr/adr017-configurable-gate-mode-and-required-checks.md docs/adr/adr018-workflow-packaging-canonical-host-and-versioning.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/adr019-required-check-enforcement-live-exercise.md` exists, is NEW, and has exactly four level-two headings in the order Context, Decision, Consequences, What was NOT verified (`grep -c '^## '` returns 4)
    - The header block is four lines with no YAML frontmatter, and the Addresses line names VAL-02
    - The Consequences section quotes the refusal text VERBATIM and carries the three head SHAs, the shared tree hash, the three run ids and the three merge states inline — not as links to evidence files
    - The four retired assumptions are each named with the observation that retired them
    - What was NOT verified states explicitly that ADR-017 item 4 and ADR-018 item 7 are NARROWED rather than fully closed, because both are scoped to `security-platform`'s own `main`, and quotes item 4's text
    - `git diff` against `adr017-*.md` and `adr018-*.md` is EMPTY — neither append-only file was modified
    - `docs/adr/README.md` has exactly one new row, appended last, with the same four columns and link form as the ADR-018 row; no prior row changed
  </acceptance_criteria>
  <done>The project's decision record says, in its own conventional form, that a required check was made required and GitHub refused a merge because of it — and says honestly which half of the two older items that closes.</done>
</task>

<task type="auto">
  <name>Task 2: Update the adoption guide, close VAL-02, and populate the validation map</name>
  <files>docs/adoption-guide.md, .planning/REQUIREMENTS.md, .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-VALIDATION.md</files>
  <read_first>
    - docs/adoption-guide.md lines 340–416 — section 8 in full, including the exit-code list, the self-lockout warning and the sentence about refusals being features rather than friction.
    - scripts/check-adoption-guide.sh lines 1–40 — the three-way exit contract and how the five contexts are derived from the host clone's YAML rather than hard-coded, so an edit that mangles an em dash fails the gate.
    - .planning/REQUIREMENTS.md — the VAL-02 entry and traceability row plan 01 added, both still open.
    - .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-VALIDATION.md — the eight per-task rows whose Task ID, Plan and Wave columns this plan finalises against what actually ran.
  </read_first>
  <action>
    Strengthen section 8 of `docs/adoption-guide.md` where the exercise earned it, and only there. The
    honest edit is to replace hedged language about the apply path being unexercised with the
    measurement: the script's `--apply` has now been run live against a real repository's ruleset, the
    five contexts became required, a pull request with one red required check was refused, and the
    ruleset was restored from a captured before-document. Add the bounded-window-plus-mandatory-restore
    procedure this phase invented as the recommended shape for a first adoption on a repository whose
    default branch does not yet carry the workflow — that is a genuinely new piece of guidance and it is
    the phase's most reusable output for the remaining repositories. Keep it surgical: do not restructure
    the section, do not retype a context string, and do not soften the self-lockout warning, which the
    exercise confirmed rather than contradicted.

    Run `bash scripts/check-adoption-guide.sh` after the edit and record its output. It must report 15
    passed / 0 failed. If it fails, read which assertion failed before changing anything — Phase 21 hit
    a case where a hard line break split a literal string the gate matched on, and the fix was moving
    the line break, not rewording the sentence.

    Then close VAL-02 in `.planning/REQUIREMENTS.md`: flip its checkbox from unchecked to checked and
    its traceability row from In Progress to Complete. The count and footer edits were made in plan 01,
    so verify them rather than repeating them — the file should read 15 total, 15 mapped, 0 unmapped.
    This is the point at which marking is legitimate: the refusal was witnessed and the target
    repository was restored. STATE.md records two prior occasions where a requirement was marked
    complete from a plan whose deliverable actually shipped later and the mark had to be reverted; this
    plan is the one that ships it.

    Finally populate `.planning/phases/22-.../22-VALIDATION.md`. Replace every `TBD` in the Task ID,
    Plan and Wave columns with the real values from the plans that ran, and set each row's status from
    its actual result. Three rows need correcting rather than merely filling: the third-verdict row as
    drafted says `gh run rerun`, and the phase deliberately used an empty commit instead for the reason
    18-05 recorded, so rewrite that row's command to the empty-commit-plus-tree-hash form; the Wave 0
    bullet listing exits 2, 3, 4 and 5 should say that exit 3 is not offline-triggerable — the script's
    own merge cannot drop a pre-existing type, so only 2, 4 and 5 were rehearsed and that is what plan
    03 observed; and the manual-only row should name the plan-03 checkpoint that actually happened
    rather than a target-repo confirmation that the operator settled before planning began. Set
    `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter only if both are true, and
    tick the Validation Sign-Off boxes that the executed phase actually satisfied.
  </action>
  <verify>
    <automated>bash scripts/check-adoption-guide.sh &amp;&amp; grep -q '\- \[x\] \*\*VAL-02\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| VAL-02 | Phase 22 | Complete |' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'TBD' .planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-adoption-guide.sh` reports 15 passed / 0 failed after the edit, and its output is quoted in the SUMMARY
    - The section 8 diff is surgical: `git diff --stat -- docs/adoption-guide.md` shows a change confined to that section, no context string was retyped, and the self-lockout warning is intact
    - The bounded-window-plus-restore procedure is present in section 8 and names the condition that makes it necessary — a default branch that does not yet carry the workflow
    - `.planning/REQUIREMENTS.md` contains `- [x] **VAL-02**` and `| VAL-02 | Phase 22 | Complete |`, and its Coverage block reads 15 total, 15 mapped, 0 unmapped
    - `22-VALIDATION.md` contains no `TBD`; every row carries a real Task ID, Plan and Wave and a status that matches what the SUMMARYs recorded
    - The third-verdict row no longer says `gh run rerun`; it names the empty-commit re-trigger and the tree-hash invariant
    - The Wave 0 section states that exit 3 was not offline-triggerable and that 2, 4 and 5 were the rehearsed guards
    - `git diff` against `docs/adr/adr017-*.md` and `docs/adr/adr018-*.md` is still empty
  </acceptance_criteria>
  <done>The requirement register, the adoption guide and the phase's own validation contract all agree with what was measured, and the standing gate still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence to published guidance | Anything the adoption guide states as measured will be acted on by future repository rollouts |
| append-only ADR record to a new record | A new file may reference older ones; it may never modify them |
| requirement register to milestone completion | A prematurely ticked requirement makes a milestone audit lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-28 | Tampering | `docs/adr/adr017-*.md` and `adr018-*.md` | mitigate | Append-only enforced by CLAUDE.md and asserted by an empty `git diff` against both files in this plan's acceptance criteria; ADR-019 quotes and references, never edits |
| T-22-29 | Repudiation | overclaiming the closure of ADR-017 item 4 and ADR-018 item 7 | mitigate | ADR-019's "What was NOT verified" opens by stating both items are scoped to `security-platform`'s own `main` and that this exercise NARROWS rather than closes them; item 4's text is quoted so the scoping is checkable in place |
| T-22-30 | Tampering | `scripts/check-adoption-guide.sh` or a lint config edited to make a gate pass | mitigate | The gate script is not in this plan's `files_modified`; the acceptance criterion requires the gate to pass unmodified, and the five contexts are derived from the host clone's YAML rather than compared against hard-coded copies |
| T-22-31 | Repudiation | VAL-02 marked complete before the exercise was restored | mitigate | The plan's stated precondition is plan 05's empty restore diff; the STATE.md precedent of two reverted premature marks is cited in the task text |
| T-22-32 | Information Disclosure | evidence quoted inline in a public ADR | mitigate | Plan 05's `gho_`/`ghp_` grep covers every evidence file before this plan quotes from them; quoted values are SHAs, run ids, rule types and GitHub's own refusal text, none of which is secret |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs nothing |
</threat_model>

<verification>
- `bash scripts/check-adoption-guide.sh` reports 15 passed / 0 failed
- `bash repos/security-platform/scripts/check-workflow-uploads.sh` and
  `bash repos/security-platform/scripts/check-detector-parity.sh` both pass — the full suite for the phase gate
- `docs/adr/adr019-required-check-enforcement-live-exercise.md` exists with exactly four level-two headings
- `git diff` against `docs/adr/adr017-*.md` and `docs/adr/adr018-*.md` empty
- `.planning/REQUIREMENTS.md` shows VAL-02 checked, Complete, and a 15/15/0 Coverage block
- `22-VALIDATION.md` free of `TBD`
- `diff 22-evidence/rules-before.txt 22-evidence/rules-restored.txt` still empty
</verification>

<success_criteria>
- ADR-019 written as a new file in the project's ADR form, carrying the three verdicts and the verbatim refusal inline
- ADR-017 and ADR-018 untouched, with their relevant items closed by reference and explicitly narrowed rather than overclaimed
- Adoption guide section 8 reporting the apply path as measured, plus the bounded-window procedure, with the standing gate still green
- VAL-02 checked and mapped to Phase 22, with a 15/15/0 coverage block
- The phase's validation map populated with real task, plan and wave values and its two drafted defects corrected
</success_criteria>

<output>
Create `.planning/phases/22-branch-protection-apply-live-exercise-needs-explicit-target-/22-06-SUMMARY.md` when done.
Record: the ADR-019 filename and its four section headings, the index row as added, the adoption-guide
diff stat, the gate output verbatim, the REQUIREMENTS coverage block after the edit, and the
before/after of the three corrected VALIDATION rows.
</output>
