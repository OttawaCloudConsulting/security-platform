---
phase: 19-pipeline-validation-via-branch-target-prs
plan: 07
type: execute
wave: 7
depends_on: [19-06]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [VAL-01]

must_haves:
  truths:
    - "PR #10's out-of-band merge is recorded as a dated fact with its merge commit, not smoothed into the original narrative"
    - "D-04's main-landing requirement is verified satisfied from origin/main, by PR #10's merge, re-read live rather than inherited from a plan"
    - "The replacement validation PR's fate is an explicit operator decision, taken with both options and their consequences stated"
    - "The repository ends with no GATE_MODE variable, an unchanged main ruleset, and no open Phase 19 pull request"
    - "All four success criteria have recorded evidence with run ids, check-run names and alert numbers, spread across three pull requests, and SC2's departure from its literal wording is stated"
    - "The fork-PR gate-bypass answer and the fixture-permanence consequence are recorded and handed to Phase 20, not tested or solved here"
  artifacts:
    - path: ".planning/phases/19-pipeline-validation-via-branch-target-prs/19-07-SUMMARY.md"
      provides: "the PR #10 deviation record, the D-10 decision verbatim, the phase-close state of the repository, and the SC1-SC4 evidence index"
      contains: "VAL-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VAL-01 marked Complete"
      contains: "VAL-01"
  key_links:
    - from: "PR #10's merge commit"
      to: "fixtures/vulnerable.py and fixtures/secret.env on OttawaCloudConsulting/security-platform main"
      via: "an out-of-band operator merge, verified from origin/main rather than from the local tree"
      pattern: "fixtures/vulnerable.py"
    - from: "the replacement validation PR from plan 05"
      to: "merged or closed"
      via: "the operator's D-10 choice, executed and then verified from origin/main"
      pattern: "Push Protection"
---

<objective>
Record the one deviation this phase absorbed, put the one genuinely irreversible question left — merge or
close the replacement validation pull request (D-10) — in front of the operator instead of deciding it,
execute their choice, and close the phase with the repository verifiably back in its default state.

Purpose: D-10 says the validation PR is merged or closed "once all four success criteria are captured", and
leaves the merge-vs-close choice to Claude's discretion at execution time. Discretion in this project, on an
irreversible action against a shared live repository, means asking.

**What this plan must record rather than re-tell.** D-10's decision point was reached once already, outside
this phase's control: on 2026-09-13T22:34:18Z the operator merged PR #10 — the original long-lived D-05
validation PR — through the GitHub web UI (merge commit `80e91de`), after 19-04 closed SC1 and SC3 and before
SC2 or SC4 were measured, and its head branch was auto-deleted. Plans 05 and 06 were replanned around that:
SC2 and SC4 were captured on a replacement validation PR and a clean PR cut from the post-merge `main`. Two
consequences carry into this plan. First, D-04's "both fixtures must land on `main`" requirement was
satisfied by that merge and no longer constrains the merge-vs-close choice — but it must be VERIFIED from
`origin/main` here, not inherited from a plan's prose. Second, SC2's literal ROADMAP wording ("that SAME
pull request" as SC1's) is not satisfiable and was closed on its essential claim instead; the evidence index
must say so rather than imply otherwise. This is the third recorded instance of a pull request being resolved
outside the plan flow (PR #9 in Phase 18, now PR #10), and that pattern is itself a hand-off to Phase 20.

Output: the deviation on the record with its timestamps, the operator's decision recorded verbatim, the
decision executed and verified from `origin/main`, `VAL-01` marked complete against a four-row evidence index
naming three different pull requests, and a phase-close record naming the findings that belong to Phase 20
rather than to this phase.

Sequencing note: waves are strictly sequential. This is the final plan. All four criteria are already
captured — SC1 in 19-03 and SC3 in 19-04 (both on PR #10), SC2 in 19-05 (replacement PR), SC4 in 19-06
(clean PR).
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-CONTEXT.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-RESEARCH.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-03-SUMMARY.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-04-SUMMARY.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-05-SUMMARY.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/19-06-SUMMARY.md
@.planning/phases/19-pipeline-validation-via-branch-target-prs/deferred-items.md

<interfaces>
<!-- SLUG = OttawaCloudConsulting/security-platform                                                       -->
<!--                                                                                                      -->
<!-- TWO PULL REQUESTS MATTER HERE, AND THEY ARE NOT THE SAME ONE:                                         -->
<!--   PR #10  — the ORIGINAL D-05 validation PR. ALREADY MERGED, out of band, 2026-09-13T22:34:18Z,       -->
<!--             mergeCommit 80e91de, head branch feature/phase-19-pipeline-validation (deleted). Nothing  -->
<!--             to decide; it is VERIFIED and RECORDED here, never re-litigated.                           -->
<!--   the REPLACEMENT validation PR from plan 05 on feature/phase-19-gate-mode-proof — OPEN, carrying the -->
<!--             D-19-A push-protection correction to fixtures/README.md plus two empty re-trigger commits.-->
<!--             THIS is the PR the D-10 decision is about. Read its number from plan 05's SUMMARY and     -->
<!--             confirm it live; never hardcode it from this plan.                                         -->
<!--   the CLEAN PR from plan 06 on feature/phase-19-clean-pr — already CLOSED by 19-06.                    -->
<!--                                                                                                      -->
<!-- VERIFY EVERYTHING FROM origin/main, NEVER FROM THE LOCAL TREE. Phase 15-05, 16-07 and 18-08 all set   -->
<!--   this precedent, and 18-07 was caught out by exactly the kind of merge that happened again here:      -->
<!--     git -C repos/security-platform fetch origin --prune                                               -->
<!--     git -C repos/security-platform show origin/main:fixtures/vulnerable.py | head -1                  -->
<!--     git -C repos/security-platform show origin/main:fixtures/secret.env   | head -1                   -->
<!--     git -C repos/security-platform rev-parse origin/main origin/main^{tree}                           -->
<!--                                                                                                      -->
<!-- D-04 IS ALREADY SATISFIED — VERIFY IT, DO NOT ASSUME IT. Measured at replan time (2026-09-13), and    -->
<!--   this text is NOT the evidence: origin/main = 80e91de carries fixtures/vulnerable.py,                -->
<!--   fixtures/secret.env, the fixtures/README.md rows and the scripts/smoke-scans.sh rule-id assertions. -->
<!--   That removes D-04 as a constraint on the merge-vs-close choice; it does not remove the duty to      -->
<!--   confirm it live.                                                                                     -->
<!--                                                                                                      -->
<!-- PHASE-CLOSE ASSERTIONS:                                                                               -->
<!--   gh variable list -R "$SLUG"                                    -> must print NOTHING (D-09)         -->
<!--   gh api repos/$SLUG/rules/branches/main --jq '.[].type'         -> deletion, non_fast_forward only   -->
<!--   gh pr list -R "$SLUG" --state open                             -> no Phase 19 PR left open           -->
<!--                                                                                                      -->
<!-- THREE THINGS TO RECORD AND HAND ON, NOT TO TEST OR SOLVE:                                             -->
<!--   Q1 — a fork PR cannot read repository variables, so `vars.GATE_MODE` is "" and the `||` chain falls  -->
<!--        through to 'report-only': the gate SILENTLY FAILS OPEN on fork PRs. Source is a GitHub staff    -->
<!--        answer in an official community discussion, not a docs page, and the thread is unresolved since -->
<!--        Jan 2023. [CITED: github.com/orgs/community/discussions/44322] D-07 scopes every PR in this     -->
<!--        phase to the same repository, so this was deliberately NOT tested. A consumer repo that needs   -->
<!--        blocking on fork PRs must pass a literal `with: gate_mode: blocking`, which pr-security.yml's   -->
<!--        own comment already anticipates. Hand to Phase 20.                                              -->
<!--        DO NOT edit the stale "UNVERIFIED / handed to Phase 19" comment in pr-security.yml — it is a    -->
<!--        workflow file and CONTEXT's boundary excludes workflow edits. Hand that one-line fix on too.    -->
<!--   Pitfall 5 — fixtures/ is permanent, so every PR in this repo produces findings in all five jobs      -->
<!--        forever, and report-only is currently the ONLY state in which this repo's own PRs merge green.  -->
<!--        That makes D-09's revert not merely cautious sequencing. Phase 18 D-07 step 3 (required checks) -->
<!--        stays deferred. Hand to Phase 20.                                                               -->
<!--   PROCESS — twice now (PR #9 in Phase 18, PR #10 here) a pull request has been resolved outside the    -->
<!--        plan that owned the decision, and the second time it invalidated a planned measurement. Record  -->
<!--        it as an observation about how this project and its operator interact, with both dates, and     -->
<!--        hand it on. It is not a defect to fix inside this phase.                                         -->
</interfaces>

<notes>
**Do not decide D-10 on your own initiative.** Task 1 is where the operator decides, and it decides the fate
of the REPLACEMENT validation PR only. Do not run `gh pr merge` or `gh pr close` before their reply.

**PR #10 is not a decision, it is a record.** It is already merged. Present it as the current state with its
timestamp and merge commit, verify what it delivered, and move on. Do not offer an action against it and do
not re-argue whether it should have been merged.

**D-04 no longer constrains "close" — but only because the merge already happened.** Verify that from
`origin/main` before relying on it. If for any reason `origin/main` does NOT carry both fixtures, STOP: the
close option would then strand them again and the decision's options change.

**Verify from `origin/main`, never from the local working tree.** The local checkout may be on any branch and
15-05, 16-07 and 18-08 all set this precedent explicitly. The local repo also still carries a stale
`feature/phase-19-pipeline-validation` branch whose remote is gone — leave it alone.

**Do not touch the ruleset and do not set any repository variable.** Required-checks adoption is Phase 18
D-07 step 3 and stays deferred (D-09).

**Do not edit any workflow file**, including the stale `pr-security.yml` comment that names Phase 19 — it is
a workflow edit and CONTEXT's boundary excludes those. Record it as a one-line fix for Phase 20.

**Two commit streams.** The `REQUIREMENTS.md` edit and the SUMMARY belong to this outer docs repo. The PR
action is server-side on the inner repo.
</notes>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Record PR #10's out-of-band merge, then decide the replacement validation PR's fate</name>
  <files></files>
  <read_first>
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/19-03-SUMMARY.md, 19-04-SUMMARY.md, 19-05-SUMMARY.md and 19-06-SUMMARY.md (all four criteria's evidence, including plan 05's deviation record and the replacement PR's number — the decision is the last step of a completed phase and the operator should see what was proven)
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/19-CONTEXT.md (D-04 makes the fixtures permanent and D-10 leaves this choice to execution time — the two decisions that bound the options)
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/deferred-items.md §D-19-A (the push-protection correction carried by the replacement PR — what "close" would leave uncorrected on `main`)
  </read_first>
  <action>
    FIRST establish the record for BOTH pull requests, read live and never copied from a plan:

    1. `gh pr view 10 -R OttawaCloudConsulting/security-platform --json
       number,state,mergedAt,mergeCommit,headRefName`. Expect `MERGED`, `2026-09-13T22:34:18Z`, merge commit
       `80e91de…`, head branch `feature/phase-19-pipeline-validation`. Record all four verbatim and state the
       deviation plainly: this merge happened outside the plan flow, after SC1 and SC3 were captured and
       before SC2 or SC4 were measured, which is why plans 05 and 06 were replanned onto new pull requests.
       Present it as a recorded event, not as a decision to be revisited.
    2. `git -C repos/security-platform fetch origin --prune`, then `git show origin/main:fixtures/vulnerable.py
       | head -1` and `git show origin/main:fixtures/secret.env | head -1` — both must print the
       `INTENTIONALLY VULNERABLE` header. This is the live confirmation that PR #10's merge satisfied D-04.
       If either is missing, STOP and report: the options below change.
    3. The replacement validation PR from plan 05 (branch `feature/phase-19-gate-mode-proof`) — read its
       number and `gh pr view <N> --json state,mergedAt,mergeCommit,mergeable,mergeStateStatus`. If it has
       ALREADY been merged or closed by a human — which is now twice-precedented, PR #9 and PR #10 — present
       that as the current state, record it, and ask the operator only to confirm it rather than offering an
       action that is no longer available.

    Then summarise the four captured criteria in four lines, each with its pull request number and run id, so
    the decision is made against the finished evidence rather than in the abstract. Note in that summary that
    SC1 and SC3 sit on PR #10, SC2 on the replacement PR and SC4 on the clean PR, and that SC2's literal
    "that same pull request" wording was not satisfiable after PR #10's merge.

    Then present the two options below with their consequences stated plainly, and recommend `merge`. Then
    stop and wait. Do NOT run `gh pr merge` or `gh pr close` before the reply. Record the selection and its
    reasoning verbatim in the SUMMARY either way — a deliberate "close" is a decision, not an absence of one.
    If the operator selects `close`, ask in the same exchange who owns landing the D-19-A `fixtures/README.md`
    correction on `main`, and record that answer too.
  </action>
  <decision>
    Whether the REPLACEMENT Phase 19 validation pull request (branch `feature/phase-19-gate-mode-proof`,
    opened by plan 05 after PR #10 was merged out of band) against `OttawaCloudConsulting/security-platform`
    `main` is MERGED or CLOSED now that all four success criteria are captured (D-10).
  </decision>
  <context>
    All four criteria are captured: SC1 (a detection from each of the five jobs) in 19-03 and SC3 (one finding
    traced source → Security tab entry → retained artifact, with a human confirming the rendered alert) in
    19-04, both on PR #10; SC2 (one PR failing under blocking and passing under report-only on a
    byte-identical tree) in 19-05 on the replacement PR; and SC4 (a clean PR, five green) in 19-06 on a third,
    now-closed PR. The repository has no `GATE_MODE` variable and its `main` ruleset is unchanged.

    D-04 no longer shapes this choice. It required the two new fixtures to reach `main` "via this PR or a
    named follow-up", and PR #10's merge on 2026-09-13 already put `fixtures/vulnerable.py`,
    `fixtures/secret.env`, the `fixtures/README.md` rows and the `scripts/smoke-scans.sh` rule-id assertions
    on `main` — confirmed from `origin/main` in this task, not assumed. What the replacement PR still carries
    is the D-19-A correction to `fixtures/README.md`: the measured fact that a SERVER-SIDE GitHub Push
    Protection layer (`GH013`) blocked 19-03's push and that `git push --no-verify` cannot skip it, which the
    file on `main` currently omits. It also carries two empty commits whose only purpose was to re-trigger
    runs on an identical tree — they are part of the SC2 evidence trail.

    Note what merging does NOT do: `main` is never analysed by code scanning (ADR-016 D-02 keeps the trigger
    `pull_request`-only), so merging creates no alerts on the default branch. The `fixtures/secret.env`
    history consequence is already irreversible — it happened with PR #10's merge, not with this decision.
  </context>
  <options>
    <option id="merge">
      <name>RECOMMENDED — merge the replacement validation PR into `main`</name>
      <pros>Lands the D-19-A correction, so `fixtures/README.md` on `main` stops documenting `--no-verify` as the only obstacle and names the server-side control that actually blocked a push in this phase. Matches how Phases 15, 16, 17 and 18 each closed. Leaves no follow-up debt and no open Phase 19 pull request.</pros>
      <cons>The two empty re-trigger commits from 19-05 land in `main`'s history alongside the documentation commit — harmless, and they are the SC2 evidence trail.</cons>
    </option>
    <option id="close">
      <name>Close the PR without merging, and land the documentation correction separately</name>
      <pros>Keeps the two empty commits out of `main`'s history. The fixtures are already on `main` via PR #10, so closing strands nothing that D-04 required.</pros>
      <cons>`fixtures/README.md` on `main` keeps its incomplete bypass paragraph, so deferred item D-19-A stays open and needs a named owner in the same breath or it is never corrected. Phase 20 then inherits a fixtures README that understates the control stack the phase measured.</cons>
    </option>
  </options>
  <resume-signal>Select: merge, or close — and if close, name who lands the D-19-A correction.</resume-signal>
  <acceptance_criteria>
    - PR #10's `state`, `mergedAt`, `mergeCommit` and `headRefName` were read LIVE and recorded verbatim, presented as a recorded out-of-band event rather than as a decision to revisit
    - `git show origin/main:fixtures/vulnerable.py` and `…:fixtures/secret.env` were run in this task and both printed the `INTENTIONALLY VULNERABLE` header, confirming D-04 live before the options relied on it
    - The replacement PR's number was taken from plan 05's SUMMARY and confirmed live, and its `state`, `mergedAt`, `mergeCommit` and `mergeable` were read BEFORE the options were presented; an already-resolved state was surfaced rather than an unavailable action offered
    - The four captured criteria are summarised with their PR numbers and run ids, naming which PR carries which criterion, before the question
    - SC2's departure from its literal "that same pull request" wording is stated in the summary, not omitted
    - Both options are presented with their consequences, `merge` is recommended, and the `close` option's cost is stated as D-19-A rather than as stranded fixtures
    - The operator's selection and reasoning are recorded VERBATIM in the SUMMARY
    - If `close` was selected, the named owner for landing the D-19-A correction is recorded
    - No `gh pr merge` or `gh pr close` ran during this task
  </acceptance_criteria>
  <done>The out-of-band merge is on the record as a fact, and the one irreversible choice left in this phase was made by the operator, on the finished evidence, and recorded in their own words.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the decision, verify the repository's end state, and close the phase</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/19-07-SUMMARY.md (the PR #10 record and the operator's decision as recorded in Task 1 — execute what they chose, not what was recommended)
    - .planning/REQUIREMENTS.md (the VAL-01 line and the requirement-status table, so the mark-complete edit lands in both places the file tracks it)
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/19-RESEARCH.md §Open Questions, Q1 and §Common Pitfalls, Pitfall 5 (the two findings that are handed to Phase 20 with their citations, rather than tested or solved here)
    - .planning/phases/19-pipeline-validation-via-branch-target-prs/deferred-items.md (D-19-A's status after this decision, plus D-19-B through D-19-E, which stay open and are listed as handed on)
  </read_first>
  <action>
    Execute the operator's choice exactly, against the REPLACEMENT validation PR only. If `merge`: `gh pr
    merge <N> -R OttawaCloudConsulting/security-platform --merge` (a merge commit, matching how Phases 15, 16
    and 18 each closed). If `close`: `gh pr close <N>` and record the named owner for the D-19-A correction.
    If that PR was already resolved when Task 1 read it, execute nothing and record that. Never touch PR #10.

    Verify the outcome FROM `origin/main`, never from the local working tree: `git -C repos/security-platform
    fetch origin --prune`, then record `git rev-parse origin/main` and `git rev-parse origin/main^{tree}`, and
    the merge commit SHA from `gh pr view <N> --json mergeCommit` if the choice was merge.

    Verify D-04's landing independently of this decision, because it was satisfied by PR #10 and not by the
    replacement PR: `git show origin/main:fixtures/vulnerable.py | head -1` and `git show
    origin/main:fixtures/secret.env | head -1` must both print the byte-exact `INTENTIONALLY VULNERABLE`
    header; `fixtures/README.md` on `origin/main` must carry the `vulnerable.py` and `secret.env` Fixture
    Reference rows; and `scripts/smoke-scans.sh` on `origin/main` must carry the two 19-02 rule-id assertions
    (`eval-detected`, `aws-access-token`). Record PR #10's merge commit as the vehicle that delivered them.
    If the choice was `merge`, additionally confirm the D-19-A correction is now on `origin/main` by grepping
    `git show origin/main:fixtures/README.md` for `GH013` / `Push Protection`; if the choice was `close`,
    record that it is NOT there and name the owner instead.

    Assert the phase-close state of the repository: `gh variable list` prints nothing (D-09);
    `rules/branches/main` still returns only `deletion` and `non_fast_forward`, unchanged from 19-03's
    preflight; `gh pr list --state open` shows no Phase 19 pull request left open — the replacement PR
    resolved, the clean PR closed by 19-06, PR #10 long since merged. If any of these differs, record the
    actual value and STOP rather than correcting it silently.

    Mark `VAL-01` complete in `.planning/REQUIREMENTS.md` — both the checklist line and the requirement-status
    table row. Use `gsd-sdk query requirements.mark-complete` driven from this plan's `requirements`
    frontmatter if available; otherwise edit both places directly and say which method was used. Re-read the
    file afterwards and confirm both locations changed, since 17-01 recorded a case where a mark-complete was
    applied to requirements that had not actually shipped and had to be reverted.

    Write the SC1–SC4 evidence index into the SUMMARY: one row per criterion naming the plan that captured
    it, the PULL REQUEST number, the run id, and the specific artefact of proof (check-run conclusions, alert
    number and `html_url`, tree hash, or artifact name). The index spans THREE pull requests — SC1 and SC3 on
    PR #10, SC2 on the replacement PR, SC4 on the clean PR — and must state, in the SC2 row or immediately
    under the table, that SC2's literal ROADMAP wording ("that same pull request" as SC1's) became
    unsatisfiable when PR #10 was merged out of band, and that SC2 was closed on its essential claim instead:
    one pull request, two gate modes, opposite verdicts, byte-identical trees, with the same five seeded
    fixture categories in the scanned checkout. This index is what a verifier reads instead of re-running
    anything.

    Record the deviation itself as the first section of the SUMMARY, with both dates: PR #10 merged
    2026-09-13T22:34:18Z (merge commit `80e91de`, head branch auto-deleted) after 19-04 and before SC2 or SC4
    were measured, and PR #9 merged 2026-09-12T12:48:34Z before 18-08 ran. Two pull requests resolved outside
    the plan that owned the decision, the second of which invalidated a planned measurement and forced plans
    05 and 06 to be replanned. State it as an observation about how the work and the operator interact, hand
    it to Phase 20, and do not propose a process fix inside this phase.

    Record the two standing hand-offs to Phase 20 with their status stated honestly:
    Q1 — repository variables are not passed to fork-triggered workflows, so on a fork PR `vars.GATE_MODE` is
    empty and the chain falls through to `report-only`, silently failing open. Source is a GitHub staff answer
    in an official community discussion, not a documentation page, and the thread has been unresolved since
    January 2023 [CITED: github.com/orgs/community/discussions/44322]. This was deliberately NOT tested —
    D-07 scopes every PR in this phase to the same repository — and the consumer-template consequence is that
    a repo needing blocking on fork PRs must pass a literal `with: gate_mode: blocking`. Note also that
    `pr-security.yml` still carries a stale comment handing this question to Phase 19; correcting it is a
    workflow edit and therefore outside this phase's boundary, so it goes to Phase 20 as a one-line fix.
    Pitfall 5 — `fixtures/` is permanent on `main`, so every pull request in this repository produces findings
    in all five jobs forever and report-only is currently the only state in which its own PRs can merge green.
    This is why D-09's revert is not merely cautious sequencing, and why Phase 18 D-07 step 3 stays deferred.

    List the deferred items this phase leaves open (`deferred-items.md` D-19-B through D-19-E, plus D-19-A if
    the operator chose `close`) with their owners, so nothing in that file is silently dropped at phase close.

    Commit the outer-repo changes with message `docs(19-07): close phase 19 and mark VAL-01 complete`.
  </action>
  <verify>
    <automated>cd repos/security-platform && git fetch origin --prune --quiet && gh pr list -R OttawaCloudConsulting/security-platform --head feature/phase-19-gate-mode-proof --state all --json number,state,mergedAt,mergeCommit</automated>
    <automated>gh pr view 10 -R OttawaCloudConsulting/security-platform --json number,state,mergedAt,mergeCommit; echo "PR #10 is a RECORD, not an action — must read MERGED with commit 80e91de"</automated>
    <automated>cd repos/security-platform && git show origin/main:fixtures/vulnerable.py 2>&1 | head -1; git show origin/main:fixtures/secret.env 2>&1 | head -1; echo "--- both must print the INTENTIONALLY VULNERABLE header: D-04 landed via PR #10 ---"</automated>
    <automated>cd repos/security-platform && git show origin/main:fixtures/README.md | grep -c 'vulnerable\.py'; git show origin/main:fixtures/README.md | grep -c 'secret\.env'; git show origin/main:scripts/smoke-scans.sh | grep -c 'eval-detected'; git show origin/main:scripts/smoke-scans.sh | grep -c 'aws-access-token'; echo "documentation and gate assertions must be on main too, not stranded on a branch"</automated>
    <automated>cd repos/security-platform && git show origin/main:fixtures/README.md | grep -c 'GH013\|Push Protection'; echo "non-zero ONLY if the operator chose merge — if they chose close this must be 0 and an owner must be named in the SUMMARY"</automated>
    <automated>cd repos/security-platform && git rev-parse origin/main origin/main^{tree}</automated>
    <automated>gh variable list -R OttawaCloudConsulting/security-platform; echo "D-09: must print nothing"</automated>
    <automated>gh api repos/OttawaCloudConsulting/security-platform/rules/branches/main --jq '.[].type'; echo "must still be exactly deletion and non_fast_forward — unchanged from 19-03's preflight"</automated>
    <automated>gh pr list -R OttawaCloudConsulting/security-platform --state open --json number,title,headRefName; echo "no Phase 19 pull request may remain open"</automated>
    <automated>grep -n 'VAL-01' .planning/REQUIREMENTS.md; echo "both the checklist line and the status-table row must show complete"</automated>
    <automated>git -C . log --oneline -1; git -C . show --stat --oneline HEAD | head -10</automated>
  </verify>
  <acceptance_criteria>
    - The operator's chosen action was executed exactly against the REPLACEMENT validation PR — merge as a merge commit, or close with the named D-19-A owner recorded — and if it was already resolved, nothing was executed and that is stated
    - PR #10 was read but never acted on; its `MERGED` state and merge commit are recorded as the phase's deviation, with the date and the fact that it preceded SC2 and SC4
    - D-04 is verified FROM `origin/main` independently of this decision: both fixture files print the byte-exact `INTENTIONALLY VULNERABLE` header, `fixtures/README.md` carries the `vulnerable.py` and `secret.env` rows, and `scripts/smoke-scans.sh` carries `eval-detected` and `aws-access-token` — with PR #10's merge commit named as the vehicle
    - `origin/main`'s SHA and tree hash are recorded, plus the replacement PR's merge commit SHA if merged
    - The D-19-A correction's presence on `origin/main` is asserted either way — present if merged, absent with a named owner if closed
    - `gh variable list` prints nothing (D-09 satisfied at phase close)
    - `rules/branches/main` returns exactly `deletion` and `non_fast_forward`, unchanged from 19-03's preflight, and no ruleset write occurred in this phase
    - `gh pr list --state open` shows no Phase 19 pull request left open
    - `VAL-01` is marked complete in BOTH places `.planning/REQUIREMENTS.md` tracks it, verified by re-reading the file, with the method used stated
    - The SC1–SC4 evidence index has four rows naming the capturing plan, the PULL REQUEST number, the run id and the specific proof artefact, spanning three pull requests, and states SC2's departure from its literal wording with the essential claim it was closed on instead
    - The deviation section carries both out-of-band merges with their dates — PR #9 (2026-09-12T12:48:34Z) and PR #10 (2026-09-13T22:34:18Z) — and is handed to Phase 20 as an observation rather than fixed here
    - The Q1 fork-PR finding is recorded with its citation, with "deliberately not tested" stated and the consumer-template consequence and the stale `pr-security.yml` comment both handed to Phase 20
    - The Pitfall 5 fixture-permanence finding is recorded with its consequence for Phase 18 D-07 step 3
    - Every still-open item in `deferred-items.md` is listed at phase close with an owner
    - No workflow file was edited anywhere in this phase
  </acceptance_criteria>
  <done>The operator's decision is executed and verified from origin/main, the out-of-band merge is on the record with its date, VAL-01 is complete against a four-row index spanning three pull requests, and the repository sits in its default state — no GATE_MODE variable, an unchanged ruleset, and no open Phase 19 pull request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → `main` of a shared live public repository | A merge is irreversible in practice |
| Operator → `main` outside the plan flow | A human merge through the web UI can invalidate a planned measurement; the phase record must absorb it rather than re-tell the plan as written |
| Local working tree → claims about `origin/main` | The local checkout may be on any branch and still carries a stale branch whose remote is gone; only `git show origin/main:…` is evidence |
| Phase record → downstream phases | An unrecorded open question becomes an untested assumption in Phase 20's consumer template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-30 | Elevation of Privilege | Claude merging or closing a PR on a shared repository on its own initiative | mitigate | A blocking `checkpoint:decision` precedes any action; the plan forbids `gh pr merge`/`gh pr close` before the operator's reply; the reply is recorded verbatim; PR #10 is explicitly read-only here. |
| T-19-31 | Repudiation | A merge claimed from the local working tree, which may be on any branch and may be stale | mitigate | Every post-merge assertion reads `git show origin/main:…` after an explicit `git fetch --prune`; `origin/main` SHA, tree hash and the merge commit SHA are recorded. Precedent: 15-05, 16-07, 18-08. |
| T-19-32 | Information Disclosure | `fixtures/secret.env` is permanent in the history of every future branch on a PUBLIC repository | accept | Intended by D-04 and already irreversible through PR #10's merge, which this plan records rather than authorises. The values are synthetic; `--redact` remains on every gitleaks invocation so no value is republished in a world-downloadable artifact. |
| T-19-33 | Tampering | The phase closing with `GATE_MODE` still set or the `main` ruleset silently changed | mitigate | Both are asserted at phase close: `gh variable list` empty, `rules/branches/main` returning exactly the two types 19-03 recorded. A difference stops the plan rather than being corrected silently. |
| T-19-34 | Spoofing | `VAL-01` marked complete against evidence that was never captured | mitigate | The mark-complete is paired with a four-row SC1–SC4 evidence index naming plan, PULL REQUEST, run id and proof artefact, and the file is re-read after the edit. 17-01's reverted mark-complete is the precedent for the re-read. |
| T-19-35 | Elevation of Privilege | The fork-PR gate bypass left unrecorded, so Phase 20 ships a consumer template that silently fails open | mitigate | Recorded with its citation, its unresolved status, and the literal `with: gate_mode:` workaround, and explicitly handed to Phase 20 — not tested here, since D-07 scopes every PR in this phase to the same repository. |
| T-19-39 | Repudiation | The phase record reading as though PR #10 were merged by plan 07 as designed, hiding that SC2's and SC4's measurement conditions changed underneath the phase | mitigate | The deviation is the FIRST section of the SUMMARY with both dates and merge commits, Task 1 reads PR #10's state live before presenting anything, and the SC2 row states its departure from the literal ROADMAP wording. |
| T-19-40 | Spoofing | D-04 recorded as satisfied on the strength of this plan's own prose, which was written at replan time and not at execution time | mitigate | Task 2 re-runs `git show origin/main:…` for both fixtures, the README rows and the smoke-gate assertions, and records PR #10's merge commit as the vehicle; a missing file stops the plan. |
| T-19-SC | Tampering | Supply chain — package installs | mitigate | No package installs; the only file edited is `.planning/REQUIREMENTS.md` in the outer docs repo. No dependency manifest, workflow or action pin is touched anywhere in this phase. |
</threat_model>

<verification>
- PR #10's `MERGED` state, `mergedAt` and merge commit are read live and recorded as the phase's deviation, alongside PR #9's
- The operator's D-10 selection for the REPLACEMENT validation PR is recorded verbatim and was made before any `gh pr merge`/`gh pr close` ran
- `git show origin/main:fixtures/vulnerable.py` and `…:fixtures/secret.env` print the byte-exact header; `fixtures/README.md` and `scripts/smoke-scans.sh` on `origin/main` carry the 19-02 changes — D-04 verified live, attributed to PR #10
- The D-19-A correction is present on `origin/main` (merge path) or its absence is recorded with a named owner (close path)
- `gh variable list -R OttawaCloudConsulting/security-platform` prints nothing
- `rules/branches/main` returns exactly `deletion` and `non_fast_forward`
- No Phase 19 pull request is left open
- `VAL-01` shows complete in both places `.planning/REQUIREMENTS.md` tracks it
- The SUMMARY carries a four-row SC1–SC4 evidence index spanning three pull requests, the SC2 wording note, the deviation section, the two Phase 20 hand-offs with citations, and the still-open deferred items with owners
</verification>

<success_criteria>
The phase closes on the record it actually produced: PR #10's out-of-band merge is documented as a dated fact
with its merge commit and its effect on the measurement plan; D-04 is verified satisfied from `origin/main`
and attributed to that merge; the replacement validation PR's fate was decided by the operator on the
finished evidence and executed exactly as chosen; the repository ends with no `GATE_MODE` variable, an
unchanged `main` ruleset and no open Phase 19 pull request; `VAL-01` is complete against a four-row evidence
index spanning three pull requests that states plainly where SC2 departed from its literal wording; and the
fork-PR gate bypass, the fixture-permanence consequence and the out-of-band-merge pattern are recorded with
their citations and handed to Phase 20 rather than tested or solved here.
</success_criteria>

<output>
Create `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-07-SUMMARY.md` when done. It must
open with the deviation record — PR #10 merged out of band on 2026-09-13T22:34:18Z (merge commit `80e91de`,
head branch deleted) before SC2 or SC4 were measured, PR #9 merged similarly on 2026-09-12T12:48:34Z, and
what each cost. It must then contain: the replacement PR's pre-decision state as read live; the operator's
selection and reasoning verbatim; the executed action and its `origin/main` verification (fixture headers,
README rows, smoke-gate assertions, the D-19-A correction's presence or absence, `origin/main` SHA and tree
hash, merge commit SHA); the phase-close assertion table (empty variable list, unchanged ruleset, no open
Phase 19 PRs); the four-row SC1–SC4 evidence index naming three pull requests with the SC2 wording note; the
`VAL-01` mark-complete with the method used; the still-open deferred items with owners; and the Phase 20
hand-offs with their citations and their "not tested / not solved here" status stated plainly.
</output>
