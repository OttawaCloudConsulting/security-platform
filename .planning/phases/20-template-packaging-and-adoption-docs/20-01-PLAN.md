---
phase: 20-template-packaging-and-adoption-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DIST-06, DIST-07]

must_haves:
  truths:
    - "Whether github/codeql-action/upload-sarif SUCCEEDS or FAILS on a private repository in this account is a measured fact, not an inference (settles assumption A1)"
    - "The pilot repositories for the SC1 and SC2 live proofs are named and operator-confirmed (settles Q4)"
    - "The Q2 disposition for the six SARIF verify steps follows from the measurement — guard if the upload fails, leave untouched if it succeeds"
    - "No file in security_solution or repos/security-platform is modified by this plan"
  artifacts:
    - path: ".planning/phases/20-template-packaging-and-adoption-docs/20-01-SUMMARY.md"
      provides: "A1 measurement (run id, step outcome, analyses endpoint response), Q4 pilot confirmation, Q2 disposition with the exact guard expression plan 04 must apply or omit"
      contains: "upload outcome="
  key_links:
    - from: "the probe workflow's GITHUB_TOKEN on a PRIVATE repository"
      to: "the code-scanning SARIF ingestion endpoint"
      via: "github/codeql-action/upload-sarif with security-events: write granted"
      pattern: "upload outcome="
---

<objective>
Settle the two questions that gate the canonical workflow's shape before a single line of it is edited:
does `upload-sarif` actually fail on a private repository in this account (RESEARCH assumption A1, the
premise under Pitfall 1 and Open Question Q2), and which repositories are the SC1/SC2 pilots (Q4).

Purpose: RESEARCH measured only the code-scanning **read** endpoint (403 on the private
`aws-zabbix-monitoring-solution`, 404 "no analysis found" on the public `terraform-pipelines`). Whether the
**upload** 403s was inferred, never measured. That inference is load-bearing: if uploads fail, the six
`Verify … SARIF upload landed` steps turn every private consumer's job red regardless of `gate_mode`, and
plan 04 must add a capability guard before `v1` is tagged. If uploads succeed, adding that guard would
silently skip a working assertion — a broken-but-green regression dressed as a fix. Tagging first and
discovering this later costs a `v1.0.1` and invalidates the adoption guide's private-repo section.

Output: a measurement, an operator-confirmed pilot list, and a written disposition that plan 04 executes
verbatim. No tracked file changes.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-VALIDATION.md

**Two-repo / three-repo warning.** This phase spans `security_solution` (this repo — documentation) and
`repos/security-platform` (a separate clone with its own remote — the canonical workflow host under the
AMENDED D-01). This plan touches **neither**. It operates only against pilot repositories via `gh` and a
throwaway branch in one of them. Do not edit any file in either tracked repo here.

**Parallel-worktree warning.** If this plan runs in an isolated git worktree, `repos/` (gitignored, one line
in `.gitignore`) will be **absent**. This plan does not need it. Pilot repositories are reached through
`gh`/`git clone` into the session scratchpad, never through `repos/`.

**Already decided — do not re-open.** D-01 (AMENDED): `OttawaCloudConsulting/security-platform` is the
canonical host; nothing is published from `security_solution`. Q1 is RESOLVED — no `gh repo create`, no
`origin` re-point, no history push, no gitleaks history preflight. RESEARCH C-2, C-6, Pitfall 7, Pattern 4
and Q5 are MOOT or INVERTED per the reconciliation table in 20-PATTERNS.md.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the pilot repositories and authorise live activity in them (Q4)</name>
  <read_first>
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md` §Open Questions Q4 and §Runtime State Inventory (ruleset ids `12760793` on `terraform-pipelines`, `[]` on `aws-zabbix-monitoring-solution`)
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-VALIDATION.md` §Manual-Only Verifications
  </read_first>
  <files>none — this task asks, waits and records; it edits no file</files>
  <action>Present the decision below to the operator with the three options and their measured tradeoffs, then STOP and wait. Do not pick an option, do not proceed to Task 2, and do not touch any pilot repository before the reply arrives. When it arrives, quote it verbatim into the SUMMARY and carry the named repositories forward as the pilots for every later plan in this phase.</action>
  <verify>
    <automated>bash -c 'grep -qiE "option-(a|b|c)" .planning/phases/20-template-packaging-and-adoption-docs/20-01-SUMMARY.md || { echo "no operator selection recorded"; exit 1; }'</automated>
  </verify>
  <done>The operator has named the public pilot, the private pilot (or none), and the SUMMARY quotes the reply verbatim.</done>
  <decision>
    Which repositories does this phase touch for its live proofs, and is branch/PR/workflow-run activity in
    them authorised?
  </decision>
  <context>
    Every live criterion in this phase (SC1 copy-paste proof, SC2 `uses:` proof, and the A1/Q2 private-repo
    measurement in Task 2 below) requires opening a branch and running GitHub Actions inside a repository
    that is NOT one of this phase's two owned repos. RESEARCH probed two candidates live: both are already
    cloned under `repos/`, neither has a `.github/` directory (no workflow filename collision), and their
    measured shapes differ usefully — `terraform-pipelines` is PUBLIC with 36 `.tf` files, zero
    `package-lock.json`, zero `requirements*.txt` and zero Dockerfiles (so it exercises the public/SARIF
    path, tflint findings, and three clean ecosystem skips including the new container skip);
    `aws-zabbix-monitoring-solution` is PRIVATE with one `package-lock.json` (so it exercises the private
    403 path). Branch protection is explicitly out of scope for these pilots — see the constraint below.
  </context>
  <options>
    <option id="option-a">
      <name>RESEARCH Q4 recommendation (default)</name>
      <pros>Both already cloned and probed; public/private split covers every measured failure mode; no new repo needed. `terraform-pipelines` carries an existing ruleset to read (never write).</pros>
      <cons>Two of the operator's real repositories carry a probe branch and one or two pull requests until closed.</cons>
    </option>
    <option id="option-b">
      <name>Different pilot(s) named by the operator</name>
      <pros>Operator may prefer a lower-traffic or throwaway repo.</pros>
      <cons>Needs re-probing (visibility, ruleset state, ecosystems present) before plan 10 can assert anything; RESEARCH's measured facts would not transfer.</cons>
    </option>
    <option id="option-c">
      <name>Public pilot only — skip the private measurement</name>
      <pros>Nothing is run inside a private repo.</pros>
      <cons>A1 stays unmeasured, so Q2 can only be answered by RESEARCH's own mitigation (c) ("scope SC2 to public consumers, record private adoption as a follow-up"). The adoption guide's private-repo section then ships as an unverified inference, and 37 of the account's 60 repos are private.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The operator's reply names, explicitly, the repository used for the SC1/SC2 proofs and the repository (or none) used for the A1 probe.
    - The chosen option is recorded in `20-01-SUMMARY.md` with the operator's reply quoted verbatim.
    - If option-c is chosen, the SUMMARY states that Task 2 is SKIPPED, that A1 remains UNMEASURED, and that plan 04's guard task is therefore NOT to be applied (RESEARCH mitigation (c) taken instead) — and plan 08/09 must label the guide's private-repo section as inferred.
    - No branch protection / ruleset write and no `gh variable set` is authorised by this checkpoint for any pilot. STATE records required-checks adoption as deferred past this phase; a ruleset `--apply` on a pilot is a production change with the Pitfall 5 lockout risk and is explicitly out of scope.
  </acceptance_criteria>
  <resume-signal>Reply with `option-a`, `option-b` plus the repository names, or `option-c`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Measure whether upload-sarif succeeds on a private repository (A1)</name>
  <files>none tracked — a throwaway branch in the private pilot repository, deleted before the task ends</files>
  <read_first>
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md` §Common Pitfalls → Pitfall 1, and §Assumptions Log rows A1 and A5
    - `repos/security-platform/.github/workflows/security.yml` lines 100-120 (the `Verify Semgrep SARIF upload landed` step — the exact assertion shape whose fate this measurement decides) and lines 90-99 (the `Upload … SARIF` step's pinned action and `continue-on-error`)
    - `.planning/phases/17-sarif-upload-and-artifact-retention/17-05-SUMMARY.md` — the local PAT carries `gist, read:org, repo, workflow` and **no** `security_events` scope; every code-scanning read succeeded anyway because `security-platform` is PUBLIC
  </read_first>
  <action>
    Skip this task entirely if Task 1 returned `option-c`; record the skip and its consequence in the SUMMARY.

    Measure the capability with a workflow run, not with a local API call. A direct
    `POST /repos/{owner}/{repo}/code-scanning/sarifs` from the CLI is INVALID as evidence here: the local
    token has no `security_events` scope (17-05), so a 403 would measure the token, not the repository.
    Only the in-workflow `GITHUB_TOKEN` with `security-events: write` granted is a clean measurement.

    Clone the private pilot into the session scratchpad directory (never into `repos/`, never into this
    repo's tree). Cut a branch named `chore/phase-20-sarif-capability-probe` from `origin/main`. Add one
    file, `.github/workflows/sarif-capability-probe.yml`, triggered `on: push` (NOT `workflow_dispatch` —
    dispatch from a non-default branch is not discoverable without extra steps). Grant it exactly
    `contents: read`, `security-events: write`, `actions: read` at job level. Its steps, in order:
    checkout pinned to `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1`; a step that
    writes a **synthetic** SARIF 2.1.0 document containing exactly one result from a driver named
    `phase20-probe`, pointing at `README.md` line 1 with a benign message and NO content copied out of the
    repository; an upload step with `id: up`, `continue-on-error: true`, pinned to
    `github/codeql-action/upload-sarif@b96794f015dfd88f77b49b1c93e0fa7110f94c63  # v4.38.0`, with
    `category: phase20-probe`; and a final step that prints `upload outcome=${{ steps.up.outcome }}` on a
    line of its own so it is greppable in the run log.

    Push the branch, wait for the run to complete, then capture — by `gh api` / `gh run view --log`, never
    by recall — (1) the run id and conclusion, (2) the literal `upload outcome=` line, (3) the response of
    `gh api repos/<pilot>/code-scanning/analyses` before and after the run, and (4) if the upload reported
    success, whether an analysis with category `phase20-probe` actually appeared (a `success` outcome with
    no analysis is a third, distinct result that must be recorded as such, not rounded to either side).

    Clean up: delete the remote branch and the scratchpad clone. If an analysis landed, attempt
    `DELETE /repos/<pilot>/code-scanning/analyses/<id>` and record the response verbatim whether it
    succeeds or not — do not retry, do not escalate scopes.
  </action>
  <verify>
    <automated>bash -c 'grep -q "upload outcome=\(success\|failure\)" "$SCRATCH/probe-run.log" && grep -Eq "^(HTTP/|\{|\[)" "$SCRATCH/analyses-after.txt"' # both evidence files captured from gh output, not transcribed</automated>
  </verify>
  <acceptance_criteria>
    - A run id exists on the private pilot and its log contains exactly one `upload outcome=` line whose value is one of `success` or `failure`.
    - The `code-scanning/analyses` response captured AFTER the run is recorded verbatim, including its HTTP status when non-200.
    - The SUMMARY states the result as one of exactly three outcomes: (i) upload `failure` — A1 CONFIRMED; (ii) upload `success` AND an analysis with category `phase20-probe` present — A1 REFUTED; (iii) upload `success` but no analysis present — A1 partially refuted, ingestion silently dropped.
    - `gh api repos/<pilot>/branches/chore/phase-20-sarif-capability-probe` returns 404 at the end of the task (branch deleted).
    - No file under `repos/` and no file tracked by `security_solution` or `security-platform` was created or modified.
  </acceptance_criteria>
  <done>The private-repo SARIF upload capability is a measured fact with run-log and API evidence, and the probe branch is gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the Q2 disposition that follows from the measurement</name>
  <read_first>
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md` §Open Questions Q2 (the three mitigations and why only (b) works in Mode B)
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md` — the eleven verify steps with their exact line numbers, and the note that none carries `continue-on-error`
  </read_first>
  <files>none — this task presents the measurement and records the disposition</files>
  <action>Present Task 2's three captured values (run id, the literal `upload outcome=` line, the analyses response after the run) and the disposition that follows from them, then STOP and wait. Do not apply any workflow change here — plan 04 owns the edit. Record the operator's reply verbatim along with the exact guard expression, or the explicit "no guard", and the exact list of step names it applies to.</action>
  <verify>
    <automated>bash -c 'S=.planning/phases/20-template-packaging-and-adoption-docs/20-01-SUMMARY.md; grep -q "upload outcome=" "$S" && grep -qiE "guard|no guard" "$S"'</automated>
  </verify>
  <done>The SUMMARY states the disposition, the literal guard expression or its absence, and the step list plan 04 must apply it to.</done>
  <what-built>
    Task 2's measurement of `upload-sarif` against a private repository in this account, with run id, the
    literal `upload outcome=` line, and the code-scanning analyses responses before and after.
  </what-built>
  <how-to-verify>
    1. Read the three recorded values (run id, `upload outcome=` line, analyses response after the run).
    2. Confirm the stated disposition follows from them:
       - A1 CONFIRMED (upload failed) → plan 04 compounds `&& github.event.repository.private == false`
         onto the **six SARIF verify steps only** (security.yml lines 112, 258, 461, 712, 863, 1006), and
         onto **none** of the five artifact verify steps (185, 311, 773, 916, 1059) — artifact upload uses
         `ACTIONS_RUNTIME_TOKEN` and is unaffected by code-scanning availability. Zero new substitution
         points, so D-04 survives intact, and it is the only mitigation that works in Mode B.
       - A1 REFUTED (upload succeeded and an analysis landed) → plan 04 adds NO guard, deletes no
         assertion, and records the refutation so the adoption guide's private-repo section says
         "measured working" instead of repeating Pitfall 1.
       - Silent-drop outcome (success, no analysis) → plan 04 adds the same guard as CONFIRMED, and the
         guide records that a green upload step is not proof of ingestion on a private repo.
    3. Confirm no option deletes a verify step. Deleting assertions is the broken-but-green regression the
       verify steps exist to prevent (T-20-05); a capability guard skips a check that cannot pass, an
       edit that removes it hides a check that could.
  </how-to-verify>
  <acceptance_criteria>
    - `20-01-SUMMARY.md` names the disposition, the exact guard expression (or explicitly "no guard"), and the exact list of step names it applies to.
    - The operator's reply is quoted verbatim in the SUMMARY.
    - The SUMMARY states that `requirements.mark-complete` was deliberately NOT invoked (17-01 / 19-01 precedent — DIST-06/07/08 are marked complete only by plan 12).
  </acceptance_criteria>
  <resume-signal>Type `approved`, or state a different disposition.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI token → GitHub API | The local PAT lacks `security_events`; using it to probe an upload would measure the token, not the repository |
| workflow `GITHUB_TOKEN` → code-scanning ingestion | The only boundary whose behaviour this plan measures |
| this phase's repos → a third-party (pilot) repository | A branch and a workflow run are created inside a repository this phase does not own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Repudiation | the six SARIF verify steps in `security.yml` | mitigate | Disposition options are guard-or-nothing; deleting an assertion is excluded by Task 3's acceptance criteria |
| T-20-10 | Information disclosure | probe SARIF uploaded to a private repo | mitigate | Synthetic one-result SARIF from driver `phase20-probe` pointing at `README.md:1`; no repository content embedded; branch deleted and analysis deletion attempted and recorded |
| T-20-08 | Tampering | pilot repository rulesets | mitigate | Task 1's acceptance criteria withhold all ruleset-write and `gh variable set` authorisation; reads only |
| T-20-SC | Tampering | package-manager installs | accept | This phase installs no npm/pip/cargo package (RESEARCH §Package Legitimacy Audit: N/A). Action SHAs are reused verbatim from `security.yml`, never re-resolved |
</threat_model>

<verification>
- `20-01-SUMMARY.md` records: pilot repositories (operator-confirmed), the A1 result as one of the three enumerated outcomes with run id and log line, and the Q2 disposition with the literal guard expression and the exact step list.
- `git status --porcelain` in this repo shows no change outside `.planning/`.
- `repos/security-platform` is untouched: no new branch, no commit, no modified file.
</verification>

<success_criteria>
Plan 04 can be executed without any further judgement call about the guard, and plan 10 knows which
repository to open its pilot PRs against.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-01-SUMMARY.md` when done.
</output>
