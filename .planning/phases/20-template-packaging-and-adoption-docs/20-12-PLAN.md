---
phase: 20-template-packaging-and-adoption-docs
plan: 12
type: execute
wave: 11
depends_on: [20-11]
files_modified:
  - docs/adoption-guide.md
  - docs/development-security-stack-option-1.md
  - CLAUDE.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "Every command in the adoption guide has been executed against a real repository and the guide now shows the output that was actually observed"
    - "The blueprint's illustrative GitHub Actions section can no longer be mistaken for the deployable template — it is retitled and points at the canonical file"
    - "A reader arriving at this repository learns from CLAUDE.md that the canonical workflows live in security-platform and that docs/adoption-guide.md is the adoption procedure"
    - "The blueprint's ASCII diagrams, its four-phase layered structure, its tool coverage matrices and its comparison table are untouched"
  artifacts:
    - path: "docs/adoption-guide.md"
      provides: "Adoption guide corrected against three live pilot runs, with the proving PR numbers and run ids recorded"
      contains: "Proven"
    - path: "docs/development-security-stack-option-1.md"
      provides: "Blueprint section retitled as an illustration, pointing at the canonical workflow and the adoption guide"
      contains: "adoption-guide"
  key_links:
    - from: "docs/development-security-stack-option-1.md §Complete GitHub Actions Workflow"
      to: "the canonical workflow at OttawaCloudConsulting/security-platform and docs/adoption-guide.md"
      via: "a retitle plus a pointer paragraph, leaving the illustrative YAML in place as an illustration"
      pattern: "adoption-guide"
---

<objective>
Make the documentation match what the three live pilot runs actually showed, and remove the last way a reader can
mistake an illustration for the deployable template.

Purpose: 20-VALIDATION.md's doc-exec requirement is that every command in the adoption guide "actually works" —
executed against a real repository with real output pasted. Plans 10 and 11 executed them and recorded every
discrepancy; this plan is where the guide stops being a plausible document and becomes a verified one. RESEARCH
Open Question Q3 is settled here by its recommended option (a): the blueprint's `## Complete GitHub Actions
Workflow` section is illustrative and stale — literal `@<SHA>` placeholders, `--config auto`, `|| true`, an
`on: push: branches: [main]` trigger, no `workflow_call`, no `gate_mode` — so dropping it into a repository does
not produce a working scan run and it cannot satisfy SC1. Retitling it and pointing at the canonical file is the
smallest change that kills the drift risk while preserving the four-phase structure CLAUDE.md protects.

Output: a corrected guide, a retitled blueprint section, and the two CLAUDE.md structure facts a new reader needs.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-10-SUMMARY.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-11-SUMMARY.md

**THIS REPO ONLY.** All three files are in `security_solution`. Nothing in `repos/security-platform` changes.

**CLAUDE.md editing constraints are non-negotiable.** Preserve the ASCII architecture diagrams. Preserve the
four-phase layered structure (Workstation → CI/CD → K8s Infrastructure → Runtime) — the `### Phase N —` heading
sequence at lines 2008 and 2069 must survive intact. Preserve the tool coverage matrices and the security tools
comparison table. `docs/adr/` is append-only, so nothing in this plan edits an existing ADR.

**The blueprint is ~2,400 lines and has pre-existing lint state.** Do not chase a zero-violation target on it.
Capture `markdownlint-cli2 docs/development-security-stack-option-1.md` violation counts BEFORE and AFTER the edit
and require that the count does not increase, per file and per rule.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the adoption guide against the three live pilot runs</name>
  <files>docs/adoption-guide.md</files>
  <read_first>
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-10-SUMMARY.md` — the "guide corrections required" section, the two public pilot PR numbers and run ids, the four detect-step log lines, the artifact and analyses sets for a Terraform-only repository, and the ruleset dry-run output
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-11-SUMMARY.md` — the private pilot's per-step verify outcomes, its `code-scanning/analyses` response, its claim-by-claim comparison against the guide, and the operator disposition
    - `docs/adoption-guide.md` in full — the document being corrected; every `# Expected:` block is a claim that now has an observation next to it
    - `scripts/check-adoption-guide.sh` — the gate that must still exit 0 after the edits
  </read_first>
  <action>
    Work through both SUMMARYs' discrepancy lists and correct the guide so each documented output matches what was
    observed. Where an expected output was right, leave it and do not churn the file. Where it was wrong, replace
    it with the observed text — not a paraphrase, and not an average of the two.

    Specific corrections to make, each sourced from a measurement:
    - Replace any illustrative output in the preflight section with the real responses captured against the public
      and private pilots, keeping both branches (404 available / 403 unavailable, a ruleset row / `[]`).
    - Put the real artifact set and code-scanning category set for a Terraform-only repository into section 6,
      including the absence of a `trivy-image` analysis on a Dockerfile-free repository, so a reader does not read
      a missing category as a fault.
    - Put the observed private-repository behaviour into the private-repository section: the per-step outcome of
      the six SARIF verify steps and the five artifact verify steps, and the measured statement of where the
      findings live on a private repository. If plan 11's operator disposition chose a `v1.0.1` correction, say so
      explicitly, name the version that fixes it, and do not describe the fixed behaviour as though it already
      ships.
    - Add the Mode A / Mode B context-parity fact plan 10 measured: both modes produce byte-identical check-run
      names, which is why one branch-protection procedure serves both.
    - Add a short "Proven in" note naming the three pilot pull requests and run ids and the date. This is the
      difference between a guide a reader trusts and one they test themselves.
    - Fix any pitfall in the troubleshooting table whose symptom text did not match what a run actually printed.

    Re-run `bash scripts/check-adoption-guide.sh` (exit 0) and `markdownlint-cli2 docs/adoption-guide.md` (zero
    violations). Commit.
  </action>
  <verify>
    <automated>bash -c 'set -eu; markdownlint-cli2 docs/adoption-guide.md && bash scripts/check-adoption-guide.sh && grep -c "Proven" docs/adoption-guide.md | awk "{exit !(\$1>=1)}"'</automated>
  </verify>
  <acceptance_criteria>
    - Every discrepancy listed in `20-10-SUMMARY.md` and `20-11-SUMMARY.md` is either corrected in the guide or explicitly recorded in this plan's SUMMARY as deliberately not corrected, with a reason.
    - The guide names the three pilot pull requests and their run ids in a "Proven in" note.
    - The private-repository section's per-step claims match plan 11's observed outcomes; if a `v1.0.1` correction is pending, the guide states the current shipped behaviour and the pending fix separately.
    - The Mode A / Mode B context-parity fact is stated.
    - `bash scripts/check-adoption-guide.sh` exits 0 and `markdownlint-cli2 docs/adoption-guide.md` reports zero violations.
    - No fenced command block in the guide remains without an executed-and-observed output, verified by comparing the count of fenced `bash` blocks against the count of `# Expected:`-annotated blocks and accounting for any difference in the SUMMARY.
  </acceptance_criteria>
  <done>Every instruction in the guide has been run against a real repository, and the guide shows what happened.</done>
</task>

<task type="auto">
  <name>Task 2: Retitle the blueprint's illustrative workflow section and record the two structure facts in CLAUDE.md</name>
  <files>docs/development-security-stack-option-1.md, CLAUDE.md</files>
  <read_first>
    - `docs/development-security-stack-option-1.md` lines 1450-1475 — the `## Complete GitHub Actions Workflow` heading, its two framing paragraphs (which credit Grype and describe a `pull_request` plus `push: branches: [main]` trigger), and the start of the illustrative YAML block
    - `docs/development-security-stack-option-1.md` lines 1690-1710 — the end of that section and the following `## Viewing Results — Where to Look` heading at line 1699, so the edit stays inside one section
    - `docs/development-security-stack-option-1.md` lines 2008-2069 — `### Phase 2 — CI/CD Security Gate (GitHub Actions)`, the branch-protection guidance 18-06 corrected, which the adoption guide cross-references and this plan must not restate or disturb
    - `CLAUDE.md` lines 1-20 — the `## What This Repository Is` and `## Project Structure` sections, the `- path — description` bullet shape, and the stale "(ADR-001 through ADR-014)" parenthetical
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-RESEARCH.md` §Anti-Patterns to Avoid — the enumerated defects that make this section unusable as a template, and §Open Questions Q3's three options with its recommendation
  </read_first>
  <action>
    Capture `markdownlint-cli2 docs/development-security-stack-option-1.md` output BEFORE editing and save the
    per-rule violation counts.

    Retitle `## Complete GitHub Actions Workflow` to name it an illustration — a heading that cannot be mistaken
    for a deployable artifact — and insert a short pointer paragraph immediately under it, before the existing
    framing text, stating: the YAML below is illustrative and is NOT the deployable template; the canonical,
    live-validated workflow is `.github/workflows/security.yml` in `OttawaCloudConsulting/security-platform`,
    published at `v1`; the adoption procedure for both consumption modes is `docs/adoption-guide.md`; and the
    specific ways this illustration differs from what ships — `@<SHA>` placeholders rather than real pins,
    `--config auto` (a hard error alongside `--metrics=off` in the live workflow), `|| true` silent fallbacks, an
    `on: push: branches: [main]` trigger the canonical file does not use, and no `workflow_call` and no
    `gate_mode`. Keep the illustrative YAML block itself in place: ADR-004's `@<SHA>` placeholder convention is
    deliberate in this document, and replacing 240 lines of illustration is a larger diff than the drift risk
    warrants. Do not renumber or move any `### Phase N —` heading, do not touch the ASCII diagrams, and do not
    edit §Phase 2's branch-protection guidance — the adoption guide cross-references it.

    In `CLAUDE.md`, add to `## Project Structure`, in the existing `- path — description` shape, one bullet for
    `docs/adoption-guide.md` (the adoption procedure for both consumption modes) and one for `scripts/` (the
    standing documentation gate created in plan 02, invoked as `bash scripts/check-adoption-guide.sh`). Add one
    sentence to `## What This Repository Is` stating that the canonical GitHub Actions workflows live in
    `OttawaCloudConsulting/security-platform`, not here — a reader arriving at a reference-documentation project
    otherwise has no way to know where the executable artifacts are. Correct the stale parenthetical
    "(ADR-001 through ADR-014)" to run through ADR-018, which plan 13 creates; if you would rather not reference a
    file that does not exist yet, state the range as through ADR-017 and leave plan 13 to extend it — but say which
    you chose and why in the SUMMARY rather than leaving the number wrong.

    Re-run `markdownlint-cli2` on both files and compare the blueprint's per-rule counts against the pre-edit
    capture. The count must not increase for any rule.
  </action>
  <verify>
    <automated>bash -c 'set -eu; markdownlint-cli2 CLAUDE.md; grep -c "adoption-guide" docs/development-security-stack-option-1.md | awk "{exit !(\$1>=1)}"; grep -c "adoption-guide" CLAUDE.md | awk "{exit !(\$1>=1)}"; grep -c "security-platform" CLAUDE.md | awk "{exit !(\$1>=1)}"; grep -c "^### Phase 2 — CI/CD Security Gate (GitHub Actions)$" docs/development-security-stack-option-1.md | grep -qx 1; grep -c "^### Phase 3 — Self-Hosted Infrastructure (Nexus + DefectDojo)$" docs/development-security-stack-option-1.md | grep -qx 1'</automated>
  </verify>
  <acceptance_criteria>
    - The retitled heading no longer reads as a deployable artifact, and a pointer paragraph directly beneath it names the canonical file, the `v1` tag and `docs/adoption-guide.md`, and enumerates at least four of the illustration's specific differences from what ships.
    - `grep -c "^### Phase 2 — CI/CD Security Gate (GitHub Actions)$"` and `grep -c "^### Phase 3 — Self-Hosted Infrastructure (Nexus + DefectDojo)$"` each return 1 — the four-phase heading sequence is intact.
    - `git diff --stat docs/development-security-stack-option-1.md` shows no change inside the ASCII diagram blocks, the tool coverage matrices, the comparison table, or §Phase 2's body — verified by diffing those regions specifically, not by inspecting the stat line.
    - The illustrative YAML block is still present and its `@<SHA>` placeholders are unchanged (`grep -c "@<SHA>"` is equal before and after).
    - `markdownlint-cli2` per-rule violation counts on the blueprint do not increase for any rule; the before and after counts are recorded in the SUMMARY.
    - `CLAUDE.md` gains the two structure bullets and the canonical-workflow-location sentence, passes `markdownlint-cli2` with zero violations, and its ADR range parenthetical is either correct or explicitly handed to plan 13 with the choice recorded.
  </acceptance_criteria>
  <done>No reader of this repository can mistake the illustration for the template, and CLAUDE.md tells them where the real thing lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| illustrative YAML → a reader's clipboard | The blueprint's section is the most likely thing a reader copies, and it is the least safe |
| documented output → reader's trust | An unexecuted `# Expected:` block is an assertion, not evidence |
| large-document edit → protected content | The blueprint carries diagrams, matrices and a heading structure CLAUDE.md forbids disturbing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Tampering / Supply chain | the blueprint's illustrative workflow section | mitigate | Retitled, with a pointer paragraph naming the canonical file and enumerating the illustration's unsafe constructs (`--config auto`, `|| true`, placeholder pins, no `gate_mode`) |
| T-20-24 | Repudiation | unexecuted documented output | mitigate | Every discrepancy from the three pilot runs corrected or explicitly recorded, plus a "Proven in" note naming the PRs and run ids |
| T-20-25 | Tampering | protected blueprint content | mitigate | Region-specific diffs for the diagrams, matrices, comparison table and §Phase 2; heading-sequence greps; `@<SHA>` count equality; per-rule lint counts not increasing |
| T-20-12 | Tampering | frozen strings after editing | mitigate | `scripts/check-adoption-guide.sh` re-run to exit 0, so the five contexts and every ref shape are re-asserted after the corrections |
| T-20-SC | Tampering | package-manager installs | accept | None; documentation edits only |
</threat_model>

<verification>
- `bash scripts/check-adoption-guide.sh` → exit 0; `markdownlint-cli2 docs/adoption-guide.md CLAUDE.md` → zero violations.
- Blueprint per-rule lint counts unchanged or lower; `### Phase 2` / `### Phase 3` headings intact; `@<SHA>` count unchanged.
- Every pilot discrepancy either corrected or recorded with a reason.
</verification>

<success_criteria>
DIST-08's documentation is verified by execution rather than by review, and the last stale GitHub Actions template
in the project is labelled as an illustration.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-12-SUMMARY.md` when done. Record the
before/after blueprint lint counts and the full discrepancy disposition list. `requirements.mark-complete` is
deliberately NOT invoked — plan 13 owns it.
</output>
