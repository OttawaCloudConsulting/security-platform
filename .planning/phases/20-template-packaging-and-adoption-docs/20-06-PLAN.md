---
phase: 20-template-packaging-and-adoption-docs
plan: 06
type: execute
wave: 5
depends_on: [20-05]
files_modified: []
autonomous: false
requirements: [DIST-06, DIST-07]

must_haves:
  truths:
    - "The portability pass was proven live before it was published: five security / … check runs concluded on a real pull request against the canonical host"
    - "The scan RESULTS did not change — each scanner's finding count and rule-id set is compared against the Phase 19 baseline, never assumed equal"
    - "The container job took the FOUND branch on this repository, proving the detect-then-build path end to end"
    - "All eleven intolerant upload-verify assertions concluded, or a skip is explained by the plan 04 capability guard"
    - "The two file deletions reached the operator for review before anything merged to main"
  artifacts:
    - path: ".planning/phases/20-template-packaging-and-adoption-docs/20-06-SUMMARY.md"
      provides: "PR number, head SHA, run id, five check-run conclusions, per-scanner counts against the 19-06 baseline, artifact list, SARIF categories, merge commit SHA read from origin/main, and the tag authorisation for plan 07"
      contains: "merge commit"
  key_links:
    - from: "the portability-pass commits on feature/phase-20-template-packaging"
      to: "five concluding security / … check runs on the pull request head SHA"
      via: "pr-security.yml's pull_request trigger calling the modified security.yml by relative ref"
      pattern: "security / "
---

<objective>
Prove the portability pass live on the one repository where all five scanners reliably fire, then merge it.

Purpose: `security-platform` is the only repository in the account whose permanent `fixtures/` tree guarantees
every scanner finds something (19-06), and it keeps `uses: ./.github/workflows/security.yml` precisely so its
own pull requests exercise its own workflow changes before any tag exists. This is the last chance to catch a
portability regression while the fix is still a commit rather than a `v1.0.1`. The criterion is NOT "green":
under `report-only` every check is green by construction — 19-06 recorded five green checks over a run
reporting 8 semgrep / 11 gitleaks / 14 checkov / 58 trivy-image / 6 trivy-fs / 3 tflint findings. The criterion
is that the same scanners still find the same things through inlined detection and a guarded container build.

Output: a merged `main` on the canonical host carrying the portable workflow, the evidence recorded, and the
tag authorisation captured for plan 07.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-PATTERNS.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-03-SUMMARY.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-04-SUMMARY.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-05-SUMMARY.md

**HOST REPO ONLY.** Repeat the HOST PREFLIGHT: clone if `repos/security-platform/.git` is absent,
`fetch origin`, `checkout feature/phase-20-template-packaging`, and confirm the branch carries all four prior
plans' commits before opening anything.

**`GATE_MODE` must stay ABSENT.** 19-05 deleted the repository variable after its measurement window and 19-07
confirmed `gh variable list` prints nothing. Confirm it is still absent and do not set it — this plan measures
the portability pass, not gate behaviour.

**Fork/Dependabot guard consequence.** The run must be a SAME-REPO, non-Dependabot pull request, or all eleven
verify assertions skip instead of proving anything (17-03).

**Never read evidence from the local working tree.** Every value comes from `gh api` or `gh run view --log`.
15-05, 16-07 and 18-08 all verified merged state from `origin/main` via `git show`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the pull request and collect the live evidence</name>
  <files>none — this task pushes and reads; it authors no tracked file</files>
  <read_first>
    - `.planning/phases/19-pipeline-validation-via-branch-target-prs/19-06-SUMMARY.md` — the baseline finding counts and the "green is report-only tolerance, never zero findings" framing
    - `.planning/phases/17-sarif-upload-and-artifact-retention/17-05-SUMMARY.md` — the evidence method: artifacts via `actions/runs/<id>/artifacts` (`total_count`, `expires_at`), categories via `code-scanning/analyses`, contexts via `commits/<sha>/check-runs` filtered on `app.id == 15368`; and the twelve byte-exact check names observed there
    - `.planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md` — the three tflint rule ids this fixture set fires
    - `repos/security-platform/.github/workflows/security.yml` — the step names whose log lines this task greps: `Detect npm lockfiles`, `Detect Python requirements files`, `Detect Terraform files`, `Detect Dockerfile`, `Build image from discovered Dockerfile`
  </read_first>
  <action>
    Confirm `gh variable list -R OttawaCloudConsulting/security-platform` prints nothing. Push the branch if the
    remote is behind, then open a pull request against `main` whose body names the portability pass (P-1..P-6),
    the Q2 disposition from plan 04, and the two deletions from plan 05. Wait for the run to finish.

    Capture, by `gh api` and `gh run view --log` only:
    1. PR number, head SHA, run id, run conclusion.
    2. Check-run names on the head SHA from `commits/<sha>/check-runs` filtered on `app.id == 15368` — expect
       the five `security / …` contexts, each with a non-null conclusion. Record every OTHER check name that
       appears (17-05 saw twelve total, including six `github-advanced-security` per-driver checks and an
       external GitGuardian check); an extra name is recorded, never treated as a failure.
    3. For each of the four detect steps, the literal `FOUND n …` or `SKIP: …` line. On this repository expect
       FOUND for all four. A SKIP here means the inlined pathspec regressed against a tree the scripts handled —
       stop and report rather than continuing.
    4. The `Build image from discovered Dockerfile` log, confirming the discovered path is `fixtures/Dockerfile`
       and the derived build context is `fixtures`.
    5. Per-scanner finding counts, plus rule-id sets where a set is the sharper instrument (tflint, and the
       semgrep/gitleaks fixture attributions). Compare each against the 19-06 baseline and the 16-05 tflint
       ids, and record a per-scanner verdict: equal, or changed with the delta and a hypothesis. A changed
       count is a portability regression signal and must be investigated in this plan, not deferred.
    6. The artifact list: five names (`semgrep-results`, `checkov-results`, `sca-results`,
       `trivy-image-results`, `gitleaks-results`), each with non-zero size, each `expires_at` exactly 90 days
       after the run's `created_at`.
    7. `code-scanning/analyses` for this run's ref: the six distinct categories `semgrep`, `checkov`,
       `trivy-fs`, `tflint`, `trivy-image`, `gitleaks`.
    8. The outcome of all eleven intolerant verify steps. If plan 04 applied the capability guard, the six
       SARIF verifies still RUN here because this repository is public — a skip would mean the guard condition
       is inverted, which must be fixed before merge.

    Do not merge, do not set any variable, do not touch any ruleset.
  </action>
  <verify>
    <automated>bash -c 'set -eu; R=OttawaCloudConsulting/security-platform; SHA=$(gh api repos/$R/pulls/"$PR" --jq .head.sha); N=$(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368)] | length"); [ "$N" = 5 ] || { echo "expected 5 Actions check runs, got $N"; exit 1; }; U=$(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368) | select(.conclusion==null)] | length"); [ "$U" = 0 ] || { echo "$U check runs unresolved"; exit 1; }; A=$(gh api repos/$R/actions/runs/"$RUN"/artifacts --jq .total_count); [ "$A" = 5 ] || { echo "expected 5 artifacts, got $A"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five `app.id == 15368` check runs exist on the head SHA, all with a non-null conclusion, and their names byte-match the five frozen contexts (diffed against names derived from `security.yml` job names; U+2014 confirmed by byte dump).
    - Four `FOUND` lines are recorded verbatim, one per detect step, and zero `SKIP:` lines appear among them.
    - The build step's log names `fixtures/Dockerfile` and the context `fixtures`.
    - A per-scanner comparison table exists with an explicit verdict against the 19-06 baseline; any delta carries a hypothesis and an investigation result.
    - Artifact `total_count` is 5, every artifact non-zero, every `expires_at` exactly 90 days after `created_at`.
    - Six distinct code-scanning categories are recorded for the run's ref.
    - All eleven verify-step outcomes are recorded; each either concluded or is explained by the capability guard with the guard expression quoted.
    - `gh variable list` produced no output, recorded verbatim.
    - The PR is OPEN and unmerged at the end of this task.
  </acceptance_criteria>
  <done>The portable workflow ran on a real pull request and produced the same findings as the pre-portability baseline, every value read from the API.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator review of the portability pass, the two deletions, and the tag authorisation</name>
  <read_first>
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-05-SUMMARY.md` — the two deleted files and the defects each carried
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-04-SUMMARY.md` — the Q2 disposition actually applied
    - the evidence captured in Task 1
  </read_first>
  <files>none — this task presents evidence and records a decision</files>
  <action>Present Task 1's evidence (five check-run conclusions, the per-scanner comparison against the 19-06 baseline, the four FOUND lines, the artifact and category sets, the eleven verify outcomes), plus the two deletions from plan 05 and the tag question, then STOP and wait. Do not merge and do not tag here. Record the reply verbatim and state, in words plan 07 can act on, whether tagging is authorised.</action>
  <verify>
    <automated>bash -c 'S=.planning/phases/20-template-packaging-and-adoption-docs/20-06-SUMMARY.md; grep -qiE "option-(a|b|c)" "$S" && grep -qiE "tag (is )?(authoris|authoriz)|tag withheld|not authoris" "$S"'</automated>
  </verify>
  <done>The operator's merge decision and the tag authorisation are both recorded in words, not inferred from a green check.</done>
  <decision>
    Merge the portability pull request to `main`, and may plan 07 cut `v1.0.0` and `v1` from the resulting merge
    commit?
  </decision>
  <context>
    Two things here deserve more than a glance at a green tick.

    The deletions: `cicd/.github/workflows/security.yml` (203 lines, the pre-Phase-14 GitHub Actions draft) and
    `cicd/renovate.json` are removed. The rationale is recorded with the defects measured in the deleted file,
    and git history retains both — but it is the one irreversible-feeling change in the pull request.

    The tag: once `v1` exists, consumers can pin it and the reference is public. `v1` is a MOVING tag by design
    and `v1.0.0` is immutable; the first publication is effectively a commitment.

    Also confirm Task 1's per-scanner verdicts read correctly to you — the portability pass is supposed to change
    WHERE detection happens, not WHAT is found.
  </context>
  <options>
    <option id="option-a">
      <name>Merge and authorise the tag</name>
      <pros>Plan 07 proceeds autonomously; plan 10's pilots can pin `@v1`.</pros>
      <cons>Deletions and the first published tag land on one approval.</cons>
    </option>
    <option id="option-b">
      <name>Merge only — hold the tag for a second look</name>
      <pros>The workflow lands on `main` and keeps self-scanning while the tag decision waits.</pros>
      <cons>Plan 07 opens with its own blocking checkpoint, one extra round trip.</cons>
    </option>
    <option id="option-c">
      <name>Do not merge — changes requested</name>
      <pros>Nothing lands until the named concern is addressed.</pros>
      <cons>Plans 07 onward are blocked; the concern becomes a gap-closure plan.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The operator's reply is quoted verbatim in `20-06-SUMMARY.md`.
    - The SUMMARY states explicitly, in those words, whether plan 07 is authorised to tag — plan 07 reads this to decide whether it opens with its own checkpoint.
    - If option-c: the concern is recorded as a blocker with enough detail to plan against, and Task 3 is not executed.
  </acceptance_criteria>
  <resume-signal>Reply with `option-a`, `option-b`, or `option-c` plus the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge and verify the merged state from origin/main</name>
  <files>none — this task merges and reads</files>
  <read_first>
    - `.planning/phases/18-configurable-gate-mode-and-branch-protection/18-08-SUMMARY.md` — the verification method: confirm merged state from `origin/main` by `git show` and compare tree hashes; never read the local working tree as evidence
    - `.planning/phases/16-sca-ecosystem-coverage/16-07-SUMMARY.md` — the merge style used by every prior phase PR (`--merge`, a merge commit) and the note that the merged remote branch was deliberately left in place
  </read_first>
  <action>
    Only if Task 2 returned option-a or option-b. Merge with `gh pr merge --merge` — a merge commit, matching the
    PR #5 / #7 / #9 / #11 precedent, not squash and not rebase. Then `git fetch origin` and verify from
    `origin/main` by `git show`, never from the working tree:
    - the merge commit SHA and its two parents;
    - `origin/main:.github/workflows/security.yml` contains zero NON-COMMENT occurrences of
      `bash scripts/detect-` and of `fixtures/` — filter with `grep -v '^[[:space:]]*#'` first, because
      plan 03's corrected P-6a/P-6b comments legitimately mention both;
    - `origin/main:cicd/.github/workflows/security.yml` and `origin/main:cicd/renovate.json` no longer resolve;
    - the merged tree hash equals the tree hash measured on the PR head in Task 1.

    Then check out `origin/main` in a detached or fresh state and run `bash scripts/check-detector-parity.sh` and
    `bash scripts/check-workflow-uploads.sh` against the MERGED files, recording both exit codes. Leave the
    remote feature branch in place unless the operator asked for its deletion.
  </action>
  <verify>
    <automated>bash -c 'set -eu; cd repos/security-platform; git fetch origin; git show origin/main:.github/workflows/security.yml > /tmp/p20-main-sec.yml; grep -v "^[[:space:]]*#" /tmp/p20-main-sec.yml > /tmp/p20-main-nc.yml; grep -c "bash scripts/detect-" /tmp/p20-main-nc.yml | grep -qx 0; grep -c "fixtures/" /tmp/p20-main-nc.yml | grep -qx 0; git cat-file -e origin/main:cicd/renovate.json 2>/dev/null && { echo "renovate.json still on main"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `origin/main` carries a merge commit whose two parents are the pre-merge `main` and the PR head SHA recorded in Task 1.
    - `git show origin/main:.github/workflows/security.yml` contains zero NON-COMMENT occurrences of `bash scripts/detect-` and of `fixtures/`, counted with comment lines excluded.
    - `git cat-file -e origin/main:cicd/.github/workflows/security.yml` and `…:cicd/renovate.json` both fail.
    - The merged tree hash equals the Task 1 PR-head tree hash, quoted in the SUMMARY.
    - `check-detector-parity.sh` and `check-workflow-uploads.sh` both exit 0 against the merged files, with the commands run from a checkout of `origin/main`.
    - The SUMMARY records the merge commit SHA and states that it was read from `origin/main`, not from the local branch.
  </acceptance_criteria>
  <done>The canonical host's `main` carries a portable, self-proven workflow and no stale deployable copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch → `main` of a public repository | After merge, anyone can fetch these files; the merge is the publication event |
| local working tree → evidence | A claim read from the local checkout is not evidence about the remote |
| `report-only` tolerance → interpretation of green | Green checks here mean tolerated findings, never zero findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | scan results after the portability pass | mitigate | Per-scanner counts and rule-id sets compared against the 19-06 / 16-05 baselines with a stated verdict; a delta blocks the merge until investigated |
| T-20-05 | Repudiation | the eleven intolerant verify steps | mitigate | Every outcome recorded; a skip on this PUBLIC repository is treated as an inverted guard, not as a pass |
| T-20-16 | Repudiation | "green means clean" misreading | mitigate | The objective and the SUMMARY state the finding counts alongside the conclusions, following 19-06's framing |
| T-20-09 | Elevation of privilege | required checks / rulesets | accept | Nothing is made required in this phase (18-05 leave-unrequired, re-confirmed 19-07); no ruleset call is made here |
| T-20-06 | Tampering | first published tag | mitigate | Tag authorisation is an explicit operator decision at Task 2, separable from merge approval |
| T-20-SC | Tampering | package-manager installs | accept | None; the run installs only the already-SHA-pinned tooling the workflow has used since Phase 15 |
</threat_model>

<verification>
- Five concluded `security / …` check runs, five artifacts at 90-day retention, six SARIF categories.
- Four `FOUND` detect lines and a build log naming `fixtures/Dockerfile`.
- Per-scanner comparison table with verdicts against the 19-06 baseline.
- Merged state verified from `origin/main` by `git show`, with both offline gates exiting 0 against it.
</verification>

<success_criteria>
The canonical workflow is portable, proven, and on `main` — and the operator has seen the deletions and the
tag question before either became permanent.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-06-SUMMARY.md` when done. It must state the
tag authorisation in words plan 07 can act on. `requirements.mark-complete` is deliberately NOT invoked —
plan 12 owns it.
</output>
