---
phase: 20-template-packaging-and-adoption-docs
plan: 10
type: execute
wave: 9
depends_on: [20-01, 20-09]
files_modified: []
autonomous: true
requirements: [DIST-06, DIST-07]

must_haves:
  truths:
    - "Dropping the copy-paste bundle into a repository that was never part of this project produces a working scan run — five security / … check runs concluding on a real pull request (SC1)"
    - "A second repository calls the workflow by external reference against the published tag and produces the same five contexts (SC2)"
    - "The clean-skip path is proven on a real repository: npm, Python and container skip with their byte-exact SKIP: lines while Terraform is found"
    - "Every command executed came from the adoption guide, so the guide is proven by use rather than by review"
    - "No ruleset was written and no repository variable was set on the pilot"
  artifacts:
    - path: ".planning/phases/20-template-packaging-and-adoption-docs/20-10-SUMMARY.md"
      provides: "SC1 and SC2 evidence: two PR numbers, two run ids, check-run names, detect-step log lines, artifact lists, code-scanning analyses, the ruleset dry-run comparison, and every guide correction the runs revealed"
      contains: "SC1"
  key_links:
    - from: "the three files fetched at the v1 tag into a pilot repository"
      to: "five concluding security / … check runs on that repository's pull request"
      via: "Mode A copy-paste, zero edits"
      pattern: "security / "
    - from: "a one-file caller in a pilot repository"
      to: "the canonical workflow at the published tag"
      via: "uses: OttawaCloudConsulting/security-platform/.github/workflows/security.yml@v1"
      pattern: "security.yml@v1"
---

<objective>
Prove both consumption modes on a repository that had nothing to do with building them, by executing the
adoption guide's own commands.

Purpose: SC1 and SC2 are the only criteria in this phase that cannot be satisfied statically, and the ROADMAP
goal is that a repository can adopt the pipeline "by following documentation alone". The strongest available
evidence for that sentence is an executor who follows the guide verbatim and records where it was wrong. The
pilot also exercises what `security-platform` structurally cannot: a repository with Terraform but no npm, no
Python and no Dockerfile, which is where the inlined detectors and the new container guard have to produce three
clean skips rather than three failures.

Output: two pull requests on the public pilot with full live evidence, a ruleset dry run that writes nothing,
and a list of guide corrections for plan 12.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-CONTEXT.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-VALIDATION.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-01-SUMMARY.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-07-SUMMARY.md
@.planning/phases/20-template-packaging-and-adoption-docs/20-09-SUMMARY.md

**HOST PREFLIGHT (Task 2 only, before reading `set-required-checks.sh`).** If `repos/security-platform/.git`
does not exist (the normal case in an isolated parallel worktree — `.gitignore` excludes `repos/`), clone it
first: `git clone https://github.com/OttawaCloudConsulting/security-platform.git repos/security-platform`, then
`git -C repos/security-platform fetch origin && git -C repos/security-platform checkout origin/main`. Task 1
does not touch `repos/security-platform` and needs no preflight.

**VERIFY-STEP VARIABLES.** The `<verify>` blocks below reference `$PILOT`, `$PR_A`, `$RUN_A`, `$SHA_A`, `$PR_B`
by name. Before running each task's `<verify>` command, export these in the same shell from the values captured
during `<action>`: `export PILOT=<owner>/<pilot-repo-slug>`, `export PR_A=<Task 1 PR number>`,
`export RUN_A=<Task 1 run id>`, `export SHA_A=<Task 1 head SHA>` (Task 1's verify only needs `PILOT`/`PR_A`/`RUN_A`;
Task 2's verify additionally needs `PR_B` and `SHA_A`). Record the exported values in the SUMMARY so a re-run of
the verify command doesn't require re-deriving them.

**THE PILOT IS A THIRD REPOSITORY.** Use the PUBLIC pilot confirmed by the operator at plan 01 Task 1 — the
default is `OttawaCloudConsulting/terraform-pipelines`. Clone it into the session scratchpad; do NOT work inside
`repos/terraform-pipelines`, whose local checkout sits on an unrelated branch (`feature/add-pre-commit`). Branch
both pilots from `origin/main`. Measured facts to expect there: 36 `.tf` files, zero `package-lock.json`, zero
`requirements*.txt`, zero Dockerfiles, no `.github/` directory at all (so no workflow filename collision), and
an existing ruleset id `12760793` in `active` enforcement.

**Two independent branches, not one branch edited twice.** Mode A and Mode B are separate adoption paths; each
gets its own branch cut from `origin/main` and its own pull request, so neither run's evidence depends on the
other's files.

**Hard prohibitions on the pilot.** No `gh variable set` (the `report-only` default is what SC1 and SC2 measure).
No ruleset write: `set-required-checks.sh` runs with `--out` only, never `--apply`. STATE records required-checks
adoption as deferred past this phase, and a `--apply` here carries the self-lockout risk documented in the guide.

**Execute the guide, do not improvise.** Take each command from `docs/adoption-guide.md` as written. When a
command's real output differs from the guide's `# Expected:` block, record BOTH verbatim and carry the
discrepancy to plan 12 rather than silently fixing the guide here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mode A on the public pilot — SC1</name>
  <files>none in this repo — a branch and a pull request in the pilot repository</files>
  <read_first>
    - `docs/adoption-guide.md` sections 2, 4 and 6 — the preflight probes, the three `curl` commands, the offline post-copy check, and what a green first run means. These are the instructions being executed
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-07-SUMMARY.md` — the three raw URLs at `/v1/` and the moving-tag cache observation
    - `repos/security-platform/.github/workflows/security.yml` at `origin/main` — the four byte-exact `SKIP:` and `FOUND` strings this run's logs are checked against
  </read_first>
  <action>
    Run the guide's four preflight probes against the pilot and record each command's real output next to the
    guide's expected output. Confirm `gh variable list` shows no `GATE_MODE`.

    Cut `chore/adopt-security-pipeline-mode-a` from `origin/main`. Execute the guide's Mode A steps exactly: the
    three `curl -fsSL` fetches at `/v1/` into `.github/workflows/security.yml`, `.github/workflows/pr-security.yml`
    and `.github/dependabot.yml`, zero edits. Verify byte-identity against the tag by diffing each fetched file
    against `gh api repos/OttawaCloudConsulting/security-platform/contents/<path>?ref=v1` content, so a stale CDN
    copy cannot slip through unnoticed. Run the guide's offline post-copy check (`actionlint`, `yamllint -d relaxed`)
    and record the exit codes. Commit and push, open a pull request, wait for the run.

    Capture, by `gh api` and `gh run view --log` only:
    1. PR number, head SHA, run id, run conclusion, and whether the pull request is MERGEABLE.
    2. The check-run names on the head SHA filtered on `app.id == 15368` — the five `security / …` contexts, each
       with a non-null conclusion, byte-compared against the names derived from the canonical workflow.
    3. The four detect-step log lines. Expect `FOUND` for Terraform and the byte-exact `SKIP:` line for npm, for
       Python and for Dockerfile. This is the clean-skip proof the `security-platform` run structurally cannot
       give. A failure or an error-shaped log in the sca or container job here is an SC1 failure, not a nuisance.
    4. The artifact list. The sca artifact carries `if-no-files-found: warn` by Phase 17's deliberate exception,
       so record exactly which artifacts appear and which are empty rather than asserting a count of five blindly;
       state the expected set for a Terraform-only repository and compare.
    5. `code-scanning/analyses` for the run's ref — the pilot is PUBLIC, so analyses are expected; record the
       categories that appear and the ones that do not (a Dockerfile-free repository produces no `trivy-image`
       analysis).
    6. The outcome of every verify step that ran, and every one that skipped with the reason.

    Leave the pull request OPEN. Do not merge, do not close, do not delete the branch — plan 13 carries the fate
    decision to the operator.
  </action>
  <verify>
    <automated>bash -c 'set -eu; R=$PILOT; SHA=$(gh api repos/$R/pulls/"$PR_A" --jq .head.sha); N=$(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368)] | length"); [ "$N" = 5 ] || { echo "expected 5 Actions check runs, got $N"; exit 1; }; U=$(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368) | select(.conclusion==null)] | length"); [ "$U" = 0 ] || { echo "$U unresolved"; exit 1; }; M=$(gh api repos/$R/pulls/"$PR_A" --jq .mergeable_state); echo "mergeable_state=$M"; gh run view "$RUN_A" -R $R --log | grep -q "SKIP: no Dockerfile found" || { echo "container clean-skip line absent"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five `app.id == 15368` check runs on the head SHA, all concluded, names byte-matching the five frozen contexts.
    - The pull request is mergeable (its `mergeable_state` recorded verbatim).
    - The run log contains the byte-exact `SKIP: no Dockerfile found — container sub-scan not applicable to this repository`, the npm `SKIP:` line, the Python `SKIP:` line, and a Terraform `FOUND` line — all four quoted in the SUMMARY.
    - Each of the three fetched files is byte-identical to the `contents?ref=v1` blob, proven by diff.
    - `actionlint` and `yamllint -d relaxed` exited 0 on the copied workflows before the push.
    - The artifact set and the code-scanning categories are recorded with the expected-versus-observed comparison for a Terraform-only repository.
    - `gh variable list -R $PILOT` output is recorded and contains no `GATE_MODE`.
    - No ruleset call of any kind was made in this task; the pull request is OPEN at the end.
    - Every guide command whose real output differed from its `# Expected:` block is recorded with both texts.
  </acceptance_criteria>
  <done>SC1 is measured: a repository outside this project ran the pipeline from a copy-paste of the published tag, with three clean ecosystem skips.</done>
</task>

<task type="auto">
  <name>Task 2: Mode B on the public pilot — SC2 — and a write-nothing branch-protection dry run</name>
  <files>none in this repo — a second branch and pull request in the pilot repository</files>
  <read_first>
    - `docs/adoption-guide.md` sections 5, 8 and 9 — the single Mode B caller file, the D-07 ordering with the contexts-reading command, and the Dependabot wiring
    - `.planning/phases/20-template-packaging-and-adoption-docs/20-07-SUMMARY.md` — the exact `uses:` string and the `v1` tag's commit SHA
    - `repos/security-platform/scripts/set-required-checks.sh` lines 1-70 and its `usage()` block — the real flag names, the dry-run default, and the seven exit codes
  </read_first>
  <action>
    Cut `chore/adopt-security-pipeline-mode-b` from `origin/main` — a fresh branch, not Task 1's. Create only
    `.github/workflows/pr-security.yml` from the guide's Mode B snippet, plus `.github/dependabot.yml`. No
    `security.yml`, no `with:` block, `name: security`, job-level `contents: read` + `security-events: write` +
    `actions: read`, and `uses: OttawaCloudConsulting/security-platform/.github/workflows/security.yml@v1` with no
    trailing version comment. Run `actionlint` and `yamllint -d relaxed` locally, commit, push, open a pull
    request, wait for the run.

    Capture the same evidence set as Task 1 (PR number, head SHA, run id, conclusion, mergeability; five
    `app.id == 15368` contexts concluded and byte-matching; the four detect-step lines; artifacts; analyses; verify
    outcomes), and additionally:
    - the resolved workflow reference in the run's metadata, confirming the external ref actually resolved to the
      `v1` tag's commit rather than to anything else;
    - a comparison of the five context names between Task 1's Mode A run and this Mode B run. They must be
      identical — the contexts derive from the caller's job id plus the callee's job names, which are the same in
      both modes. A difference here would invalidate the guide's branch-protection section for one of the modes.

    Then exercise the guide's branch-protection section in its write-nothing form. FIRST capture the live
    baseline — `gh api repos/<pilot>/rules/branches/main --jq '[.[].type] | sort | join(",")'` into
    `/tmp/p20-rules-before.txt` — because the verify command below diffs against it. Then run
    `bash scripts/set-required-checks.sh --repo <pilot> --ruleset 12760793 --out <scratch>/merged.json` from inside
    `repos/security-platform` (the script cds to its own repository root). Record its exit code, the rule-type
    before/after comparison it prints, and confirm from `gh api repos/<pilot>/rules/branches/main` that the LIVE
    ruleset is byte-unchanged after the dry run. Do not pass `--apply`. Also record what the script does NOT do:
    it has no `POST /rulesets` create path, which matters for any consumer whose `/rulesets` read returns `[]`.

    Leave both pull requests OPEN.
  </action>
  <verify>
    <automated>bash -c 'set -eu; R=$PILOT; SHA=$(gh api repos/$R/pulls/"$PR_B" --jq .head.sha); N=$(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368)] | length"); [ "$N" = 5 ] || { echo "expected 5, got $N"; exit 1; }; diff <(gh api repos/$R/commits/$SHA/check-runs --jq "[.check_runs[] | select(.app.id==15368) | .name] | sort | .[]") <(gh api repos/$R/commits/"$SHA_A"/check-runs --jq "[.check_runs[] | select(.app.id==15368) | .name] | sort | .[]") || { echo "Mode A and Mode B context names differ"; exit 1; }; gh api repos/$R/rules/branches/main --jq "[.[].type] | sort | join(\",\")" > /tmp/p20-rules-after.txt; diff /tmp/p20-rules-before.txt /tmp/p20-rules-after.txt || { echo "pilot ruleset changed during a dry run"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The Mode B branch contains exactly two new files (`.github/workflows/pr-security.yml`, `.github/dependabot.yml`) and no `security.yml`.
    - Exactly five `app.id == 15368` check runs, all concluded, and their sorted name list is byte-identical to Task 1's.
    - The run metadata shows the external reference resolving to the `v1` tag's commit SHA recorded in `20-07-SUMMARY.md`.
    - `set-required-checks.sh` exited 0 in dry-run mode, wrote its merged document to the scratch path, and printed a rule-type before/after comparison showing no type dropped.
    - `gh api repos/<pilot>/rules/branches/main` returns the same sorted rule-type list before and after the dry run.
    - `--apply` was never passed; `gh variable set` was never run; the SUMMARY states both as verified absences.
    - Both pull requests are OPEN at the end of the plan.
  </acceptance_criteria>
  <done>SC2 is measured: an external `uses:` reference against the published tag produces the same five contexts as the copy-paste bundle, and the branch-protection procedure was rehearsed without touching live settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the published tag → a third repository's CI | This is the first time code from the canonical host executes in a repository that did not author it |
| pilot repository settings → this phase's scope | The pilot's ruleset and variables are live production configuration for another project |
| CDN-fetched file → what the pilot actually runs | A stale or altered fetch would be executed verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering | pilot ruleset `12760793` | mitigate | Dry run only (`--out`), `--apply` never passed, and the live rule-type list diffed before and after to prove nothing changed |
| T-20-17 | Tampering | the three CDN-fetched files | mitigate | Each diffed against the `contents?ref=v1` blob before the push, so a cached or altered response cannot reach the pull request unnoticed |
| T-20-22 | Tampering | Mode A / Mode B context drift | mitigate | The two runs' sorted context name lists are diffed against each other; a difference invalidates the branch-protection section for one mode and fails the task |
| T-20-01 | Tampering | clean-skip path on a real consumer | mitigate | The three byte-exact `SKIP:` lines are required evidence, so a silent false skip or a hard failure is both visible and disqualifying |
| T-20-09 | Elevation of privilege | required checks on the pilot | accept | Nothing is made required; adoption of required checks stays deferred per STATE, and the guide documents the sequence for when the operator chooses to |
| T-20-SC | Tampering | package-manager installs | accept | None on the pilot; the workflow installs only the SHA-pinned tooling already reviewed in Phases 14-17 |
</threat_model>

<verification>
- Two pull requests on the public pilot, each with five concluded `security / …` check runs whose names match each other byte-for-byte.
- The four detect-step log lines from Mode A quoted verbatim, including three `SKIP:` lines.
- The Mode B run resolved to the `v1` tag's commit.
- The pilot's `rules/branches/main` rule-type list is unchanged across the dry run.
- Every guide command whose observed output differed from its documented expectation is recorded for plan 12.
</verification>

<success_criteria>
SC1 and SC2 are measured on a repository outside this project, and the guide's own commands were the only
instructions used.
</success_criteria>

<output>
Create `.planning/phases/20-template-packaging-and-adoption-docs/20-10-SUMMARY.md` when done. Include a
"guide corrections required" section — even if empty, state that it is empty and that each fenced command block
was executed. `requirements.mark-complete` is deliberately NOT invoked — plan 13 owns it.
</output>
