---
phase: 21-docs-cleanup-close-remaining-phase-20-deferred-items
plan: 04
type: execute
wave: 2
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "deferred-items.md records items #4 and #6 as CLOSED with reproducible grep evidence, so the tracking file no longer contradicts the repository state"
    - "The record notes that the 2026-09-14 re-check's 'all 5 filenames diverge' claim about item #4 was inaccurate — 3 of 5 matched live"
    - "The record states that milestone-4-defectdojo.md was fixed in-phase under SE-1, so no new deferred item was created by this phase"
    - "The full phase verification suite passes across all three edited documents"
  artifacts:
    - path: ".planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md"
      provides: "Phase 21 status re-check section closing items #4 and #6"
      contains: "Status re-check 2026-09-15 (Phase 21)"
  key_links:
    - from: ".planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md"
      to: "docs/milestone-plan/milestone-2-cicd-gate.md and docs/adoption-guide.md"
      via: "closure evidence expressed as the exact grep/gate commands a reader can re-run"
      pattern: "grep -c -i grype"
---

<objective>
Close Phase 17 deferred items #4 and #6 in the tracking file by appending a Phase 21 status re-check
section to `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md`, then run the full
phase verification suite across all three documents this phase edited.

Purpose: the phase is named "close remaining Phase 20 deferred items" and CONTEXT.md names
`deferred-items.md` as the source of truth for the two items being closed. Leaving both marked OPEN after
fixing them would create exactly the kind of doc-vs-reality drift this phase exists to end. Phase 20.1
established the pattern: append a new dated status re-check section rather than editing earlier rows, so
the record of what was believed when stays intact.

Planner note: this closure record is planner discretion following the Phase 20.1 pattern — it is not one
of CONTEXT.md's locked decisions (D-01 through D-07 cover only the two doc edits).

Output: one appended section in `deferred-items.md`, plus a recorded full-suite verification run.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read these before writing:

- `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` — the WHOLE file (it is
  short). Lines 1–33 are the original deferred table; lines 35–58 are the Phase 20.1 status re-check whose
  format this plan reuses.
- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-RESEARCH.md` lines 245–267
  (the token-by-token L78 table proving the 2026-09-14 claim about item #4 was inaccurate — three of five
  filenames matched live) and lines 435–486 (SE-1, SE-2, SE-3 scope decisions)
- `21-01-SUMMARY.md`, `21-02-SUMMARY.md`, `21-03-SUMMARY.md` from this phase directory — the measured
  before/after evidence this record cites. Do not restate a number from memory; copy it from the summary
  that measured it, or re-run the command.

<scope_boundaries>
Items that stay OPEN and must NOT be marked closed:

- **#2** — the blueprint's Syft+Grype illustrative SCA example in `docs/development-security-stack-option-1.md`
  (lines 41, 61, 130, 304-366, 1580-1594, 2076). Explicitly deferred by 21-CONTEXT.md as an unbounded
  blueprint change.
- **#3** — the blueprint's `push: branches: [main]` trigger divergence, no current owner.
- Broken relative ADR links in the blueprint doc — still open, no owner, not in this phase's scope.

Out-of-scope grype occurrences that are NOT drift and must not be reported as unfixed:
`docs/milestone-plan/milestone-1-workstation.md` and the three
`docs/milestone-1-workstation/{INSTALLATION,USER,DEVELOPER}_GUIDE.md` files (Grype is genuinely installed as
a workstation tool there), and `docs/adr/adr001`, `adr010`, `adr015` (ADRs are append-only per repo
CLAUDE.md — historical decisions are never retroactively corrected).

Also NOT in scope for this plan: the ROADMAP.md Phase 21 goal sentence still mentions "7 stale `# v4`
action-version comments in the blueprint." That item was closed by Phase 20.1 and dropped from scope by the
user (21-CONTEXT.md `<domain>`). Do not edit the roadmap goal here; just make sure this record states the
drop so a later verifier does not report it as unfixed work.
</scope_boundaries>

<tree_state_note>
The repository working tree currently carries 367 unrelated modified files under `.claude/` (an in-flight
GSD tooling update, measured 2026-09-15). `docs/`, `scripts/` and the Phase 17 planning directory are clean.
Every "only this file changed" assertion in this plan is therefore path-scoped; a bare
`git diff --name-only` or bare `git status --porcelain` would list the `.claude/` churn. Those `.claude/`
modifications are outside this phase and are NOT a finding — do not report them and do not clean them up.
</tree_state_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Phase 21 status re-check section to deferred-items.md</name>
  <files>.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md</files>
  <read_first>
    - `.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` (whole file — reuse the
      Phase 20.1 section's exact table shape: `| # | Live status | Evidence |`, `**CLOSED**` in bold, a
      trailing `**Net: …**` paragraph)
    - `21-01-SUMMARY.md`, `21-02-SUMMARY.md`, `21-03-SUMMARY.md` in this phase directory (the measured
      evidence to cite)
    - `21-RESEARCH.md` lines 245–267 and 435–486
  </read_first>
  <action>
Append a new section at the end of the file. Do not edit, reword, or delete any existing row — including
the 2026-09-14 row for item #4 that this plan corrects. The earlier record stands as what was believed on
that date; the correction goes in the new section. This mirrors how Phase 20.1 handled the same situation.

Heading: `## Status re-check 2026-09-15 (Phase 21)`, with a blank line above and below it.

Open with one short paragraph stating that items #4 and #6 were closed by Phase 21's two documentation
edits, that `docs/milestone-plan/milestone-4-defectdojo.md` was brought into scope by a user-approved
amendment (SE-1) so this phase created no new deferred item, and that the verdicts below were measured, not
recalled.

Then a table with the same three columns as the 2026-09-14 section — `| # | Live status | Evidence |` —
carrying exactly these rows:

- Row `4`: status `**CLOSED**`. Evidence must include the reproducible command and its result —
  `grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md` returns `0` (five sites fixed: the M2-F1
  feature-table cell, the two M2-F1 bullets, the M2-F3 JSON output list, the M2-F3 DefectDojo parser row) —
  and must record the correction to the 2026-09-14 entry: its all-five-filenames-diverge claim about line
  78 was inaccurate. Paraphrase that claim rather than quoting it verbatim, so the correction can never be
  mistaken for a second copy of the original row. Specifically: `semgrep-results.json`,
  `checkov-results.json` and `gitleaks-results.json` matched the live workflow exactly, and only
  `grype-results.json` and `trivy-results.json` diverged. Both are now `trivy-fs.json` plus
  `npm-audit-<N>.json` / `pip-audit-<N>.json`, and `trivy-image.json` respectively.
- Row `6`: status `**CLOSED**`. Evidence: `docs/adoption-guide.md` §6 now carries the
  `### SARIF Upload Limits at Consumer Scale` subsection (use the exact heading text recorded in
  21-03-SUMMARY.md) documenting the top-5,000-results-per-run display truncation, the four rejection
  ceilings (10 MB gzipped per file, 20 runs per file, 25,000 results per run, 25,000 rules per run) and the
  1,000,000-alert repository cap with no self-service recovery, cited to
  `docs.github.com/en/code-security/reference/code-scanning/sarif-files/…`; and
  `bash scripts/check-adoption-guide.sh` still reports `PASSED 15 / FAILED 0`.
- Row `SE-1` (a new identifier, not one of the original seven): status `**CLOSED — added to scope by
  user-approved amendment**`. Evidence: `grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md`
  returns `0`; line 75's parser list now matches milestone-2's, and line 105's dedup example was changed
  from the now-impossible Trivy-vs-Grype pair to Trivy filesystem plus npm audit for the same npm package.
- Row `2`: status `OPEN — unchanged, still deferred`. Evidence: the blueprint's Syft+Grype illustrative SCA
  example remains at `docs/development-security-stack-option-1.md` lines 41, 61, 130, 304-366, 1580-1594,
  2076; 21-CONTEXT.md explicitly defers it as an unbounded blueprint change.
- Row `7`: status `CLOSED by Phase 20.1 — re-confirmed, dropped from Phase 21 scope`. Evidence: the seven
  `actions/checkout@<SHA>  # v4` comments were already corrected to `# v7` in Phase 20.1; the three `# v4`
  comments remaining in that file are on `github/codeql-action/upload-sarif` and match the live pin
  `v4.38.0`, so they are correct, not stale. ROADMAP.md's Phase 21 goal sentence still names this item;
  the user dropped it, and no code or doc change was needed.

Close with a bold `**Net: …**` paragraph in the same style as the 2026-09-14 section: state how many items
are now closed, that items #2 and #3 plus the broken relative ADR links in the blueprint remain OPEN with
#3 and the ADR links still carrying no owner, and that Phase 21 created no new deferred item.

Formatting: every table row on one line, pipes aligned as loosely as the existing section (the file does
not pad columns). Put a blank line above and below the table and above and below the heading. Use U+2014
EM DASH where the existing rows use one. This file is inside `.planning/` and is not covered by the
adoption-guide gate, but keep it markdownlint-clean anyway: run `markdownlint-cli2` on it and expect no new
errors relative to its pre-edit state (measure the pre-edit state first — if the file already has
violations, do not fix them, just do not add any).
  </action>
  <verify>
    <automated>grep -qF 'Status re-check 2026-09-15 (Phase 21)' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md &amp;&amp; grep -qF 'SE-1' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md &amp;&amp; test "$(grep -c 'Status re-check' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md)" -eq 2 &amp;&amp; test "$(git diff --numstat .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md | awk '{print $2}')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'Status re-check 2026-09-15 (Phase 21)' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` passes
    - `test "$(grep -c 'Status re-check' .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md)" -eq 2` passes (the 2026-09-14 section still present, exactly one new one added)
    - `git diff --numstat .planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md` reports 0 in the deletions column (purely additive — no earlier row edited or removed)
    - `grep -qF 'SE-1' <file>` passes and the new section names both `milestone-4-defectdojo.md` and `trivy-image.json`
    - The 2026-09-14 section is byte-unchanged, proven by `git diff --numstat` reporting 0 deletions (do NOT assert an occurrence count on the phrase "5 filenames that all diverge" — the new section paraphrases that claim rather than quoting it, so a count assertion would be ambiguous)
    - The new section contains `grep -c -i grype`, `PASSED 15 / FAILED 0`, `1,000,000` and `top 5,000` as evidence strings
    - `git diff --name-only -- .planning/phases/17-sarif-upload-and-artifact-retention` lists only `deferred-items.md` (path-scoped on purpose — see `<tree_state_note>`)
  </acceptance_criteria>
  <done>A dated Phase 21 status re-check section closes items #4, #6 and SE-1 with reproducible evidence, re-confirms #7's earlier closure and #2's deferral, corrects the 2026-09-14 five-filenames claim without editing it, and the edit is purely additive.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase verification suite and record the measured output</name>
  <files>.planning/phases/17-sarif-upload-and-artifact-retention/deferred-items.md</files>
  <read_first>
    - `21-RESEARCH.md` lines 713–751 (Test Framework, Phase Requirements → Test Map, Sampling Rate — the
      per-requirement assertions this suite discharges)
    - `21-RESEARCH.md` lines 935–947 (the phase verification command set)
    - `21-01-SUMMARY.md`, `21-02-SUMMARY.md`, `21-03-SUMMARY.md` (the per-plan measurements this run
      cross-checks)
  </read_first>
  <action>
Execute the full suite and read every output — a checkpoint is "I ran it, here is what happened," not "I
believe this works." Run each command separately so a failure is attributable:

1. `grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md` — expect `0` (DIST-06).
2. `grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md` — expect `0` (DIST-06, SE-1).
3. `markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md docs/milestone-plan/milestone-4-defectdojo.md docs/adoption-guide.md` — expect `Summary: 0 error(s)`, exit 0.
4. `bash scripts/check-adoption-guide.sh` — expect `check-adoption-guide: PASSED 15 / FAILED 0`, exit 0
   (DIST-08). Invoke with `bash`, never `./`, and never set the executable bit.
5. Content assertions on the guide: `top 5,000`, `25,000 results per run`, `25,000 rules per run`,
   `20 runs per file`, `10 MB`, `1,000,000` all present; `integrating-with-code-scanning`, `soft limit`,
   `sca-results.json` all absent. Check `sca-results.json` against the three edited files only, not
   `docs/`-wide — no `docs/`-wide baseline for that token was ever measured.
6. `git status --porcelain -- docs/ scripts/ .planning/phases/17-sarif-upload-and-artifact-retention .planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items` — path-scoped deliberately, because the tree carries 367 unrelated modified files under `.claude/` from an in-flight GSD tooling update that are not this phase's business and are not a finding. Confirm that within those paths the only modified files are
   `docs/milestone-plan/milestone-2-cicd-gate.md`, `docs/milestone-plan/milestone-4-defectdojo.md`,
   `docs/adoption-guide.md`, `.planning/` artifacts for this phase, and nothing else. In particular
   `scripts/check-adoption-guide.sh`, `.markdownlint.jsonc`, `.markdownlint-cli2.yaml`,
   `docs/development-security-stack-option-1.md`, `docs/milestone-plan/milestone-1-workstation.md`,
   `docs/milestone-1-workstation/*` and `docs/adr/*` must all be unmodified — the last four are the
   collateral-damage surfaces a bulk grype substitution would have hit.

If anything fails, STOP and report the raw output, your theory and the proposed fix. Do not adjust the gate
script, a lint config, or a summary to make a check pass.

Record every measured value in the summary. No file edit is expected in this task; if step 6 shows an
unexpected modified file, that is a finding to report, not something to clean up silently.
  </action>
  <verify>
    <automated>test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0 &amp;&amp; test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0 &amp;&amp; markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md docs/milestone-plan/milestone-4-defectdojo.md docs/adoption-guide.md 2>&amp;1 | grep -q 'Summary: 0 error' &amp;&amp; bash scripts/check-adoption-guide.sh | grep -q 'PASSED 15 / FAILED 0' &amp;&amp; grep -qF 'top 5,000' docs/adoption-guide.md &amp;&amp; test "$(git status --porcelain scripts/check-adoption-guide.sh .markdownlint.jsonc .markdownlint-cli2.yaml docs/development-security-stack-option-1.md docs/milestone-plan/milestone-1-workstation.md docs/adr | wc -l | tr -d ' ')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0` and the same for `milestone-4-defectdojo.md` both pass
    - `markdownlint-cli2` across all three edited docs prints `Summary: 0 error(s)` and exits 0
    - `bash scripts/check-adoption-guide.sh` exits 0 with `check-adoption-guide: PASSED 15 / FAILED 0`
    - `grep -qF 'top 5,000'`, `grep -qF '25,000 results per run'`, `grep -qF '25,000 rules per run'`, `grep -qF '20 runs per file'`, `grep -qF '10 MB'`, `grep -qF '1,000,000'` all pass against `docs/adoption-guide.md`
    - `test "$(grep -c -F 'integrating-with-code-scanning' docs/adoption-guide.md)" -eq 0`, `test "$(grep -c -i 'soft limit' docs/adoption-guide.md)" -eq 0` and `test "$(grep -c -F 'sca-results.json' docs/adoption-guide.md docs/milestone-plan/milestone-2-cicd-gate.md docs/milestone-plan/milestone-4-defectdojo.md | grep -v ':0' | wc -l | tr -d ' ')" -eq 0` all pass (scoped to the three edited files)
    - `git status --porcelain scripts/check-adoption-guide.sh .markdownlint.jsonc .markdownlint-cli2.yaml docs/development-security-stack-option-1.md docs/milestone-plan/milestone-1-workstation.md docs/milestone-1-workstation docs/adr` produces no output (no collateral damage, no gate tampering, no ADR modified)
    - `test "$(grep -rc -i grype docs/milestone-1-workstation/ | grep -v ':0' | wc -l | tr -d ' ')" -ge 1` passes (the legitimate workstation Grype references were deliberately left intact)
  </acceptance_criteria>
  <done>All six verification steps were executed and their real output recorded: zero Grype in both milestone docs, lint clean across three files, gate at PASSED 15 / FAILED 0, all six limit values present in the guide, and no collateral or gate-script modification.</done>
</task>

</tasks>

<threat_model>
N/A as a code threat model — this plan appends to a planning record and runs read-only verification
commands. Two documentation-integrity controls apply and are asserted above: the phase must not tamper with
`scripts/check-adoption-guide.sh` or any lint config to make a check pass (tampering vector T-20-12), and
the phase must not rewrite the append-only ADRs or the M1 workstation docs where Grype is a legitimate
installed tool (verified still intact by this plan's final acceptance criterion).
</threat_model>

<verification>
```bash
test "$(grep -c -i grype docs/milestone-plan/milestone-2-cicd-gate.md)" -eq 0
test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0
markdownlint-cli2 docs/milestone-plan/milestone-2-cicd-gate.md docs/milestone-plan/milestone-4-defectdojo.md docs/adoption-guide.md
bash scripts/check-adoption-guide.sh
git status --porcelain -- docs/ scripts/ .planning/phases/17-sarif-upload-and-artifact-retention
```
</verification>

<success_criteria>
- `deferred-items.md` carries a dated Phase 21 status re-check closing #4, #6 and SE-1, preserving every
  earlier row verbatim
- The record states that Phase 21 created no new deferred item, and that #2, #3 and the broken ADR links
  remain OPEN
- Full suite green: zero Grype in both milestone docs, `Summary: 0 error(s)` across the three edited docs,
  gate at PASSED 15 / FAILED 0
- No modification to the gate script, lint configs, the blueprint, the M1 workstation docs, or any ADR
</success_criteria>

<output>
Create `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-04-SUMMARY.md` when done.
Record the full suite output verbatim, and note for the verifier that ROADMAP.md's Phase 21 goal sentence
still mentions the "7 stale `# v4` comments" item, which was dropped from scope by user decision
(21-CONTEXT.md `<domain>`) and closed earlier by Phase 20.1 — it is not an unfixed gap.
</output>
