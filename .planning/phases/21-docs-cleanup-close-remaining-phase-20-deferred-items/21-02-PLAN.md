---
phase: 21-docs-cleanup-close-remaining-phase-20-deferred-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/milestone-plan/milestone-4-defectdojo.md
autonomous: true
requirements: [DIST-06, DIST-08]

must_haves:
  truths:
    - "milestone-4-defectdojo.md names the same DefectDojo parser set as milestone-2-cicd-gate.md — the two milestone docs do not contradict each other on the same contract"
    - "The M4-F4 deduplication example describes a pair of scanners that actually co-occur in the live pipeline"
    - "Grype appears nowhere in milestone-4-defectdojo.md"
    - "The document introduces no markdownlint violation (baseline was 0 errors)"
  artifacts:
    - path: "docs/milestone-plan/milestone-4-defectdojo.md"
      provides: "M4-F3 parser list and M4-F4 dedup example reconciled to the live SCA tool set"
      contains: "pip-audit Scan"
  key_links:
    - from: "docs/milestone-plan/milestone-4-defectdojo.md"
      to: "docs/milestone-plan/milestone-2-cicd-gate.md"
      via: "identical DefectDojo parser list on both docs' parser rows"
      pattern: "NPM Audit v7\\+ Scan"
---

<objective>
Apply the same live-SCA-tools correction to `docs/milestone-plan/milestone-4-defectdojo.md` that plan
21-01 applies to milestone-2: the M4-F3 parser list (line 75) carries the identical stale `Anchore Grype`
entry, and the M4-F4 deduplication example (line 105) describes importing the same CVE "from both Trivy
and Grype" — an outcome that is now impossible because the two scanners no longer coexist in the
pipeline.

Purpose: SE-1 from RESEARCH.md, user-approved as an in-scope expansion beyond CONTEXT.md's original
milestone-2-only scope. Fixing milestone-2 and leaving milestone-4 would replace one inconsistency
(doc vs. pipeline) with a worse one (doc vs. doc) on the exact same DefectDojo contract.

Output: one edited markdown file, `grep -c -i grype` = 0, markdownlint still 0 errors.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Read these ranges only (RESEARCH.md is 1,097 lines — do not read it whole):

- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-RESEARCH.md` lines 172–222
  (CONFLICT-2 — the Trivy Scan collision and the D-04′ wording this plan reuses verbatim in shape)
- same file lines 414–486 (DefectDojo `get_scan_types()` strings from parser source; SE-1 stating the
  milestone-4 drift and the two defensible outcomes; SE-3 listing the seven grype files that are NOT in
  scope)
- same file lines 223–297 (live SCA job composition — Trivy filesystem, npm audit, pip-audit, tflint)
- `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-CONTEXT.md` (D-01 scoped
  the edit to milestone-2 only; this plan exists because the user explicitly approved widening it)

<amendments>
<!-- User-approved amendments from the orchestrator. These OVERRIDE CONTEXT.md's literal scope. -->

- **SE-1 APPROVED IN SCOPE.** `docs/milestone-plan/milestone-4-defectdojo.md` lines 75 and 105 receive the
  same live-SCA-tools correction as milestone-2. This file was NOT in CONTEXT.md's original scope; it is
  in scope now. Neither line may be silently folded into plan 21-01's tasks — they are this plan's work.
- **D-04 AMENDED — per-job qualification required, exact API strings.** The verified DefectDojo
  `get_scan_types()` values are `Trivy Scan`, `NPM Audit v7+ Scan` (NOT "NPM Audit Version 7+", which is a
  docs-site display label only, and NOT "NPM Audit Scan", which is the legacy v6-and-older parser) and
  `pip-audit Scan` (lowercase `p`, hyphen — not "Pip-Audit Scan"). Leave `Semgrep JSON Report`,
  `Checkov Scan` and `Gitleaks Scan` untouched; all three already match DefectDojo source exactly.
- **Never write `sca-results.json`.** `sca-results` is an artifact name, not a filename.
</amendments>

<live_ground_truth>
<!-- Verified 2026-09-15 from the live workflow via gh api and from DefectDojo/django-DefectDojo master
     dojo/tools/*/parser.py. Use these, never another markdown file, as the source of fact. -->

Live SCA job (`sca`, name `SCA — Trivy Filesystem`, artifact `sca-results`) emits `trivy-fs.json`,
`npm-audit-<N>.json`, `pip-audit-<N>.json`, `trivy-fs.sarif`, `tflint.sarif`. The container job
(`container`) emits `trivy-image.json` and `trivy-image.sarif`. There is no Grype step anywhere in the
workflow — Grype was removed from the pipeline in Phases 15/16.

DefectDojo parser strings, per file:

- `trivy-fs.json` and `trivy-image.json` → `Trivy Scan` (`dojo/tools/trivy/parser.py`)
- `npm-audit-<N>.json` → `NPM Audit v7+ Scan` (`dojo/tools/npm_audit_7_plus/parser.py`; the runner's npm
  is 10.x, so the v7+ parser is the applicable one)
- `pip-audit-<N>.json` → `pip-audit Scan` (`dojo/tools/pip_audit/parser.py`)
- `tflint.sarif` → `SARIF` generic parser (`dojo/tools/sarif/parser.py`)
- `Anchore Grype` is still a real, shipping DefectDojo parser. What changed is that the pipeline no longer
  produces input for it. This is a pipeline-output fix, not a parser-name correction.
</live_ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the M4-F3 DefectDojo parser list (line 75)</name>
  <files>docs/milestone-plan/milestone-4-defectdojo.md</files>
  <read_first>
    - `docs/milestone-plan/milestone-4-defectdojo.md` lines 68–90 (the whole M4-F3 section)
    - `docs/milestone-plan/milestone-2-cicd-gate.md` line 85 (after plan 21-01 lands, this is the parser
      list this line must agree with; if 21-01 has not landed yet, match the target wording given below —
      the two plans are independent and either order is fine)
    - `21-RESEARCH.md` lines 172–222 (CONFLICT-2, D-04′ wording)
    - `21-RESEARCH.md` lines 414–434 (parser strings from DefectDojo source)
  </read_first>
  <action>
Run `git status --porcelain docs/milestone-plan/milestone-4-defectdojo.md` and confirm it is clean before
editing. If dirty, STOP and report.

One single-line Edit. Line 75 currently reads
`- 5 scanner parsers: `Semgrep JSON Report`, `Checkov Scan`, `Anchore Grype`, `Trivy Scan`, `Gitleaks Scan``.

Replace the whole bullet with
`- Scanner parsers, one import call per JSON artifact: `Semgrep JSON Report`, `Checkov Scan`, `Trivy Scan` (SCA filesystem — plus `NPM Audit v7+ Scan` and `pip-audit Scan` where the ecosystem sub-scans fire), `Trivy Scan` (container image), `Gitleaks Scan``.

Three things to get right:

1. The lead-in changes from "5 scanner parsers:" to "Scanner parsers, one import call per JSON artifact:"
   because the corrected list names seven parser strings across five jobs — keeping the literal count "5"
   in front of a seven-item list would be a new inaccuracy.
2. Every parser string stays inside backticks, matching this line's existing style (this differs from
   milestone-2 line 85, which uses no backticks — preserve each file's own convention rather than
   normalising them).
3. Use U+2014 EM DASH inside the parenthetical, matching the file's existing dash style.

Do not modify line 82 ("all 5 scan result files"), the `DEFECTDOJO_API_TOKEN` bullets, or anything else in
M4-F3. Line 105 is Task 2's. Never use `grep -rl grype | xargs sed` — seven other `docs/` files contain
"grype" legitimately (M1 workstation docs, where Grype really is installed, and three append-only ADRs).
  </action>
  <verify>
    <automated>test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 1 &amp;&amp; grep -qF 'NPM Audit v7+ Scan' docs/milestone-plan/milestone-4-defectdojo.md &amp;&amp; grep -qF 'pip-audit Scan' docs/milestone-plan/milestone-4-defectdojo.md &amp;&amp; test "$(grep -c '5 scanner parsers' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0 &amp;&amp; markdownlint-cli2 docs/milestone-plan/milestone-4-defectdojo.md 2>&amp;1 | grep -q 'Summary: 0 error'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 1` passes (line 75 cleared; only line 105 remains, which is Task 2's)
    - `test "$(grep -c 'Anchore Grype' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0` passes
    - `grep -qF 'NPM Audit v7+ Scan'` and `grep -qF 'pip-audit Scan'` both pass against the file
    - `test "$(grep -c 'NPM Audit Scan\|NPM Audit Version 7\|Pip-Audit Scan' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0` passes (no wrong parser-string variants introduced)
    - `test "$(grep -o 'Trivy Scan' docs/milestone-plan/milestone-4-defectdojo.md | wc -l | tr -d ' ')" -eq 2` passes and both are qualified: `grep -qF 'Trivy Scan` (SCA filesystem'` and `grep -qF 'Trivy Scan` (container image)'` both pass
    - `test "$(grep -c '5 scanner parsers' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0` passes (the false count is gone)
    - `grep -qF 'Semgrep JSON Report'`, `grep -qF 'Checkov Scan'`, `grep -qF 'Gitleaks Scan'` still pass
    - `markdownlint-cli2 docs/milestone-plan/milestone-4-defectdojo.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff --name-only` lists only `docs/milestone-plan/milestone-4-defectdojo.md`
  </acceptance_criteria>
  <done>Line 75 names the live parser set with both Trivy slots qualified and no false item count; lint clean; only the intended file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the impossible Trivy-vs-Grype deduplication example (line 105)</name>
  <files>docs/milestone-plan/milestone-4-defectdojo.md</files>
  <read_first>
    - `docs/milestone-plan/milestone-4-defectdojo.md` lines 96–112 (the whole M4-F4 section)
    - `21-RESEARCH.md` lines 440–470 (SE-1, including the replacement pair RESEARCH vetted)
    - `21-RESEARCH.md` lines 268–285 (live SCA job step-by-step — what actually co-occurs)
  </read_first>
  <action>
One single-line Edit. Line 105 currently reads
`- Deduplication enabled: import the same CVE from both Trivy and Grype — DefectDojo merges them into a single finding`.

The example is not merely stale, it is impossible: there is no Grype run to import a duplicate CVE from.
Replace the whole bullet with a pair that genuinely co-occurs in the live SCA job — Trivy's filesystem
scan and npm audit both report advisories for the same npm dependency:

`- Deduplication enabled: import the same CVE from both the SCA job's Trivy filesystem scan (`trivy-fs.json`) and npm audit (`npm-audit-<N>.json`) for the same npm package — DefectDojo merges them into a single finding`.

Use exactly this pair. Do not substitute a different example (for example Trivy filesystem vs. Trivy
image, which scan different targets and would not normally produce the same CVE for the same component).
Keep U+2014 EM DASH before "DefectDojo merges". Keep the bullet on one line. Filenames go in backticks,
matching this file's convention.

Leave the other three M4-F4 Done Criteria bullets and the Key Components list unchanged.
  </action>
  <verify>
    <automated>test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0 &amp;&amp; grep -qF 'npm-audit-<N>.json' docs/milestone-plan/milestone-4-defectdojo.md &amp;&amp; grep -qF 'trivy-fs.json' docs/milestone-plan/milestone-4-defectdojo.md &amp;&amp; markdownlint-cli2 docs/milestone-plan/milestone-4-defectdojo.md 2>&amp;1 | grep -q 'Summary: 0 error'</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0` passes (SE-1 fully closed)
    - `grep -qF 'trivy-fs.json'` and `grep -qF 'npm-audit-<N>.json'` both pass against the file
    - `test "$(grep -c 'sca-results.json' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0` passes
    - `test "$(grep -c '^- Deduplication enabled:' docs/milestone-plan/milestone-4-defectdojo.md)" -eq 1` passes (one bullet, not duplicated)
    - `markdownlint-cli2 docs/milestone-plan/milestone-4-defectdojo.md` prints `Summary: 0 error(s)` and exits 0
    - `git diff --name-only` lists only `docs/milestone-plan/milestone-4-defectdojo.md`
  </acceptance_criteria>
  <done>The dedup example names two scanners that actually co-occur; zero Grype references remain in the file; lint clean.</done>
</task>

</tasks>

<threat_model>
N/A — documentation-only change, no code path, no input handling, no credential, no runtime behaviour.
The relevant control is documentation integrity (ASVS V14, documentation sense): two milestone docs
stating different DefectDojo contracts would send a future M4 implementer to a parser the pipeline never
feeds. Mitigated by copying every parser string from DefectDojo's own `get_scan_types()` source rather
than composing it, and by the grep acceptance criteria above. Hard rule carried from RESEARCH: no task in
this phase may modify `scripts/check-adoption-guide.sh`.
</threat_model>

<verification>
```bash
test "$(grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md)" -eq 0 && echo "GRYPE-CLEAR"
markdownlint-cli2 docs/milestone-plan/milestone-4-defectdojo.md   # expect: Summary: 0 error(s)
git diff --name-only                                              # expect: only the milestone-4 doc
```
</verification>

<success_criteria>
- `grep -c -i grype docs/milestone-plan/milestone-4-defectdojo.md` returns 0
- Line 75 names `NPM Audit v7+ Scan` and `pip-audit Scan` with both `Trivy Scan` slots job-qualified and no "5 scanner parsers" count
- Line 105's dedup example names Trivy filesystem and npm audit
- markdownlint-cli2 reports `Summary: 0 error(s)` (baseline preserved)
- `git diff --name-only` lists exactly one file
</success_criteria>

<output>
Create `.planning/phases/21-docs-cleanup-close-remaining-phase-20-deferred-items/21-02-SUMMARY.md` when done.
Record: measured `grep -c -i grype` before/after, the markdownlint summary line, and an explicit note that
this file was added to phase scope by user-approved amendment SE-1 (it is absent from 21-CONTEXT.md's
locked decisions).
</output>
