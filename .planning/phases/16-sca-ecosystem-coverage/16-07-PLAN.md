---
phase: 16-sca-ecosystem-coverage
plan: 07
type: execute
wave: 7
depends_on: [16-05, 16-06]
files_modified: []
autonomous: false
requirements: [SCA-01, SCA-02, SCA-03]

must_haves:
  truths:
    - "A human has seen the live SCA job evidence for all three ecosystems and approved the merge"
    - "After the merge, `main` in OttawaCloudConsulting/security-platform carries the four-tool sca job, the three detector scripts, the extended smoke gate and the new fixtures"
    - "The verbatim `sca` check-run name is re-recorded as unchanged, so Phase 18 can rely on it"
    - "The partial satisfaction of Criterion 3 (missing constraints and unpinned modules, not floating ranges) is stated in the phase record, not glossed"
    - "The facts Phase 17 must not re-derive are handed forward explicitly"
  artifacts:
    - path: ".planning/phases/16-sca-ecosystem-coverage/16-07-SUMMARY.md"
      provides: "Human sign-off, merge record, criterion-by-criterion verdict, and the Phase 17/18 hand-forwards"
      contains: "MERGED"
  key_links:
    - from: "OttawaCloudConsulting/security-platform pull request"
      to: "OttawaCloudConsulting/security-platform main"
      via: "human-approved merge"
      pattern: "main"
---

<objective>
Put the live evidence in front of the user, get an explicit decision, and — only then — merge the Phase 16
branch so Phase 17 builds on a current default branch. Close the phase with an honest criterion-by-criterion
verdict and the hand-forwards Phases 17 and 18 depend on.

Purpose: merging is an irreversible action on a shared branch, and the project's rules require confirmation
before those. Two of this phase's outcomes also need a human judgement rather than an assertion: whether
partial satisfaction of Criterion 3 ("floating" ranges are not flagged by tflint's default ruleset) is
acceptable as shipped, and whether keeping the now-inaccurate `SCA — Trivy Filesystem` check-run name is
still the right call given Phase 18 hard-codes it.

Output: no source file changes. A human decision, a merged pull request, and a SUMMARY that closes
SCA-01/02/03 and records what the next two phases must not re-derive.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/development_environment/security_solution/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-sca-ecosystem-coverage/16-03-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md
@.planning/phases/16-sca-ecosystem-coverage/16-06-SUMMARY.md
</context>

<notes>
**TARGET REPOSITORY.** The merge happens on `OttawaCloudConsulting/security-platform` via `gh pr merge`.
Resolve the slug at runtime:
`SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##')`.
**NEVER run `git push` from the outer repository** — the two histories are disjoint.

**Do not merge before the checkpoint returns an approval.** `workflow.auto_advance` does not apply to an
irreversible action on a shared branch.

**Verify the merge from the remote, not the local tree.** Phase 15-05 recorded this: confirm `origin/main`
with `git -C repos/security-platform fetch origin` then `git show origin/main:<path>`, never by inspecting
the local working tree.
</notes>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human sign-off on the three ecosystem sub-scans and the merge</name>
  <files></files>
  <action>
    Present the evidence before asking the question. Post the pull request URL, the five check-run names
    exactly as GitHub reports them, and the three per-criterion log excerpts recorded in 16-05-SUMMARY —
    the npm severity histogram, the pip-audit advisory counts, and the tflint rule-id set with its CI
    version. Then ask the user to confirm the five points listed under how-to-verify, including the two
    that are judgement calls rather than observations: accepting Criterion 3's partial coverage (missing
    constraints and unpinned module sources, but not floating ranges), and keeping the now-inaccurate
    `SCA — Trivy Filesystem` check-run name because Phase 18 hard-codes it. Record the user's literal
    response. Do not merge, do not modify any file, and do not proceed to Task 2 without an explicit
    approval — `auto_advance` does not apply to an irreversible action on a shared branch.
  </action>
  <read_first>
    - .planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md (the live evidence to present)
    - .planning/phases/16-sca-ecosystem-coverage/16-03-SUMMARY.md (the local gate result, including the clean-skip probes)
    - .planning/ROADMAP.md Phase 16 Success Criteria
  </read_first>
  <what-built>
    Four scanners now run inside the single existing `sca` job: the Phase 15 Trivy filesystem sweep plus
    npm audit (SCA-01), pip-audit (SCA-02) and tflint (SCA-03). Each is guarded by a shared detector script
    that logs on both the found and the skipped branch, and each is followed by a report-content check that
    is deliberately not tolerated, so an error-object report turns red instead of reading as clean. The
    fixtures gained a Python requirements file and, in Terraform, an unconstrained-and-used provider plus an
    unpinned registry module. The local smoke gate covers all four sub-scans and proves the clean-skip path
    in an empty repository. ADR-015 and the blueprint's tool tables record the tflint adoption.
  </what-built>
  <how-to-verify>
    1. Open the pull request recorded in 16-05-SUMMARY and go to its Checks view. Confirm five checks, the
       SCA one still named `security / SCA — Trivy Filesystem`, all green, and the PR reporting mergeable.
    2. Open the SCA job log. Confirm three `FOUND n …` detector lines, then the npm severity histogram
       (Criterion 1), the pip-audit advisory counts (Criterion 2), and the tflint rule-id set (Criterion 3).
    3. Confirm you accept the Criterion 3 limitation as shipped: tflint's default ruleset flags MISSING
       provider constraints and unpinned module sources, but does NOT flag a floating range such as
       `version = ">= 3.0"`. A custom rule was deliberately not built this phase.
    4. Confirm you accept that the `sca` check-run name stays `SCA — Trivy Filesystem` despite now covering
       four tools, because Phase 18's required-check list hard-codes it.
    5. Confirm Criterion 4's evidence is acceptable: the clean-skip path is proven by the smoke gate's
       empty-repository test and the workflow's static step guards, not by a live observation — the product
       repo contains all three ecosystems, so nothing skips there.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the merge, or describe what must change first.</resume-signal>
  <verify>
    <automated>SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##'); PR=$(gh pr list --repo "$SLUG" --head feature/phase-16-sca-ecosystem-coverage --json number --jq '.[0].number'); gh pr view --repo "$SLUG" "$PR" --json number,state,mergeable --jq '"pr=\(.number) state=\(.state) mergeable=\(.mergeable)"'</automated>
  </verify>
  <acceptance_criteria>
    - The pull request URL, the five check names and the three per-criterion log excerpts were presented to the user before the question was asked
    - The user's literal response is recorded in the SUMMARY
    - No merge occurred before an explicit approval
    - If the user declined or raised an issue, the SUMMARY records it and the merge task does not run
  </acceptance_criteria>
  <done>An explicit human decision on the evidence, the Criterion 3 limitation, and the frozen check-run name is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Merge to main, verify from the remote, and close the phase record</name>
  <files></files>
  <read_first>
    - .planning/phases/15-five-parallel-scan-jobs/15-05-SUMMARY.md (the Phase 15 merge-and-verify procedure, including the remote-verification rule)
    - .planning/REQUIREMENTS.md (the SCA-01/02/03 rows to mark complete)
    - .planning/phases/16-sca-ecosystem-coverage/16-05-SUMMARY.md (the hand-forward facts to carry into the phase record)
  </read_first>
  <action>
    Only after an approval was recorded in Task 1: merge the PR with `gh pr merge --repo "$SLUG" <pr> --merge`,
    matching the Phase 15 precedent (a merge commit, not squash, not rebase). Record the resulting merge
    commit SHA.

    Verify from the remote, never the local tree: `git -C repos/security-platform fetch origin`, then
    confirm with `git show origin/main:<path>` that `main` carries the four-tool `sca` job in
    `.github/workflows/security.yml`, all three `scripts/detect-*.sh`, the extended `scripts/smoke-scans.sh`,
    `fixtures/requirements.txt` and the extended `fixtures/main.tf`. Re-record the verbatim `sca` job
    `name:` value from `origin/main` as the authoritative string for Phase 18.

    Then close the phase record. Mark SCA-01, SCA-02 and SCA-03 complete in `.planning/REQUIREMENTS.md` and
    update the ROADMAP Phase 16 row. In the SUMMARY, give a criterion-by-criterion verdict — including that
    Criterion 3 is met for missing constraints and unpinned module sources but NOT for loose version ranges,
    and that Criterion 4 is met via the empty-repo negative test and static guards rather than a live skip.

    Carry forward, explicitly, for Phase 17: reports are numbered per input (`npm-audit-<n>.json`,
    `pip-audit-<n>.json`), so artifact upload cannot assume fixed filenames; neither npm audit nor pip-audit
    emits SARIF while tflint does, so Phase 17 Criterion 4's conversion step applies to two of the three
    tools; pip-audit's JSON carries no severity or CVSS field; and `--audit-level` does not filter npm's
    report even though Trivy's `--severity` does filter its own — the two must not be generalised to each
    other. For Phase 18: the frozen check-run name, and that gate mode must not re-derive severity
    thresholds because every tool kept its native semantics under D-04.
  </action>
  <verify>
    <automated>SLUG=$(git -C repos/security-platform remote get-url origin | sed -E 's#^.*github\.com[:/]##; s#\.git$##'); PR=$(gh pr list --repo "$SLUG" --state merged --head feature/phase-16-sca-ecosystem-coverage --json number,mergeCommit --jq '.[0] | "pr=\(.number) merge=\(.mergeCommit.oid)"'); echo "$PR" | grep -q 'merge='</automated>
    <automated>git -C repos/security-platform fetch origin && git -C repos/security-platform show origin/main:.github/workflows/security.yml | grep -q 'bash scripts/detect-terraform.sh' && git -C repos/security-platform show origin/main:fixtures/requirements.txt | grep -q 'requests==2.19.1' && git -C repos/security-platform show origin/main:scripts/detect-npm.sh | grep -q 'package-lock.json' && git -C repos/security-platform show origin/main:scripts/smoke-scans.sh | grep -q 'run_scan_rc'</automated>
    <automated>F=.planning/phases/16-sca-ecosystem-coverage/16-07-SUMMARY.md; test -f "$F" && grep -q 'MERGED' "$F" && grep -qi 'criterion 3' "$F" && grep -qi 'criterion 4' "$F" && grep -q 'SCA — Trivy Filesystem' "$F" && grep -qi 'phase 17' "$F"; grep -q '\[x\] \*\*SCA-01\*\*' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*SCA-02\*\*' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*SCA-03\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The PR state is `MERGED` and the merge commit SHA is recorded in the SUMMARY
    - `git show origin/main:…` confirms the four-tool workflow, all three detector scripts, the extended smoke gate and both fixture changes are on the remote default branch
    - The verbatim `sca` job `name:` is re-recorded from `origin/main`, not from the local tree
    - SCA-01, SCA-02 and SCA-03 are marked complete in `.planning/REQUIREMENTS.md` and the ROADMAP Phase 16 row is updated
    - The SUMMARY gives a verdict per success criterion, stating Criterion 3's partial coverage and Criterion 4's evidence source without overstating either
    - The SUMMARY lists the four Phase 17 hand-forwards and the two Phase 18 hand-forwards named in the action
  </acceptance_criteria>
  <done>The phase is merged, verified from the remote, and closed with an honest verdict and explicit hand-forwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull request → `main` | An irreversible change to the shared default branch other phases build on |
| phase record → future phases | Phases 17 and 18 act on what this SUMMARY says; an overstated claim propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-27 | Elevation of Privilege | An automated merge to a shared default branch without human authorisation | mitigate | `checkpoint:human-verify` with `gate="blocking"`; merge task runs only after an approval is recorded; `auto_advance` does not apply |
| T-16-28 | Repudiation | Claiming full Criterion 3 coverage when loose ranges are not flagged | mitigate | The checkpoint asks the user to accept the limitation explicitly; the SUMMARY must state it; acceptance criteria assert the wording is present |
| T-16-29 | Spoofing | Verifying the merge from the local working tree, which can diverge from the remote | mitigate | Verification uses `git fetch` then `git show origin/main:<path>` — the Phase 15-05 rule, applied again |
| T-16-30 | Repudiation | Phase 18 configuring branch protection against a check name read from a stale record | mitigate | The verbatim `sca` job name is re-recorded from `origin/main` after the merge |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. All package decisions were fixed and verified in 16-04 against 16-RESEARCH's Package Legitimacy Audit |
</threat_model>

<verification>
- PR state `MERGED` with a recorded merge commit SHA
- `origin/main` carries the workflow, the three detectors, the extended smoke gate and both fixture changes
- The `sca` check-run name re-recorded from the remote
- SCA-01/02/03 marked complete in REQUIREMENTS.md; ROADMAP Phase 16 row updated
- SUMMARY contains a per-criterion verdict and the Phase 17/18 hand-forwards
</verification>

<success_criteria>
Phase 16 is merged with human authorisation, verified against the remote default branch, and closed with a
record that states what was proven, what was only partially satisfied, and what the next two phases must
not re-derive.
</success_criteria>

<output>
Create `.planning/phases/16-sca-ecosystem-coverage/16-07-SUMMARY.md` when done, containing the user's literal
approval response, the merge commit SHA, the remote-verified file list, the verbatim `sca` check-run name,
the criterion-by-criterion verdict, and the Phase 17 and Phase 18 hand-forwards.
</output>
